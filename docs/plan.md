# TestPilot Master Plan

> 更新日期：2026-04-14
> 基線版本：v0.0.3-draft  
> 規劃版本：v0.1.0（第三次重構基線）

---

## 1. 目標

TestPilot 的主目標是：

1. 以 YAML 驅動 DUT / STA / Endpoint 的 deterministic 測試流程。
2. 以 structured evidence 固化每次 run / case / retry 的正式結果。
3. 將 `xlsx` 對外交付報告與 `md/json` 診斷報告分離。
4. 以 Copilot SDK 承接 control-plane 與 operator UX，而不是接管最終 verdict。
5. 在不引入 Codex CLI workaround 的前提下，完成第三次重構。

---

## 2. 現況快照（2026-04-14）

> 2026-04-15 latest continuation after D047 revalidation: latest committed closure remains `D600 WiFi7STARole.NSTRSupport` via official rerun `20260415T173554269251`, and overlay compare still holds at `395 / 420 full matches`、`25 mismatches`、`43` metadata drifts. Workbook authority for `D600` is row `600`; the stale row `416` is retired, `results_reference.v4.0.3` is refreshed from `Fail / Fail / Fail` back to workbook `Pass / Pass / Pass`, and both focused live survey plus official rerun exact-close tri-band `WiFi.Radio.1/2/3.Capabilities.WiFi7STARole.NSTRSupport=1` with `diagnostic_status=Pass`. A compare overlay refresh already folded in the authoritative scan reruns `D277 getScanResults() Bandwidth` (`20260411T205454026707`) and `D290 getScanResults() CentreChannel` (`20260411T220324862766`), so both rows now present source-backed `Pass / Fail / Pass` instead of stale `Fail / Fail / Fail`, and the per-band summary stays `5g 397/23`, `6g 395/25`, `2.4g 398/22`. Official rerun `20260415T180502444191` re-confirms `D020 FrequencyCapabilities` as a verified/source-backed fail-shaped mismatch: both attempts stop at `result_5g.FrequencyCapabilities` because workbook still expects empty while the live getter and driver normalization remain `5GHz`, with 6G / 2.4G still exact-closing `6GHz` / `2.4GHz`. Official rerun `20260415T182628238198` now also re-confirms `D047 SupportedHe160MCS` as a workbook/source authority conflict: the rerun exact-closes source/runtime-correct `Not Supported / N/A / N/A` with `diagnostic_status=Pass`, but the same-window 5G getter still returns `error=4 / parameter not found` while sibling `Rx/TxSupportedHe160MCS` values remain present for the same AssociatedDevice entry. A focused serialwrap survey still parks `D588 SSID MLDUnit` as a workbook/source-driver authority blocker: tri-band getter stays `-1 / -1 / -1`, workbook `H588` candidates `wl -i wlX mld_unit 0` and `wl -i wlX mld_unit` both return `wl: Unsupported`, and `/tmp/wl*_hapd.conf` exposes no `mld_unit=` fallback. The focused blocker rerun `20260415T131457874117` for `D508 SSID WMM AC_BE Stats WmmFailedBytesSent` remains unchanged: 5G exact-closes `0 / 0 / 0`, 6G exact-closes `708116 / 708116 / 708116`, but 2.4G `getSSIDStats()` plus the direct getter both stay at `0` while `wl2 wme_counters` `AC_BE` tx failed bytes stays at `90`, so `D508` remains a localized 2.4G zero-getter blocker and the exploratory workbook-faithful rewrite stays rolled back. The focused serialwrap survey for `D524 SSID WMM AC_BE Stats WmmPacketsSent` also remains parked: tri-band `getSSIDStats()` and the direct getter exact-close `452/452`, `510/510`, `547/547`, but same-window `wl wme_counters` `AC_BE` `tx frames` drift to `896`, `1015`, and `1090`, so there is still no stable independent oracle for a workbook-faithful rewrite; the blocker note is captured in `plugins/wifi_llapi/reports/D524_block.md`. The earlier `D490`, `D481`, `D482`, `D485`, `D454`, and `D371` blocker states remain in force; targeted runtime/budget guardrails are `1251 passed`, full repo regression is `1660 passed`, `D355-D357` stay in the CSI-client placeholder bucket, `D359 AccessPoint.IsolationEnable` remains parked behind the current single-STA lab shape, and `D414/D415` stay in readiness review because workbook `G` requires a dual-STA 802.11k split that the current single-STA lab path still cannot satisfy. There is still no clean workbook-pass-ready single case left in the compare-open set, so the next investigative track shifts to the shared-6G blocker review, starting with `D179 Radio.Ampdu`.
> 2026-04-14 latest getter-path readiness note: the recovered `getRadioAirStats()` path now exact-closes both `D257` and `D261` without reintroducing the earlier empty-array / `wl1 bss` / STA disconnect blocker. The remaining non-green lab signal is still 6G post-verify `dut_ocv_not_zero`, so active blockers remain `D047` authority conflict plus the shared 6G baseline manifestations in `D179` and `D181`, while `D211` and `D204` stay parked clarification items.
> 2026-04-14 latest continuation after D201: `D201 Radio.ImplicitBeamFormingSupported` is now aligned via official rerun `20260414T162439231118`. Workbook authority is row `201`, not stale row `164`, and the rerun exact-closes tri-band getter `ImplicitBeamFormingSupported=1/1/1`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `316 / 420 full matches`、`104 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D202 Radio.Interference`.
> 2026-04-14 latest continuation after D200: `D200 Radio.ImplicitBeamFormingEnabled` is now aligned via official rerun `20260414T161411193999`. Workbook authority is row `200`, not stale row `163`, and the rerun exact-closes tri-band getter `ImplicitBeamFormingEnabled=1/1/1`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `315 / 420 full matches`、`105 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D201 Radio.ImplicitBeamFormingSupported`.
> 2026-04-14 latest continuation after D199: `D199 Radio.IEEE80211rSupported` is now aligned via official rerun `20260414T160329947246`. Workbook authority is row `199`, not stale row `162`, and the rerun exact-closes tri-band getter `IEEE80211rSupported=1/1/1`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `314 / 420 full matches`、`106 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D200 Radio.ImplicitBeamFormingEnabled`.
> 2026-04-14 latest continuation after D198: `D198 Radio.IEEE80211kSupported` is now aligned via official rerun `20260414T152334632094`. Workbook authority is row `198`, not stale row `161`, and the rerun exact-closes tri-band getter `IEEE80211kSupported=1/1/1`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `313 / 420 full matches`、`107 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D199 Radio.IEEE80211rSupported`.
> 2026-04-14 latest continuation after D197: `D197 Radio.IEEE80211hSupported` is now aligned via official rerun `20260414T151631032947`. Workbook authority is row `197`, not stale row `160`, and the rerun exact-closes tri-band getter `IEEE80211hSupported=1/0/0`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `312 / 420 full matches`、`108 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D198 Radio.IEEE80211kSupported`.
> 2026-04-14 latest continuation after D196: `D196 Radio.IEEE80211hEnabled` is now aligned via official rerun `20260414T150830713390`. Workbook authority is row `196`, not stale row `159`, and the rerun exact-closes tri-band getter `IEEE80211hEnabled=1/0/0`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `311 / 420 full matches`、`109 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D197 Radio.IEEE80211hSupported`.
> 2026-04-14 latest continuation after D195: `D195 Radio.IEEE80211_Caps` is now aligned via official rerun `20260414T145819251839`. Workbook authority is row `195`, not stale row `158`, and the rerun exact-closes tri-band `IEEE80211_Caps` getter strings; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `310 / 420 full matches`、`110 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D196 Radio.IEEE80211hEnabled`.
> 2026-04-14 latest continuation after D194: `D194 Radio.HeCapsSupported` is now aligned via official rerun `20260414T143051719431`. Workbook authority is row `194`, not stale row `157`, and the rerun exact-closes tri-band getter `HeCapsSupported="DL_OFDMA,UL_OFDMA,DL_MUMIMO,UL_MUMIMO"`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `309 / 420 full matches`、`111 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D195 Radio.IEEE80211_Caps`.
> 2026-04-14 latest continuation after D193: `D193 Radio.HeCapsEnabled` is now aligned via official rerun `20260414T142054369177`. Workbook authority is row `193`, not stale row `156`, and the rerun exact-closes tri-band getter `HeCapsEnabled="DEFAULT"`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `308 / 420 full matches`、`112 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D194 Radio.HeCapsSupported`.
> 2026-04-14 latest continuation after D192: `D192 Radio.GuardInterval` is now aligned via official rerun `20260414T140146826061`. Workbook authority is row `192`, not stale row `155`, and the rerun exact-closes tri-band getter `GuardInterval="Auto"`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `307 / 420 full matches`、`113 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D193 Radio.HeCapsEnabled`.
> 2026-04-14 latest continuation after D191: `D191 Radio.ExplicitBeamFormingSupported` is now aligned via official rerun `20260414T134150940705`. Workbook authority is row `191`, not stale row `154`, and the rerun exact-closes tri-band getter `ExplicitBeamFormingSupported=1`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `306 / 420 full matches`、`114 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D192 Radio.GuardInterval`.
> 2026-04-14 latest blocker after D191: `D181 Radio.FragmentationThreshold` remains blocked as another manifestation of the shared 6G `DUT + STA` baseline bring-up failure already seen in `D179`. The clean-start trial rerun `20260414T111023418516` never reached case-step execution because `verify_env` kept failing on `wl1 bss` / STA 6G recovery (`sta_baseline_bss[1] not ready after 60s cmd=wl -i wl1 bss`, `STA band baseline/connect failed`, `6G ocv fix did not stabilize wl1 after retries`). Only partial xlsx `20260414_BGW720-0403_wifi_LLAPI_20260414T111023418516.xlsx` was emitted, so the provisional workbook-faithful `D181/D182` setter rewrites stay rolled back. Active blockers remain `D047` authority conflict plus the shared 6G baseline blocker manifested in `D179` and `D181`.
> 2026-04-14 latest continuation after D190: `D190 Radio.ExplicitBeamFormingEnabled` is now aligned via official rerun `20260414T133109929684`. Workbook authority is row `190`, not stale row `153`, and the rerun exact-closes tri-band getter `ExplicitBeamFormingEnabled=1`; the landed case now refreshes stale raw `Fail / Fail / Fail` to workbook-consistent `Pass / Pass / Pass`. Targeted getter/runtime guardrails remain `201 passed`, command-budget remains `1 passed`, final full repo regression remains `1662 passed`, overlay compare is now `305 / 420 full matches`、`115 mismatches`、`58 metadata drifts`, and the next ready non-blocked compare-open case moves to `D191 Radio.ExplicitBeamFormingSupported`.
> 2026-04-14 latest blocker after D190: `D181 Radio.FragmentationThreshold` remains blocked as another manifestation of the shared 6G `DUT + STA` baseline bring-up failure already seen in `D179`. The clean-start trial rerun `20260414T111023418516` never reached case-step execution because `verify_env` kept failing on `wl1 bss` / STA 6G recovery (`sta_baseline_bss[1] not ready after 60s cmd=wl -i wl1 bss`, `STA band baseline/connect failed`, `6G ocv fix did not stabilize wl1 after retries`). Only partial xlsx `20260414_BGW720-0403_wifi_LLAPI_20260414T111023418516.xlsx` was emitted, so the provisional workbook-faithful `D181/D182` setter rewrites stay rolled back. Active blockers remain `D047` authority conflict plus the shared 6G baseline blocker manifested in `D179` and `D181`.
> 2026-04-14 latest continuation after D180/D184-D187: `D180 Radio.Amsdu` is now aligned via official rerun `20260414T111010511593`, and `D184-D187` are now aligned via official reruns `20260414T111624033199` / `20260414T111633789177` / `20260414T111643078674` / `20260414T111652454052`. These five landed cases are all low-risk radio getter closures: workbook authority now moves to rows `180` / `184-187`, stale raw `Fail / Fail / Fail` is refreshed to `Pass / Pass / Pass`, and the reruns exact-close tri-band values `Amsdu=-1` and `NrActiveRxAntenna` / `NrActiveTxAntenna` / `NrRxAntenna` / `NrTxAntenna` = `4`. Targeted getter/runtime guardrails are `201 passed`, command-budget guardrail is `1 passed`, refreshed overlay compare is now `304 / 420 full matches`、`116 mismatches`、`58 metadata drifts`, and no full repo regression rerun was executed in this turn.
> 2026-04-14 latest blocker after D180/D184-D187: `D181 Radio.FragmentationThreshold` is now blocked as another manifestation of the shared 6G `DUT + STA` baseline bring-up failure already seen in `D179`. The clean-start trial rerun `20260414T111023418516` never reached case-step execution because `verify_env` kept failing on `wl1 bss` / STA 6G recovery (`sta_baseline_bss[1] not ready after 60s cmd=wl -i wl1 bss`, `STA band baseline/connect failed`, `6G ocv fix did not stabilize wl1 after retries`). Only partial xlsx `20260414_BGW720-0403_wifi_LLAPI_20260414T111023418516.xlsx` was emitted, so the provisional workbook-faithful `D181/D182` setter rewrites were rolled back to respect the YAML writeback gate. Active blockers are now `D047` authority conflict plus the shared 6G baseline blocker manifested in `D179` and `D181`, and the next ready non-blocked compare-open case moves to `D190 Radio.ExplicitBeamFormingEnabled`.
> 2026-04-13 latest continuation after D179 blocker: workbook row `179` requires an active STA baseline before `wl -i wlx ampdu` can be judged, so the older DUT-only replay was rejected by focused rerun `20260413T175446838229` (`AfterSet0GetterAmpdu5g=0` but `AfterSet0DriverAmpdu5g=1`, with no per-case STA evidence). The trial YAML now declares explicit `DUT + STA` tri-band topology and targeted D179/runtime plus command-budget guardrails are `4 passed`, but clean-start rerun `20260413T182427454124` never reached step execution because 6G `verify_env` kept failing on wl1 recovery (`6G ocv fix did not stabilize wl1 after retries`, `sta_baseline_bss[1] not ready after 60s cmd=wl -i wl1 bss`, `STA 6g link check failed ... Not connected.`). `D179` is therefore a shared 6G DUT+STA baseline blocker rather than a closure; final full repo regression remains `1662 passed`, overlay compare stays `298 / 420 full matches`、`122 mismatches`、`58 metadata drifts`, `D047` remains the authority blocker, and the next ready actionable compare-open case moves to `D180 Radio.Amsdu`.
> 2026-04-13 latest continuation after D178: `D178 Radio.ChannelLoad` is now aligned via official rerun `20260413T172222999250`. Workbook authority is row `178`, workbook v4.0.3 remains `Pass / Pass / Pass`, and the old authored shape has now been retired: stale row `141` plus bare getters left 6G exposed to cache drift, so the landed case now collapses each band into one same-window tight-capture step that explicitly refreshes `getRadioAirStats()`, rereads `ChannelLoad?`, and derives in-use survey load from `iw dev wlX survey dump`. The rerun exact-closes `AirLoad == ChannelLoad == SurveyChannelLoad` on all three bands under the current lab environment (`5G=84`, `6G=62`, `2.4G=98`), with the 2.4G path intentionally using floor-based survey derivation so `69/70` closes to `98` instead of `99`. Targeted D178/runtime plus command-budget guardrails are `4 passed`, full repo regression remains `1662 passed`, overlay compare is now `298 / 420 full matches`、`122 mismatches`、`58 metadata drifts`, `D020` remains the verified fail-shaped mismatch, `D047` remains the active authority blocker, and the next ready actionable compare-open case moves to `D179 Radio.Ampdu`.
> 2026-04-13 latest continuation after D053: `D053 TxBytes` is now aligned via official rerun `20260413T164447027184`. Workbook authority is row `53`, workbook v4.0.3 remains `Pass / Pass / Pass`, and the old authored shape has now been retired: splitting getter / snapshot / driver across separate DUT steps caused official-runner drift, so the landed case collapses each band into one same-window tight-capture step. The rerun exact-closes `AssocTxBytes == TxBytes == DriverTxBytes` on all three bands under the generic tri-band baseline (`5G=992`, `6G=25207`, `2.4G=25586`) while STA remains stably linked to `testpilot5G` / `testpilot6G` / `testpilot2G`. Targeted D053/runtime plus command-budget guardrails are `8 passed`, full repo regression is now `1662 passed`, overlay compare is now `297 / 420 full matches`、`123 mismatches`、`58 metadata drifts`, `D020` remains the verified fail-shaped mismatch, `D047` remains the active authority blocker, and the next ready actionable compare-open case moves to `D178 Radio.ChannelLoad`.
> 2026-04-13 latest continuation after D047 blocker: `D050 SupportedVhtMCS` is now aligned via official rerun `20260413T000249620932`. Unlike `D047`, workbook row `50` is internally consistent: answer columns `R/S/T` are `Pass / Not Supported / Not Supported`, workbook `G/H` already treat the standalone getter as equivalent to sibling `RxSupportedVhtMCS` / `TxSupportedVhtMCS`, and note `V50` keeps `6G/2.4G` on `Not Supported`. Current rerun exact-closes the 5G pass shape on the generic `testpilot5G` baseline with same-station `MACAddress="2C:59:17:00:04:85"`, `SupportedVhtMCS? -> error=4 / parameter not found`, sibling `DriverRxSupportedVhtMCS=9,9,9,9` / `DriverTxSupportedVhtMCS=9,9,9,9`, and matching `wl0 sta_info` `VHT caps` / `MCS SET` / `VHT SET` lines. The landed case now projects workbook-consistent `Pass / Not Supported / Not Supported`; targeted D050 guardrails are `5 passed`, full repo regression remains `1660 passed`, overlay compare is now `296 / 420 full matches`、`124 mismatches`、`58 metadata drifts`, `D047` stays blocked as an authority conflict, and the next ready actionable compare-open case moves to `D053 TxBytes`.
> 2026-04-13 current blocker after D042: `D047 SupportedHe160MCS` is now confirmed as a workbook/source authority conflict. `compare-0401` reads answer columns `R/S/T`, so row `47` currently expects `Pass / Pass / Not Supported`; but the same workbook row still carries legacy `I/J/K = Not Support / Not Support / Not Support` and note `V47` saying pWHM only exposes `RxSupportedHe160MCS` / `TxSupportedHe160MCS`. Current 0403 installed ODL matches that live split: `WiFi.AccessPoint.{i}.AssociatedDevice.{i}.` only exposes sibling `Rx/TxSupportedHe160MCS`, while standalone `SupportedHe160MCS` exists under the endpoint model. Official rerun `20260412T235952361188` exact-closes the conflict with `error=4 / parameter not found`, sibling Rx/Tx values, and live HE capability lines. Blocker handoff is now `plugins/wifi_llapi/reports/D047_block.md`; `D020` remains the verified fail-shaped mismatch, and the next ready actionable compare-open case moves to `D050 SupportedVhtMCS`.
> 2026-04-13 latest continuation after D042: `D042 RxUnicastPacketCount` is now aligned via official rerun `20260413T145000666925`. Workbook authority is row `42`, not stale row `44`, and workbook v4.0.3 remains `Not Supported / Not Supported / Not Supported`. The rerun exact-closes the supported-band same-station counter divergence on the current lab baseline: DUT `MACAddress="2C:59:17:00:04:85"`, `RxUnicastPacketCount=0`, and driver `DriverRxUnicastPacketCount=8`, while STA remains linked to `TestPilot_BTM`. The landed case now projects workbook-consistent `Not Supported / Not Supported / Not Supported`; overlay compare is now `295 / 420 full matches`、`125 mismatches`、`58 metadata drifts`, targeted counter-stub guardrails are `2 passed`, final full repo regression remains `1660 passed`, `D020` stays in the verified fail-shaped mismatch bucket, and the next ready actionable compare-open case moves to `D047 SupportedHe160MCS`.
> 2026-04-13 latest continuation after D035: `D035 OperatingStandard` is now aligned via official rerun `20260413T144105373183`. Workbook authority is row `35`, not stale row `37` (`EncryptionMode`), and workbook v4.0.3 remains `Pass / Pass / Pass`. Current 0403 source still exposes the read-only AccessPoint AssociatedDevice `OperatingStandard` getter, while the rerun exact-closes the associated STA path on the current lab baseline: DUT `assoclist 2C:59:17:00:04:85` and `OperatingStandard="ax"` with STA still linked to `testpilot5G`. The landed case now projects workbook-consistent `Pass / Pass / Pass`; overlay compare is now `294 / 420 full matches`、`126 mismatches`、`58 metadata drifts`, targeted assocdev getter guardrails are `40 passed`, final full repo regression is `1660 passed`, `D020` stays in the verified fail-shaped mismatch bucket, and the next ready actionable compare-open case moves to `D042 RxUnicastPacketCount`.
> 2026-04-13 latest continuation after D033: `D033 MUUserPositionId` is now aligned via official rerun `20260413T142616419984`. Workbook authority is row `33`, not stale row `35` (`OperatingStandard`), and workbook v4.0.3 remains `Not Supported / Not Supported / Not Supported`. Current 0403 source survey only finds the read-only ODL declaration for `MUUserPositionId` and no active tr181-wifi implementation, while the rerun exact-closes the supported-band stub evidence: 5G `AssocMac5g=2c:59:17:00:04:85` with `MUUserPositionId=0`, 2.4G `AssocMac24g=2c:59:17:00:04:97` with `MUUserPositionId=0`, and 6G stays skipped in the current lab. The landed case now projects workbook-consistent `Not Supported / Not Supported / Not Supported`; overlay compare is now `293 / 420 full matches`、`127 mismatches`、`58 metadata drifts`, targeted MU-stub guardrails are `2 passed`, `D020` stays in the verified fail-shaped mismatch bucket, and the next ready actionable compare-open case moves to `D035 OperatingStandard`.
> 2026-04-13 latest continuation after D032: `D032 MUMimoTxPktsPercentage` is now aligned via official rerun `20260413T141305083695`. Workbook authority is row `32`, not stale row `34` (`MUUserPositionId`), and workbook v4.0.3 remains `Not Supported / Not Supported / Not Supported`. Current 0403 source survey only finds the read-only ODL declaration for `MUMimoTxPktsPercentage` and no active tr181-wifi implementation, while the rerun exact-closes the supported-band stub evidence: 5G `AssocMac5g=2c:59:17:00:04:85` with `MUMimoTxPktsPercentage=0`, 2.4G `AssocMac24g=2c:59:17:00:04:97` with `MUMimoTxPktsPercentage=0`, and 6G stays skipped in the current lab. The landed case now projects workbook-consistent `Not Supported / Not Supported / Not Supported`; overlay compare is now `292 / 420 full matches`、`128 mismatches`、`58 metadata drifts`, targeted MU-stub guardrails are `2 passed`, `D020` stays in the verified fail-shaped mismatch bucket, and the next ready actionable compare-open case moves to `D033 MUUserPositionId`.
> 2026-04-13 latest continuation after D030: `D030 MUGroupId` is now aligned via official rerun `20260413T135928729951`. Workbook authority is row `30`, not stale row `27` (`Capabilities`), and workbook v4.0.3 remains `Not Supported / Not Supported / Not Supported`. Current 0403 source survey only finds the read-only ODL declaration for `MUGroupId` and no active tr181-wifi implementation, while the rerun exact-closes the supported-band stub evidence: 5G `AssocMac5g=2c:59:17:00:04:85` with `MUGroupId=0`, 2.4G `AssocMac24g=2c:59:17:00:04:97` with `MUGroupId=0`, and 6G stays skipped in the current lab. The landed case now projects workbook-consistent `Not Supported / Not Supported / Not Supported`; overlay compare is now `291 / 420 full matches`、`129 mismatches`、`58 metadata drifts`, targeted D029/D030 not-supported guardrails are `2 passed`, `D020` stays in the verified fail-shaped mismatch bucket, and the next ready actionable compare-open case moves to `D032 MUMimoTxPktsPercentage`.
> 2026-04-13 latest continuation after D019: `D019 EncryptionMode / AssociatedDevice` is now aligned via official rerun `20260413T133308180539`. Workbook authority is row `19`, not stale row `16` (`DownlinkBandwidth`), the stale `wifi-llapi-r016-*` alias has been removed, and the older pass-shaped claim is now confirmed to have been stale row drift rather than workbook authority. The rerun exact-closes the intended fail-shaped 6G security context — STA `SSID=TestPilot_BTM`, `pairwise_cipher=CCMP`, `key_mgmt=SAE`, DUT `AssocMAC=2C:59:17:00:04:86`, and getter `EncryptionMode="Default"` — so the landed case now projects workbook-consistent `Fail / Fail / Fail` instead of the stale row-16 pass story. Overlay compare is now `290 / 420 full matches`、`130 mismatches`、`58 metadata drifts`, targeted D019 contract guardrail is `1 passed`, final full repo regression remains `1659 passed`, `D020` stays in the verified fail-shaped mismatch bucket, and the next ready actionable compare-open case moves to `D030 MuGroupID`.
> 2026-04-13 latest continuation after D014: `D014 ChargeableUserId` is now aligned via official rerun `20260413T132144592128`. Workbook authority is row `14`, not stale row `16` (`DownlinkBandwidth`), and workbook v4.0.3 remains `To be tested` / skip-shaped instead of plain pass. Current 0403 source only declares read-only `ChargeableUserId` together with Enterprise-only `RadiusChargeableUserId`; under the validated non-Enterprise `testpilot5G` / `WPA2-Personal` baseline the live associated STA `2C:59:17:00:04:85` therefore exact-closes `ChargeableUserId=""` and the landed case now projects workbook-consistent `Skip / Skip / Skip` without inventing a RADIUS path. Overlay compare is now `289 / 420 full matches`、`131 mismatches`、`58 metadata drifts`, targeted D014 / assocdev-getter guardrails are `41 passed`, final full repo regression is `1659 passed`, and the next ready actionable compare-open case moves to `D019 EncryptionMode / AssociatedDevice`.
> 2026-04-13 latest continuation after D057: `D057 TxUnicastPacketCount` is now aligned via official rerun `20260413T130448459477`. Workbook authority is row `57`, not stale row `59` (`UplinkBandwidth`), and workbook row 57 itself remains deliberately fail-shaped. The old custom `TestPilot_BTM` / `WPA3-Personal` / `SAE` replay was clean-start revalidated and rejected after COM0 still hit `wpaie set error (-7)` with `wpa_state=DISCONNECTED`, so the landed case now relies on the validated generic `testpilot5G` / `WPA2-Personal` baseline. Current 0403 rerun exact-closes same-STA fail-shaped evidence: `StaMac=AssocMAC=DriverAssocMac=2C:59:17:00:04:85`, `AssocTxPacketCount=DriverTxPacketCount=7`, `TxUnicastPacketCount=AssocTxUnicastPacketCount=0`, and `DriverTxUnicastPacketCount=7`; rerun `evaluation_verdict=Pass` with final raw `Fail / Fail / Fail`, exactly matching workbook authority. Overlay compare is now `288 / 420 full matches`、`132 mismatches`、`58 metadata drifts`, final full repo regression is `1657 passed`, and the next ready actionable compare-open case moves to `D014 ChargeableUserId`.
> 2026-04-13 latest continuation after D111-D113: `D111-D113 getStationStats() metadata drift trio` is now aligned via grouped official rerun `20260413T122417812289`. These three cases were already runtime-pass and compare-exact, but the authored files still carried a stale `source.row` drift of `113/114/115` instead of workbook-authoritative `111/112/113` for `AssociationTime`, `AuthenticationState`, and `AvgSignalStrength`. The cleanup is intentionally metadata-only: D111 exact-closes `AssociationTime="2026-04-07T21:50:29Z"`, D112 exact-closes `AuthenticationState=1`, and D113 exact-closes `AvgSignalStrength=0`, all in a single grouped rerun with `3/3 Pass`. Overlay compare therefore remains `287 / 420 full matches`、`133 mismatches`、`58 metadata drifts`, full repo regression remains `1658 passed`, and the next ready actionable compare-open case returns to `D057 TxUnicastPacketCount`.
> 2026-04-13 latest continuation after D110: `D110 getStationStats() Active / AccessPoint` is now aligned via official rerun `20260413T121358780961`. Workbook authority is row `110`, not row `112` (`AssociationTime`), and the stale case had the same nested-field drift as D109: evaluator read `AffiliatedSta[].Active=0` instead of the top-level `Active=1`. The calibrated closure now keeps the same-station MAC proof from `wl assoclist` + `getStationStats()`, then adds the workbook-H driver oracle by matching the `AUTHORIZED` line from `wl sta_info ${STA_MAC}`; the final exact-close is `AssocMac=2C:59:17:00:04:85`, `StationStatsMac=2C:59:17:00:04:85`, `TopLevelActive=1`, `StatsMatchesAssoc=1`, and `DriverAuthorized=1`. An initial strict `grep '^state:'` replay only proved the shell shape was too brittle, so the landed version keeps the same driver oracle but tolerates empty grep output and fails on evidence instead of step-shape. Targeted D110 tests remain `3 passed`, command budget remains `1 passed`, final full repo regression remains `1658 passed`, overlay compare is now `287 / 420 full matches`、`133 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D111`.
> 2026-04-13 latest continuation after D109: `D109 getStationStats() / AccessPoint` is now aligned via official rerun `20260413T115620062809`. Workbook authority is row `109`, not row `111` (`AssociationTime`), and the stale case had drifted in two coupled ways: it parsed nested `AffiliatedSta[].Active=0` instead of the top-level `Active=1`, and it tried to replay workbook `H` with `hostapd_cli sta` even though the current 0403 baseline exposes `/tmp/wl0_hapd.conf` without a matching `/var/run/hostapd/wl0` control socket. The calibrated closure now uses `wl assoclist` as the stable driver-side association oracle, extracts `TopLevelActive=1` explicitly, and exact-closes same-station evidence through `AssocMac` + `StationStatsMac` + `StatsMatchesAssoc=1`; the shared runtime guardrail also now treats `hostapd_cli` as an executable token instead of silently falling back to `verification_command`. Targeted D109 tests remain `4 passed`, command budget remains `1 passed`, final full repo regression is now `1658 passed`, overlay compare is now `286 / 420 full matches`、`134 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D110`.
> 2026-04-13 latest continuation after D108: `D108 UUID / AccessPoint.WPS` is now aligned via official rerun `20260413T113456092168`. Workbook authority is row `108`, not row `110`, and the real mismatch was a stale row plus an over-broad all-band pass story: the authored case treated UUID as `Pass / Pass / Pass`, but workbook row 108 keeps 6G `Not Supported` because wl1 hostapd has no `uuid=` projection even though the getter still returns a valid UUID. The calibrated case now exact-closes the real workbook shape: AP1 / AP3 / AP5 all return the same valid UUID via getter, wl0 / wl2 project the same value via hostapd `uuid=`, and wl1 exact-closes `HostapdUuid6g=`. Targeted D108 tests remain `3 passed`, command budget remains `1 passed`, final full repo regression remains `1657 passed`, overlay compare is now `285 / 420 full matches`、`135 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D109`.
> 2026-04-13 latest continuation after D106: `D106 RelayCredentialsEnable / AccessPoint.WPS` is now aligned via official rerun `20260413T112544193230`. Workbook authority is row `106`, not row `108` (`UUID`), and the real mismatch was another stale metadata/results_reference closure: the authored case already exact-closed live getter evidence `RelayCredentialsEnable=0` on all three bands, but it still pinned the row to `108` and synthetic raw `Pass / Fail / Pass`. Current 0403 source only exposes `RelayCredentialsEnable` as a persistent default-false bool and source survey found no active `wps_cred_processing` backing, so the calibrated case now keeps the tri-band getter evidence while aligning `results_reference` back to workbook row-106 authority `Not Supported / Not Supported / Not Supported`. Targeted D106 tests remain `2 passed`, command budget remains `1 passed`, final full repo regression remains `1657 passed`, overlay compare is now `284 / 420 full matches`、`136 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D108`.
> 2026-04-13 latest continuation after D105: `D105 PairingInProgress / AccessPoint.WPS` is now aligned via official rerun `20260413T111530183752`. Workbook authority is row `105`, not row `107`, and the real mismatch was a stale getter-only downgrade: row `105` is a method case that enables WPS, calls `InitiateWPSPBC()`, then proves `PairingInProgress` together with hostapd `wps_get_status`. Current 0403 source still exposes both `InitiateWPSPBC()` and `PairingInProgress`, so the calibrated case now exact-closes the real PBC flow: AP1 / AP5 return `Status=Success`, `PairingInProgress=1`, `PbcStatus=Active`, while AP3 / 6G under WPA3 / SAE returns `Status=Error_Other`, `PairingInProgress6g=0`, `PbcStatus6g=` and therefore remains `Not Supported`. Targeted D105 tests remain `2 passed`, final full repo regression remains `1657 passed`, overlay compare is now `283 / 420 full matches`、`137 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D106`.
> 2026-04-13 latest continuation after D104: `D104 Enable / AccessPoint.WPS` is now aligned via official rerun `20260413T105418577078`. Workbook authority is row `104`, and the real mismatch was stale baseline-gated case semantics rather than broken runtime behavior: row `104` only needs `Enable` setter/readback plus hostapd `wps_state` projection, but the stale authored case still assumed inherited WPS states and raw `Fail / Not Supported / Fail`. Current 0403 source still keeps `WPS.Enable` as a persistent bool with default `false`, `WPS.Configured=true`, and button-triggered WPS actions gated on `[WPS.Enable==1]`, so the calibrated case now normalizes each band to `Enable=0`, replays deterministic `0 -> 1 -> 0`, and exact-closes `wps_state=2` on AP1 / AP5 and `wps_state=0` on AP3 / 6G under WPA3 / SAE. Attempt 1 hit a transient `step_6g_setter` serialwrap timeout, attempt 2 exact-closed `Pass / Not Supported / Pass`, targeted D104 tests remain `3 passed`, final full repo regression remains `1657 passed`, overlay compare is now `282 / 420 full matches`、`138 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D105`.
> 2026-04-13 latest continuation after D101: `D101 ConfigMethodsEnabled` is now aligned via official rerun `20260413T103130805176`. Workbook authority is row `101`, not row `103` (`Configured`), and the real workbook row-101 procedure is a getter + hostapd projection check rather than the stale setter replay that had drifted into the case. Direct workbook replay plus current 0403 runtime evidence close the intended shape cleanly: AP1 / AP5 exact-close `CfgEnabled=PhysicalPushButton,VirtualPushButton` together with hostapd `config_methods=physical_push_button virtual_push_button`, while AP3 / 6G under the WPA3 / SAE baseline returns `CfgEnabled=None` and no hostapd `config_methods` line, matching the workbook note that 6G WPS remains `Not Supported`. Targeted D101 tests remain `4 passed`, final full repo regression is `1657 passed`, overlay compare is now `281 / 420 full matches`、`139 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D104`.
> 2026-04-13 latest continuation after D096: `D096 UAPSDEnable` is now aligned via official rerun `20260413T095836613095`. The stale authored case was still pinned to workbook row `98` even though row `98` is actually `WDSEnable`; workbook row `96` is the real `UAPSDEnable` row and still keeps BCM v4.0.3 at `Not Supported / Not Supported / Not Supported`. Active 0403 source nevertheless still exposes a real setter path: `wld_accesspoint.odl` keeps `UAPSDEnable` as a persistent bool with default `false`, and the Broadcom apply hook still writes it into `wl wme_apsd`. The calibrated case therefore preserves the tri-band live round-trip evidence (`0 -> 1 -> 0`, hostapd `uapsd_advertisement_enabled`, driver `wme_apsd`) while aligning `results_reference` back to workbook row 96 authority. Rerun `20260413T095836613095` exact-closed that evidence in one attempt, full repo regression remains `1656 passed`, overlay compare is `280 / 420 full matches`、`140 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D101`.
> 2026-04-13 latest continuation after D093: `D093 SSIDAdvertisementEnabled` is now aligned via official rerun `20260413T094515864676`. Workbook authority is row `93`, not row `95`, and active 0403 source keeps the pass path on the advertised state: TR-181 / ODL still declare `SSIDAdvertisementEnabled` default `true`, unfwlcfg tags still map it through bool-reverse `wlHide`, and `rcl_unfwireless.c` still enforces `SSIDAdvertisementEnabled ? wlHide=0 : wlHide=1`. The stale authored case had inverted that into a hidden-state `set 0 -> expect ignore_broadcast_ssid=2` story, while the old full-run mismatch still reflected the historical setter-to-getter substitution bug. Refreshing the case to workbook row `93`, restoring the idempotent `set 1 -> getter 1 -> hostapd ignore_broadcast_ssid 0` pass path on AP1 / AP3 / AP5, and adding a resolver guardrail let rerun `20260413T094515864676` exact-close tri-band `Pass / Pass / Pass` in one attempt. Targeted D093 tests are `4 passed`, full repo regression is now `1656 passed`, overlay compare is `279 / 420 full matches`、`141 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D096`.
> 2026-04-13 latest continuation after D092: `D092 WEPKey / AccessPoint.Security` is now aligned via official rerun `20260413T092400687838`. Workbook row `92` is a mixed-support closure: AP1 / AP5 must switch `ModeEnabled=WEP-128`, then exact-close `WEPKey=AABBCCDDEEFF0` with hostapd `wep_key0=...`, while AP3 / 6G stays `Not Supported` because aligned `D088` runtime evidence still shows `ModesSupported=None,WPA3-Personal,OWE`. The stale case was still pinned to row `94` and only wrote `WEPKey` under the WPA2 / WPA3 baseline; the first confirmation rerun `20260413T092109402810` then re-proved the last remaining defect was only a case-side hostapd extractor quoting bug. Refreshing the case to workbook row `92`, moving 5G / 2.4G onto the real WEP-128 path, keeping 6G on an explicit unsupported proof branch, and reshaping the extractor to double-quote / `${line:-ABSENT}` form let rerun `20260413T092400687838` exact-close `Pass / Not Supported / Pass` in one attempt. Targeted D092 tests are `4 passed`, full repo regression is now `1655 passed`, overlay compare is `278 / 420 full matches`、`142 mismatches`、`58 metadata drifts`, and the next ready actionable open case is `D093`.
> 2026-04-12 latest continuation: `D331 MulticastPacketsSent`、`D333 PacketsSent`、`D324 BytesSent`、`D295 scan()` 都已完成 official-runner 對齊。`D331` 由 raw-first single-snapshot source-backed formula 在 rerun `20260412T040941971904` exact-close `904/904/904`、`732/732/732`、`973/973/973`；`D333` 則在 shared 6G baseline hot path 補上 driver-assoc fallback、pre-restart `ocv=0` acceptance 與 generic `sleep 15` 後，於 rerun `20260412T054702963914` exact-close `461/461/461`、`592/592/592`、`707/707/707`；`D324` 則以 raw-first single-snapshot `txbyte + matching wds txbyte` 公式在 rerun `20260412T060612008433` exact-close `1141986/1141986/1141986`、`1113827/1113827/1113827`、`1186105/1186105/1186105`；`D295` 則在 resolving rerun `20260412T064317622551` 以 prepared `STA + links` + `scan()` first-BSSID same-target presence in `wl escanresults` exact-close `62:15:db:9e:31:f1`、`86:82:fe:58:ac:a6`、`6a:d7:aa:02:d7:bf`。其後 `D281` 也已正式對齊：official rerun `20260412T080123446178` 證明 named-arg `scanCombinedData(channels=36/5/1,minRssi=-127,scanReason=Ssid)` 會回傳 paired `BSS + Spectrum`，same-target `getScanResults(minRssi=-127)` 則 exact-close `Noise`（5G `-100/-100/-100`、6G `-97/-97/-97`、2.4G `-76/-76/-76`），metadata 已由 stale row `283` 刷新為 workbook row `281`。`D282` 也已正式對齊：official rerun `20260412T080338867826` 證明相同的 named-arg `scanCombinedData().BSS` family 可 exact-close same-target `OperatingStandards`（5G `a,n,ac,ax,be`、6G `ax,be`、2.4G `b,g,n,ax`），metadata 已由 stale row `284` 刷新為 workbook row `282`。目前這個 open-set workstream 的 repo-authoritative true-open set 已清零。
> 2026-04-12 late continuation: invalid full run `20260412T084218316557` was stopped after early `D007 Fail`、`D009 FailEnv`、`D010 FailEnv`、`D011 FailTest` multi-band instability. WAL evidence pinned the remaining shared-baseline issue to a DUT 6G missing-adapter recovery path (`wl -i wl1 bss -> wl driver adapter not found`) that still tried a direct `wl1` hostapd restart. The runtime now short-circuits missing-adapter probes in `_wait_for_dut_bss_ready()` and `_apply_6g_ocv_fix()`, both boards were hard-reset and recovered back to `READY`, and patched sequential reruns `D009`/`D010`/`D011` (`20260412T110545613993` / `20260412T111048362099` / `20260412T111549474171`) all returned `Pass/Pass/Pass` on the same baseline. Regression after this fix is now `8 passed` on the targeted 6G recovery tests and `1645 passed` on the full repo suite; the next operational step is to commit/push this recovery patch and relaunch full run from current HEAD.
> 2026-04-12 latest continuation after relaunch: recovery commit `338891b7115e5c41d04f45bd79c70ce4b117cebc` is now pushed, and authoritative full run `20260412T113008433351` completed all 420 cases without reproducing the old early baseline collapse (`D004`~`D013` all stayed `Pass/Pass/Pass`). `compare-0401` on that run raised the snapshot to `235 / 420 full matches`、`185 mismatches`、`62 metadata drifts`, with `156` actionable workbook-Pass gaps. `D024 LastDataDownlinkRate` then aligned via official rerun `20260412T172957084134`: stale alias metadata was removed, `source.row` is refreshed from `21` to workbook row `24`, and live evidence exact-closed LLAPI `487400` against the source-backed driver last-tx bucket window `[487400,487500]`. Targeted D024 tests stayed `6 passed`, full repo regression stayed `1645 passed`, overlay compare is now `236 / 420 full matches`、`184 mismatches`、`62 metadata drifts`, actionable workbook-Pass gaps are down to `155`, and the next ready case is `D025`.
> 2026-04-12 latest continuation after D025: `D025 LastDataUplinkRate` is now aligned via official rerun `20260412T174551843336`. Stale alias metadata is removed, `source.row` is refreshed from `22` to workbook row `25`, and live evidence exact-closed `LastDataUplinkRate=6000` with `DriverLastUplinkRateRounded=6000`; workbook `H`, public HAL comments, and repeated runtime replays all continue to support driver `rate of last rx pkt` as the STA -> AP truth source. Targeted D025 tests stayed `3 passed`, overlay compare is now `237 / 420 full matches`、`183 mismatches`、`62 metadata drifts`, actionable workbook-Pass gaps are down to `154`, `D020` remains a verified fail-shaped mismatch, and the next ready case is `D022`.
> 2026-04-12 latest continuation after D022: `D022 HtCapabilities` is now aligned via official rerun `20260412T175538121906`. Stale alias metadata is removed, `source.row` is refreshed from `19` to workbook row `22`, and the driver oracle is now the parsed `wl sta_info` `HT caps 0x...` bitmask instead of a brittle token scrape; live evidence exact-closed `HtCapabilities="40MHz,SGI20,SGI40"` with `DriverHt40MHz=1`, `DriverHtSgi20=1`, and `DriverHtSgi40=1`. Targeted D022 tests stayed `1 passed`, overlay compare is now `238 / 420 full matches`、`182 mismatches`、`62 metadata drifts`, actionable workbook-Pass gaps are down to `153`, `D020` remains a verified fail-shaped mismatch, and the next ready case is `D034`.
> 2026-04-12 latest continuation after D072: the shared runtime bug behind setter-style single-line `capture` steps is now narrowed and fixed in `CommandResolver`: explicit executable setters are no longer replaced by synthesized readback queries before execution. `D072 MobilityDomain` first proved the real setter path via official rerun `20260412T231545173827` (`pass after retry (2/2)`), then stabilized on repeat rerun `20260412T231709014359` (`Pass` on attempt 1). Live DUT evidence exact-closes AP1/AP3/AP5 with `IEEE80211r.Enabled=1`, northbound `MobilityDomain=27476`, hostapd `mobility_domain=546B`, and one `ft_over_ds=0` line per band; committed metadata is now workbook row `72` with `results_reference.v4.0.3 = Pass / Pass / Pass`. Overlay compare recomputed on top of authoritative full run `20260412T113008433351` plus D024 / D025 / D022 / D072 reruns is now `239 / 420 full matches`、`181 mismatches`、`62 metadata drifts`, and actionable workbook-Pass gaps are down to `152`; the next ready `step_command_failed` revisit is the tight-coupled `D047` / `D050` pair, with remaining open set `D047`、`D050`、`D079`、`D088`、`D460`、`D494`.
> 2026-04-13 latest continuation after D047/D050: the tight-coupled `D047 SupportedHe160MCS` / `D050 SupportedVhtMCS` pair is now aligned via official reruns `20260412T235952361188` / `20260413T000249620932`. The closure combined a case-side revert to the authoritative generic 5G baseline (`testpilot5G` / `WPA2-Personal`) with a runtime classifier fix in `_env_command_succeeded()`: valid getter output `error=4 / message=parameter not found` now reaches `pass_criteria` evaluation instead of being preemptively marked `step_command_failed`, while direct `AssociatedDevice.*.MACAddress?` probes still require a concrete MAC. `source.row` is now refreshed to workbook rows `47` / `50`; overlay compare remains `239 / 420 full matches`、`181 mismatches`、`62 metadata drifts` because both workbook rows are non-pass semantics, and the remaining `step_command_failed` open set is `D079`、`D088`、`D460`、`D494`.
> 2026-04-13 latest continuation after D079 runtime fix: `CommandResolver.sanitize_cli_fragment()` no longer strips the `WiFi....=...` setter tail from complex shell fragments that legitimately start with variable assignment / `$(` / pipe composition. Official rerun `20260413T002418591720` confirms `D079 MACFiltering.Mode` is no longer `step_command_failed`: both attempts executed the full AP1/AP3/AP5 setter/getter sequence and converged to `Mode=Off`, no hostapd ACL lines, and `SetOffStatus*=invalid_value` on all three bands. That moves `D079` into the semantic `pass_criteria_not_satisfied` bucket because the current YAML still expects 5G `BlackList` / `deny`; the remaining `step_command_failed` open set is now `D088`、`D460`、`D494`, and the next ready revisit is `D088`.
> 2026-04-13 latest continuation after D088 closure: `D088 ModesSupported` is now aligned via official rerun `20260413T003340845889`. The only missing piece after the shared setter-capture fix was output shaping: step2/4/6 now preserve the real read-only setter path and emit parsable `error=15` / `message=is read only` lines, which lets `setter_5g/6g/24g.error` satisfy the workbook-pass criteria instead of falling back to aggregate output. `source.row` is now refreshed to workbook row `88`; overlay compare is now `240 / 420 full matches`、`180 mismatches`、`62 metadata drifts`, actionable workbook-Pass gaps are `151`, and the remaining `step_command_failed` open set is `D460`、`D494` with next ready revisit `D460`.
> 2026-04-13 latest continuation after D460/D494 closure: the last two `step_command_failed` cases are now cleared. `D460` was not a runtime blocker anymore; it was a stale workbook-mapping bug (`HECapabilities`, row `462`) even though workbook row `460` and live 0403 `wld_radio.odl` expose `HePhyCapabilities`, and official rerun `20260413T005520941756` now exact-closes the three base64 getters. `D494` required a different repair: workbook row `494` wants protected 5G `VHTCapabilities` plus plain 6G / 2.4G `parameter not found`, so the case now uses an explicit protected shell capture for 5G and parsed `error=4` / `message=parameter not found` for the other two bands; official rerun `20260413T005633950804` passes in one attempt. Overlay compare is now `242 / 420 full matches`、`178 mismatches`、`61 metadata drifts`; the focused `step_command_failed` queue is empty, `D079` remains in the semantic `pass_criteria_not_satisfied` bucket, env-only `D328` / `D336` remain separate, blocked `D053` remains, and the next ready phase is the calibration / mapping / results_reference bucket.
> 2026-04-13 latest continuation after D461: `D461 HTCapabilities` is now aligned via official rerun `20260413T010944709855`. The full-run trace had already been `evaluation_verdict=Pass`; the only real defect was stale metadata (`source.row=338`, `results_reference.v4.0.3 = Fail / Fail / Fail`) even though workbook row `461` is `Pass / Pass / Pass`. Refreshing the case to workbook row `461` closes that drift cleanly. Overlay compare is now `243 / 420 full matches`、`177 mismatches`、`61 metadata drifts`, and the next ready phase-2 mapping/results_reference revisit is `D462`.
> 2026-04-13 latest continuation after D462: `D462 BssColor` is now aligned via official rerun `20260413T011655056430`. Like `D461`, the authoritative full-run trace had already been `evaluation_verdict=Pass`; the remaining defects were stale metadata (`source.row=339`, `results_reference.v4.0.3 = Fail / Fail / Fail`) plus stale source-object mapping (`WiFi.Radio.{i}.` instead of `WiFi.Radio.{i}.IEEE80211ax.`). Refreshing the case to workbook row `462`, object `WiFi.Radio.{i}.IEEE80211ax.`, and raw `Pass / Pass / Pass` closes both the verdict mismatch and the metadata drift. Overlay compare is now `244 / 420 full matches`、`176 mismatches`、`60 metadata drifts`, and the next ready phase-2 mapping/results_reference revisit is `D463`.
> 2026-04-13 latest continuation after D463: `D463 HESIGASpatialReuseValue15Allowed` is now aligned via official rerun `20260413T012358700786`. This case stayed in the same `trace already Pass / compare still Fail-shaped` family as `D462`: the only remaining defects were stale metadata (`source.row=340`, `results_reference.v4.0.3 = Fail / Fail / Fail`) plus stale source-object mapping (`WiFi.Radio.{i}.` instead of `WiFi.Radio.{i}.IEEE80211ax.`). Refreshing it to workbook row `463`, object `WiFi.Radio.{i}.IEEE80211ax.`, and raw `Pass / Pass / Pass` closes both gaps. Overlay compare is now `245 / 420 full matches`、`175 mismatches`、`59 metadata drifts`, and the next ready phase-2 mapping/results_reference revisit is `D465`.
> 2026-04-13 latest continuation after D465: `D465 SRGInformationValid` is now aligned via official rerun `20260413T013010016650`. This case remained in the same `trace already Pass / compare still Fail-shaped` family as `D462` / `D463`, but it also kept a stale 6G live annotation (`live=1`) even though both the authoritative full-run trace and the rerun read back `0` on all three radios. Refreshing it to workbook row `465`, object `WiFi.Radio.{i}.IEEE80211ax.`, raw `Pass / Pass / Pass`, and the corrected 6G live note closes both the verdict mismatch and the metadata drift. Overlay compare is now `246 / 420 full matches`、`174 mismatches`、`58 metadata drifts`, and the next ready phase-2 mapping/results_reference revisit is `D467`.
> 2026-04-13 latest continuation after D467: `D467 RxBeamformingCapsEnabled` is now aligned via official rerun `20260413T013545364055`. This case was even lower risk than `D465`: the authoritative full-run trace was already `evaluation_verdict=Pass` with stable `DEFAULT / DEFAULT / DEFAULT`, and the only remaining defects were stale workbook row `343` plus stale raw `Fail / Fail / Fail`; no object drift or command rewrite was needed. Refreshing it to workbook row `467` and raw `Pass / Pass / Pass` raises the overlay compare to `247 / 420 full matches`、`173 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves back to `D035`, whose authoritative full-run trace already shows `evaluation_verdict=Pass` while the case still carries stale row `37`.
> 2026-04-13 latest continuation after the D035 trial: `D035 AssociatedDevice OperatingStandard` is now explicitly blocked rather than silently left in dirty local state. The local tri-band rewrite was tested via official rerun `20260413T014428270219`, but both attempts still died at `step1_5g_sta_join` (`iw dev wl0 link -> Not connected.`) even though the same STA verify-env log already showed `SSID: testpilot5G` and `wpa_state=COMPLETED`. A reconnect trial rerun `20260413T015210910141` then proved the issue had moved forward into shared runtime instability: the run no longer died immediately at 5G join, but instead looped on repeated 6G `ocv=0` / `ATTACH` recovery and was stopped without a stable final verdict. The tri-band rewrite was reverted, compare stays at `247 / 420 full matches`、`173 mismatches`、`58 metadata drifts`, `plugins/wifi_llapi/reports/D035_block.md` is now the authority file for this blocker, and the next ready workbook-Pass revisit moves to `D045`.
> 2026-04-13 latest continuation after D045: `D045 SignalStrength` is now aligned via official rerun `20260413T020657288045`. This case did not need a semantic rewrite: the authoritative trace already evaluated `Pass`, and the rerun exact-closed the same 5G AssociatedDevice MAC against `SignalStrength=-33` plus the same-STA driver sample `DriverSignalStrength=-33`. The only remaining defects were stale workbook row `47` and stale raw `2.4g=Fail`, so refreshing the case to workbook row `45` and raw `Pass / Pass / Pass` raises the overlay compare to `248 / 420 full matches`、`172 mismatches`、`58 metadata drifts`. The next ready low-risk workbook-Pass revisit now moves to `D046`.
> 2026-04-13 latest continuation after D046: `D046 SignalStrengthByChain` is now aligned via official rerun `20260413T021655844208`. This was the same metadata/results_reference closure family as `D045`: the authoritative trace already evaluated `Pass`, and the rerun exact-closed `AssociatedDevice.1.SignalStrengthByChain="-33.0,-32.0,-41.0,-34.0"` against the same-STA driver sample `DriverSignalStrengthByChain=-33.0,-32.0,-41.0,-34.0` for 5G MAC `2C:59:17:00:04:85`. The only remaining defects were stale workbook row `48` and stale raw `Fail / N/A / N/A`, so refreshing the case to workbook row `46` and raw `Pass / Pass / Pass` raises the overlay compare to `249 / 420 full matches`、`171 mismatches`、`58 metadata drifts`. The next ready source-row drift revisit now moves to `D014`.
> 2026-04-13 latest continuation after D061: `D061 UplinkShortGuard` is now aligned via official rerun `20260413T022541033440`. This stayed in the same low-risk metadata/results_reference family as `D045` / `D046`: the authoritative trace already evaluated `Pass`, and the rerun exact-closed the post-trigger AssociatedDevice snapshot `UplinkShortGuard=1` against the same-STA driver GI token `1.6us` plus the derived boolean `DriverUplinkShortGuard=1` for MAC `2C:59:17:00:04:85`. The only remaining defects were stale workbook row `63` and stale raw `Pass / N/A / N/A`, so refreshing the case to workbook row `61` and raw `Pass / Pass / Pass` raises the overlay compare to `250 / 420 full matches`、`170 mismatches`、`58 metadata drifts`. The next ready mixed-verdict workbook revisit now moves to `D028`; the previously guessed `D014` source-row drift bucket is not a workbook-Pass closure because compare still expects `Skip`.
> 2026-04-13 latest continuation after D028: `D028 MaxBandwidthSupported` is now aligned via official rerun `20260413T023259417785`. This case closes as a mixed-verdict workbook row, not as an all-pass case: executed 5G / 2.4G evidence still evaluates `Pass` (`MaxBandwidthSupported="160MHz"` on AP1 and `MaxBandwidthSupported="40MHz"` on AP5), while workbook row `28` remains explicitly fail-shaped on 6G and the current lab still skips AP3/wl1 because STA association is `BCME_NOTREADY`. Refreshing the case from stale workbook row `25` / raw `Pass / Pass / Pass` to workbook row `28` / raw `Pass / Fail / Pass` raises the overlay compare to `251 / 420 full matches`、`169 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D065`.
> 2026-04-13 latest continuation after D065: `D065 BridgeInterface` is now aligned via official rerun `20260413T024240506323`. This returned to the low-risk metadata/results_reference family: the authoritative trace already evaluated `Pass`, and the rerun exact-closed AP1 / AP3 / AP5 `BridgeInterface="br-lan"` against both hostapd `bridge=br-lan` config lines and the live Linux bridge masters `BridgeMaster5g/6g/24g=br-lan`. The only remaining defects were stale workbook row `67` and stale raw `Fail / Fail / Fail`, so refreshing it to workbook row `65` and raw `Pass / Pass / Pass` raises the overlay compare to `252 / 420 full matches`、`168 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D081`.
> 2026-04-13 latest continuation after D081: `D081 MBOEnable` is now aligned via official rerun `20260413T025449283775`. This case required a source-backed oracle rewrite rather than a metadata-only refresh: active 0403 source `wifi_ap.c` maps both `handle_set_ap_mbo_enable()` and `handle_get_ap_mbo_enable()` to `wl -i <if> mbo ap_enable`, so the earlier hostapd `mbo=` fail-shaped probe was not aligned to the real backing field. The committed rewrite now forces a clean `MBOEnable=0` baseline in setup, then exact-closes `ubus-cli ... MBOEnable?` against direct `wl mbo ap_enable` readback across `0 -> 1 -> 0` on AP1 / AP3 / AP5; refreshing the metadata from workbook row `75` to row `81` and raw `Pass / Pass / Pass` raises the overlay compare to `253 / 420 full matches`、`167 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D094`.
> 2026-04-13 latest continuation after D094: `D094 AccessPoint.Status` is now aligned via official rerun `20260413T030202219754`. This returned to the low-risk metadata/results_reference family: the authoritative full-run trace had already exact-closed tri-band `Status="Enabled"` against direct driver `wl -i wl{0,1,2} bss = up`, and the isolated rerun reproduced the same one-attempt Pass shape. The only remaining defects were stale workbook row `96`, stale raw `Fail / Fail / Fail`, and an internal `COM0/COM1` transport note mismatch, so refreshing the case to workbook row `94`, raw `Pass / Pass / Pass`, and consistent COM1 transport raises the overlay compare to `254 / 420 full matches`、`166 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D095`.
> 2026-04-13 latest continuation after D095: `D095 UAPSDCapability` is now aligned via official rerun `20260413T030853360475`. This returned to the low-risk metadata/results_reference family again: the authoritative full-run trace had already exact-closed tri-band read-only `UAPSDCapability=1`, and the isolated rerun reproduced the same shape together with `HapdUapsd=0` and `DriverWmeApsd=0`, which indicate the feature is not currently active but do not contradict the capability getter. The only remaining defects were stale workbook row `97`, stale raw `Fail / Fail / Fail`, and an internal `COM0/COM1` transport note mismatch, so refreshing the case to workbook row `95`, raw `Pass / Pass / Pass`, and consistent COM1 transport raises the overlay compare to `255 / 420 full matches`、`165 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D098`.
> 2026-04-13 latest continuation after D098: `D098 WDSEnable` is now aligned via official rerun `20260413T031458311484`. This remained in the low-risk metadata/results_reference family: the authoritative full-run trace had already exact-closed tri-band setter round-trip `baseline=0 -> set=1 -> restore=0` against direct driver `dwds 0 -> 1 -> 0`, and the isolated rerun reproduced the same one-attempt Pass shape on AP1 / AP3 / AP5. The only remaining defects were stale workbook row `100`, stale raw `Fail / Fail / Fail`, and an internal `COM0/COM1` transport note mismatch, so refreshing the case to workbook row `98`, raw `Pass / Pass / Pass`, and consistent COM1 transport raises the overlay compare to `256 / 420 full matches`、`164 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D099`.
> 2026-04-13 latest continuation after D099: `D099 WMMCapability` is now aligned via official rerun `20260413T032521733067`. This also returned to the low-risk metadata/results_reference family after source review: active 0403 still routes `wifi_getApWMMCapability()` through `wldm_AccessPoint_WMMCapability()`, which probes driver iovar `wme` and returns `TRUE` on successful read, with no 6G-specific branch in the getter path. The authoritative full-run trace had already exact-closed tri-band `WMMCapability=1` and `hostapd wmm_enabled=1`, and the isolated rerun reproduced the same one-attempt Pass shape. The only remaining defects were stale workbook row `101`, stale raw `Pass / Fail / Pass`, and an internal `COM0/COM1` transport note mismatch, so refreshing the case to workbook row `99`, raw `Pass / Pass / Pass`, and consistent COM1 transport raises the overlay compare to `257 / 420 full matches`、`163 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D114`.
> 2026-04-13 latest continuation after D114: `D114 getStationStats() AvgSignalStrengthByChain` is now aligned via official rerun `20260413T033856175894`. This was a source-backed stale-scope closure rather than a pure metadata refresh: active 0403 `whm_brcm_ap_mlo_fillAssocDevInfo()` still fills `pAD->AvgSignalStrengthByChain = wld_ad_getAvgSignalStrengthByChain(pAD)`, and the ODL still exposes the field as a volatile read-only int32 with no band-specific branch. The old case had been left as a 5G-only artifact (`Fail / N/A / N/A`) even though workbook row `114` expects tri-band Pass, so the committed rewrite expands the topology and steps to a tri-band sequential `getStationStats()` replay. Official rerun `20260413T033856175894` exact-closed `AvgSignalStrengthByChain=-33` (5G), `-85` (6G), and `-23` (2.4G); verify_env had to absorb transient 6G OCV/hostapd recovery noise, but the case itself still finished `Pass` in one attempt. This raises the overlay compare to `258 / 420 full matches`、`162 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D115`.
> 2026-04-13 latest continuation after D115: `D115 getStationStats() ConnectionDuration` is now aligned via official rerun `20260413T035856845825`. This was another source-backed stale-scope rewrite, but for a live counter rather than a static field: the ODL still exposes `ConnectionDuration` as a volatile read-only uint32, `wlgetStationInfo()` still parses driver `wl sta_info ... in network` into `connectTime`, and `local_wl_util.c` still copies `staInfo.connectTime` into the higher-level station structure with no band-specific branch in the path. The old case had been left as a 5G-only `Fail / N/A / N/A` artifact even though workbook row `115` expects tri-band Pass, so the committed rewrite expands it to tri-band sequential validation and proves the counter is live by reading `getStationStats()` twice per band and cross-checking the same STA against driver age. Official rerun `20260413T035856845825` exact-closed `7 -> 10 <= 12` on 5G, `11 -> 14 <= 16` on 6G, and `9 -> 13 <= 15` on 2.4G; verify_env again had to absorb transient 6G OCV and wl0 supplicant recovery noise, but the case still finished `Pass` in one attempt. This raises the overlay compare to `259 / 420 full matches`、`161 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D174`.
> 2026-04-13 latest continuation after D174: `D174 ActiveAntennaCtrl` is now aligned via official rerun `20260413T042647797154`. This returned to the low-risk metadata/results_reference family, but with a source-backed explanation for why workbook `Pass` legitimately coexists with getter value `-1`: active 0403 `wld_radio.odl` still declares `ActiveAntennaCtrl` as a persistent int32 defaulting to `-1`, `wld.h` still keeps `actAntennaCtrl` / `txChainCtrl` / `rxChainCtrl` in the radio model, and the vendor path (`whm_brcm_rad_antenna_map()` / `whm_brcm_rad_mod_chains()`) still uses `actAntennaCtrl` as the fallback when specific chain controls are unset. So the northbound getter staying at `-1` is the driver-default sentinel, while downstream `wl txchain` / `wl rxchain` masks remain compatible concrete realizations rather than a contradiction. The authoritative full-run trace had already exact-closed `ActiveAntennaCtrl=-1` on all three radios, and the isolated rerun reproduced the same one-attempt Pass shape. The only remaining defects were stale workbook row `138` and stale raw `Fail / Fail / Fail`, so refreshing the case to workbook row `174` and raw `Pass / Pass / Pass` raises the overlay compare to `260 / 420 full matches`、`160 mismatches`、`58 metadata drifts`. The next ready workbook-Pass revisit now moves to `D176`.
> 2026-04-13 latest continuation after D176: `D176 BeaconPeriod` is now aligned via official rerun `20260413T044907394777`. This one was not another low-risk metadata closure: workbook row `176` explicitly expects a writable AP-side setter path (`100 -> 1000 -> 100`) plus downstream hostapd `beacon_int` convergence, so the stale getter-only authored shape at row `139` / raw `Fail / Fail / Fail` had to be replaced with a source-backed AP-only rewrite. The committed case now forces tri-band `BeaconPeriod=100` in setup, then exact-closes `ubus-cli ... BeaconPeriod?` and `/tmp/wl{0,1,2}_hapd.conf` `beacon_int` across `100 -> 1000 -> 100` on all three radios in one official rerun. Refreshing the case to workbook row `176` / raw `Pass / Pass / Pass` raises the overlay compare to `261 / 420 full matches`、`159 mismatches`、`58 metadata drifts`; `D020` remains the verified fail-shaped mismatch, `D035` remains blocked, and the next clean stale-radio rewrite target now moves to `D188`.
> 2026-04-13 latest continuation after D188: `D188 DTIMPeriod` is now aligned via official rerun `20260413T050318932313`. This was another workbook-authoritative AP-only setter rewrite rather than a metadata-only refresh: workbook row `188` explicitly expects default getter `3`, writable `DTIMPeriod=7`, and downstream hostapd `dtim_period=7`, then a clean restore back to `3`. The committed case now forces tri-band `DTIMPeriod=3` in setup, then exact-closes `ubus-cli ... DTIMPeriod?` and `/tmp/wl{0,1,2}_hapd.conf` `dtim_period` across `3 -> 7 -> 3` on all three radios in one official rerun. Refreshing the case from stale workbook row `151` / raw `Fail / Fail / Fail` to workbook row `188` / raw `Pass / Pass / Pass` raises the overlay compare to `262 / 420 full matches`、`158 mismatches`、`58 metadata drifts`; `D020` remains the verified fail-shaped mismatch, `D035` remains blocked, and the next ready workbook-Pass revisit now moves to `D034`.
> 2026-04-13 latest continuation after D034: `D034 Noise` is now aligned via official rerun `20260413T052709875993`. The first confirmation rerun `20260413T052117345208` failed in `setup_env` (`iw dev wl0 link -> Not connected.`), which exposed the real blocker: the case still carried a stale custom `TestPilot_BTM` / `WPA3-Personal` baseline instead of the authoritative generic 5G path already proven by full run `20260412T113008433351`. Pulling D034 back to `testpilot5G` / `WPA2-Personal`, dropping the custom `sta_env_setup`, refreshing stale workbook row `36` to row `34`, and rewriting the stale `Noise=0` oracle into a same-STA live noise compare restored the real pass path; the rerun exact-closed `AssociatedDevice.1.Noise=-100` against `DriverNoise=-100` for MAC `2C:59:17:00:04:85`. Overlay compare is now `263 / 420 full matches`、`157 mismatches`、`58 drifts`; `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, and the next ready workbook-Pass revisit now moves to `D059`.
> 2026-04-13 latest continuation after D059: `D059 UplinkBandwidth` is now aligned via official rerun `20260413T055159421076`. This was not a metadata-only refresh: the case still carried stale workbook row `61`, a drifted custom `TestPilot_BTM` / `WPA3-Personal` baseline, and a weak driver reread path that never drove deterministic uplink traffic before sampling `rx nrate`. The committed rewrite restores the authoritative generic `testpilot5G` / `WPA2-Personal` baseline already proven by full run `20260412T113008433351`, adds an explicit STA uplink trigger (`ifconfig wl0 192.168.1.3 ...` plus `ping -I wl0 -c 8 -W 1 192.168.1.1`), and re-reads the same post-trigger `AssociatedDevice.1` slot against `wl sta_info ... rx nrate`. Official rerun exact-closes `UplinkBandwidth=20`, `AssocMacAfterTrigger=DriverAssocMac=2C:59:17:00:04:85`, and `DriverUplinkBandwidth=20`; metadata is now workbook row `59` / raw `Pass / Pass / Pass`. Overlay compare is now `264 / 420 full matches`、`156 mismatches`、`58 metadata drifts`, full repo regression is `1654 passed`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, and the next ready workbook-Pass revisit now moves to `D060`.
> 2026-04-13 latest continuation after D060: `D060 UplinkMCS` is now aligned via official rerun `20260413T060855269192`. This was a row+parser closure rather than a semantic rewrite: the authoritative full-run trace had already exact-closed same-STA `UplinkMCS` against `wl sta_info ... rx nrate`, but the case still carried stale workbook row `62` and a fragile step4 pipeline that extracted duplicate MAC tokens because `sed ... MACAddress` was missing `| head -n 1`. Refreshing metadata back to workbook row `60` and hardening the same-STA extractor removed that validator-only failure shape; the rerun exact-closed `UplinkMCS=10`, `AssocMacAfterTrigger=DriverAssocMac=2C:59:17:00:04:85`, and `DriverUplinkMCS=10`. Overlay compare is now `265 / 420 full matches`、`155 mismatches`、`58 metadata drifts`, full repo regression remains `1654 passed`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, and the next ready workbook-Pass revisit now moves to `D062`.
> 2026-04-13 latest continuation after D062: `D062 VendorOUI` is now aligned via official rerun `20260413T061743676049`. This was not a fail-shaped source gap: both the authoritative full-run trace and the confirmation rerun exact-closed the same non-empty VendorOUI list through the direct getter, the same-entry `AssociatedDevice.1` snapshot, and the same-STA `wl sta_info` driver capture. The real defects were stale workbook row `64` and an outdated fail-shaped contract that still expected `VendorOUI=""` while live 0403 returned `00:90:4C,00:10:18,00:50:F2,50:6F:9A` consistently for the same associated STA. Refreshing metadata back to workbook row `62` and restoring the pass-shaped same-STA equality contract removed the mismatch; the rerun exact-closed `VendorOUI=AssocVendorOUI=DriverVendorOUIList=00:90:4C,00:10:18,00:50:F2,50:6F:9A` with `DriverVendorOUICount=4`. Overlay compare is now `266 / 420 full matches`、`154 mismatches`、`58 metadata drifts`, full repo regression remains `1654 passed`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, and the next ready workbook-Pass revisit now moves to `D063`.
> 2026-04-13 latest continuation after D063: `D063 VhtCapabilities` is now aligned via official rerun `20260413T062615392940`. This was another row+oracle refresh, but with a source-backed subset nuance rather than plain getter/driver equality: the authoritative full-run trace had already shown a concrete same-STA direct getter and snapshot, yet the case still carried stale workbook row `65` and a fail-shaped contract that expected `VhtCapabilities=""`. The first confirmation rerun proved that exact equality against the `wl sta_info` `VHT caps` line was too strict because live 0403 exposed `VhtCapabilities=SGI80,SGI160,SU-BFR,SU-BFE,MU-BFE` through LLAPI/snapshot while the human-readable driver line only carried the subset `SGI80,SGI160,SU-BFR,SU-BFE`; source evidence in `wlu_common.c`, `local_wl_util.c`, and `swl_staCap.h` confirms those are related but not identical renderings. Refreshing metadata back to workbook row `63`, restoring the pass-shaped non-empty same-STA getter/snapshot contract, and keeping the driver as a same-STA subset sanity oracle removed the mismatch; the rerun exact-closed `VhtCapabilities=AssocVhtCapabilities=SGI80,SGI160,SU-BFR,SU-BFE,MU-BFE` with driver subset `SGI80,SGI160,SU-BFR,SU-BFE`. Overlay compare is now `267 / 420 full matches`、`153 mismatches`、`58 metadata drifts`, full repo regression remains `1654 passed`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable workbook-Pass revisit now moves to `D070`.
> 2026-04-13 latest continuation after D070: `D070 Enable` is now aligned via official rerun `20260413T063442091882`. This was a clean workbook row/oracle reset rather than a runtime bug: the case still carried stale workbook row `72` and an over-authored AP toggle/readback path, even though workbook row `70` only expects tri-band `Enable=1` with the driver BSS state up. Pulling the case back to workbook row `70`, keeping it AP-only, and reducing the oracle to per-band `Enable` getter plus `wl -i wl{0,1,2} bss` removed the mismatch cleanly; the rerun exact-closed `Enable5g=1`, `Enable6g=1`, `Enable24g=1`, and `DriverBss5g/6g/24g=up` in one attempt. Overlay compare is now `268 / 420 full matches`、`152 mismatches`、`58 metadata drifts`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable workbook-Pass revisit now moves to `D071`.
> 2026-04-13 latest continuation after D071: `D071 FTOverDSEnable` is now aligned via official rerun `20260413T064002607672`. This was not a runtime semantic gap: the case still carried stale workbook row `73`, pre-applied `IEEE80211r.Enabled` / `MobilityDomain` writes in `sta_env_setup`, and stale `results_reference.v4.0.3 = To be tested / To be tested / To be tested`. That stale setup order made `setup_env` sample `wl -i wl0/wl1 bss` during transient 11r hostapd restart/down instead of the workbook row `71` execution order. Refreshing metadata back to workbook row `71`, limiting `sta_env_setup` to AP baseline bring-up, and marking `results_reference.v4.0.3 = Pass / Pass / Pass` removed the mismatch; the rerun exact-closed tri-band `FTOverDSEnable=0 -> 1 -> 0` together with `MobilityDomain=4660` and hostapd `mobility_domain=3412` / `ft_over_ds` transitions on AP1 / AP3 / AP5. Overlay compare is now `269 / 420 full matches`、`151 mismatches`、`58 metadata drifts`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D079`.
> 2026-04-13 latest continuation after D079: `D079 MACFiltering.Mode` is now aligned via official rerun `20260413T065809885285`. This was not a true fail-shaped runtime gap: the case still carried stale workbook row `73`, stale raw `Fail / Fail / Fail`, and an incorrect all-Off assumption even though workbook row `79` expects a deterministic tri-band baseline with AP1 `WhiteList` / `macaddr_acl=1` / `accept` and AP3/AP5 `BlackList` / `macaddr_acl=0` / `deny`. Live source-backed probes first proved that workbook pass path, then confirmation rerun `20260413T065611644145` showed the only remaining blocker was a non-authoritative `wl -i wl{0,1,2} bss` setup gate that sampled transient hostapd restart noise (`--wlX FSM DONE--` / `down`). Removing that gate let the rerun exact-close tri-band `Mode=Off -> invalid value` with after-state invariance (`AfterMode5g=WhiteList`, `AfterMode6g/24g=BlackList`). Overlay compare is now `270 / 420 full matches`、`150 mismatches`、`58 metadata drifts`, full repo regression is `1653 passed`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D080`.
> 2026-04-13 latest continuation after D080: `D080 MaxAssociatedDevices` is now aligned via official rerun `20260413T071746618166`. This closure did not need a firmware semantic rewrite: the authoritative full-run trace had already shown the true workbook row `80` pass path as tri-band getter plus hostapd `max_num_sta` convergence `32 -> 31 -> 32`, but the case still carried stale workbook row `74`, stale raw `Fail / Fail / Fail`, and a polluted setter capture where the bare ubus object line was glued into the requested-value field. Refreshing the metadata to workbook row `80`, reshaping the setter steps to emit explicit `RequestedTempMax*` / `SetterEchoMax*` fields, and exact-closing each AP against both getter and hostapd `max_num_sta` removed that false fail cleanly; the rerun proves `31` during the temp phase and `32` again after restore on AP1 / AP3 / AP5. Overlay compare is now `271 / 420 full matches`、`149 mismatches`、`58 metadata drifts`, full repo regression remains `1653 passed`, and the next ready actionable open case now moves to `D082`.
> 2026-04-13 latest continuation after D082: `D082 MultiAPType` is now aligned via official rerun `20260413T075200621380`. The stale case had three coupled drifts: metadata still pointed at workbook row `76`, the setter only toggled `AP1/AP3/AP5` even though workbook row `82` uses wildcard `WiFi.AccessPoint.*.MultiAPType=FronthaulBSS`, and the driver-map normalizer used a broken `sed 's/ */ /g'` shape that exploded `0x3` / `0x1` into character-spaced output. Live rerun evidence proved that each radio pair must move together (`AP1/AP2`, `AP3/AP4`, `AP5/AP6`) before `/tmp/wlX_hapd.conf` can legitimately converge to two `multi_ap=2` lines. Refreshing the case to workbook row `82`, reconstructing the dual-role baseline on all six APs in setup, switching each band to paired setter/restore, replacing map normalization with `tr -s ' ' | xargs`, and giving setup a longer settle let the rerun exact-close tri-band `FronthaulBSS,BackhaulBSS -> FronthaulBSS -> FronthaulBSS,BackhaulBSS` together with hostapd `multi_ap=3/3 -> 2/2 -> 3/3` and driver `0x3 -> 0x1 -> 0x3`. Overlay compare is now `272 / 420 full matches`、`148 mismatches`、`58 metadata drifts`, full repo regression remains `1653 passed`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D083` metadata drift.
> 2026-04-13 latest continuation after D083: `D083 Neighbour` is now aligned via official rerun `20260413T080405422245`. This was a metadata-only closure: the authoritative full-run trace had already exact-closed workbook row `83` as the tri-band AP-only add/delete lifecycle `empty -> single entry -> empty`, but the case still carried stale workbook row `77`. Refreshing `source.row` to `83` and replaying the official rerun re-proved the same workbook path on AP1 / AP3 / AP5: 5G exact-closes `11:22:33:44:55:66 / 36`, 6G exact-closes `11:22:33:44:55:77 / 1`, and 2.4G exact-closes `11:22:33:44:55:88 / 11`, with all three bands returning to `ABSENT` after delete. Overlay compare remains `272 / 420 full matches`、`148 mismatches`、`58 metadata drifts`, full repo regression remains `1653 passed`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D084`.
> 2026-04-13 latest continuation after D084: `D084 EncryptionMode / AccessPoint.Security` is now aligned via official rerun `20260413T081301178883`. This returned to the low-risk metadata/results_reference family once workbook row `84` was read directly: the row itself is explicitly `Not Supported / Not Supported / Not Supported` with comment `hardcode in pwhm`, while the live tri-band probe still shows northbound getter `EncryptionMode="Default"` on AP1 / AP3 / AP5 even though the matching hostapd configs expose real `CCMP` ciphers (`WPA-PSK` on 5G / 2.4G, `SAE` on 6G). So the stale authored case was already checking the right runtime shape, but it was pinned to old workbook row `78` and raw `Pass / Pass / Pass`. Refreshing the case to workbook row `84` and raw `Not Supported / Not Supported / Not Supported` cleanly closes the mismatch without changing commands or pass_criteria. Overlay compare is now `273 / 420 full matches`、`147 mismatches`、`58 metadata drifts`, full repo regression remains `1653 passed`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D085`.
> 2026-04-13 latest continuation after D085: `D085 KeyPassPhrase / AccessPoint.Security` is now aligned via official rerun `20260413T082022613657`. The authoritative full-run trace had already shown that the quoted leading-zero setter/readback path was healthy on AP1 / AP3 / AP5, but the stale case still carried old workbook row `79` plus a non-authoritative 6G `sae_password=` side-channel gate that workbook row `85` never required. Refreshing metadata to workbook row `85`, keeping the real quoted setter/readback plus hostapd `wpa_passphrase=` convergence as the pass path, and dropping the stale 6G SAE gate let the rerun exact-close tri-band `00000000 -> 0689388783 -> 00000000` in one attempt. Targeted D085 tests remain `3 passed`; full repo regression remains `1653 passed`. Overlay compare is now `274 / 420 full matches`、`146 mismatches`、`58 metadata drifts`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D086`.
> 2026-04-13 latest continuation after D086: `D086 MFPConfig / AccessPoint.Security` is now aligned via official rerun `20260413T083419287730`. This stayed in the same low-risk workbook row/results_reference family as `D084`: workbook row `86` is explicitly `Not Supported / Not Supported / Not Supported` with comment `hardcode in pwhm`, while active 0403 ODL still declares `MFPConfig = "Disabled"` and scopes it to WPA2 applicability. The stale case was pinned to old workbook row `80` and raw `Pass / Pass / Pass`, even though the live probe it already encoded was really demonstrating a default-biased getter (`Disabled` on all bands) against hostapd enforcement derived from live mode (`ieee80211w=0` on WPA2 5G / 2.4G, `ieee80211w=2` on WPA3 6G). Refreshing metadata to workbook row `86` and raw `Not Supported / Not Supported / Not Supported` lets the rerun stay green while compare closes back to workbook authority. Targeted D086 tests remain `3 passed`; full repo regression remains `1653 passed`. Overlay compare is now `275 / 420 full matches`、`145 mismatches`、`58 metadata drifts`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D087`.
> 2026-04-13 latest continuation after D087: `D087 ModeEnabled / AccessPoint.Security` is now aligned via official rerun `20260413T085025879532`. Workbook row `87` is a real tri-band Pass row, but the stale authored case was still pinned to old workbook row `81` and over-gated on non-authoritative cleanup restore plus exact `wpa_key_mgmt` token matching. The authoritative row-87 intent is simpler: all three bands accept `ModeEnabled=WPA3-Personal`, getter reads back `WPA3-Personal`, and hostapd converges to the WPA3/SAE family with `ieee80211w=2`; restore remains cleanup only. Refreshing metadata to workbook row `87` and aligning pass_criteria to that setter/readback authority let the rerun exact-close AP1 / AP3 / AP5 in one attempt. Targeted D087 tests remain `3 passed`; full repo regression remains `1653 passed`. Overlay compare is now `276 / 420 full matches`、`144 mismatches`、`58 metadata drifts`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D090`.
> 2026-04-13 latest continuation after D090: `D090 RekeyingInterval / AccessPoint.Security` is now aligned via official rerun `20260413T090437438519`. This was another stale row/semantics closure: workbook authority is row `90`, not row `92`, and row `90` is a tri-band Pass case built around `RekeyingInterval=0` plus hostapd `wpa_group_rekey` cross-check. The stale authored case had inverted that into a fail-shaped `set 3600 / expect divergence` story, even though the earlier live full-run trace was already showing `getter=0` and `hostapd=0` on AP1 / AP3 / AP5. Refreshing metadata to workbook row `90`, restoring the workbook row-90 zero-shape, and anchoring the source note on the tagged TR-181 data-model (`RekeyingInterval -> wpa_gtk_rekey`, default `0`) let the rerun exact-close all three bands in one attempt. Targeted D090 tests now remain `4 passed`; full repo regression is `1654 passed`. Overlay compare is now `277 / 420 full matches`、`143 mismatches`、`58 metadata drifts`, `D020` remains the verified fail-shaped mismatch, `D035` / `D053` remain blocked, `D328` / `D336` remain env-only, and the next ready actionable open case now moves to `D092`.

### 2.1 已落地能力

1. CLI 入口：`list-plugins`、`list-cases`、`run`、`wifi-llapi baseline-qualify`、`wifi-llapi build-template-report`。
2. `wifi_llapi` 已有報表導向 run path 與 row/source 對齊治理。
3. Transport：serialwrap / adb / ssh / network 已實作。serialwrap 已支援 auto-detect template（COM init 時自動偵測 prpl / openwrt / generic）與 prpl-template 復原。
4. Per-case dispatcher、selection trace、retry-aware timeout、attempt trace 已落地。
5. 正式 hot path 仍由 `setup_env -> verify_env -> execute_step -> evaluate` 決定。
6. `wifi_llapi` discoverable 官方案例仍維持 420 筆；目前正進行以 repo-root `0401.xlsx` 為 authority 的 compare-driven 單案校正，已 live 對齊 `D004`、`D005`、`D006`、`D007`、`D009`、`D010`、`D011`、`D024`、`D111`。
7. 最新 `compare-0401` 疊代到 run `20260402T105808547293` 後，狀態為 **264 / 420 full matches**、**156 mismatches**；`D011 AvgSignalStrength`、`D015 ConnectionDuration`、`D016 DownlinkBandwidth`、`D017 DownlinkMCS`、`D018 DownlinkShortGuard` 與 `D023 Inactive` 已完成 row 11 / row 15 / row 16 / row 17 / row 18 / row 23 三 band live 對齊，`D013 Capabilities` 與 `D020 FrequencyCapabilities` 維持已驗證的 fail-shaped mismatch；`D024/D025/D026` 的 offline survey 也已確認現有 YAML metadata 仍沿用舊 row `21/22/23`、實際 `0401.xlsx` row 應為 `24/25/26`。2026-04-10 的後續 patch-driven loop 已再向前推進到 `D283 blocker`：`D111` 已 live 對齊成 plain `Pass`；`D211 OperatingStandards` 已完成 workbook row `211` replay + source-backed triage，確認 getter 會在 `be/ax` 間切換，但 AX phase 仍保留 `wl0/wl1/wl2 eht features=127`，6G `/tmp/wl1_hapd.conf` 也仍有 secondary `ieee80211be=1`，因此 D211 目前維持 blocked/fail-shaped mismatch；`D262 getRadioAirStats():void` 已完成 source-backed revalidation，確認 6G `[""]` 只是 `WiFi.Radio.2.Status="Down"` 時 `_getRadioAirStats()` 的 early-return shape，經 `baseline-qualify --band 6g` 重新拉回 all-radios `Up` 後，live rerun `20260410T115313185471` 已成 plain `Pass`，且 YAML row drift 已由 `264` 刷新為 workbook row `262`；`D263 getRadioStats() BroadcastPacketsReceived`、`D264 getRadioStats() BroadcastPacketsSent`、`D265 getRadioStats() BytesReceived`、`D266 getRadioStats() BytesSent`、`D267 getRadioStats() DiscardPacketsReceived`、`D268 getRadioStats() DiscardPacketsSent`、`D269 getRadioStats() ErrorsReceived`、`D270 getRadioStats() ErrorsSent`、`D271 getRadioStats() MulticastPacketsReceived`、`D272 getRadioStats() MulticastPacketsSent`、`D273 getRadioStats() PacketsReceived`、`D274 getRadioStats() PacketsSent`、`D275 getRadioStats() UnicastPacketsReceived`、`D276 getRadioStats() UnicastPacketsSent` 也都已完成 source-backed green-lock。這一串 Radio.Stats family 現在已明確證明：workbook rows `263-276` 中，多數 proc/sample-zero heuristics 在 0403 都只是 stale oracle，真正的 authoritative semantics 來自 `whm_brcm_api_ext.c` 先填 `ifstats`、`whm_brcm_rad_get_counters_fromfile()` 再覆寫 driver `wl ... counters`，以及 TX/RX multicast/broadcast subtraction merge 後的最終 live shape；其中 `D270` 是少數仍與 workbook zero/proc heuristic 完全對齊、屬於 row drift only 的例外。`D272` live compare 已進一步確認 workbook row `272` 的 `dev_extstats TX_multipkg` 不是穩定三 band oracle；`D273/D274` live compare 也確認 5G/2.4G 偏向 driver `rxframe/txframe`，但 6G 仍常貼近舊 proc shape；`D275/D276` 則再證明 unicast 欄位在 5G/2.4G 多半符合 `frame - mcast` 的 derived shape，而 6G 仍可能 collapse 成 zero-like output。對應 YAML row drift 也已由 `265/266/267/268/269/270/271/272/273/274/275/276/277/278/281/282` 刷新為 workbook row `263/264/265/266/267/268/269/270/271/272/273/274/275/276/279/280`。接著 `D277 getScanResults() Bandwidth` 的 blocker 又被進一步縮小：初始 DUT gate 問題可透過 `serialwrap session recover --selector COM0 --force` 拉回 `COM0 state=READY`，早先 direct probe 也已證明 5G/6G raw full payload 會卡在 serialwrap 層，而 repo-side scan timeout fallback 與對應 regression（resolver tests `30 passed`、完整 plugin runtime regression `1223 passed`）雖已落地，isolated rerun 仍未能 authoritatively 完成；WAL 顯示它在 raw `WiFi.Radio.2.getScanResults()` 後被 serialwrap recovery 注入 `^C`，DUT 隨即回 `Please press Enter to activate this console.`，`COM0` 也暫時掉到 `ATTACHED / PROMPT_TIMEOUT` 才能再 recover 回 `READY`。因此目前真正卡住的仍是 6G long scan output 觸發 broker/console recovery，而不是已證明的 `Bandwidth` semantic mismatch；另外，5G/2.4G workbook-style diagnostics 已可從 `iw dev wl0/wl2 scan` 取到 width/centre 對位訊號，但 6G 當前 visible target 仍缺少 clean `iw` width / centre-channel oracle。`D277` 仍以 blocker 形式保留。其後 `D278 getScanResults() BSSID` 已用 workbook-style target compare 完成 live 對齊：isolated rerun `20260410T135946424063` 先從 LLAPI 擷取每個 band 的第一個 target BSSID，再以同 target 對 `iw dev wl0/wl1/wl2 scan`，三個 band 皆 matched (`38:88:71:2f:f6:a7` / `3a:06:e6:2b:a3:1a` / `8c:19:b5:6e:85:e1`)，且 YAML row drift 已由 `280` 刷新為 workbook row `278`。再往下一筆 `D279 getScanResults() Channel` 也已完成 live 對齊：第一次 isolated run `20260410T140447198030` 揭露了鬆散 `/Channel = /` parser 誤讀 `CentreChannel` 的 bug（錯把 `42/31/3` 當成 `Channel`），修正為只吃真正的 `^[[:space:]]*Channel = ` 後，第二次 isolated rerun `20260410T140714322443` 已讓 LLAPI `Channel` 與同 target `iw freq -> channel` authoritatively 對齊（`36/36`、`5/5`、`1/1`），且 YAML row drift 已由 `281` 刷新為 workbook row `279`。`D280 getScanResults() EncryptionMode` 也已完成 live 對齊：isolated rerun `20260410T143122662554` 以每個 band 的第一個 WPA 類 target 為 authority，證明 0403 source 仍只把 neighboring WiFi `EncryptionMode` 定義成 `TKIP / AES / TKIPandAES / None`，但 live getter 持續回 `Default`；同 target `iw dev wl0/wl1/wl2 scan` 又都顯示 `Pairwise ciphers: CCMP`，正規化後為 `AES`，因此此案已 source-backed 鎖定成 fail-shaped mismatch (`38:88:71:2f:f6:a7` WPA2-Personal / `3a:06:e6:2b:a3:1a` WPA3-Personal / `8c:19:b5:6e:85:e1` WPA2-WPA3-Personal；LLAPI 全部 `Default`，iw 全部 `AES`)；D280 YAML row drift 也已由 `282` 刷新為 workbook row `280`。接著 `D281 getScanResults() Noise` 雖然 source trace 仍明確指向 `wl -i wlX escanresults` `noise: ` token，但兩次 isolated workbook-style replay（`20260410T155529962490` / `20260410T155932807150`）都沒有產生穩定的 same-target live path：第一輪只有 5G 對齊（`-100/-100`），6G 缺 `WlNoise6g`、2.4G 漂移成 `-80/-77`；第二輪又改成 5G/6G 都缺 `WlNoise`，2.4G 仍漂移成 `-80/-79`。因此 D281 目前必須維持 blocked、拒絕落地 rewrite，committed YAML 也暫時維持未修改的 stale row `283`。`D282 getScanResults() OperatingStandards` 則在新的 workbook-style rewrite 試作後也正式收斂成 blocker：trial isolated rerun `20260410T163026194231` 連兩次都無法完成 deterministic all-band replay，5G target 只剩 `a,n,ac,ax`、6G 沒有 same-target `WlOperatingStandards6g`、2.4G 則漂移成 `LLAPI b,g,n,ax` vs `wl b,g,n,ax,be`；額外的 controlled baseline probe 也沒有關閉缺口（`testpilot5G` LLAPI `a,n,ac,ax,be` vs wl `a,n,ac,ax`，`testpilot6G` 不在 replay snapshot，`testpilot2G` 不在 LLAPI scan 但 wl 仍看到 EHT-capable baseline BSSID）。因此 D282 trial rewrite 已拒絕落地、committed YAML 維持原 generic shape 與 stale row `284`。`D283 getScanResults() RSSI` 也已確認不是 semantic replay問題，而是沿著同一條 raw 6G scan transport blocker 失敗：舊 full run `20260409T213837737224` 已記錄 `step_6g_scan command failed` / `serialwrap cmd status timeout`，新的 isolated rerun `20260410T164405221878` 又在 `setup_env` 之後整段掛住、沒有 step output，也沒有 per-case `agent_trace` JSON 落盤；停止該 run 後，`COM0` 也短暫回到 `ATTACHED / PROMPT_TIMEOUT` 才由 self-test 拉回 `READY`。因此 D283 目前也維持 blocked，committed YAML 保持未修改；next ready case in the current repo inventory=`D284`。
8. 最新 detached full run `20260411T074146043202` 用 current local YAML overlay 重算後，快照已更新為 **220 / 420 full matches**、**200 mismatches**、**67 metadata drifts**。若改用 `evaluation_verdict` 解讀真正 workbook-Pass 缺口，尚有 **77** 個 carried-forward cases 未收斂；在 current local repo inventory 中，patch-scope true-open set 已縮到 **12** 個：`D281-D287`、`D290`、`D295`、`D324`、`D331`、`D333`。其中 `D532 getSpectrumInfo ourUsage` 與 `D533 getSpectrumInfo availability` 已在 isolated rerun `20260411T183356920330` / `20260411T183405281629` 重新驗成 plain `Pass`；`D295 scan()` 現已正式落成 blocker（`plugins/wifi_llapi/reports/D295_block.md`），而 `D324 BytesSent` 也經 isolated rerun `20260411T190338070996` 重新證明 base `wlX if_counters txbyte` 不是 durable oracle，現已同樣回退成 blocker（`plugins/wifi_llapi/reports/D324_block.md`）
   - latest continuation after that: the detached compare counts above are still pre-`D330`/`D332`/`D335` rewrite evidence. In the current local repo state, `D322 BroadcastPacketsSent` is aligned via resolving official rerun `20260412T002445088386`: the earlier `+1` blocker turned out to be a runner-timing issue right after the 6G OCV repair path, so keeping the workbook `/proc $24` oracle but inserting a short post-`verify_env` settle (`sleep 2`) let attempt 2 exact-close 5G/6G/2.4G at `4596/4596/4596`, `4772/4772/4772`, and `5121/5121/5121`, and the committed metadata is now refreshed from stale row `246` to workbook row `322`. `D324 BytesSent` remains blocked in `plugins/wifi_llapi/reports/D324_block.md`: the older isolated rerun had already invalidated base `wlX if_counters txbyte`, and the follow-up official WDS-sum rerun `20260412T005627796136` proved that even `wlX txbyte + matching wds txbyte` is not yet commit-worthy in the real runner path. 5G kept a `direct < getSSIDStats < driver` staircase on both attempts (`148970905 / 148971033 / 148971377`, then `149022147 / 149022215 / 149022559`), 6G exact-closed only on attempt 2, and 2.4G still left a residual driver lead. Active pWHM source now shows why: direct `Stats.*` and `getSSIDStats()` both independently call `s_updateSsidStatsValues()` before serializing output, so the sequential equality itself is still race-prone. `D330 MulticastPacketsReceived` is aligned via isolated rerun `20260411T191809490680`, `D331 MulticastPacketsSent` remains blocked in `plugins/wifi_llapi/reports/D331_block.md`: the earlier source-backed trials first exposed a fixed 5G `driver = direct + 4` drift, the superseding official rerun `20260411T234124237416` widened that into a non-durable 5G mismatch (`286001 / 286006`, then `286140 / 286192`), and the post-`verify_env` settle retrial `20260412T003609854183` only moved the residual failure to 6G (`181336 / 181336 / 181337`, then `181375 / 181375 / 181377`) while 5G/2.4G exact-closed on both attempts. `D332 PacketsReceived` is aligned via stale replay `20260411T194312398713` + source-backed rerun `20260411T194647490016`, and `D333 PacketsSent` remains blocked in `plugins/wifi_llapi/reports/D333_block.md`: the earlier source-backed rewrite first kept a fixed 5G `driver = direct + 5` drift, and the post-`verify_env` settle retrial `20260412T004816450100` narrowed that into `5G +1` on attempt 1 and `6G +2` on attempt 2 while 5G/2.4G exact-closed by the end of the replay. The local rewrites for `D324` / `D331` / `D333` are all rolled back again, so the next ready open-set revisit now returns to `D281`. `D335 UnicastPacketsReceived` is aligned via stale replay `20260411T200329824574` + source-backed rerun `20260411T200851584762`, and `D336 UnicastPacketsSent` is now aligned via stale replay `20260411T201639103833` + resolving official rerun `20260412T000744842751` after switching the driver oracle to the unsigned txframe/d11_txmulti formula: attempt 1 still drifted on 6G by `+1` (`24709 / 24710 / 24710`), but attempt 2 exact-closed all three bands (`27172/27172/27172`, `24703/24703/24703`, `17117/17117/17117`). `D277 getScanResults() Bandwidth` is now also aligned via isolated rerun `20260411T205454026707`: transport-safe first-object capture removed the old 6G full-scan recovery blocker, 5G/2.4G exact-close against same-target `wl escanresults` Chanspec bandwidth (`80/80`, `20/20`), and 6G is now carried as a source-backed fail-shaped mismatch (`320/160`) at workbook row `277`
   - latest continuation after that: `D281 getScanResults() Noise` remains blocked, but the blocker is now cleaner and narrower. The earlier blocker had already corrected the source model to active public per-channel spectrum back-fill (`wifiGen_rad_getScanResults()` + `s_updateScanResultsWithSpectrumInfo()`). A newer split-step same-channel public-spectrum rewrite then removed the old temp-script/same-target transport failure, but three consecutive official reruns still settled as `Fail -> pass after retry -> Fail`: `20260412T023953395097` failed twice on 2.4G `Noise=-80` vs `SpectrumNoise=-78`, `20260412T024026359700` passed only after retry with attempt 1 at `-80/-82` and attempt 2 exact-close, and `20260412T024310730084` failed again at `-80/-78`. 5G and 6G exact-close throughout, so D281 now stays blocked specifically on unstable 2.4G spectrum-cache replay; the local rewrite is rolled back again, the committed YAML remains unchanged at stale row `283`, and `plugins/wifi_llapi/reports/D281_block.md` is updated as the latest blocker authority; the next ready runtime-triage case in the current repo inventory is `D282`
   - latest continuation after that: `D282 getScanResults() OperatingStandards` still remains blocked, but the blocker is now source-backed to a concrete sibling oracle. Active 0403 source tracing in `pwhm-v7.6.38/src/RadMgt/wld_rad_scan.c` now shows `getScanResults()` serializes only `ssid->operatingStandards`, while sibling ubus method `WiFi.NeighboringWiFiDiagnostic()` serializes both `OperatingStandards` and `SupportedStandards` from the same `wld_scanResultSSID_t`. Live probes proved the sibling path is real — 2.4G first target `04:70:56:D2:22:4F` exact-closed `OperatingStandards=b,g,n,ax` on both paths — but the diagnostic currently returns `FailedRadios = "wl0,wl1"` / `ReportingRadios = "wl2"`, so 5G/6G still have no durable same-target sibling replay. Therefore D282 stays blocked, the committed YAML remains unchanged at stale row `284`, `plugins/wifi_llapi/reports/D282_block.md` is updated as the latest blocker authority, and the next ready runtime-triage case in the current repo inventory is `D324`
   - latest continuation after that: `D283 getScanResults() RSSI` is now aligned. The old blocker was the committed full-payload transport shape rather than the row semantics: isolated rerun `20260411T214050136894` still hung after `setup_env`, but a D277-style transport-safe first-object capture removed that failure path while preserving the active 0403 public source family. Current source trace still shows `D283 RSSI` and `D286 SignalStrength` are both serialized from the same `ssid->rssi`, so the committed D283 oracle now locks the durable public facts on the first serialized scan object only: parseable BSSID + numeric `RSSI` + `SignalStrength == RSSI`. Official reruns `20260412T013944779069` and `20260412T014018880783` both exact-closed 5G `38:88:71:2f:f6:a7 / -66 / -66`, 6G `6e:15:db:9e:33:72 / -95 / -95`, and 2.4G `2c:59:17:00:03:f7 / -47 / -47`, so the committed metadata is now refreshed from stale row `285` to workbook row `283`. `D286` remains blocked on its own unresolved replay model, and the next ready runtime-triage case in the current repo inventory is `D284`
   - latest continuation after that: `D284 getScanResults() SecurityModeEnabled` is now aligned. The earlier blockers were unstable 6G selectors rather than an unreplayable public field: the first-WPA-target and associated-BSSID rewrites drifted between `.ROAMTEST_RSNO_P10P_1`, `OpenWrt_1`, and missing same-target LLAPI placeholders, but a D283-style transport-safe first-object capture now locks one durable same-target replay across all three bands. Official reruns `20260412T015141491861` and `20260412T015235280960` both exact-closed 5G `38:88:71:2f:f6:a7 / WPA2-Personal / WPA2-Personal`, 6G `6e:15:db:9e:33:72 / WPA3-Personal / WPA3-Personal`, and 2.4G `2c:59:17:00:03:f7 / WPA2-Personal / WPA2-Personal`, so the committed metadata is now refreshed from stale row `286` to workbook row `284`, and the next ready runtime-triage case in the current repo inventory is `D285`
   - latest continuation after that: `D285 getScanResults() SignalNoiseRatio` is now aligned. The old blocker was the raw-driver replay rather than the public row itself: 0403 neighboring scan internals still carry `SignalStrength` plus `Noise`, while the serialized public scan object exposes `SignalNoiseRatio`. Switching D285 to a D283-style transport-safe first-object capture let the committed replay validate the durable public invariant `SignalNoiseRatio == RSSI - Noise` on the same target. Official reruns `20260412T020817105728` and `20260412T020839239161` both exact-closed 5G `38:88:71:2f:f6:a7 / -66 / -100 / 34`, 6G `6e:15:db:9e:33:72 / -95 / -97 / 2`, and 2.4G `2c:59:17:00:03:f7 / -47 / -80 / 33`, so the committed metadata is now refreshed from stale row `287` to workbook row `285`, and the next ready runtime-triage case in the current repo inventory is `D286`
   - latest continuation after that: `D286 getScanResults() SignalStrength` is now aligned. The old blocker was the external replay model rather than the public row itself: active 0403 source tracing already showed `SignalStrength` and `RSSI` are the same public `ssid->rssi` family, so D286 can use the same D283-style transport-safe first-object capture instead of chasing `iw` / raw-driver drift. Official reruns `20260412T021725610895` and `20260412T021748934770` both exact-closed 5G `38:88:71:2f:f6:a7 / -66 / -66`, 6G `6e:15:db:9e:33:72 / -95 / -95`, and 2.4G `2c:59:17:00:03:f7 / -47 / -47`, so the committed metadata is now refreshed from stale row `288` to workbook row `286`, and the next ready runtime-triage case in the current repo inventory is `D287`
    - latest continuation after that: `D287 getScanResults() SSID` is now aligned. The old blocker was the stale 6G selector rather than the public field itself: once the case switched to the same transport-safe first-object capture already proven by D283-D286, the live replay no longer chased `3a:06:e6:2b:a3:1a` and instead locked the stable first serialized 6G target `6e:15:db:9e:33:72`. After fixing one repo-side extractor bug that had briefly matched `BSSID` as `SSID`, official reruns `20260412T022708923585` and `20260412T022738004752` both exact-closed 5G `38:88:71:2f:f6:a7 / Verizon_Z4RY7R / Verizon_Z4RY7R`, 6G `6e:15:db:9e:33:72 / **TELUS0227 / **TELUS0227`, and 2.4G `2c:59:17:00:03:f7 / OpenWrt_1 / OpenWrt_1`. Therefore the committed YAML is now refreshed from stale row `289` to workbook row `287`, `plugins/wifi_llapi/reports/D287_block.md` is retained as historical resolution notes, and the next ready case in the current repo inventory returns to `D281`
    - latest continuation after that: `D281 getScanResults() Noise` still does not clear official durability. The split-step same-channel public-spectrum replay is now the right source family and no longer trips the old 6G staged-command failure, but three consecutive official reruns still settle as `Fail -> pass after retry -> Fail`, with all surviving mismatches isolated to 2.4G `Noise=-80` versus `SpectrumNoise=-78/-82`. Therefore the local rewrite is rolled back again, the committed YAML remains unchanged at stale row `283`, `plugins/wifi_llapi/reports/D281_block.md` is the latest blocker authority, and the superseding next ready case in the current repo inventory is `D282`
    - latest continuation after that: `D282 getScanResults() OperatingStandards` still does not clear official durability. The active pWHM ubus path now exposes a same-source sibling candidate in `WiFi.NeighboringWiFiDiagnostic()`: `getScanResults()` serializes only `OperatingStandards`, while the diagnostic serializer emits both `OperatingStandards` and `SupportedStandards` from the same `wld_scanResultSSID_t`. Live probes already exact-closed 2.4G same-target `OperatingStandards=b,g,n,ax`, but the diagnostic still reports `FailedRadios = "wl0,wl1"` / `ReportingRadios = "wl2"`, so 5G/6G still have no durable same-target sibling replay. Therefore the local YAML remains unchanged at stale row `284`, `plugins/wifi_llapi/reports/D282_block.md` is the latest blocker authority, and the superseding next ready case in the current repo inventory is `D324`
   - latest continuation after that: `D284 getScanResults() SecurityModeEnabled` also had to be blocked. Source trace still maps neighboring `SecurityModeEnabled` from `AKM Suites` / `RSN` via `_wldm_get_securitymode_encryptionmode()`, and isolated rerun `20260410T170750425931` did prove same-target replay on 5G (`38:88:71:2f:f6:a7` -> `WPA2-Personal`) and 2.4G (`8c:19:b5:6e:85:e1` -> `WPA2-WPA3-Personal`), but 6G would not freeze to one target: LLAPI chose `3a:06:e6:2b:a3:1a` (`.ROAMTEST_RSNO_P10P_1`) while same-target `iw` emitted `IwSecurityMode6g=None`, and follow-up manual probes showed LLAPI can also expose `2C:59:17:00:19:96` (`OpenWrt_1`) as another `WPA3-Personal` target while `iw` prefers that associated BSSID instead. A second isolated rerun `20260410T171358112868` then tried an associated-BSSID selector, but the 6G LLAPI step emitted no `LlapiBssid6g`, so the `iw` step placeholder could not resolve. Therefore the D284 trial rewrite was rejected, the committed YAML stays unchanged at stale row `286`, post-revert full plugin runtime regression is still `1223 passed`, and the next ready case in the current repo inventory is `D285`
7.1. latest continuation after that: `D285 getScanResults() SignalNoiseRatio` also had to be blocked. `compare-0401.md` still maps the case to workbook row `285`, but the committed YAML stays unchanged at stale row `287`; source survey showed 0403 neighboring scan internals still center on `RSSI` / `SignalStrength` plus `Noise`, while `wld_radio.odl` / public `wld.h` only expose a derived `SignalNoiseRatio` at the scan-result model layer. Standalone LLAPI replay did prove the field exists (`3A:06:E6:2B:A3:1A` -> `RSSI=-93`, `Noise=-97`, `SignalNoiseRatio=4` on 6G; `8C:19:B5:6E:85:E1` -> `RSSI=-46`, `Noise=-80`, `SignalNoiseRatio=34` on 2.4G), but source-backed same-target replay still would not close: direct `wl -i wl1 escanresults` could not find the 6G LLAPI first BSSID at all, while a same-target 2.4G raw `wl -i wl2 escanresults` probe did find `8C:19:B5:6E:85:E1` yet reported `RSSI=-54 dBm`, `noise=-75 dBm`, `SNR=21 dB`, drifting far from LLAPI `34`. Therefore the D285 trial rewrite was rejected, the committed YAML stays unchanged, and the superseding next ready case in the current repo inventory is `D286`
7.2. latest continuation after that: `D286 getScanResults() SignalStrength` also had to be blocked. Source trace still keeps neighboring `SignalStrength` on the raw `RSSI: ` token path, so a workbook-style same-target compare was trialed against `iw`/raw scan evidence. Isolated rerun `20260410T181105027445` stayed deterministic but did not close: 5G same-target `38:88:71:2f:f6:a7` remained `LLAPI=-64` vs `iw=-65` on both attempts, 6G `3a:06:e6:2b:a3:1a` emitted no same-target `IwSignalStrength6g`, and 2.4G `8c:19:b5:6e:85:e1` drifted to `LLAPI=-46` vs `iw=-55/-56`. Follow-up raw `wl -i wl0/wl1/wl2 escanresults` probes only partially closed the gap: 5G same-target raw RSSI matched at `-64`, but 6G still could not find `3A:06:E6:2B:A3:1A` and 2.4G same-target raw RSSI still drifted at `-54 dBm`. Therefore the D286 trial rewrite was rejected, the committed YAML was reverted to its original generic shape (`source.row=288` still stale), and the superseding next ready case in the current repo inventory is `D287`
7.3. latest continuation after that: `D287 getScanResults() SSID` also had to be blocked. Source trace still keeps neighboring `SSID` on the raw `SSID: ` token path, and the workbook-style same-target trial did prove the parser/evidence path is sound on 5G and 2.4G: rerun `20260410T182739821870` locked `38:88:71:2f:f6:a7` -> `Verizon_Z4RY7R` and `8c:19:b5:6e:85:e1` -> `TMOBILE-85DF-TDK-2G` identically across LLAPI and `iw`. But 6G still would not close: LLAPI exposed `3a:06:e6:2b:a3:1a` -> `.ROAMTEST_RSNO_P10P_1`, while same-target `iw` emitted no `IwSSID6g`, and a direct raw `wl -i wl1 escanresults | grep -m1 -B2 -A1 'BSSID: 3A:06:E6:2B:A3:1A'` probe also came back empty. Therefore the D287 trial rewrite was rejected, the committed YAML was reverted to its original generic shape (`source.row=289` still stale), and the superseding next ready case in the current repo inventory is `D288`
7.4. latest continuation after that: `D288 getScanResults() WPSConfigMethodsSupported` is now aligned. Source survey had already shown 0403 still exposes `WPSConfigMethodsSupported` in the scan-result model while the neighboring scan live payload returns an empty-string shape on all three bands; the remaining gap turned out to be repo-side parsing, because the raw ubus transcript keeps empty strings as `WPSConfigMethodsSupported = "",` and `_extract_key_values()` does not capture that trailing-comma form. The committed case now uses an explicit extractor command to normalize each band to `WPSConfigMethodsSupported=`, isolated rerun `20260410T183630583633` passed on all three bands, the YAML row metadata is refreshed from stale row `290` to workbook row `288`, targeted D288 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready case in the current repo inventory is `D289`
7.5. latest continuation after that: `D289 getScanResults() Radio` is now aligned as a source-backed fail-shaped absence. The active 0403 scan model `wld_radio.odl` `scanresult_t` does not define `Radio`, and the active HAL neighboring struct `wifi_neighbor_ap2_t` also comments out `ap_Radio`, so the live getter has no backing field to populate. The committed case now uses an explicit extractor command to normalize the missing member to `Radio=`, isolated rerun `20260410T185434305989` emitted `Radio=` on all three bands with `diagnostic_status=Pass`, the YAML row metadata is refreshed from stale row `291` to workbook row `289`, full plugin runtime regression remains `1223 passed`, and the superseding next ready case in the current repo inventory is `D290`
7.6. latest continuation after that: `D290 getScanResults() CentreChannel` is now aligned in the local repo state. Source-backed survey had already confirmed the field itself is real on 0403 (`wld_radio.odl` declares `CentreChannel`, `wld_rad_scan.c` copies `ssid->centreChannel`, and `wld_nl80211_parser.c` computes it through `swl_chanspec_getCentreChannel()` with a 20MHz fallback), but the old blocker only lacked the right same-target replay path. After one-shot environment repair (`wifi-llapi baseline-qualify --repeat-count 1 --soak-minutes 0`), fresh isolated rerun `20260411T220324862766` closed same-target raw `wl escanresults` Chanspec replay on all three bands: 5G exact-closes at `42/42`, 2.4G exact-closes at `1/1`, and 6G is now locked as a source-backed fail-shaped mismatch `31/15` on BSSID `6e:15:db:9e:33:72`. Therefore the committed YAML is refreshed from stale row `292` to workbook row `290`, `results_reference.v4.0.3` is now `Pass / Fail / Pass`, targeted D290 guardrails are green, and the superseding next ready case in the current repo inventory is `D529`
7.7. latest continuation after that: `D529 getSpectrumInfo channel` is now aligned in the local repo state. Active 0403 source keeps the public field on `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "channel", llEntry->channel)`, and fresh isolated rerun `20260411T221613327385` plus repeated direct probes now lock the first serialized spectrum-entry channels at `36 / 2 / 1` on `5g / 6g / 2.4g`. Therefore the committed case fixes the template metadata shape (`object=WiFi.Radio.{i}.`, `api=getSpectrumInfo()`), upgrades the old generic numeric regex to explicit first-entry channel extractors while keeping workbook row `529` at plain `Pass / Pass / Pass`, and the superseding next ready case in the current repo inventory is `D530`
7.8. latest continuation after that: `D530 getSpectrumInfo noiselevel` is now aligned in the local repo state, but it stays a dynamic numeric case rather than a fixed-value case. Active 0403 source keeps the public field on `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(int32_t, "noiselevel", llEntry->noiselevel)`, so the field is a live survey reading rather than a stable constant. A first exact-value trial was rejected after 2.4G drifted across retries/reruns (`-75 / -77 / -78`), so the committed case only fixes the template metadata shape (`object=WiFi.Radio.{i}.`, `api=getSpectrumInfo()`), preserves the source-correct numeric regex pass shape, and uses isolated rerun `20260411T222349217612` as the green lock; the superseding next ready case in the current repo inventory is `D531`
7.9. latest continuation after that: `D531 getSpectrumInfo accesspoints` is now aligned in the local repo state, and it follows the same metadata-only dynamic numeric pattern. Active 0403 source keeps the public field on `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "accesspoints", llEntry->nrCoChannelAP)`, so the field is a survey-driven co-channel AP count rather than a fixed constant. Therefore the committed case only fixes the template metadata shape (`object=WiFi.Radio.{i}.`, `api=getSpectrumInfo()`), preserves the source-correct numeric regex pass shape, and uses isolated rerun `20260411T223140870454` as the green lock; the superseding next ready case in the current repo inventory is `D532`
7.10. latest continuation after that: `D532 getSpectrumInfo ourUsage` is now aligned in the local repo state, and it follows the same metadata-only dynamic numeric pattern. Active 0403 source keeps the public field on `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "ourUsage", llEntry->ourUsage)`, while `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()` derives that live percentage from survey airtime (`ourTime / total_time`). Fresh isolated rerun `20260411T223658523608` passed cleanly with the generic numeric verdict shape. The same workbook re-check also exposed a stale `source.row` drift across the whole spectrum batch, so `D528-D533` are now corrected to the actual workbook rows `528-533` instead of the old `530-535` carry-over; the superseding next ready case in the current repo inventory is `D533`
7.11. latest continuation after that: `D533 getSpectrumInfo availability` is now aligned in the local repo state as the last metadata-only dynamic numeric case in this spectrum batch. Active 0403 source keeps the public field on `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "availability", llEntry->availability)`, while `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()` derives the live percentage from survey idle/free-time timing. Fresh isolated rerun `20260411T224035464927` passed cleanly with the generic numeric verdict shape, so the `D528-D533` spectrum block is now fully row-corrected and aligned; the superseding next unresolved queue in the current repo inventory returns to `D322`, `D331`, `D333`, and `D336`
7.7. latest continuation after that: `D295 scan()` is now aligned as a plain `Pass/Pass/Pass` row. The committed YAML metadata is refreshed from stale row `220` to workbook row `295`, and manual serialwrap replay proved the preconditions are already satisfied on live 0403 (`WiFi.Radio.1/2/3.Enable=1`, `Status="Up"`). The final oracle now stays intentionally small and driver-backed: `WiFi.Radio.1.scan()` returned and the first 5G target `38:88:71:2F:F6:A7` matched `wl0 escanresults`; `WiFi.Radio.2.scan()` returned and the first 6G target `6E:15:DB:9E:33:72` matched `wl1 escanresults`; `WiFi.Radio.3.scan()` returned and the first 2.4G target `2E:59:17:00:06:F8` matched `wl2 escanresults`. A direct post-scan `getScanResults()` follow-up still reproduced a prompt-timeout on COM0, so D295 should stay calibrated on `scan() returned + same-target driver-cache match` rather than chaining a second oversized read. The superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D298`
7.8. latest continuation after that: `D298 startScan()` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `targets/BGW720-300/fs/etc/amx/wld/wld_radio.odl`, which declares `startScan()` and documents the expected failure modes (`Unable to start scan`, `A scan is already running`); the live 0403 path in `wldm_lib_wifi.c` then shows the method driving `wldm_xbrcm_scan(..., "scan")` and immediately fetching `num_scan_results` plus `scan_results`. Manual serialwrap replay now matches that contract on all three bands: `WiFi.Radio.1/2/3.startScan()` each returned `[ "" ]`, while the post-call driver cache stayed populated with visible BSSIDs on every band (`wl0` first BSSID `A8:A2:37:4F:8C:5C`, `wl1` first BSSID `6E:15:DB:9E:33:72`, `wl2` first BSSID `62:82:FE:58:AC:B5`). The committed metadata is refreshed from stale row `223` to workbook row `298`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D299`
7.9. latest continuation after that: `D299 stopScan()` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `targets/BGW720-300/fs/etc/amx/wld/wld_radio.odl` (`void stopScan()`), in `src/RadMgt/wld_rad_scan.c` where `_stopScan()` delegates to `wld_scan_stop(pR)`, and in `wld_scan_stop()` where the active scan path first requires `wld_scan_isRunning()` before calling `pRad->pFA->mfn_wrad_stop_scan(pRad)`; the public nl80211 surface also exposes the abort path through `wld_rad_nl80211_abortScan()` / `wld_nl80211_abortScan(...)`. Live serialwrap replay on `COM0` then closed a source-backed state-bit oracle on all three bands via `WiFi.Radio.N.ScanResults.ScanInProgress`: 5G proved `0 -> startScan() -> 1 -> stopScan() -> 0` and a second restart loop returned to `1/0` again, while 6G and 2.4G each proved `0 -> 1 -> 0` with short per-band probes. One longer combined 6G+2.4G probe did hit `PROMPT_TIMEOUT_RECOVERED`, so the committed oracle deliberately stays on short per-band `startScan() -> ScanInProgress=1 -> stopScan() -> ScanInProgress=0` replays. The committed metadata is refreshed from stale row `224` to workbook row `299`, targeted D299 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D300`
7.10. latest continuation after that: `D300 getSSIDStats() BroadcastPacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `targets/BGW720-300/fs/etc/amx/wld/wld_ssid.odl`, which exposes both `Stats.BroadcastPacketsReceived` and `htable getSSIDStats()`; `src/wld_ssid.c` then routes `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()`, where the 0403 `false && (pAP->status != APSTI_ENABLED)` fallback path keeps `wld_updateVAPStats(pAP, NULL)` live, and `src/wld_statsmon.c` fills `BroadcastPacketsReceived` from `rxBroadcastPackets`. Live serialwrap replay on `COM0` closed a three-way numeric oracle on every band: 5G matched `363 / 363 / 363`, 6G matched `144 / 144 / 144`, and 2.4G matched `113 / 113 / 113` across extracted `getSSIDStats()`, direct `WiFi.SSID.{i}.Stats.BroadcastPacketsReceived?`, and `/proc/net/dev_extstats` field `$23` for `wl0/wl1/wl2`. One longer all-band extraction probe did hit `PROMPT_TIMEOUT_RECOVERED`, so the committed oracle stays on short per-band probes. The committed metadata is refreshed from stale row `225` to workbook row `300`, targeted D300 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D301`
7.11. latest continuation after that: `D301 getSSIDStats() BroadcastPacketsSent` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `targets/BGW720-300/fs/etc/amx/wld/wld_ssid.odl`, which exposes both `Stats.BroadcastPacketsSent` and `htable getSSIDStats()`; `src/wld_ssid.c` then routes `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()`, where the 0403 fallback path still keeps `wld_updateVAPStats(pAP, NULL)` live, and `src/wld_statsmon.c` fills `BroadcastPacketsSent` from `txBroadcastPackets`. Live serialwrap replay on `COM0` closed the matching three-way numeric oracle on every band: 5G matched `1432 / 1432 / 1432`, 6G matched `1590 / 1590 / 1590`, and 2.4G matched `1680 / 1680 / 1680` across extracted `getSSIDStats()`, direct `WiFi.SSID.{i}.Stats.BroadcastPacketsSent?`, and `/proc/net/dev_extstats` field `$24` for `wl0/wl1/wl2`. The committed metadata is refreshed from stale row `226` to workbook row `301`, targeted D301 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D302`
7.12. latest continuation after that: `D302 getSSIDStats() BytesReceived` is now blocked. Active 0403 source still shows this field is real: `wld_ssid.c` copies endpoint `rxbyte` into `pStats->BytesReceived`, while `wld_statsmon.c` also aggregates `pSrc->rxBytes` into the same stats structure. Live replay on `COM0` then proved `direct Stats == getSSIDStats()` on all three bands (`139402 / 139402`, `43232 / 43232`, `33066 / 33066`, with a focused 5G rerun still at `139618 / 139618`), but no deterministic independent oracle would close. The old D323-style `/proc/net/dev` / `/proc/net/dev_extstats` field `$2` path is now confirmed stale, and a later source trace through `whm_brcm_vap_update_ap_stats()` / `whm_brcm_get_if_stats()` showed the current 0403 override actually reads `wl if_counters rxbyte`: that newer oracle exact-closes on 6G/2.4G and narrows 5G to a stable `+104` delta (`140482` getter vs `140378` `if_counters`), but still does not close all three bands. Therefore the trial rewrite was rejected, the committed YAML remains unchanged at stale row `227`, the blocker handoff lives in `plugins/wifi_llapi/reports/D302_block.md`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D303`
7.13. latest continuation after that: `D303 getSSIDStats() BytesSent` is now blocked as well. Active 0403 source still shows this field is real: `wld_ssid.c` copies endpoint `txbyte` into `pStats->BytesSent`, while `wld_statsmon.c` also aggregates `pSrc->txBytes` into the same stats structure. Live replay on `COM0` only partially closed the method path: 5G and 2.4G held `direct Stats == getSSIDStats()` (`95452542` and `67883586`), while 6G first drifted slightly (`60438235` vs `60439059`) and only re-closed on a focused rerun (`60582625 / 60582625`). But no deterministic independent oracle would close: `/proc/net/dev` and `/proc/net/dev_extstats` field `$10` stayed around `66798080 / 64667313 / 66738514` and `66750876 / 64691013 / 66691690`, `wl counters txbyte` stayed materially higher (`114950286 / 80944128 / 77082489`), sub-interface counters `wl0.1/wl1.1/wl2.1` were blank / zero-like, and `AssociatedDevice.*.TxBytes` also failed to map cleanly. Therefore the trial rewrite was rejected, the committed YAML remains unchanged at stale row `228`, the blocker handoff lives in `plugins/wifi_llapi/reports/D303_block.md`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D304`
7.14. latest continuation after that: `D304 getSSIDStats() DiscardPacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` overwrites `DiscardPacketsReceived` from `wl if_counters rxdiscard`. Live serialwrap replay on `COM0` then closed a three-way numeric oracle on every band: 5G, 6G, and 2.4G all matched `direct Stats / getSSIDStats() / wl if_counters rxdiscard = 0 / 0 / 0`. The older D325-style `/proc/net/dev_extstats` field `$5` path stayed stale at `565 / 3752 / 356`, so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/if_counters` probes rather than the proc-file heuristic. The committed metadata is refreshed from stale row `229` to workbook row `304`, targeted D304 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D305`
7.15. latest continuation after that: `D305 getSSIDStats() DiscardPacketsSent` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` overwrites `DiscardPacketsSent` from `wl if_counters txdiscard`. Live serialwrap replay on `COM0` then closed the matching three-way numeric oracle on every band: 5G, 6G, and 2.4G all matched `direct Stats / getSSIDStats() / wl if_counters txdiscard = 0 / 0 / 0`. The older D326-style `/proc/net/dev_extstats` field `$13` path stayed stale at `184 / 3467 / 87`, so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/if_counters` probes rather than the proc-file heuristic. The committed metadata is refreshed from stale row `230` to workbook row `305`, targeted D305 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D306`
7.16. latest continuation after that: `D306 getSSIDStats() ErrorsReceived` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` overwrites `ErrorsReceived` from `wl if_counters rxerror`. Live serialwrap replay on `COM0` then closed a four-way numeric oracle on every band: 5G, 6G, and 2.4G all matched `direct Stats / getSSIDStats() / wl if_counters rxerror / /proc/net/dev_extstats $4 = 0 / 0 / 0 / 0`. The committed metadata is refreshed from stale row `231` to workbook row `306`, targeted D306 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D307`
7.17. latest continuation after that: `D307 getSSIDStats() ErrorsSent` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` first seeds base-VAP `ErrorsSent` from `wl if_counters txerror` and `whm_brcm_vap_ap_stats_accu()` then accumulates matching `wds*` interface stats back into `SSID.stats`. Live serialwrap replay on `COM0` closed that source-backed oracle: 5G matched `direct/getSSIDStats/wds0.0.1 if_counters txerror = 56 / 56 / 56`, a focused 6G rerun matched `direct/getSSIDStats/wds1.0.1 if_counters txerror = 46 / 46 / 46`, and 2.4G held `direct/getSSIDStats/wl2 if_counters txerror = 0 / 0 / 0` with no matching WDS peer. The older D328-style `/proc/net/dev_extstats` field `$12` heuristic stayed at `0` on the base wl interfaces and is therefore stale for 5G/6G in the current 0403 baseline. The committed metadata is refreshed from stale row `232` to workbook row `307`, targeted D307 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D308`
7.18. latest continuation after that: `D308 getSSIDStats() FailedRetransCount` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` overwrites `FailedRetransCount` from `wl if_counters txretransfail`; if any matching `wds*` interface exists, the same `whm_brcm_vap_ap_stats_accu()` path would accumulate it, but the current live baseline stayed at zero. Live serialwrap replay on `COM0` then closed the matching three-way numeric oracle on every band: 5G, 6G, and 2.4G all matched `direct Stats / getSSIDStats() / wl if_counters txretransfail = 0 / 0 / 0`. The committed metadata is refreshed from stale row `233` to workbook row `308`, targeted D308 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D309`
7.19. latest continuation after that: `D309 getSSIDStats() MulticastPacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` first seeds `MulticastPacketsReceived` from `wl if_counters rxmulti`, `whm_brcm_vap_ap_stats_accu()` can accumulate matching `wds*` interface stats, and `whm_brcm_vap.c` then subtracts `tmp_stats.BroadcastPacketsReceived` before clamping the field at zero. Live serialwrap replay on `COM0` closed that source-backed formula on every band: 5G matched `direct / getSSIDStats / wl0 if_counters rxmulti / BroadcastPacketsReceived = 0 / 0 / 10 / 363`, 6G matched `0 / 0 / 0 / 145`, and 2.4G matched `0 / 0 / 0 / 113`, so all three bands authoritatively land at `max((rxmulti + matching wds_rxmulti) - BroadcastPacketsReceived, 0) = 0`. The older D330-style `/proc/net/dev_extstats` field `$9` heuristic stayed stale at `363 / 146 / 113`, so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/if_counters + broadcast-subtraction` probes. The committed metadata is refreshed from stale row `234` to workbook row `309`, targeted D309 tests are `3 passed`, full plugin runtime regression remains `1223 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D310`
7.20. latest continuation after that: `D310 getSSIDStats() MulticastPacketsSent` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` first seeds `MulticastPacketsSent` from `wl if_counters txmulti`, `whm_brcm_vap_ap_stats_accu()` can accumulate matching `wds*` interface stats, and `whm_brcm_vap.c` then subtracts `tmp_stats.BroadcastPacketsSent` before clamping the field at zero. Live serialwrap replay on `COM0` closed that source-backed formula on every band: 5G matched `direct / getSSIDStats / wl0 if_counters txmulti / BroadcastPacketsSent = 135098 / 135098 / 136632 / 1534`, 6G matched `76033 / 76033 / 77722 / 1689`, and 2.4G matched `150648 / 150648 / 152429 / 1781`, so all three bands authoritatively land at `max((txmulti + matching wds_txmulti) - BroadcastPacketsSent, 0)`. The older D331-style `/proc/net/dev_extstats` field `$18` heuristic stayed stale at `154277 / 148836 / 154585`, so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/if_counters + broadcast-subtraction` probes. The committed metadata is refreshed from stale row `235` to workbook row `310`, targeted D310 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D311`
7.21. latest continuation after that: `D311 getSSIDStats() PacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` first seeds `PacketsReceived` from `wl if_counters rxframe` and `whm_brcm_vap_ap_stats_accu()` can accumulate matching `wds*` interface packet counts before the final SSID stats snapshot is returned. Live serialwrap replay on `COM0` closed that source-backed oracle on every band: 5G matched `direct / getSSIDStats / wl0 if_counters rxframe / wds0.0.1 rxframe = 1082 / 1082 / 1080 / 2`, 6G matched `292 / 292 / 292 / 0`, and 2.4G matched `220 / 220 / 220 / 0`. The older D332-style `/proc/net/dev_extstats` field `$3` heuristic stayed stale at `1086 / 438 / 333`, so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/if_counters + WDS accumulation` probes. The committed metadata is refreshed from stale row `236` to workbook row `311`, targeted D311 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D312`
7.22. latest continuation after that: `D312 getSSIDStats() PacketsSent` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` first seeds `PacketsSent` from `wl if_counters txframe` and `whm_brcm_vap_ap_stats_accu()` can accumulate matching `wds*` interface packet counts before the final SSID stats snapshot is returned. Live serialwrap replay on `COM0` closed that source-backed oracle on every band: 5G matched `direct / getSSIDStats / wl0 if_counters txframe / wds0.0.1 txframe = 192311 / 192311 / 157138 / 35173`, 6G matched `91211 / 91211 / 89703 / 1510`, and 2.4G matched `156926 / 156926 / 156926 / 0`. The older D333-style `/proc/net/dev_extstats` field `$11` heuristic is no longer an all-band authority: 5G stayed at the base `wl0` counter `157138`, 6G drifted to `151237`, and only 2.4G still exact-closed because no matching WDS peer existed. The committed oracle therefore stays on short per-band `direct/getSSIDStats/if_counters + WDS accumulation` probes. The committed metadata is refreshed from stale row `237` to workbook row `312`, targeted D312 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D313`
7.23. latest continuation after that: `D313 getSSIDStats() RetransCount` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`, where `whm_brcm_get_if_stats()` seeds `RetransCount` from `wl if_counters txretrans` and `whm_brcm_vap_ap_stats_accu()` would accumulate matching `wds*` interface values when present. Live serialwrap replay on `COM0` closed that source-backed oracle on every band: 5G, 6G, and 2.4G all matched `direct / getSSIDStats / wl if_counters txretrans / matching wds txretrans = 0 / 0 / 0 / 0`. The adjacent D334 direct case already carries the same 0403 zero-shape comment (`direct Stats matched getSSIDStats(), but workbook v4.0.3 still remains To be tested`), so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/if_counters` probes rather than the stale wording. The committed metadata is refreshed from stale row `238` to workbook row `313`, targeted D313 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D314`
7.24. latest continuation after that: `D314 getSSIDStats() UnicastPacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`: `whm_brcm_get_if_stats()` first seeds `PacketsReceived` from `wl if_counters rxframe`, seeds `MulticastPacketsReceived` from `wl if_counters rxmulti`, and derives `UnicastPacketsReceived = PacketsReceived - MulticastPacketsReceived` before `whm_brcm_vap.c` later subtracts broadcast packets from the visible multicast field only. Live serialwrap replay on `COM0` closed that source-backed oracle on every band: 5G matched `direct / getSSIDStats / wl0 if_counters rxframe / wl0 if_counters rxmulti / wds0.0.1 rxframe / wds0.0.1 rxmulti = 1084 / 1084 / 1092 / 10 / 2 / 0`, 6G matched `292 / 292 / 292 / 0 / 0 / 0`, and 2.4G matched `220 / 220 / 220 / 0 / 0 / 0`. The older D335-style `/proc/net/dev_extstats` field `$21` heuristic stayed stale at `360 / 146 / 107`, so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/(rxframe + WDS rxframe) - (raw rxmulti + WDS rxmulti)` probes. The committed metadata is refreshed from stale row `239` to workbook row `314`, targeted D314 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D315`
7.25. latest continuation after that: `D315 getSSIDStats() UnicastPacketsSent` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`: `whm_brcm_get_if_stats()` first seeds `PacketsSent` from `wl if_counters txframe`, seeds `MulticastPacketsSent` from `wl if_counters txmulti`, and derives `UnicastPacketsSent = PacketsSent - MulticastPacketsSent` before `whm_brcm_vap.c` later subtracts broadcast packets from the visible multicast field only. Live serialwrap replay on `COM0` closed that source-backed oracle on every band: the focused 5G rerun matched `direct / getSSIDStats / wl0 if_counters txframe / wl0 if_counters txmulti / wds0.0.1 txframe / wds0.0.1 txmulti = 55992 / 55992 / 159684 / 140207 / 36515 / 0`, 6G matched `13317 / 13317 / 92193 / 81711 / 2835 / 0`, and 2.4G matched `2338 / 2338 / 159419 / 157081 / 0 / 0`. The older D336-style `/proc/net/dev_extstats` field `$22` heuristic stayed stale at `0` on all three bands, so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/(txframe + WDS txframe) - (raw txmulti + WDS txmulti)` probes. The committed metadata is refreshed from stale row `240` to workbook row `315`, targeted D315 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D316`
7.26. latest continuation after that: `D316 getSSIDStats() UnknownProtoPacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, and the active 0403 path again runs `_SSID_getSSIDStats()` through `s_updateSsidStatsValues()` into `whm_brcm_vap_update_ap_stats()`: `whm_brcm_get_if_stats()` seeds `UnknownProtoPacketsReceived` from `wl if_counters rxunknownprotopkts`, `whm_brcm_vap_copy_stats()` preserves that field for the base VAP snapshot, and `whm_brcm_vap_ap_stats_accu()` can accumulate matching `wds*` interface values before the final SSID stats snapshot is returned. Live serialwrap replay on `COM0` closed that source-backed oracle on every band: 5G matched `direct / getSSIDStats / wl0 if_counters rxunknownprotopkts / wds0.0.1 rxunknownprotopkts = 0 / 0 / 0 / 0`, 6G matched `0 / 0 / 0 / 0`, and 2.4G matched `0 / 0 / 0 / 0`. The adjacent D337 direct case still stays workbook-gated as `To be tested`, but its v4.0.3 comment already records the same multiband zero-shape (`direct Stats matched getSSIDStats()`), so the committed oracle deliberately stays on short per-band `direct/getSSIDStats/if_counters + WDS accumulation` probes. The committed metadata is refreshed from stale row `241` to workbook row `316`, targeted D316 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D317`
7.27. latest continuation after that: `D317 BSSID` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, where `WiFi.SSID.{i}.BSSID` is exposed as a read-only SSID property, and `dm_info.c` still advertises the same `BSSID` field on the active `WiFi.SSID` object. Live serialwrap replay on `COM0` then closed the independent oracle on every band by matching the property against both interface views: 5G `ubus BSSID / iw dev wl0 info addr / wl -i wl0 cur_etheraddr = 2c:59:17:00:19:95 / 2c:59:17:00:19:95 / 2c:59:17:00:19:95`, 6G matched `2c:59:17:00:19:96 / 2c:59:17:00:19:96 / 2c:59:17:00:19:96`, and 2.4G matched `2c:59:17:00:19:a7 / 2c:59:17:00:19:a7 / 2c:59:17:00:19:a7`. The workbook v4.0.3 comment already states the same `matching iw dev info` shape, so the committed oracle deliberately stays on short per-band `ubus BSSID -> iw dev info -> wl cur_etheraddr` probes. The committed metadata is refreshed from stale row `242` to workbook row `317`, targeted D317 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D319`
7.28. latest continuation after that: `D319 MACAddress` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, where `WiFi.SSID.{i}.MACAddress` remains exposed as a read-only SSID property on the active object model. Live serialwrap replay on `COM0` then closed a four-way independent oracle on every band by matching the property against three interface views: 5G `ubus MACAddress / iw dev wl0 info addr / ifconfig wl0 HWaddr / wl -i wl0 cur_etheraddr = 2c:59:17:00:19:95 / 2c:59:17:00:19:95 / 2c:59:17:00:19:95 / 2c:59:17:00:19:95`, 6G matched `2c:59:17:00:19:96 / 2c:59:17:00:19:96 / 2c:59:17:00:19:96 / 2c:59:17:00:19:96`, and 2.4G matched `2c:59:17:00:19:a7 / 2c:59:17:00:19:a7 / 2c:59:17:00:19:a7 / 2c:59:17:00:19:a7`. The committed oracle therefore stays on short per-band `ubus MACAddress -> iw dev info -> ifconfig -> wl cur_etheraddr` probes. The committed metadata is refreshed from stale row `321` to workbook row `319`, targeted D319 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D320`
7.29. latest continuation after that: `D320 SSID` is now aligned as a plain `Pass/Pass/Pass` row. Source-backed support is explicit in `wld_ssid.odl`, where `WiFi.SSID.{i}.SSID` remains exposed as a read-only SSID property on the active object model. Live serialwrap replay on `COM0` then closed the independent oracle on every band by matching the getter against the driver SSID view: 5G `ubus SSID / wl -i wl0 ssid = testpilot5G / testpilot5G`, 6G matched `testpilot6G / testpilot6G`, and 2.4G matched `testpilot2G / testpilot2G`. The committed oracle therefore stays on short per-band `ubus SSID -> wl ssid` probes. The committed metadata is refreshed from stale row `322` to workbook row `320`, targeted D320 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D321`
7.30. latest continuation after that: `D321 BroadcastPacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. Active 0403 source-backed support is already consistent with the previously closed D300 family, and live serialwrap replay on `COM0` closed the full three-way direct-property oracle on every band: 5G `direct Stats / getSSIDStats / /proc/net/dev_extstats $23 = 363 / 363 / 363`, 6G matched `147 / 147 / 147`, and 2.4G matched `113 / 113 / 113`. The committed oracle therefore stays on short per-band `Stats.BroadcastPacketsReceived -> getSSIDStats().BroadcastPacketsReceived -> /proc $23` probes instead of the older noisy multi-step traffic procedure. The committed metadata is refreshed from stale row `245` to workbook row `321`, targeted D321 tests are `3 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D322`
7.31. latest continuation after that: the resolver now preserves authored `getSSIDStats()` shell pipelines for direct-stats capture steps instead of overwriting them with synthesized plain property queries. That regression is locked by `test_multiband_direct_stats_preserve_authored_getssid_pipeline_commands`, the command-budget inventory moved from `612` to `618` because `D321/D322` now legitimately carry six long `getSSIDStats | sed` commands, and the full repo regression is green at `1634 passed`
7.32. latest continuation after that: `D321 BroadcastPacketsReceived` has now been rerun through the actual `testpilot run` path and truly passes as-authored in live runner execution (`run_id=20260411T002336420469`): 5G `364 / 364 / 364`, 6G `149 / 149 / 149`, and 2.4G `113 / 113 / 113` across direct/getSSIDStats//proc. The sibling `D322 BroadcastPacketsSent` also now executes the authored `getSSIDStats()` pipeline, but it remains blocked after two live attempts because 5G keeps drifting at `1644 / 1644 / 1645` then `1647 / 1647 / 1648` across direct/getSSIDStats//proc `$24`, while 6G/2.4G exact-close. Active 0403 source in `whm_brcm_vap.c` restores final public `BroadcastPacketsSent` from `tmp_stats` after later counter reads, so the old `/proc $24` equality is still treated as a stale heuristic; blocker handoff is recorded in `plugins/wifi_llapi/reports/D322_block.md`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D323`
7.33. latest continuation after that: `D323 BytesReceived` is now aligned in the real runner path. The earlier blocker turned out to be a stale workbook `/proc/net/dev_extstats` `$2` heuristic plus an incorrect source explanation; corrected 0403 tracing now shows `whm_brcm_get_if_stats()` seeds `BytesReceived` from `wl if_counters rxbyte`, `whm_brcm_vap_ap_stats_accu()` adds matching `wds*` `rxbyte`, and `whm_brcm_vap_update_ap_stats()` does not restore `BytesReceived` from `tmp_stats`. After rewriting the direct-property case to that source-backed oracle, official rerun `20260411T231952006453` exact-closed on all three bands: 5G `276282 / 276282 / 276282`, 6G `122610 / 122610 / 122610`, and 2.4G `73193 / 73193 / 73193` across direct/getSSIDStats/driver. The committed metadata is refreshed from stale row `247` to workbook row `323`, `plugins/wifi_llapi/reports/D323_block.md` is retained as historical resolution notes, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D324`
7.34. latest continuation after that: `D324 BytesSent` is now aligned as a plain `Pass/Pass/Pass` row. The first real runner replay (`20260411T005329099506`) proved workbook row `324`'s old `/proc/net/dev_extstats` `$10` compare was stale, but follow-up source trace through `whm_brcm_get_if_stats()` plus live serialwrap probes closed the real 0403 oracle at `wl if_counters txbyte`; after rewriting the direct-property case to that source-backed readback, real runner rerun `20260411T010328768651` exact-closed on all three bands: 5G `79412272 / 79412272 / 79412272`, 6G `47109866 / 47109866 / 47109866`, and 2.4G `79349200 / 79349200 / 79349200` across direct/getSSIDStats/if_counters. The committed metadata is refreshed from stale row `248` to workbook row `324`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D325`
7.35. latest continuation after that: `D325 DiscardPacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. The prior real runner replay (`20260411T010859993578`) proved workbook row `325`'s old `/proc/net/dev_extstats` `$5` compare was stale (`565 / 3854 / 356`-style proc drift), while active 0403 source had already been locked by the aligned `D304` path to `whm_brcm_get_if_stats()` / `wl if_counters rxdiscard`; after rewriting the direct-property case to that source-backed readback, real runner rerun `20260411T011321267947` exact-closed on all three bands: 5G `0 / 0 / 0`, 6G `0 / 0 / 0`, and 2.4G `0 / 0 / 0` across direct/getSSIDStats/if_counters. The committed metadata is refreshed from stale row `249` to workbook row `325`, targeted official-case validation is `1 passed`, full repo regression is green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D326`
7.36. latest continuation after that: `D326 DiscardPacketsSent` is now aligned as a plain `Pass/Pass/Pass` row. The prior real runner replay (`20260411T012137925510`) proved workbook row `326`'s old `/proc/net/dev_extstats` `$13` compare was stale (`184 / 3467 / 87`-style proc drift), while active 0403 source is now re-confirmed by the fresh D326 survey and the already aligned `D305` path to `whm_brcm_get_if_stats()` / `wl if_counters txdiscard`; after rewriting the direct-property case to that source-backed readback, real runner rerun `20260411T012538161460` exact-closed on all three bands: 5G `0 / 0 / 0`, 6G `0 / 0 / 0`, and 2.4G `0 / 0 / 0` across direct/getSSIDStats/if_counters. The committed metadata is refreshed from stale row `250` to workbook row `326`, targeted official-case validation is `1 passed`, full repo regression is green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D327`
7.37. latest continuation after that: `D327 ErrorsReceived` is now aligned as a plain `Pass/Pass/Pass` row. The first real runner replay (`20260411T013241354703`) already exact-closed the legacy `/proc/net/dev_extstats` `$4` compare at zero on all three bands, but active 0403 source is now re-confirmed by the fresh D327 survey and the already aligned `D306` path to `whm_brcm_get_if_stats()` / `wl if_counters rxerror`; after refreshing the direct-property case to that source-backed readback, real runner rerun `20260411T013801878458` still exact-closed on all three bands: 5G `0 / 0 / 0`, 6G `0 / 0 / 0`, and 2.4G `0 / 0 / 0` across direct/getSSIDStats/if_counters. The committed metadata is refreshed from stale row `251` to workbook row `327`, targeted official-case validation is `1 passed`, full repo regression remains green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D328`
7.38. latest continuation after that: `D328 ErrorsSent` is now aligned as a plain `Pass/Pass/Pass` row. The first real runner replay (`20260411T014458979418`) already exact-closed the legacy `/proc/net/dev_extstats` `$12` compare at zero on all three bands, but active 0403 source is now re-confirmed by the fresh D328 survey and the already aligned `D307` path: `whm_brcm_get_if_stats()` seeds `ErrorsSent` from `wl if_counters txerror`, while `whm_brcm_vap_ap_stats_accu()` can accumulate matching `wds*` stats if present. A focused live probe also showed this is a real moving counter rather than a fixed workbook sample (`5G=1`, `6G=3347`, `2.4G=0` across direct/getSSIDStats/txerror). After refreshing the direct-property case to the source-backed `txerror` readback, real runner rerun `20260411T015126498621` still exact-closed on all three bands: 5G `0 / 0 / 0`, 6G `0 / 0 / 0`, and 2.4G `0 / 0 / 0` across direct/getSSIDStats/if_counters. The committed metadata is refreshed from stale row `252` to workbook row `328`, targeted official-case validation is `1 passed`, full repo regression remains green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D329`
7.39. latest continuation after that: `D329 FailedRetransCount` is now aligned as a plain `Pass/Pass/Pass` row. The first real runner replay (`20260411T015905984272`) exact-closed direct Stats and getSSIDStats() at zero on all three bands, but it still lacked an independent driver oracle. Active 0403 source is now re-confirmed by the fresh D329 survey and the already aligned `D308` path: `whm_brcm_get_if_stats()` writes `FailedRetransCount` from `wl if_counters txretransfail`, while `whm_brcm_vap_ap_stats_accu()` would only add matching `wds*` peer counters if they existed. A focused live probe then exact-closed `direct / getSSIDStats / txretransfail` at `0 / 0 / 0` on all three bands and also confirmed there is no active `wds*` peer in the current baseline. After refreshing the direct-property case to the source-backed `txretransfail` readback, real runner rerun `20260411T020534026608` still exact-closed on all three bands: 5G `0 / 0 / 0`, 6G `0 / 0 / 0`, and 2.4G `0 / 0 / 0` across direct/getSSIDStats/if_counters. The committed metadata is refreshed from stale row `253` to workbook row `329`, targeted official-case validation is `1 passed`, full repo regression remains green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D334`
7.40. latest continuation after that: `D334 RetransCount` is now aligned as a plain `Pass/Pass/Pass` row. The first real runner replay (`20260411T021238026451`) exact-closed direct Stats and getSSIDStats(), but it still lacked an independent driver oracle and the original extractor could ambiguously match `FailedRetransCount`. Active 0403 source is now re-confirmed by the fresh D334 survey at `wldm_SSID_TrafficStats()` -> `wl if_counters txretrans`, with no WDS accumulation on the direct-property path. A focused live probe then closed the source-backed low-32 driver view at 5G `4294967295`, 6G `4294963915`, and 2.4G `0` across direct/getSSIDStats/low-32(txretrans). After refreshing the direct-property case to use anchored `getSSIDStats()` extraction plus the low-32 `txretrans` oracle, real runner rerun `20260411T022030741126` passed on retry: attempt 1 still saw a transient 6G drift (`4294967294` vs driver `0`), but attempt 2 exact-closed all three bands at `0 / 0 / 0`. The committed metadata is refreshed from stale row `258` to workbook row `334`, targeted official-case validation is `1 passed`, the command-budget inventory now tracks `630` long official-case commands, full repo regression remains green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D337`
7.41. latest continuation after that: `D337 UnknownProtoPacketsReceived` is now aligned as a plain `Pass/Pass/Pass` row. The first real runner replay (`20260411T023258929853`) already exact-closed direct Stats and getSSIDStats() at zero on all three bands, but it still lacked an independent driver oracle. Active 0403 source is now re-confirmed by the fresh D337 survey at `wldm_SSID_TrafficStats()` -> `wl if_counters rxbadprotopkts`, without WDS accumulation on the direct-property path. A focused live probe then exact-closed `direct / getSSIDStats / rxbadprotopkts` at `0 / 0 / 0` on all three bands, while the adjacent getSSIDStats-family `rxunknownprotopkts` view also stayed `0 / 0 / 0`; this explains why the workbook-era replay exact-closed even though the two counter families are distinct in source. After refreshing the direct-property case to the source-backed `rxbadprotopkts` oracle, real runner rerun `20260411T024443960794` exact-closed all three bands at `0 / 0 / 0`, targeted official-case validation is `1 passed`, the command-budget inventory now tracks `633` long official-case commands, full repo regression remains green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D406`
7.42. latest continuation after that: `D406 MultipleRetryCount` is now aligned as a plain `Pass/Pass/Pass` row. The first real runner replay (`20260411T025549740195`) already exact-closed direct Stats and getSSIDStats() at zero on all three bands, but it still lacked an independent driver oracle. Active 0403 source is now re-confirmed at `wldm_SSID_TrafficStats()` -> `wl if_counters txretrie`; a focused live probe then exact-closed `direct / getSSIDStats / txretrie` at `0 / 0 / 0` on 5G, 6G, and 2.4G, while `ls /sys/class/net | grep '^wds'` stayed empty in the current baseline. After refreshing the direct-property case to use anchored `getSSIDStats()` extraction plus the `txretrie` driver oracle, real runner rerun `20260411T025954644775` exact-closed all three bands at `0 / 0 / 0`, targeted official-case validation is `1225 passed`, the command-budget inventory now tracks `636` long official-case commands, full repo regression remains green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D407`
7.43. latest continuation after that: `D407 RetryCount` is now aligned as a plain `Pass/Pass/Pass` row. The first real runner replay (`20260411T031324456196`) already exact-closed direct Stats and getSSIDStats() at zero on all three bands, but the workbook-era extractor still ambiguously matched `MultipleRetryCount` and there was no independent driver oracle. Active 0403 source is now re-confirmed at `wldm_SSID_TrafficStats()` -> `wl if_counters txretry`; a focused live probe then exact-closed `direct / getSSIDStats / txretry` at `0 / 0 / 0` on 5G, 6G, and 2.4G, while `ls /sys/class/net | grep '^wds'` again stayed empty in the current baseline. After refreshing the direct-property case to use anchored `RetryCount` extraction plus the `txretry` driver oracle, real runner rerun `20260411T031645170662` exact-closed all three bands at `0 / 0 / 0`, targeted official-case validation is `1225 passed`, the command-budget inventory now tracks `642` long official-case commands, full repo regression remains green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D528`
7.44. latest continuation after that: `D528 getSpectrumInfo bandwidth` is now aligned as a plain `Pass/Pass/Pass` row. The workbook-era replay (`20260411T032529278022`) had already proved the numeric-only regex was stale because active 0403 returned `bandwidth="20MHz"` on all three bands. Active 0403 source is now closed at `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(cstring_t, "bandwidth", swl_bandwidth_str[llEntry->bandwidth])`, while the sync refresh path `wifiGen_rad_getSpectrumInfo(..., update=false)` seeds survey-derived spectrum entries with `SWL_BW_20MHZ` in `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()`. After refreshing the case to the source-backed exact string shape and updating the spectrum guardrail fixture to use live-shaped synthetic output, real runner rerun `20260411T034134858534` exact-closed on all three bands at `20MHz`, targeted official-case validation stayed green at `1225 passed`, full repo regression remained green at `1634 passed`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D529`
7.45. latest continuation after that: shared baseline/runtime hardening has now cleared the clean-start blocker that had re-opened `D324 BytesSent`. Unresolved placeholder `sta_env_setup` templates are now treated as templates and skipped in favor of deterministic auto-baseline, `_env_command_succeeded()` now rejects missing wpa config / ctrl-ifname / placeholder-traffic failures, and the 6G OCV stabilization loop now treats a restarted `wl1` `hostapd` process as sufficient even when the control socket is briefly absent during clean start. Clean-start official rerun `20260412T033924192464` therefore no longer failed at `assoc_5g` / baseline setup: both attempts reached `evaluate`, attempt 1 failed at 5G `329835 / 329835 / 305843`, and attempt 2 failed at 5G `562142 / 562414 / 537347` while 6G/2.4G only showed smaller driver lag (`402490 / 402490 / 402332`, `611651 / 611651 / 611493`). So the shared environment blocker is cleared, but `D324` itself remains blocked as a runner-path counter-refresh problem; `plugins/wifi_llapi/reports/D324_block.md` is now the superseding authority, targeted guardrails are `11 passed`, full repo regression is `1640 passed`, and the next ready open-set revisit moves to `D331`
7.46. latest continuation after that: `D331 MulticastPacketsSent` is now aligned in the current repo state. The superseding clean-start official rerun `20260412T040941971904` proved the residual `+1/+2` drift was still a command-order artifact inside the runner path: reading raw `wl if_counters txmulti (+ matching wds txmulti)` **before** the public snapshot, then parsing `MulticastPacketsSent` and `BroadcastPacketsSent` from a single `getSSIDStats()` capture, closes the source-backed formula `max((txmulti + matching wds txmulti) - BroadcastPacketsSent, 0)` against the direct getter inside one per-band snapshot. Attempt 1 exact-closed all three bands at `904/904/904`, `732/732/732`, and `973/973/973`, so the committed case is now refreshed from stale row `255` to workbook row `331`, `results_reference.v4.0.3` is now `Pass/Pass/Pass`, `plugins/wifi_llapi/reports/D331_block.md` is retained only as historical trial evidence, and the next ready open-set revisit moves to `D333`
7.31. latest continuation after that: the resolver now preserves authored `getSSIDStats()` shell pipelines for direct-stats capture steps instead of overwriting them with synthesized plain property queries. That regression is locked by `test_multiband_direct_stats_preserve_authored_getssid_pipeline_commands`, the command-budget inventory moved from `612` to `618` because `D321/D322` now legitimately carry six long `getSSIDStats | sed` commands, and the full repo regression is green at `1634 passed`
7.32. latest continuation after that: `D321 BroadcastPacketsReceived` has now been rerun through the actual `testpilot run` path and truly passes as-authored in live runner execution (`run_id=20260411T002336420469`): 5G `364 / 364 / 364`, 6G `149 / 149 / 149`, and 2.4G `113 / 113 / 113` across direct/getSSIDStats//proc. The sibling `D322 BroadcastPacketsSent` also now executes the authored `getSSIDStats()` pipeline, but it remains blocked after two live attempts because 5G keeps drifting at `1644 / 1644 / 1645` then `1647 / 1647 / 1648` across direct/getSSIDStats//proc `$24`, while 6G/2.4G exact-close. Active 0403 source in `whm_brcm_vap.c` restores final public `BroadcastPacketsSent` from `tmp_stats` after later counter reads, so the old `/proc $24` equality is still treated as a stale heuristic; blocker handoff is recorded in `plugins/wifi_llapi/reports/D322_block.md`, and the superseding next ready patch-driven workbook-Pass case in the current repo inventory is `D323`
8. `wifi_llapi_template.xlsx` 已重新由 repo-root `0401.xlsx` 重建，alignment manifest 現在回指 `source_workbook=0401.xlsx`；先前的 template row drift 不再是 `D017/D018` 的假 warning 來源。
9. 本輪 campaign 已固定忽略 workbook `F` 欄；manual procedure authority 改以 `Wifi_LLAPI` 的 `G=Test steps` / `H=Command Output` 為準。
10. preflight guardrail audit 已完成：official-case multiline block scalar ban 仍有效，serialwrap 120-char staging guardrail 仍有效，並新增 official-case `>120` 字元 command inventory guardrail；目前 inventory 為 **612**，先作為 tracked risk 保留，依賴 transport temp-script staging 執行。
11. baseline hardening 已完成；`wifi-llapi baseline-qualify` 已落地 standalone qualification workflow（preflight snapshot / serial recover / conditional `firstboot` / repeat + soak loop）。最新完整 full repo suite 為 **1627 passed**；最新 regression 已再往前推進到 6G OCV stabilization loop（targeted `5 passed`、plugin runtime `1222 passed`、full suite `1627 passed`），而先前的 STA 2.4G reconnect fallback hardening仍維持在 clean-start prefix 內不再出現 `sta_24g verify attempt` warning。2026-04-10 的 workbook replay / source-backed triage 又把 `D111 getStationStats() AssociationTime` 收斂成 parser bug：`_extract_key_values()` 對 `AssociationTime = "YYYY-MM-DDTHH:MM:SSZ",` 先 strip quote 再去尾逗號，留下錯誤的尾端 `"`；修正順序後，live single-case rerun `20260410T110659169758` 已讓 `D111` plain `Pass`，完整 `plugins/wifi_llapi/tests/test_wifi_llapi_plugin_runtime.py` 亦提升到 `1223 passed`。
12. lab UART baseline qualification 已正式通過：`COM0/COM1` 在 `5G`、`6G`、`2.4G` 三個 band 都達成 `repeat-count=5` 與 `soak-minutes=15` 的連續成功門檻，top-level `overall_status=stable`。`6G` 先前 blocker 已由「stale hostapd control socket + restart 後需 explicit `wl1 bss up`」收斂並修入 repo hot path；在 2026-04-09 的後續 triage 中，又進一步收斂出 `D009/D010/D012` 共用的 STA 6G pre-start 漏洞：deterministic path 在啟 `wpa_supplicant` 前缺少 `wl -i wl1 up` / `ifconfig wl1 up`。最新 drift follow-up 再證明：這個修正雖必要，但在 full-run sequential context 下還需要更強的 client-mode rebase（`ifconfig wl1 down` / `wl -i wl1 down` / `wl -i wl1 ap 0`）才能把 6G STA 從漂移狀態拉回 managed client。這一層已寫回 `band-baselines.yaml` + runtime hot path，後續又補上 custom 6G DUT-side OCV gate 與 D019/D027 timing hardening；第二次 clean-start targeted rerun `20260409T133204470798` 已讓 `D019/D027` 兩案都變成 plain `Pass`。`D006` 也已證明不再是 baseline blocker，而是 `sendBssTransferRequest()` 真實 API error。舊 cold-boot full run `20260409T133621374859` 之後已在 evidence through `D072` 後刻意停止；其唯一 environment failure 是舊 code 的 `D032 sta_band_not_ready`。替代性的 dual-`firstboot` targeted rerun `20260409T162704156901` 先證明 `D028-D033` 已不再重現 skip-step environment blocker；再往後的 source/runtime triage 又把 `D028` 首輪的 DUT 5G/2.4G warm-up bounce 收斂成 `_dut_baseline_commands()` 在 primary AP 仍 enable 時直接重寫 SSID/security 的 sequencing 問題，repo 現已改成 primary AP 先 disable 再 reconfig。後續補上的 STA 2.4G reconnect hardening（`iw dev wl2 link` -> `wl -i wl2 status` fallback + `sleep 8`）已在 dual-`firstboot` rerun `20260409T182347586948` 再次維持 `D028/D030/D032/D033` plain `Pass`、`D029` testcase/data-model `WiFi.AccessPoint.1.AssociatedDevice.1.Mode` parameter-not-found fail、`D031` `results_reference.v4.0.3=Fail` 的 fail-shaped `Not Supported` stub mismatch，而且 rerun transcript 已不再出現 `sta_24g verify attempt` warning；這個 prefix slice 的 cold-start 環境噪聲已可視為清除。在此之後，6G OCV hot path 又補成短窗口 `ocv/socket/bss` stabilization loop；clean-start single-case rerun `20260409T204714048332` 已先把 `D004` 驗成 plain `Pass`，接著 dual-`firstboot` prefix rerun `20260409T205434740233` 進一步驗成 `D004/D005` plain `Pass`、`D006` `pass after retry (2/2)`，舊的 `D005/D006 FailEnv` prefix 已消失。較早的 session-attached official full run `run_id=20260409T212035314017` 為避免 session-lifetime interruption 已主動 supersede；目前正式持續執行中的 detached full run 為 `run_id=20260409T213837737224`，並掛上每小時 Telegram progress notifier。
13. reboot 後的 default lab baseline 已重建並落成文件：5G `testpilot5G` / 2.4G `testpilot2G` 使用 `WPA2-Personal + 00000000`，6G `testpilot6G` 固定使用 `WPA3-Personal + key_mgmt=SAE + 00000000`。
14. 第三次重構的 Copilot SDK 深度研究已完成；repo 內只保留收斂後的規格與計畫結論，詳細研究筆記改為 local-only。
15. 校正工作守則已補強為「不中斷持續作業」：commit、簡短狀態回覆與 targeted tests pass 都不是停點；只要沒有 blocker 或使用者要求暫停，就必須在同一輪把下一個 ready case 直接推進到 `in_progress`。
16. **3x full run determinism 驗證完成**（2026-03-25）：420 cases × 3 runs 全部 exit=0，Run 2/3 verdict 100% 一致，Run 1 僅 2/355 rows 差異（baseline warm-up 效應）。修復 4 個 live runtime bugs：DUT kernel flood、5G ModeEnabled reversion、6G connect fatal、multi-line printf split。詳見 `plugins/wifi_llapi/reports/3x_determinism_report_20260325.md`。
17. **serialwrap human-agent 共存**（2026-03-26）：新增 `wal.reset` / `wal.current_seq` RPC + `session.bind` 冪等化（ser-dep PR #18）。testpilot 改用 `wal.reset` 取代 daemon restart，保留 human console (minicom) 連線（PR #7）。WAL fan-out 驗證通過。
18. **Copilot SDK 原生支援 Azure BYOK**（2026-03-27 確認）：`SessionConfig.provider` 支援 `type: "azure"`，`COPILOT_PROVIDER_*` env vars 可切換認證。詳見下方 §8。
19. **`wifi_llapi` live remediation 已接入 hot path**（2026-03-31）：`on_failure -> structured decision -> whitelist remediation executor -> on_retry` 已落地；範圍限定於 safe environment repair（serial session recover / STA reconnect / band baseline / env reverify），不改 testcase semantics 或 final verdict authority。

### 2.2 尚未落地

1. P2 Environment modules（topology / provisioner / validator）— 低優先。
2. P3-02 Monitor subsystem — 規格未定。
3. R5-04 Session / device binding 嚴格化 — 低優先。
4. skill/mcp 對 session request 的深度整合仍未接完；目前 hot path 已接上 hook dispatcher、advisory collector、live remediation coordinator 與 md/json reporter projection。

---

## 3. 不可改變的設計決策

### 3.1 Control plane / verdict plane 分層

- **Copilot SDK control plane**：session、resume、hooks、custom agents、skills、selective MCP、operator UX。
- **Deterministic verdict kernel**：YAML semantics、transport execution、pass criteria comparison、canonical result、report projection。

### 3.2 Final verdict authority

- `plugin.evaluate()` 與 deterministic rerun outcome 才能決定最終 `Pass/Fail`。
- advisory agent 可以補充 root cause / suggestion / diagnostic status，但不可覆寫 canonical evidence。

### 3.3 報告投影分離

- `xlsx`：Pass / Fail only
- `md/json`：`Pass`、`PassAfterRemediation`、`FailEnv`、`FailConfig`、`FailTest`、`Inconclusive` + root cause + suggestion + remediation history

### 3.4 Agent / model policy（第三次重構目標）

1. Priority 1：`copilot + gpt-5.4 + high`
2. Priority 2：`copilot + sonnet-4.6 + high`
3. Priority 3：`copilot + gpt-5-mini + high`
4. 不再維持 Codex CLI workaround 相容層。

### 3.5 執行策略不變

- 粒度：`per_case`
- 排程：`sequential (max_concurrency=1)`
- 失敗策略：`retry_then_fail_and_continue`
- Timeout：retry-aware、cap 保留
- Selection trace：mandatory

---

## 4. 第三次重構 Phase

### Phase R4：Copilot SDK 控制平面

| ID | 項目 | 輸出 | 狀態 |
|---|---|---|---|
| R4-00 | 第三次重構研究 / 文件基線 | 研究報告與 docs sync 完成 | done |
| R4-01 | Copilot SDK session foundation | SDK session wire-in with create/cleanup lifecycle | done |
| R4-02 | hook policy layer | 6 lifecycle hooks: pre/post_case, pre/post_step, on_failure, on_retry | done |
| R4-03 | custom agents roles | executor / advisor / remediation / observer + role merging | done |
| R4-04 | skills packages | SkillRegistry + SKILL.md discovery + role-based resolution | done |
| R4-05 | advisory agent outputs | AdvisoryOutput + AdvisoryCollector + IHook handler factory | done |
| R4-06 | remediation planner loop | post-run planner + in-run safe remediation loop（failure snapshot / whitelist executor / retry trace） | done |
| R4-07 | runtime policy alignment | plugin agent-config / runner policy 改為 copilot-only order | done |
| R4-08 | selective MCP | MCPRegistry + role-selective server management | done |

### Phase R5：Deterministic kernel 補強

| ID | 項目 | 輸出 | 狀態 |
|---|---|---|---|
| R5-01 | serialwrap RC 擷取修正 | FIRST match return code | done |
| R5-02 | Plugin loader 改良 | sys.path try/finally cleanup | done |
| R5-03 | openpyxl API 隔離 | excel_adapter.py 封裝 | done |
| R5-04 | Session / device binding 嚴格化 | 減少 fallback、避免靜默選錯裝置 | pending |
| R5-05 | 測試覆蓋 >80% | coverage baseline 81% (2492 stmts) | done |
| R5-06 | MD/JSON report projector | IReporter + MarkdownReporter + JsonReporter | done |
| R5-07 | `execute_step` heuristic 收斂 | CommandResolver strategy pattern refactor | done |
| R5-08 | control-plane / verdict-plane 邊界測試 | 15 boundary tests for hook/retry/timeout/band | done |

### 4.3 舊 Phase 的位置

- R1：Report / 指令清理（低風險，已大幅落地）
- R2：Orchestrator 解耦（仍重要）
- R3：Plugin Template / PluginBase 瘦身（仍重要）
- R4：第三次重構，正式導入 Copilot SDK control plane
- R5：第三次重構期間同步進行的 kernel hardening

### 執行順序

```text
R1 / R2 / R3 kernel 邊界整理
    -> R4-01 ~ R4-04 control-plane foundation
    -> R5-04 / R5-07 / R5-08 kernel boundary hardening
    -> R4-05 / R4-06 advisory audit + live safe remediation
    -> R4-08 selective MCP
```

---

## 5. 歷史 Phase（摘要）

| Phase | 內容 | 狀態 |
|---|---|---|
| P0 | Scaffold（目錄 / pyproject / loader / schema / cli） | done |
| P1 | Transport Layer（serialwrap / adb / ssh / network） | done |
| P2 | Environment Management（topology / provisioner / validator） | partial |
| P3 | Core Engine（runner loop / monitor / reporter / verdict merge） | done (except P3-02 Monitor) |
| P4 | Wifi_LLAPI Plugin（完整 runtime + cases） | done |
| P5 | CLI & Integration（dispatcher / trace / retry / tests） | done |

---

## 6. 風險與 Gate

1. **Copilot SDK 為 technical preview**：先導入 control plane，再逐步放大使用範圍。
2. **不可把 YAML 當主要 prompt**：正式測試仍由 kernel 執行。
3. **不可讓 advisory output 直接變更 verdict**：必須經 whitelist remediation + deterministic rerun。
4. **不得為舊 Codex CLI policy 補 workaround**：新 policy 以 Copilot SDK 為主體。
5. **文件與政策需一致化**：`spec.md`、`plan.md`、`todos.md`、`README.md`、`AGENTS.md` 必須同步。

---

## 7. 文件治理與參考

1. `docs/todos.md` 是唯一待辦與狀態來源。
2. `docs/spec.md` 是系統規格與架構邊界來源。
3. 第三次重構深度研究的 repo 內基線以 `docs/spec.md` 與本檔收斂結論為準；詳細研究筆記維持 local-only。
4. `README.md` 提供對外的 current/target 摘要。
5. `AGENTS.md` 定義專案級 agent/model/policy 規則。

---

## 8. Azure OpenAI BYOK 整合計畫（2026-03-27）

### 8.1 背景

test team 沒有 GitHub Copilot 存取權限。需要讓 testpilot 建立的 agent session 可以透過 Azure OpenAI API 認證，而非 GitHub OAuth。

### 8.2 核心發現

**Copilot SDK v0.1.23 原生支援 Azure BYOK**：

- `SessionConfig.provider` TypedDict 支援 `type: Literal["openai", "azure", "anthropic"]`
- `ProviderConfig` 欄位：`type`, `base_url`, `api_key`, `bearer_token`, `wire_api`, `azure`
- `AzureProviderOptions` 欄位：`api_version`
- SDK `client.py:486-489`：`create_session()` 會讀取 `provider` 並傳給 CLI server
- SDK `client.py:1048-1076`：`_convert_provider_to_wire_format()` 做 snake_case → camelCase 轉換
- **`CopilotSessionRequest` 已有 `provider` 欄位**（`copilot_session.py:61`），`_base_config()` 已處理傳遞

Copilot CLI 也有官方 BYOK 支援（`copilot help providers`）：
```bash
COPILOT_PROVIDER_TYPE=azure \
COPILOT_PROVIDER_BASE_URL=https://my-resource.openai.azure.com \
COPILOT_PROVIDER_API_KEY=your-key-here \
COPILOT_MODEL=gpt-4 \
copilot
```

### 8.3 設計決策

| 決策 | 選擇 | 理由 |
|------|------|------|
| API 風格 | Chat Completions | Azure 所有版本支援，相容性最佳 |
| 認證切換方式 | env vars + agent-config.yaml | 跟 Copilot CLI 官方 env var 一致 |
| 架構改動 | 不新建 provider 層 | SDK 原生支援，只需傳遞 config |
| 新增 dependency | 無 | 完全用現有 Copilot SDK 功能 |

### 8.4 使用方式

**外層（Copilot CLI 對話，如現在）+ 內層（testpilot session）可各自選擇認證**：

| 層 | 認證 | 設定方式 |
|---|---|---|
| 外層 Copilot CLI | GitHub OAuth（預設）或 Azure BYOK | `COPILOT_PROVIDER_*` env vars |
| 內層 testpilot session | GitHub OAuth（預設）或 Azure BYOK | agent-config.yaml `provider` 或 env vars |

兩層完全獨立，互不影響。

**使用 Azure 的完整設定**：
```bash
# 必要 env vars（從 Azure Portal 取得）
export COPILOT_PROVIDER_TYPE=azure
export COPILOT_PROVIDER_BASE_URL=https://your-resource.openai.azure.com
export COPILOT_PROVIDER_API_KEY=your-azure-api-key
export COPILOT_MODEL=gpt-4o                            # Azure deployment name

# 選填（有預設值）
export COPILOT_PROVIDER_AZURE_API_VERSION=2024-10-21   # 預設 2024-10-21
export COPILOT_PROVIDER_WIRE_API=completions            # 預設 completions

# 指令完全不變
testpilot run wifi_llapi
testpilot run wifi_llapi --case wifi-llapi-D004-kickstation
```

**或在 agent-config.yaml 中配置**（避免 key 用 env var 引用）：
```yaml
provider:
  type: azure
  base_url: ${COPILOT_PROVIDER_BASE_URL}
  api_key: ${COPILOT_PROVIDER_API_KEY}
  wire_api: completions
  azure:
    api_version: "2024-10-21"
```

### 8.5 Azure 資訊需求

從 Azure Portal 取得（需公司 Azure 管理員提供）：

| 項目 | 說明 | 取得位置 |
|------|------|---------|
| API Key | Azure OpenAI 資源金鑰 | Azure Portal → Keys and Endpoint |
| Endpoint URL | 資源專屬 URL（公司指定，非通用） | Azure Portal → Keys and Endpoint |
| Deployment name | 管理員部署的模型名稱 | Azure Portal → Model deployments |

Endpoint URL 格式：`https://{資源名稱}.openai.azure.com`。同一組 key/endpoint 可供多個應用共用（n8n、testpilot 等），但共享 TPM 配額。

連線驗證：`curl -I https://your-resource.openai.azure.com/` 回 HTTP 404 即表示網路可達。

### 8.6 實作 Todos

#### T1: copilot_session.py — 讀取 provider config

- 新增 `_resolve_provider_config(agent_config: dict) -> dict | None`
  - 優先讀 `agent_config["provider"]` section
  - Fallback 讀 `COPILOT_PROVIDER_*` env vars
  - 支援 `${VAR_NAME}` 內插（YAML 值引用 env var）
  - 回傳 `ProviderConfig` dict 或 None
- 更新 `build_case_session_plan()` 接受 `agent_config` 參數
  - 呼叫 `_resolve_provider_config()` 取得 provider
  - 如果有 provider，放進 session plan dict（key: `"provider"`）
  - 如果沒有，維持現狀（GitHub OAuth）

檔案：`src/testpilot/core/copilot_session.py`

#### T2: orchestrator.py — 傳遞 provider config

- `_create_case_session()` 從 session plan 取 `provider` config
- 傳入 `CopilotSessionRequest(provider=provider_config)`
- 更新 `_run_wifi_llapi()` 中 `build_case_session_plan()` 的呼叫，加入 `agent_config`
- 不改其他邏輯

檔案：`src/testpilot/core/orchestrator.py`

#### T3: 測試

- 新建 `tests/test_azure_byok.py`
- 測試 `_resolve_provider_config()`：
  - env vars only → 正確建構 provider dict
  - YAML config only → 正確讀取
  - 兩者都有 → YAML 優先
  - 都沒有 → 回傳 None
  - `${VAR}` 內插
- 測試 `build_case_session_plan()` with provider
- Mock SDK `create_session` 驗證 provider 正確傳入 request
- 驗證無 provider config 時行為不變
- 確認既有 tests 不受影響

#### T4: 文件更新

- `README.md`：Azure BYOK setup section（env vars + 使用方式）
- `plugins/wifi_llapi/agent-config.yaml`：加入 provider 範例（註解形式）
- `AGENTS.md`：更新 Plugin Agent Config Policy，加入 provider 說明

### 8.7 預計檔案變更

| 檔案 | 變更 |
|------|------|
| `src/testpilot/core/copilot_session.py` | 新增 `_resolve_provider_config()`，更新 `build_case_session_plan()` |
| `src/testpilot/core/orchestrator.py` | `_create_case_session()` 傳遞 provider，`_run_wifi_llapi()` 傳 agent_config |
| `tests/test_azure_byok.py` | **新建** — provider config 解析 + session plan 測試 |
| `plugins/wifi_llapi/agent-config.yaml` | 加入 provider 範例註解 |
| `README.md` | Azure BYOK setup section |
| `AGENTS.md` | 更新 policy |

### 8.8 關鍵原則

1. **Zero breaking change**：沒有 `COPILOT_PROVIDER_*` env vars 且 config 沒有 `provider` section 時，行為完全不變。
2. **Env var naming 一致**：跟 Copilot CLI 官方 env var 名稱一致（`COPILOT_PROVIDER_*`），不另外發明。
3. **Config override**：agent-config.yaml 的 `provider` section 可覆蓋 env vars（per-plugin 需求）。
4. **No new dependencies**：完全用現有 Copilot SDK 功能，不需要 `openai` package。

### 8.9 分支

- Branch: `feat/azure-openai-support`（已建立，base: `main`）
- 狀態：**Planning — 待開工**
