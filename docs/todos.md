# TestPilot Todo Board（Single Source of Truth）

## 治理規則

1. 本檔是專案唯一待辦清單。
2. 狀態僅允許：`pending`、`in_progress`、`done`、`blocked`。
3. 非 Plan Mode：不得增減項次，不得調整 ID。
4. 非 Plan Mode：僅可更新「狀態」與必要註記。
5. 已核准的重構計畫實作，需對齊 `docs/plan.md` 的 phase 與邊界。
6. 本檔狀態需同步反映 `docs/plan.md` 的現況快照。

---

## Phase 0：Scaffold

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P0-01 | scaffold（目錄與 pyproject） | done | 已建立 |
| P0-02 | plugin-base（PluginBase + Loader） | done | 已建立 |
| P0-03 | case-schema 驗證 | done | 已建立 |
| P0-04 | testbed-config 解析 | done | 已建立 |
| P0-05 | transport-base（StubTransport） | done | 已建立 |
| P0-06 | skeleton-cli | done | 已具備 `list-plugins/list-cases/run` |
| P0-07 | skeleton-orchestrator | done | 已可載入 plugin/cases |
| P0-08 | wifi-plugin-stub | done | plugin hook 目前仍為 stub |
| P0-09 | docs-init | done | 已完成 master plan/todos/phases 對齊 |
| P0-10 | github-push | done | v0.0.3-draft tag 已推送 |

## Phase 1：Transport Layer

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P1-01 | serial-wrap transport | done | 已完成 serialwrap transport，實機全量 run 驗證 |
| P1-02 | adb transport | done | `src/testpilot/transport/adb.py` |
| P1-03 | ssh transport | done | `src/testpilot/transport/ssh.py` |
| P1-04 | network utils（ping/arping/iperf） | done | `src/testpilot/transport/network.py` |

## Phase 2：Environment Management

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P2-01 | topology module | pending | `src/testpilot/env/` 目前僅 `__init__.py` |
| P2-02 | provisioner module | pending | 尚未實作 |
| P2-03 | validator module | pending | 尚未實作 |

## Phase 3：Core Engine

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P3-01 | test-runner loop | done | ExecutionEngine + runner_selector 已落地 (R2-01) |
| P3-02 | monitor subsystem | pending | 尚未實作 |
| P3-03 | reporter（MD/JSON） | done | IReporter + MarkdownReporter + JsonReporter 已落地 (R5-06) |
| P3-04 | verdict merge policy | done | 與 R4-05/R4-06 合併；AdvisoryCollector + diagnostic_status / remediation_history 投影已落地 |
| P3-05 | post-run remediation loop | done | 與 R4-06 合併；RemediationPlanner 保留 post-run 彙整，wifi_llapi 已接上 in-run safe remediation loop |

## Phase 4：Wifi_LLAPI Plugin

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P4-01 | wifi-plugin full implementation | done | setup/verify/execute/evaluate 已完成 |
| P4-02 | case-getRadioStats | done | legacy compatibility fixture 已保留，改為 underscore-prefixed explicit fixture，不進 discover_cases |
| P4-03 | case-kickStation | done | legacy compatibility fixture 已保留，改為 underscore-prefixed explicit fixture，不進 discover_cases |
| P4-04 | 420 case source 對齊治理 | in_progress | discoverable inventory 維持 420；目前依 `0401.xlsx` compare-driven single-case loop 持續 live 對齊，manual procedure authority 固定為 workbook `G/H`（忽略 `F`）；最新 detached full run `20260411T074146043202` 以 current local YAML overlay 重算為 `220/420` full matches、`200` mismatches、`67` metadata drift；若改用 evaluation-based workbook-Pass gap interpretation，尚有 `77` cases，其中 patch-scope open 為 `18`（`D277`、`D281-D287`、`D290`、`D295`、`D322`、`D324`、`D330-D333`、`D335-D336`），且 `D532/D533` 已於 isolated rerun `20260411T183356920330/20260411T183405281629` 收斂成 plain `Pass`，`D295` 已正式落成 blocker（`plugins/wifi_llapi/reports/D295_block.md`），`D324` 也於 isolated rerun `20260411T190338070996` 重新證明 base `wlX if_counters txbyte` 不是 durable oracle、已正式回退成 blocker（`plugins/wifi_llapi/reports/D324_block.md`）。`D015`/`D016`/`D017`/`D018`/`D023` 已 live 對齊 row 15 / row 16 / row 17 / row 18 / row 23，`D011`/`D013`/`D020` 維持已驗證 fail-shaped mismatch，`D024/D025/D026` 的 offline survey 也已確認現有 YAML `source.row` 仍是舊 `21/22/23`、實際 `0401.xlsx` row 應為 `24/25/26`，且舊 fail trace 的 step4 其實都已有 matching driver evidence；lab 端 `COM0/COM1` 已完成 multi-band `repeat=5 / soak=15` qualification。2026-04-09 的 targeted triage 已把 `D006` unresolved placeholder 收斂成 assoc fallback + 真實 API error、把 `D013` pass-criteria 對齊 current live shape，並確認 `D009/D010/D012` 共用 root cause 是 6G STA pre-start 缺少 `wl1 up/ifconfig up`；後續 drift follow-up 再把 full-run-only 6G client 漂移收斂到 `ifconfig wl1 down` / `wl -i wl1 down` / `wl -i wl1 ap 0` 的 client-mode rebase，修正已同步寫回 `band-baselines.yaml` + runtime hot path；再往後的 custom 6G gate/timing hardening 已讓 clean-start rerun `D019/D027` 兩案都變成 plain `Pass`。後續 source triage 又把 `D032` 的 explicit `action: skip` 先做 `6g` band prepare 問題收斂成 `execute_step()` sequencing bug，現已改成 skip short-circuit；source search 也確認這個 shape 只存在 `D028-D033`，後半段 official cases 不會再命中同一 hot path。接著新的 source/runtime triage 又把 `D028` clean-start 的 DUT 5G/2.4G BSS warm-up bounce 收斂成 `_dut_baseline_commands()` 在 primary AP 仍 enable 時直接重寫 SSID/security 的 sequencing 問題，repo 現已改成 primary AP 先 disable 再 reconfig。最後再補上的 2.4G reconnect hardening（`_connect_with_retry()` multi-verify fallback：`iw link -> wl status`，並把 settle 拉到 8 秒）已在 dual-`firstboot` rerun `20260409T182347586948` 再次維持 `4 Pass / 2 Fail` same shape：`D028/D030/D032/D033` plain `Pass`，`D029` 僅剩 testcase/data-model `WiFi.AccessPoint.1.AssociatedDevice.1.Mode` parameter-not-found fail，`D031` 維持 `results_reference` fail-shaped `Not Supported` stub mismatch；而 rerun transcript 已無 `sta_24g verify attempt` warning，`D028-D033` 的 cold-start environment blocker 可視為已清除。最新 dual-`firstboot` prefix rerun `20260409T205434740233` 又把 `D004/D005` 驗成 plain `Pass`、把 `D006` 驗成 `pass after retry (2/2)`，舊 `D005/D006 FailEnv` prefix 已不再重現；較早的 session-attached full run `20260409T212035314017` 已為避免 session-lifetime interruption 主動 supersede，正式持續執行中的 detached full run 目前為 `20260409T213837737224`，並掛上每小時 Telegram progress notifier。2026-04-10 的 workbook replay / source-backed triage 先把 `D111 getStationStats() AssociationTime` 收斂成 parser bug：`_extract_key_values()` 對 `AssociationTime = "YYYY-MM-DDTHH:MM:SSZ",` 先 strip quote 再去尾逗號，留下錯誤尾端 `"`；修正後 live single-case rerun `20260410T110659169758` 已讓 `D111` plain `Pass`，相關 targeted parser/D111 tests `4 passed`，完整 plugin runtime regression `1223 passed`。同日後續的 `D211 OperatingStandards` workbook row `211` replay 又確認 getter 會在 `be/ax` 間切換，但 AX phase 仍保留 `wl0/wl1/wl2 eht features=127`，6G `/tmp/wl1_hapd.conf` 也仍有 secondary `ieee80211be=1`；D211 因此維持 blocked/fail-shaped mismatch。接著 `D262 getRadioAirStats():void` 又被 source-backed revalidation 收斂：6G `[""]` 只是 `WiFi.Radio.2.Status="Down"` 時 `_getRadioAirStats()` 的 early-return shape，經 `wifi-llapi baseline-qualify --band 6g --repeat-count 1 --soak-minutes 0` 拉回三個 radio 都 `Status="Up"` 後，live single-case rerun `20260410T115313185471` 已成 plain `Pass`，且 YAML row drift 已由 `264` 刷新為 workbook row `262`。再往下一筆的 `D263 getRadioStats() BroadcastPacketsReceived`、`D264 getRadioStats() BroadcastPacketsSent`、`D265 getRadioStats() BytesReceived`、`D266 getRadioStats() BytesSent`、`D267 getRadioStats() DiscardPacketsReceived`、`D268 getRadioStats() DiscardPacketsSent`、`D269 getRadioStats() ErrorsReceived`、`D270 getRadioStats() ErrorsSent`、`D271 getRadioStats() MulticastPacketsReceived`、`D272 getRadioStats() MulticastPacketsSent`、`D273 getRadioStats() PacketsReceived`、`D274 getRadioStats() PacketsSent`、`D275 getRadioStats() UnicastPacketsReceived`、`D276 getRadioStats() UnicastPacketsSent` 也已完成 source-backed green-lock；其中 `D272` 已進一步確認 workbook row `272` 的 `dev_extstats TX_multipkg` 不是穩定三 band oracle，`D273/D274` 已進一步確認 5G/2.4G 多半貼近 driver `rxframe/txframe` 而 6G 常貼近舊 proc shape，`D275/D276` 則再證明 unicast 欄位在 5G/2.4G 多半符合 `frame - mcast` 的 derived shape 而 6G 仍可能 collapse 成 zero-like output。對應 YAML row drift 已由 `265/266/267/268/269/270/271/272/273/274/275/276/277/278` 刷新為 workbook row `263/264/265/266/267/268/269/270/271/272/273/274/275/276`；最新 targeted method-stats tests `132 passed`，完整 plugin runtime regression `1223 passed`。接著 `D277 getScanResults() Bandwidth` 的 blocker 又被進一步縮小：初始 DUT gate 問題可透過 `serialwrap session recover --selector COM0 --force` 拉回 `COM0 state=READY`，早先 direct probe 也已證明 5G/6G raw full payload 會卡在 serialwrap 層，而 repo-side scan timeout fallback 與對應 regression（resolver tests `30 passed`、完整 plugin runtime regression `1223 passed`）雖已落地，isolated rerun 仍未能 authoritatively 完成；WAL 顯示它在 raw `WiFi.Radio.2.getScanResults()` 後被 serialwrap recovery 注入 `^C`，DUT 隨即回 `Please press Enter to activate this console.`，`COM0` 也暫時掉到 `ATTACHED / PROMPT_TIMEOUT` 才能再 recover 回 `READY`。因此目前真正卡住的仍是 6G long scan output 觸發 broker/console recovery，而不是已證明的 `Bandwidth` semantic mismatch；此外，5G/2.4G workbook-style diagnostics 已能從 `iw dev wl0/wl2 scan` 取得 width/centre 對位訊號，但 6G 當前 visible target 仍缺少 clean `iw` width / centre-channel oracle。`D277` 仍以 blocker 形式保留。其後 `D278 getScanResults() BSSID` 已用 workbook-style target compare 完成 live 對齊：isolated rerun `20260410T135946424063` 先從 LLAPI 擷取每個 band 的第一個 target BSSID，再以同 target 對 `iw dev wl0/wl1/wl2 scan`，三個 band 皆 matched（`38:88:71:2f:f6:a7` / `3a:06:e6:2b:a3:1a` / `8c:19:b5:6e:85:e1`），且 YAML row drift 已由 `280` 刷新為 workbook row `278`。再往下一筆 `D279 getScanResults() Channel` 也已完成 live 對齊：第一次 isolated run `20260410T140447198030` 揭露了鬆散 `/Channel = /` parser 誤讀 `CentreChannel` 的 bug（錯把 `42/31/3` 當成 `Channel`），修正為只吃真正的 `^[[:space:]]*Channel = ` 後，第二次 isolated rerun `20260410T140714322443` 已讓 LLAPI `Channel` 與同 target `iw freq -> channel` authoritatively 對齊（`36/36`、`5/5`、`1/1`），且 YAML row drift 已由 `281` 刷新為 workbook row `279`。`D280 getScanResults() EncryptionMode` 已再完成 live 對齊：isolated rerun `20260410T143122662554` 以每個 band 的第一個 WPA 類 target 為 authority，鎖定 0403 source enum 仍是 `TKIP/AES/TKIPandAES/None`，但 live LLAPI 持續回 `Default`，而同 target `iw dev wl0/wl1/wl2 scan` 全部都是 `Pairwise ciphers: CCMP`、正規化為 `AES`；也就是 `38:88:71:2f:f6:a7` / `3a:06:e6:2b:a3:1a` / `8c:19:b5:6e:85:e1` 三 band 都維持同一 fail-shaped mismatch。D280 YAML row drift 已由 `282` 刷新為 workbook row `280`。接著 `D281 getScanResults() Noise` 雖然 source trace 仍明確指向 `wl -i wlX escanresults` `noise: `，但兩次 isolated workbook-style replay（`20260410T155529962490` / `20260410T155932807150`）都沒有產生穩定的 same-target live path：第一輪只有 5G 對齊（`-100/-100`），6G 缺 `WlNoise6g`、2.4G 漂移成 `-80/-77`；第二輪又改成 5G/6G 都缺 `WlNoise`，2.4G 仍漂移成 `-80/-79`。因此 D281 目前維持 blocked，拒絕落地 rewrite，committed YAML 仍保留舊 generic shape 與 stale row `283`。`D282 getScanResults() OperatingStandards` 也已完成 blocker 收斂：trial isolated rerun `20260410T163026194231` 雖然打出新 live shape（5G `a,n,ac,ax`、6G `ax,be`、2.4G `b,g,n,ax`），但仍無 deterministic all-band same-target replay，且 controlled baseline probes 也沒有關閉缺口（`testpilot5G` LLAPI `a,n,ac,ax,be` vs wl `a,n,ac,ax`，`testpilot6G` / `testpilot2G` 無法形成穩定的 LLAPI+wl 對位）。因此 D282 目前也維持 blocked，trial rewrite 已拒絕落地，committed YAML 仍保留舊 generic shape 與 stale row `284`。`D283 getScanResults() RSSI` 則已確認和 D277 同源：歷史 full-run `20260409T213837737224` 已是 `step_6g_scan command failed` / `serialwrap cmd status timeout`，新的 isolated rerun `20260410T164405221878` 又在 `setup_env` 後整段掛住，沒有 step output，也沒有 per-case `agent_trace` JSON 落盤；停止 run 後 `COM0` 還短暫回到 `ATTACHED / PROMPT_TIMEOUT` 才由 self-test 拉回 `READY`。因此 D283 目前也維持 blocked，committed YAML 保持未修改；next ready patch-driven case in the current repo inventory=`D284` |

> P4-04 latest note (2026-04-10): `D284 getScanResults() SecurityModeEnabled` 已收斂成 blocker。Source 仍將 neighboring `SecurityModeEnabled` 映射到 `AKM Suites` / `RSN`，isolated rerun `20260410T170750425931` 已證明 5G（`38:88:71:2f:f6:a7` -> `WPA2-Personal`）與 2.4G（`8c:19:b5:6e:85:e1` -> `WPA2-WPA3-Personal`）可 same-target 對齊，但 6G 仍會在 `3a:06:e6:2b:a3:1a`（`.ROAMTEST_RSNO_P10P_1`）與 `2C:59:17:00:19:96`（`OpenWrt_1`）等多個 `WPA3-Personal` target 間漂移；第二次 isolated rerun `20260410T171358112868` 改用 associated-BSSID selector 也未能穩定產生 `LlapiBssid6g`。因此 D284 trial rewrite 已拒絕落地、committed YAML 維持原 generic shape 與 stale row `286`；回退後完整 plugin runtime regression 仍為 `1223 passed`；next ready case=`D285`。

> P4-04 latest note (2026-04-10, continuation): `D285 getScanResults() SignalNoiseRatio` 也已收斂成 blocker。`compare-0401.md` 對應 workbook row `285`，但 committed YAML 仍停在 stale row `287`；source survey 顯示 0403 neighboring scan internals 仍以 `RSSI` / `SignalStrength` 加 `Noise` 為核心，而 `SignalNoiseRatio` 只在 scan-result model layer 對外宣告。standalone LLAPI replay 已證明欄位存在（6G `3A:06:E6:2B:A3:1A` 回 `RSSI=-93`、`Noise=-97`、`SignalNoiseRatio=4`；2.4G `8C:19:B5:6E:85:E1` 回 `RSSI=-46`、`Noise=-80`、`SignalNoiseRatio=34`），但 source-backed same-target replay 仍無法凍結：direct `wl -i wl1 escanresults` 找不到 6G 的 LLAPI first BSSID，而 2.4G 同 target raw probe 雖找得到 `8C:19:B5:6E:85:E1`，卻只回 `RSSI=-54 dBm`、`noise=-75 dBm`、`SNR=21 dB`，與 LLAPI `34` 明顯漂移。因此 D285 trial rewrite 已拒絕落地；next ready case=`D286`。

> P4-04 latest note (2026-04-10, continuation): `D286 getScanResults() SignalStrength` 也已收斂成 blocker。source trace 仍將 neighboring `SignalStrength` 直接綁在 `RSSI: ` token path，因此先試了一版 workbook-style same-target compare；但 isolated rerun `20260410T181105027445` 仍穩定打出 `5G 38:88:71:2f:f6:a7 => LLAPI=-64 vs iw=-65`、`6G 3a:06:e6:2b:a3:1a => missing same-target IwSignalStrength6g`、`2.4G 8c:19:b5:6e:85:e1 => LLAPI=-46 vs iw=-55/-56`。後續 raw `wl -i wl0/wl1/wl2 escanresults` probe 只證明 5G same-target RSSI 可對到 `-64`，但 6G 仍找不到 `3A:06:E6:2B:A3:1A`、2.4G same-target raw RSSI 仍漂成 `-54 dBm`；因此 D286 trial rewrite 已拒絕落地、committed YAML 已回復原 generic shape 與 stale row `288`；next ready case=`D287`。

> P4-04 latest note (2026-04-11, continuation): `D281 getScanResults() Noise` 仍維持 blocker，但 authority 模型已修正。parser-fix trial `20260411T211133728869` 先證明舊 rewrite 仍有 local same-target capture bug；sanitized rerun `20260411T211327344984` 才打出真正 live shape：5G `-100/-100` exact-close、6G 穩定漂成 `-97/-102` 再到 `-97/-103`、2.4G 仍在 `-78/-76` / `-78/-78` 間擺動。最新 0403 source trace 已確認 public `getScanResults().Noise` 不是單純沿用 raw `wl escanresults` `noise:`，而是經 `wifiGen_rad_getScanResults()` / `s_updateScanResultsWithSpectrumInfo()` 以 per-channel spectrum `noiselevel` 回填；但 public `getSpectrumInfo()` 在現場仍回空 array，尚無 durable replayable oracle。故 committed YAML 仍保持 stale row `283` 不變，詳見 `plugins/wifi_llapi/reports/D281_block.md`；next ready case=`D282`。

> P4-04 latest note (2026-04-11, continuation): `D282 getScanResults() OperatingStandards` 也仍維持 blocker，但 authority 模型已修正。最新 0403 source trace 已確認目前 public ubus getter 不是沿用 legacy `_wldm_get_standards()`，而是走 nl80211 scan-result path：`swl_80211_parseInfoElementsBuffer(...)` 先把 beacon/probe IEs 解析進 `pWirelessDevIE->operatingStandards` bitmask，再複製到 `wld_scanResultSSID_t.operatingStandards`、留在 `lastScanResults`，最後用 `swl_radStd_toChar(..., SWL_RADSTD_FORMAT_STANDARD, 0)` 序列化成 public `OperatingStandards`。因此 cumulative LLAPI 形狀（如 `a,n,ac,ax`、`ax,be`）本身符合 active path；真正卡住的是 external replay 仍不是 same-source oracle：既有 isolated rerun `20260410T163026194231` 雖讓 5G exact-close，6G 仍沒有 same-target `WlOperatingStandards6g`，2.4G 仍漂成 `b,g,n,ax` vs `b,g,n,ax,be`。故 committed YAML 仍保持 stale row `284` 不變，詳見 `plugins/wifi_llapi/reports/D282_block.md`；next ready case=`D283`。

> P4-04 latest note (2026-04-11, continuation): `D283 getScanResults() RSSI` 與 `D286 getScanResults() SignalStrength` 現在視為同一條 active public `ssid->rssi` field family。fresh isolated rerun `20260411T214050136894` 已證明 committed D283 generic case 仍會在 `setup_env` 後 hang：沒有 step output、沒有 markdown/json/xlsx report files、`plugins/wifi_llapi/reports/agent_trace/20260411T214050136894/` 保持空目錄，且 stop 後 `COM0` 一度落到可 recover 的 `TARGET_UNRESPONSIVE`。同時最新 source trace 也確認這不是獨立 field：`wld_nl80211_parser.c` 先填 `pResult->rssi`，`wld_rad_scan.c` 再把同一個 `ssid->rssi` 同時序列化成 public `RSSI` 與 public `SignalStrength`；`D286_block.md` 已同步改成這個 source model，但既有 live replay 仍維持 blocker（`5G -64/-65`、6G missing same-target replay、2.4G `-46` vs `-55/-56` / raw `-54`）。故 D283 仍 blocked on committed full-payload transport shape，D286 仍 blocked on shared semantic replay gap；next ready case=`D287`。

> P4-04 latest note (2026-04-11, continuation): `D287 getScanResults() SSID` 也仍維持 blocker，但 authority 模型已修正。active 0403 public `SSID` 已確認不是單純沿用 legacy raw `SSID:` helper，而是走 nl80211 scan-result path：`wld_nl80211_parser.c` 先把 `pWirelessDevIE->ssid` 複製到 `wld_scanResultSSID_t.ssid`，`wld_rad_scan.c` 再以 `wld_ssid_to_string(...)` 序列化成 public `SSID`。既有 isolated rerun `20260410T182739821870` 仍是決定性 live evidence：5G 與 2.4G 都能對同一 target BSSID exact-close，但 6G 仍只有 LLAPI `3a:06:e6:2b:a3:1a` / `.ROAMTEST_RSNO_P10P_1`，`iw` 與 direct raw `wl -i wl1 escanresults` 都無法 replay 同一 target。故 committed YAML 仍保持 stale row `289` 不變，詳見 `plugins/wifi_llapi/reports/D287_block.md`；next ready case=`D290`。

> P4-04 latest note (2026-04-10, continuation): `D287 getScanResults() SSID` 也已收斂成 blocker。source trace 仍將 neighboring `SSID` 直接綁在 `SSID: ` token path，因此先試了一版 workbook-style same-target compare；isolated rerun `20260410T182739821870` 已證明 5G `38:88:71:2f:f6:a7 -> Verizon_Z4RY7R` 與 2.4G `8c:19:b5:6e:85:e1 -> TMOBILE-85DF-TDK-2G` 可在 LLAPI 與 `iw` 間 same-target 對齊，但 6G `3a:06:e6:2b:a3:1a` / `.ROAMTEST_RSNO_P10P_1` 仍缺 `IwSSID6g`；補做 direct raw `wl -i wl1 escanresults | grep -m1 -B2 -A1 'BSSID: 3A:06:E6:2B:A3:1A'` 也回空。因此 D287 trial rewrite 已拒絕落地、committed YAML 已回復原 generic shape 與 stale row `289`；next ready case=`D288`。

> P4-04 latest note (2026-04-10, continuation): `D288 getScanResults() WPSConfigMethodsSupported` 已完成 live 對齊。source survey 已確認 0403 scan-result model 仍對外暴露此欄位，而 neighboring scan live payload 三 band 都是空字串；真正的缺口是 repo-side parser 不會吃 raw ubus transcript 的 `WPSConfigMethodsSupported = "",` trailing-comma shape。因此 committed YAML 現已改為每個 band 先用 explicit extractor 正規化輸出 `WPSConfigMethodsSupported=`，isolated rerun `20260410T183630583633` 三 band 全數 `Pass`，YAML row drift 也已由 `290` 刷新為 workbook row `288`；targeted D288 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`；next ready case=`D289`。

> P4-04 latest note (2026-04-10, continuation): `D289 getScanResults() Radio` 已完成 live 對齊，但對齊形狀是 source-backed fail-shaped absence。active 0403 scan model `wld_radio.odl` 的 `scanresult_t` 根本沒有 `Radio`，而 active HAL neighboring struct `wifi_neighbor_ap2_t` 也把 `ap_Radio` 註解掉，所以 live getter 沒有 backing field 可填。committed YAML 現已改為每個 band 先用 explicit extractor 正規化輸出 `Radio=`，isolated rerun `20260410T185434305989` 三 band 都吐 `Radio=` 且 `diagnostic_status=Pass`，YAML row drift 也已由 `291` 刷新為 workbook row `289`；完整 plugin runtime regression 維持 `1223 passed`；next ready case=`D290`。

> P4-04 latest note (2026-04-11, continuation): `D325 DiscardPacketsReceived` 已完成 live 對齊。舊 workbook row `325` 的 `/proc/net/dev_extstats` `$5` compare 先在 real runner replay `20260411T010859993578` 證明為 stale proc heuristic（`565 / 3854 / 356`-style drift），而 active 0403 source path 則與已完成的 `D304` 一致，都是由 `whm_brcm_get_if_stats()` / `wl if_counters rxdiscard` 覆寫 public field；改寫 direct-property YAML 後，real runner rerun `20260411T011321267947` 已在三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。對應 metadata 已由 stale row `249` 刷新為 workbook row `325`，official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D326`。

> P4-04 latest note (2026-04-11, continuation): `D326 DiscardPacketsSent` 已完成 live 對齊。舊 workbook row `326` 的 `/proc/net/dev_extstats` `$13` compare 先在 real runner replay `20260411T012137925510` 證明為 stale proc heuristic（`184 / 3467 / 87`-style drift），而 active 0403 source path 則由新完成的 D326 survey 與既有 `D305` 一起再確認，都是由 `whm_brcm_get_if_stats()` / `wl if_counters txdiscard` 覆寫 public field；改寫 direct-property YAML 後，real runner rerun `20260411T012538161460` 已在三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。對應 metadata 已由 stale row `250` 刷新為 workbook row `326`，official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D327`。

> P4-04 latest note (2026-04-11, continuation): `D327 ErrorsReceived` 已完成 live 對齊。第一輪 real runner replay `20260411T013241354703` 雖然沿用舊 `/proc/net/dev_extstats` `$4` 也 still pass，但 active 0403 source path 已由新完成的 D327 survey 與既有 `D306` 一起再確認為 `whm_brcm_get_if_stats()` / `wl if_counters rxerror`；因此 direct-property YAML 仍補做 source-backed green-lock rewrite，將 metadata 自 stale row `251` 刷新為 workbook row `327`，並在 updated rerun `20260411T013801878458` 繼續維持三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D328`。

> P4-04 latest note (2026-04-11, continuation): `D328 ErrorsSent` 已完成 live 對齊。第一輪 real runner replay `20260411T014458979418` 雖然沿用舊 `/proc/net/dev_extstats` `$12` 也 still pass，但 active 0403 source-backed path 已由新完成的 D328 survey 與既有 `D307` 一起再確認為 `whm_brcm_get_if_stats()` / `wl if_counters txerror`，且在有 `wds*` peer 時可經 `whm_brcm_vap_ap_stats_accu()` 再累加。focused live probe 也直接打出此欄位是 moving counter 而不是固定 workbook 樣本：`5G=1`、`6G=3347`、`2.4G=0` across `direct / getSSIDStats / txerror`。因此 committed YAML 仍補做 source-backed green-lock rewrite，將 metadata 自 stale row `252` 刷新為 workbook row `328`，並在 updated rerun `20260411T015126498621` 繼續維持三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D329`。

> P4-04 latest note (2026-04-11, continuation): `D329 FailedRetransCount` 已完成 live 對齊。第一輪 real runner replay `20260411T015905984272` 已在三 band exact-close `direct Stats == getSSIDStats() == 0`，但當時還缺獨立 driver oracle；active 0403 source path 已由新完成的 D329 survey 與既有 `D308` 一起再確認為 `whm_brcm_get_if_stats()` / `wl if_counters txretransfail`，且只有在存在 matching `wds*` peer 時才會額外累加。focused live probe 隨後直接關掉 `direct / getSSIDStats / txretransfail = 0 / 0 / 0` on 5G/6G/2.4G，並確認 current baseline 沒有 active `wds*` peer；因此 committed YAML 補做 source-backed green-lock rewrite，將 metadata 自 stale row `253` 刷新為 workbook row `329`，updated rerun `20260411T020534026608` 也繼續維持三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D334`。

> P4-04 latest note (2026-04-11, continuation): `D334 RetransCount` 已完成 live 對齊。第一輪 real runner replay `20260411T021238026451` 已在三 band exact-close `direct Stats == getSSIDStats()`，但當時還缺獨立 driver oracle，且原本的 `getSSIDStats()` extractor 會把 `FailedRetransCount` 一起誤吃進來。active 0403 source path 已由新完成的 D334 survey 再確認為 `wldm_SSID_TrafficStats()` / `wl if_counters txretrans`，direct-property path 本身不走 WDS accumulation；focused live probe 先關掉 source-backed `direct / getSSIDStats / low-32(txretrans)`，其中 5G=`4294967295`、6G=`4294963915`、2.4G=`0`。改寫 case 後，updated rerun `20260411T022030741126` 在 retry policy 下收斂成功：attempt 1 的 6G 曾短暫漂成 `4294967294` vs driver `0`，但 attempt 2 已回到三 band exact-close `0 / 0 / 0`。因此 committed YAML 將 metadata 自 stale row `258` 刷新為 workbook row `334`，official-case guardrail `1 passed`，command-budget inventory 更新為 `630`，完整 repo regression `1634 passed`；next ready case=`D337`。

> P4-04 latest note (2026-04-11, continuation): `D337 UnknownProtoPacketsReceived` 已完成 live 對齊。第一輪 real runner replay `20260411T023258929853` 已在三 band exact-close `direct Stats == getSSIDStats() == 0`，但當時還缺獨立 driver oracle。active 0403 source path 已由新完成的 D337 survey 再確認為 `wldm_SSID_TrafficStats()` / `wl if_counters rxbadprotopkts`，direct-property path 本身不走 WDS accumulation；focused live probe 隨後關掉 `direct / getSSIDStats / rxbadprotopkts = 0 / 0 / 0` on 5G/6G/2.4G，而相鄰 getSSIDStats-family 的 `rxunknownprotopkts` 也維持 `0 / 0 / 0`，說明 workbook-era replay 雖 exact-close，但 source 上其實是兩條不同 counter family。改寫 case 後，updated rerun `20260411T024443960794` 繼續維持三 band exact-close `0 / 0 / 0`。因此 committed YAML 將 metadata 自 stale row `261` 刷新為 workbook row `337`，official-case guardrail `1 passed`，command-budget inventory 更新為 `633`，完整 repo regression `1634 passed`；next ready case=`D406`。

> P4-04 latest note (2026-04-11, continuation): `D406 MultipleRetryCount` 已完成 live 對齊。第一輪 real runner replay `20260411T025549740195` 已在三 band exact-close `direct Stats == getSSIDStats() == 0`，但當時還缺獨立 driver oracle。active 0403 source path 現已確認為 `wldm_SSID_TrafficStats()` / `wl if_counters txretrie`；focused live probe 隨後關掉 `direct / getSSIDStats / txretrie = 0 / 0 / 0` on 5G/6G/2.4G，且 current baseline `ls /sys/class/net | grep '^wds'` 為空。改寫 case 後，updated rerun `20260411T025954644775` 繼續維持三 band exact-close `0 / 0 / 0`。因此 committed YAML 將 metadata 自 stale row `301` 刷新為 workbook row `406`，official-case guardrails `1225 passed`，command-budget inventory 更新為 `636`，完整 repo regression `1634 passed`；next ready case=`D407`。

> P4-04 latest note (2026-04-11, continuation): `D407 RetryCount` 已完成 live 對齊。第一輪 real runner replay `20260411T031324456196` 已在三 band exact-close `direct Stats == getSSIDStats() == 0`，但舊 workbook extractor 仍會把 `MultipleRetryCount` 一起誤吃進來，且當時還缺獨立 driver oracle。active 0403 source path 現已確認為 `wldm_SSID_TrafficStats()` / `wl if_counters txretry`；focused live probe 隨後關掉 `direct / getSSIDStats / txretry = 0 / 0 / 0` on 5G/6G/2.4G，且 current baseline `ls /sys/class/net | grep '^wds'` 仍為空。改寫 case 後，updated rerun `20260411T031645170662` 繼續維持三 band exact-close `0 / 0 / 0`。因此 committed YAML 將 metadata 自 stale row `302` 刷新為 workbook row `407`，official-case guardrails `1225 passed`，command-budget inventory 更新為 `642`，完整 repo regression `1634 passed`；next ready case=`D528`。

> P4-04 latest note (2026-04-11, continuation): `D528 getSpectrumInfo bandwidth` 已完成 live 對齊。舊 workbook-era replay `20260411T032529278022` 已先證明 numeric-only regex `^\d+$` 不再成立，因為 active 0403 在三 band 都回 `bandwidth="20MHz"`。active 0403 source path 現已確認為 `_getSpectrumInfo()` / `s_prepareSpectrumOutput()` / `amxc_var_add_key(cstring_t, "bandwidth", swl_bandwidth_str[llEntry->bandwidth])`，而 sync refresh path `wifiGen_rad_getSpectrumInfo(..., update=false)` 在 `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()` 中會把 survey-derived spectrum entry 固定 seed 成 `SWL_BW_20MHZ`。改寫 case 後，real runner rerun `20260411T034134858534` 繼續維持三 band exact-close `20MHz / 20MHz / 20MHz`；補上的 spectrum guardrail fixture 也讓 official-case guardrails 維持 `1225 passed`，完整 repo regression 仍為 `1634 passed`；next ready case=`D529`。

> P4-04 latest note (2026-04-11, continuation): `D290 getScanResults() CentreChannel` 已完成 live 對齊。source-backed survey 先前已確認欄位本身在 0403 真實存在：`wld_radio.odl` 宣告 `CentreChannel`、`wld_rad_scan.c` 會把 `ssid->centreChannel` 寫入結果、`wld_nl80211_parser.c` 會以 `swl_chanspec_getCentreChannel()`（含 20MHz fallback）計算 live 值；真正缺的只是 same-target external replay path。這次在 one-shot `wifi-llapi baseline-qualify --repeat-count 1 --soak-minutes 0` 重新套回 deterministic DUT baseline 後，fresh isolated rerun `20260411T220324862766` 已用 same-target raw `wl escanresults` Chanspec 關掉三 band：5G `42/42` exact-close、2.4G `1/1` exact-close、6G 則鎖成 source-backed fail-shaped mismatch `LlapiCentreChannel6g=31` vs `WlCentreChannel6g=15`（同 target BSSID `6e:15:db:9e:33:72`）。故 committed YAML metadata 已由 stale row `292` 刷新為 workbook row `290`，`results_reference.v4.0.3` 現為 `Pass / Fail / Pass`，targeted D290 guardrails 已轉綠；next ready case=`D529`。

> P4-04 latest note (2026-04-11, continuation): `D529 getSpectrumInfo channel` 已完成 live 對齊。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "channel", llEntry->channel)`；fresh isolated rerun `20260411T221613327385` 加上兩輪 direct probes 也已把第一個 serialized spectrum-entry channel 鎖定為 `5g=36`、`6g=2`、`2.4g=1`。因此 committed case 已把 template metadata 對齊為 `object=WiFi.Radio.{i}.` / `api=getSpectrumInfo()`，並把舊的 generic numeric regex 收成 explicit first-entry channel extractors；workbook row 維持 `529`，`results_reference.v4.0.3` 仍為 `Pass / Pass / Pass`；next ready case=`D530`。

> P4-04 latest note (2026-04-11, continuation): `D530 getSpectrumInfo noiselevel` 已完成 live 對齊，但不採 fixed-value case。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(int32_t, "noiselevel", llEntry->noiselevel)`，因此這個欄位本質上是 survey-driven 的動態讀值，不是穩定常數。第一次 exact-value trial 已被拒絕：2.4G 在 retries/reruns 間實際漂成 `-75 / -77 / -78`，所以 committed case 只修正 template metadata 為 `object=WiFi.Radio.{i}.` / `api=getSpectrumInfo()`，保留 source-correct 的 numeric regex pass shape，並以 isolated rerun `20260411T222349217612` 作為綠鎖；next ready case=`D531`。

> P4-04 latest note (2026-04-11, continuation): `D531 getSpectrumInfo accesspoints` 已完成 live 對齊，且同樣採 metadata-only dynamic numeric case。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "accesspoints", llEntry->nrCoChannelAP)`，因此這個欄位本質上是 survey-driven 的 co-channel AP count，不是固定常數。committed case 因此只修正 template metadata 為 `object=WiFi.Radio.{i}.` / `api=getSpectrumInfo()`，保留 source-correct 的 numeric regex pass shape，並以 isolated rerun `20260411T223140870454` 作為綠鎖；next ready case=`D532`。

> P4-04 latest note (2026-04-11, continuation): `D532 getSpectrumInfo ourUsage` 已完成 live 對齊，且同樣採 metadata-only dynamic numeric case。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "ourUsage", llEntry->ourUsage)`，而 `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()` 會把 survey airtime (`ourTime / total_time`) 轉成 live 百分比；isolated rerun `20260411T223658523608` 已 clean pass。這次 workbook re-check 也同步確認 spectrum batch 整批 `source.row` 原本有 stale `+2` drift，所以 `D528-D533` 現已統一更正為實際 workbook rows `528-533`；next ready case=`D533`。

> P4-04 latest note (2026-04-11, continuation): `D533 getSpectrumInfo availability` 已完成 live 對齊，且是這個 spectrum batch 最後一筆 metadata-only dynamic numeric case。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "availability", llEntry->availability)`，而 `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()` 會把 survey idle/free-time timing 轉成 live 百分比；isolated rerun `20260411T224035464927` 已 clean pass。至此 `D528-D533` spectrum block 已完成 row-corrected alignment；next unresolved queue 回到 `D322`、`D331`、`D333`、`D336`。

> P4-04 latest note (2026-04-11, continuation): `D322 BroadcastPacketsSent` 的 trial alignment 已被回退。Focused DUT-only probes 一度 exact-close 了 authored `direct / getSSIDStats / /proc $24` block（isolated rerun `20260411T225133238319`，外加 5G x5 與 multiband x3 repeat probes），但 superseding official rerun `20260411T230829194313` 仍在兩次 attempt 都重現同一個 5G `+1` drift：`4390 / 4390 / 4391`、`4394 / 4394 / 4395`，而 6G/2.4G 只是繼續 exact-close。由於 acceptance authority 仍是 full official runner path，committed YAML metadata 必須維持 stale row `246`；D323 已在後續 official rerun 對齊，因此 remaining direct-stats unresolved queue 現在只剩 `D322`、`D331`、`D333`、`D336`。

> P4-04 latest note (2026-04-10, continuation): `D295 scan()` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。committed YAML metadata 已由 stale row `220` 刷新為 workbook row `295`；manual serialwrap replay 也已證明三個 radio 在 live 0403 上本來就是 `Enable=1` / `Status=\"Up\"`。最終採納的 oracle 是較小顆粒的 `scan() returned + same-target driver-cache match`：5G `38:88:71:2F:F6:A7` 對到 `wl0 escanresults`、6G `6E:15:DB:9E:33:72` 對到 `wl1 escanresults`、2.4G `2E:59:17:00:06:F8` 對到 `wl2 escanresults`；直接再追一輪 `getScanResults()` 則會把 COM0 推到 `PROMPT_TIMEOUT`，因此不納入 committed oracle。next ready patch-driven workbook-Pass case=`D298`。

> P4-04 latest note (2026-04-10, continuation): `D298 startScan()` 也已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_radio.odl`：`startScan()` 有明確定義，且只列出 `Unable to start scan` / `A scan is already running` 兩種 error path；`wldm_lib_wifi.c` 也顯示 live path 會先走 `wldm_xbrcm_scan(..., "scan")`，再讀回 `num_scan_results` 與 `scan_results`。manual serialwrap replay 已證明三 band 都回 `WiFi.Radio.N.startScan() returned [ \"\" ]`，而 post-call driver cache 也各自有可見 BSSID：`wl0`=`A8:A2:37:4F:8C:5C`、`wl1`=`6E:15:DB:9E:33:72`、`wl2`=`62:82:FE:58:AC:B5`。因此 committed YAML metadata 已由 stale row `223` 刷新為 workbook row `298`；next ready patch-driven workbook-Pass case=`D299`。
> P4-04 latest note (2026-04-10, continuation): `D299 stopScan()` 也已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_radio.odl` 的 `void stopScan()`，active 0403 path 也可從 `wld_rad_scan.c` / `wld_scan_stop()` 看到：`_stopScan()` 會先要求 `wld_scan_isRunning()`，再經 `pRad->pFA->mfn_wrad_stop_scan(pRad)` 進到實際 abort path；public `wld_rad_nl80211.h` / `wld_nl80211_scan.c` 也明確暴露 `wld_rad_nl80211_abortScan()` -> `wld_nl80211_abortScan(...)`。manual serialwrap replay 則用 `WiFi.Radio.N.ScanResults.ScanInProgress` 關閉 state-bit oracle：5G 已證明 `0 -> startScan() -> 1 -> stopScan() -> 0`，再做第二輪 restart 仍會回到 `1/0`；6G 與 2.4G 的短 probe 也都穩定打出 `0 -> 1 -> 0`。中途曾試過把 6G/2.4G 合併成一條較長 probe，broker 以 `PROMPT_TIMEOUT_RECOVERED` 回收，因此 committed oracle 固定維持短版 per-band `startScan() -> ScanInProgress=1 -> stopScan() -> ScanInProgress=0` 重播。committed YAML metadata 已由 stale row `224` 刷新為 workbook row `299`；targeted D299 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`；next ready patch-driven workbook-Pass case=`D300`。
> P4-04 latest note (2026-04-10, continuation): `D300 getSSIDStats() BroadcastPacketsReceived` 也已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl` 的 `Stats.BroadcastPacketsReceived` 與 `htable getSSIDStats()`；active 0403 path 也可從 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` -> `wld_updateVAPStats()` 看到，且 `wld_statsmon.c` 已把 `rxBroadcastPackets` 寫進 `BroadcastPacketsReceived`。manual serialwrap replay 則用短版 per-band 三向數值 oracle 關閉：5G `getSSIDStats/direct/proc=$23` 為 `363/363/363`，6G 為 `144/144/144`，2.4G 為 `113/113/113`。中途曾試過把三 band 合併成一條較長 extraction probe，broker 以 `PROMPT_TIMEOUT_RECOVERED` 回收，因此 committed oracle 固定維持短版 per-band `getSSIDStats() -> Stats.BroadcastPacketsReceived -> /proc/net/dev_extstats $23` 重播。committed YAML metadata 已由 stale row `225` 刷新為 workbook row `300`；targeted D300 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`；next ready patch-driven workbook-Pass case=`D301`。
> P4-04 latest note (2026-04-10, continuation): `D301 getSSIDStats() BroadcastPacketsSent` 也已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl` 的 `Stats.BroadcastPacketsSent` 與 `htable getSSIDStats()`；active 0403 path 也可從 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` -> `wld_updateVAPStats()` 看到，且 `wld_statsmon.c` 已把 `txBroadcastPackets` 寫進 `BroadcastPacketsSent`。manual serialwrap replay 則用短版 per-band 三向數值 oracle 關閉：5G `getSSIDStats/direct/proc=$24` 為 `1432/1432/1432`，6G 為 `1590/1590/1590`，2.4G 為 `1680/1680/1680`。committed YAML metadata 已由 stale row `226` 刷新為 workbook row `301`；targeted D301 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`；next ready patch-driven workbook-Pass case=`D302`。
> P4-04 latest note (2026-04-10, continuation): `D302 getSSIDStats() BytesReceived` 目前收斂成 blocker。active 0403 source 已確認欄位存在：`wld_ssid.c` 會把 endpoint `rxbyte` 複製到 `pStats->BytesReceived`，而 `wld_statsmon.c` 也會把 `pSrc->rxBytes` 聚合進同一個 stats 結構。live replay 已證明三 band 都有 `direct Stats == getSSIDStats()`（5G `139402/139402`，6G `43232/43232`，2.4G `33066/33066`，5G focused rerun 仍是 `139618/139618`），但獨立 oracle 仍關不掉：舊 D323 式 `/proc/net/dev` / `/proc/net/dev_extstats` `$2` 固定落在 `111002/46112/35326`，`wl counters rxbyte` 只在 `6G/2.4G` 對上（`43232/33066`）而 5G 持續高於 getter（`144574`），`wl0.1/wl1.1/wl2.1` sub-interface counters 全為零，`AssociatedDevice.*.RxBytes` 也無法乾淨映射。因此 D302 trial rewrite 已拒絕落地、committed YAML 維持原 stale row `227` 與 `Fail/Fail/Fail`；blocker authority file=`plugins/wifi_llapi/reports/D302_block.md`；next ready patch-driven workbook-Pass case=`D303`。
> P4-04 latest note (2026-04-10, continuation): `D303 getSSIDStats() BytesSent` 目前也收斂成 blocker。active 0403 source 已確認欄位存在：`wld_ssid.c` 會把 endpoint `txbyte` 複製到 `pStats->BytesSent`，而 `wld_statsmon.c` 也會把 `pSrc->txBytes` 聚合進同一個 stats 結構。live replay 只部分關掉 method path：5G 與 2.4G 保持 `direct Stats == getSSIDStats()`（`95452542` / `67883586`），6G 第一次 replay 則是 `60438235` vs `60439059` 小幅漂移，focused rerun 才重新關到 `60582625/60582625`。但獨立 oracle 全部仍不收斂：`/proc/net/dev` / `/proc/net/dev_extstats` `$10` 只落在約 `66798080/64667313/66738514` 與 `66750876/64691013/66691690`，`wl counters txbyte` 又明顯更高（`114950286/80944128/77082489`），`wl0.1/wl1.1/wl2.1` sub-interface counters 是 blank / zero-like，`AssociatedDevice.*.TxBytes` 也無法乾淨映射。因此 D303 trial rewrite 已拒絕落地、committed YAML 維持原 stale row `228`；blocker authority file=`plugins/wifi_llapi/reports/D303_block.md`；next ready patch-driven workbook-Pass case=`D304`。
> P4-04 latest note (2026-04-11, continuation): `D324 BytesSent` 已完成 live 對齊。第一輪 real runner replay `20260411T005329099506` 先證明 workbook `/proc/net/dev_extstats` `$10` criterion 已 stale；但 follow-up source trace 已把 active 0403 vendor path 鎖到 `whm_brcm_get_if_stats()` / `wl if_counters txbyte`，後續 manual serialwrap probes 又先在 5G / 6G / 2.4G 分別 exact-close `140077` / `125263` / `133877`。因此 committed YAML 已改用 per-band `wl if_counters txbyte` oracle，`source.row` 也由 stale `248` 刷新為 workbook row `324`；更新後 real runner rerun `20260411T010328768651` 一次即過，三 band 均為 `direct == getSSIDStats == if_counters`（5G `79412272`、6G `47109866`、2.4G `79349200`）。next ready patch-driven workbook-Pass case=`D325`。
> P4-04 latest note (2026-04-10, continuation): `D304 getSSIDStats() DiscardPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`，再由 `whm_brcm_get_if_stats()` 用 `wl if_counters rxdiscard` 覆寫 `DiscardPacketsReceived`。live serialwrap replay 已關閉短版 per-band 三向數值 oracle：5G / 6G / 2.4G 都是 `direct Stats / getSSIDStats() / wl if_counters rxdiscard = 0/0/0`；反而舊 D325 式 `/proc/net/dev_extstats` `$5` 仍停在 `565/3752/356`、已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `229` 刷新為 workbook row `304`；targeted D304 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。另外，D302 的 blocker 也已補上新 caution：未來重查必須從 `wl if_counters rxbyte` 起手（6G/2.4G exact close、5G 穩定差 `104`），不能再只沿用舊 D323 proc oracle。next ready patch-driven workbook-Pass case=`D305`。
> P4-04 latest note (2026-04-10, continuation): `D305 getSSIDStats() DiscardPacketsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`，再由 `whm_brcm_get_if_stats()` 用 `wl if_counters txdiscard` 覆寫 `DiscardPacketsSent`。live serialwrap replay 已關閉短版 per-band 三向數值 oracle：5G / 6G / 2.4G 都是 `direct Stats / getSSIDStats() / wl if_counters txdiscard = 0/0/0`；反而舊 D326 式 `/proc/net/dev_extstats` `$13` 仍停在 `184/3467/87`、已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `230` 刷新為 workbook row `305`；targeted D305 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D306`。
> P4-04 latest note (2026-04-10, continuation): `D306 getSSIDStats() ErrorsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`，再由 `whm_brcm_get_if_stats()` 用 `wl if_counters rxerror` 覆寫 `ErrorsReceived`。live serialwrap replay 已關閉四向數值 oracle：5G / 6G / 2.4G 都是 `direct Stats / getSSIDStats() / wl if_counters rxerror / /proc/net/dev_extstats $4 = 0/0/0/0`。因此 committed YAML metadata 已由 stale row `231` 刷新為 workbook row `306`；targeted D306 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D307`。
> P4-04 latest note (2026-04-10, continuation): `D307 getSSIDStats() ErrorsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters txerror` 設 base-VAP `ErrorsSent`，再由 `whm_brcm_vap_ap_stats_accu()` 把 matching `wds*` interface 的 `ErrorsSent` 累加回 `SSID.stats`。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wds0.0.1 if_counters txerror = 56/56/56`，focused 6G rerun `direct/getSSIDStats/wds1.0.1 if_counters txerror = 46/46/46`，2.4G 則是 `direct/getSSIDStats/wl2 if_counters txerror = 0/0/0`；反而舊 D328 式 `/proc/net/dev_extstats` `$12` 在 base wl interface 上維持 `0`，已確認不再是 5G/6G 的 authoritative oracle。因此 committed YAML metadata 已由 stale row `232` 刷新為 workbook row `307`；targeted D307 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D308`。
> P4-04 latest note (2026-04-10, continuation): `D308 getSSIDStats() FailedRetransCount` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`，再由 `whm_brcm_get_if_stats()` 用 `wl if_counters txretransfail` 覆寫 `FailedRetransCount`；若存在 matching `wds*` interface，仍會走同一條 `whm_brcm_vap_ap_stats_accu()` merge path，但目前 live baseline 沒有出現額外累加。live serialwrap replay 已關閉三向數值 oracle：5G / 6G / 2.4G 都是 `direct Stats / getSSIDStats() / wl if_counters txretransfail = 0/0/0`。因此 committed YAML metadata 已由 stale row `233` 刷新為 workbook row `308`；targeted D308 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D309`。
> P4-04 latest note (2026-04-10, continuation): `D309 getSSIDStats() MulticastPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters rxmulti` 設 `MulticastPacketsReceived`，再由 `whm_brcm_vap_ap_stats_accu()` 視需要累加 matching `wds*` 介面，最後在 `whm_brcm_vap.c` 以 `tmp_stats.BroadcastPacketsReceived` 做扣減並 clamp 到 `0`。live serialwrap replay 已關閉這條 source-backed formula：5G `direct/getSSIDStats/wl0 if_counters rxmulti/BroadcastPacketsReceived = 0/0/10/363`，6G `0/0/0/145`，2.4G `0/0/0/113`，因此三 band 都落在 `max((rxmulti + matching wds_rxmulti) - BroadcastPacketsReceived, 0) = 0`。反而舊 D330 式 `/proc/net/dev_extstats` `$9` 仍停在 `363/146/113`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `234` 刷新為 workbook row `309`；targeted D309 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D310`。
> P4-04 latest note (2026-04-10, continuation): `D310 getSSIDStats() MulticastPacketsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters txmulti` 設 `MulticastPacketsSent`，再由 `whm_brcm_vap_ap_stats_accu()` 視需要累加 matching `wds*` 介面，最後在 `whm_brcm_vap.c` 以 `tmp_stats.BroadcastPacketsSent` 做扣減並 clamp 到 `0`。live serialwrap replay 已關閉這條 source-backed formula：5G `direct/getSSIDStats/wl0 if_counters txmulti/BroadcastPacketsSent = 135098/135098/136632/1534`，6G `76033/76033/77722/1689`，2.4G `150648/150648/152429/1781`，因此三 band 都落在 `max((txmulti + matching wds_txmulti) - BroadcastPacketsSent, 0)`。反而舊 D331 式 `/proc/net/dev_extstats` `$18` 仍停在 `154277/148836/154585`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `235` 刷新為 workbook row `310`；targeted D310 tests `3 passed`。next ready patch-driven workbook-Pass case=`D311`。
> P4-04 latest note (2026-04-10, continuation): `D311 getSSIDStats() PacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters rxframe` 設 `PacketsReceived`，再由 `whm_brcm_vap_ap_stats_accu()` 視需要累加 matching `wds*` 介面的封包計數。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wl0 if_counters rxframe/wds0.0.1 rxframe = 1082/1082/1080/2`，6G `292/292/292/0`，2.4G `220/220/220/0`。反而舊 D332 式 `/proc/net/dev_extstats` `$3` 仍停在 `1086/438/333`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `236` 刷新為 workbook row `311`；targeted D311 tests `3 passed`。next ready patch-driven workbook-Pass case=`D312`。
> P4-04 latest note (2026-04-10, continuation): `D312 getSSIDStats() PacketsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters txframe` 設 `PacketsSent`，再由 `whm_brcm_vap_ap_stats_accu()` 視需要累加 matching `wds*` 介面的封包計數。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wl0 if_counters txframe/wds0.0.1 txframe = 192311/192311/157138/35173`，6G `91211/91211/89703/1510`，2.4G `156926/156926/156926/0`。舊 D333 式 `/proc/net/dev_extstats` `$11` 已不再是全 band authority：5G 只停在 base `wl0` 計數 `157138`，6G 漂到 `151237`，只有沒有 WDS peer 的 2.4G 仍 exact close。因此 committed YAML metadata 已由 stale row `237` 刷新為 workbook row `312`；targeted D312 tests `3 passed`。next ready patch-driven workbook-Pass case=`D313`。
> P4-04 latest note (2026-04-10, continuation): `D313 getSSIDStats() RetransCount` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：由 `whm_brcm_get_if_stats()` 以 `wl if_counters txretrans` 設 `RetransCount`，若存在 matching `wds*` 介面也會經 `whm_brcm_vap_ap_stats_accu()` 累加。live serialwrap replay 已關閉這條 source-backed oracle：5G / 6G / 2.4G 都是 `direct/getSSIDStats/wl if_counters txretrans/matching wds txretrans = 0/0/0/0`。adjacent D334 direct case 的 v4.0.3 comment 也已經寫明 `direct Stats matched getSSIDStats(), but workbook v4.0.3 still remains To be tested`，因此 committed oracle 固定維持短版 per-band `direct/getSSIDStats/if_counters` 重播，不沿用這個 stale wording。committed YAML metadata 已由 stale row `238` 刷新為 workbook row `313`；targeted D313 tests `3 passed`。next ready patch-driven workbook-Pass case=`D314`。
> P4-04 latest note (2026-04-10, continuation): `D314 getSSIDStats() UnicastPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：`whm_brcm_get_if_stats()` 先以 `wl if_counters rxframe` 設 `PacketsReceived`、以 `wl if_counters rxmulti` 設 `MulticastPacketsReceived`，再直接導出 `UnicastPacketsReceived = PacketsReceived - MulticastPacketsReceived`；之後 `whm_brcm_vap.c` 只會再去修 visible 的 multicast 欄位，不會回寫 unicast。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wl0 if_counters rxframe/wl0 if_counters rxmulti/wds0.0.1 rxframe/wds0.0.1 rxmulti = 1084/1084/1092/10/2/0`，6G `292/292/292/0/0/0`，2.4G `220/220/220/0/0/0`。反而舊 D335 式 `/proc/net/dev_extstats` `$21` 仍停在 `360/146/107`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `239` 刷新為 workbook row `314`；targeted D314 tests `3 passed`。next ready patch-driven workbook-Pass case=`D315`。
> P4-04 latest note (2026-04-10, continuation): `D315 getSSIDStats() UnicastPacketsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：`whm_brcm_get_if_stats()` 先以 `wl if_counters txframe` 設 `PacketsSent`、以 `wl if_counters txmulti` 設 `MulticastPacketsSent`，再直接導出 `UnicastPacketsSent = PacketsSent - MulticastPacketsSent`；之後 `whm_brcm_vap.c` 只會再去修 visible 的 multicast 欄位，不會回寫 unicast。live serialwrap replay 已關閉這條 source-backed oracle：focused 5G rerun `direct/getSSIDStats/wl0 if_counters txframe/wl0 if_counters txmulti/wds0.0.1 txframe/wds0.0.1 txmulti = 55992/55992/159684/140207/36515/0`，6G `13317/13317/92193/81711/2835/0`，2.4G `2338/2338/159419/157081/0/0`。反而舊 D336 式 `/proc/net/dev_extstats` `$22` 在三 band 都停在 `0`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `240` 刷新為 workbook row `315`；targeted D315 tests `3 passed`。next ready patch-driven workbook-Pass case=`D316`。
> P4-04 latest note (2026-04-10, continuation): `D316 getSSIDStats() UnknownProtoPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：`whm_brcm_get_if_stats()` 以 `wl if_counters rxunknownprotopkts` 設 `UnknownProtoPacketsReceived`，`whm_brcm_vap_copy_stats()` 會保留 base VAP 的欄位值，而 `whm_brcm_vap_ap_stats_accu()` 也可再累加 matching `wds*` 介面的同名計數。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wl0 if_counters rxunknownprotopkts/wds0.0.1 rxunknownprotopkts = 0/0/0/0`，6G `0/0/0/0`，2.4G `0/0/0/0`。adjacent D337 direct case 雖然 workbook 仍 gate 在 `To be tested`，但 v4.0.3 comment 已經記錄同一個 multiband zero-shape（`direct Stats matched getSSIDStats()`），因此 committed oracle 固定維持短版 per-band `direct/getSSIDStats/if_counters + WDS accumulation` 重播。committed YAML metadata 已由 stale row `241` 刷新為 workbook row `316`；targeted D316 tests `3 passed`。next ready patch-driven workbook-Pass case=`D317`。
> P4-04 latest note (2026-04-10, continuation): `D317 BSSID` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 `WiFi.SSID` object 也在 `dm_info.c` 持續暴露 `BSSID` 欄位。live serialwrap replay 已關閉這條獨立 oracle：5G `ubus BSSID / iw dev wl0 info addr / wl -i wl0 cur_etheraddr = 2c:59:17:00:19:95/2c:59:17:00:19:95/2c:59:17:00:19:95`，6G `2c:59:17:00:19:96/2c:59:17:00:19:96/2c:59:17:00:19:96`，2.4G `2c:59:17:00:19:a7/2c:59:17:00:19:a7/2c:59:17:00:19:a7`。workbook v4.0.3 comment 本來就記錄了同一個 `matching iw dev info` 形狀，因此 committed oracle 固定維持短版 per-band `ubus BSSID -> iw dev info -> wl cur_etheraddr` 重播。committed YAML metadata 已由 stale row `242` 刷新為 workbook row `317`；targeted D317 tests `3 passed`。next ready patch-driven workbook-Pass case=`D319`。
> P4-04 latest note (2026-04-10, continuation): `D319 MACAddress` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，其中 `WiFi.SSID.{i}.MACAddress` 仍維持 read-only SSID property。live serialwrap replay 已關閉四向獨立 oracle：5G `ubus MACAddress / iw dev wl0 info addr / ifconfig wl0 HWaddr / wl -i wl0 cur_etheraddr = 2c:59:17:00:19:95/2c:59:17:00:19:95/2c:59:17:00:19:95/2c:59:17:00:19:95`，6G `2c:59:17:00:19:96/2c:59:17:00:19:96/2c:59:17:00:19:96/2c:59:17:00:19:96`，2.4G `2c:59:17:00:19:a7/2c:59:17:00:19:a7/2c:59:17:00:19:a7/2c:59:17:00:19:a7`。因此 committed oracle 固定維持短版 per-band `ubus MACAddress -> iw dev info -> ifconfig -> wl cur_etheraddr` 重播。committed YAML metadata 已由 stale row `321` 刷新為 workbook row `319`；targeted D319 tests `3 passed`。next ready patch-driven workbook-Pass case=`D320`。
> P4-04 latest note (2026-04-10, continuation): `D320 SSID` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，其中 `WiFi.SSID.{i}.SSID` 仍維持 read-only SSID property。live serialwrap replay 已關閉這條獨立 oracle：5G `ubus SSID / wl -i wl0 ssid = testpilot5G/testpilot5G`，6G `testpilot6G/testpilot6G`，2.4G `testpilot2G/testpilot2G`。因此 committed oracle 固定維持短版 per-band `ubus SSID -> wl ssid` 重播。committed YAML metadata 已由 stale row `322` 刷新為 workbook row `320`；targeted D320 tests `3 passed`。next ready patch-driven workbook-Pass case=`D321`。
> P4-04 latest note (2026-04-10, continuation): `D321 BroadcastPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。active 0403 source-backed support 與先前已關閉的 D300 family 一致，而 live serialwrap replay 已直接關閉三向 direct-property oracle：5G `direct Stats / getSSIDStats / /proc/net/dev_extstats $23 = 363/363/363`，6G `147/147/147`，2.4G `113/113/113`。因此 committed oracle 固定維持短版 per-band `Stats.BroadcastPacketsReceived -> getSSIDStats().BroadcastPacketsReceived -> /proc $23` 重播，不再沿用較吵雜的舊 traffic procedure。committed YAML metadata 已由 stale row `245` 刷新為 workbook row `321`；targeted D321 tests `3 passed`。next ready patch-driven workbook-Pass case=`D322`。
> P4-04 latest note (2026-04-11, continuation): direct-stats capture resolver 已修正；對於 `D321/D322` 這類 authored `ubus-cli "...getSSIDStats()" | sed ...` shell pipeline，runner 不再覆寫成 synthesized plain readback。對應 regression 已補上，command-budget inventory 也同步追到 `618`，完整 full repo regression 現在是 `1634 passed`。
> P4-04 latest note (2026-04-11, continuation): `D321 BroadcastPacketsReceived` 已完成真正的 runner live revalidation。單案 rerun `20260411T002336420469` 在實際 `testpilot run` 路徑下直接通過：5G `364/364/364`、6G `149/149/149`、2.4G `113/113/113`，三 band 都維持 `direct / getSSIDStats / /proc $23` exact close。
> P4-04 latest note (2026-04-11, continuation): `D322 BroadcastPacketsSent` 目前轉為 source-backed blocker。修正 resolver 後，live rerun `20260411T002336420469` 已證明三 band `direct == getSSIDStats()` 都 exact close；但 5G 連兩次都維持 `direct / getSSIDStats / /proc $24 = 1644/1644/1645`、`1647/1647/1648`，只有 `/proc` 後讀值固定大 `+1`。0403 source (`whm_brcm_vap.c`) 也顯示 final public `BroadcastPacketsSent` 會在後段被覆寫回 `tmp_stats`，因此目前不能把 `/proc $24` 當成穩定 authoritative oracle。blocker handoff 已落在 `plugins/wifi_llapi/reports/D322_block.md`；next ready patch-driven workbook-Pass case=`D323`。
> P4-04 latest note (2026-04-11, continuation): `D323 BytesReceived` 已完成 real-runner 對齊。先前 blocker 最終被證明是 stale workbook `/proc/net/dev_extstats` `$2` heuristic 加上錯誤的 source 解讀；修正後的 0403 tracing 顯示 direct-property row 應對 `whm_brcm_get_if_stats()` 的 `wl if_counters rxbyte`，再加 matching `wds*` `rxbyte` 累加，而 `whm_brcm_vap_update_ap_stats()` 本身並不會把 `BytesReceived` 回填成 `tmp_stats`。改寫 YAML 後，official rerun `20260411T231952006453` 已在三 band exact-close `direct / getSSIDStats / driver = 276282/276282/276282`、`122610/122610/122610`、`73193/73193/73193`；committed metadata 也已由 stale row `247` 刷新為 workbook row `323`，`plugins/wifi_llapi/reports/D323_block.md` 轉為 historical resolution notes。
> P4-04 latest note (2026-04-11, continuation): `D330 MulticastPacketsReceived` 已在 current local repo state 完成 source-backed green-lock。isolated rerun `20260411T191809490680` 最終以 active 0403 公式 `max((rxmulti + matching wds_rxmulti) - BroadcastPacketsReceived, 0)` 關掉三 band：attempt 1 雖然打到 `/tmp/_tp_cmd.sh: line 2: syntax error: unterminated quoted string`，但 attempt 2 已 exact-close `direct / getSSIDStats / driver-formula = 0 / 0 / 0` on 5G / 6G / 2.4G，且 YAML metadata 已由 stale row `254` 刷新為 workbook row `330`。同一輪後續 `D331 MulticastPacketsSent` 的兩次 source-backed trial rerun（`20260411T192138186700` / `20260411T192524301950`）先把 workbook `/proc $18` heuristic 排除，但 superseding official rerun `20260411T234124237416` 仍證明公式 rewrite 不具 runner-stability：5G 從 `286001/286006` 漂到 `286140/286192`，6G/2.4G 也只在第二次 attempt 才 exact-close。local rewrite 已回退，`D331` 仍維持 blocker；remaining unresolved direct-stats queue=`D322`、`D331`、`D333`、`D336`。
> P4-04 latest note (2026-04-11, continuation): `D332 PacketsReceived` 已在 current local repo state 完成 source-backed green-lock。stale workbook replay `20260411T194312398713` 先同時重現了兩個問題：`/proc/net/dev_extstats` `$3` 在 5G 漂到 `2234/2237`、而舊 `getSSIDStats()` extractor 又 overmatch 成 `expected=0`；之後把 case metadata 刷到 workbook row `332`、把 `getSSIDStats()` extraction 錨定到真正的 `PacketsReceived` 欄位，再把 driver oracle 切到 `wl if_counters rxframe + matching wds rxframe`，source-backed rerun `20260411T194647490016` 已單次 `Pass`。同一輪後續 `D333 PacketsSent` 的 stale replay `20260411T194816992700` 與 source-backed trial `20260411T195140855058` 則收斂成 blocker：6G/2.4G 已 exact-close，但 5G 仍固定維持 `driver = direct + 5` drift（`293527/293532`、`293669/293674`），因此公式 rewrite 已回退、blocker handoff 已補到 `plugins/wifi_llapi/reports/D333_block.md`；next ready patch-driven workbook-Pass case=`D335`。
> P4-04 latest note (2026-04-11, continuation): `D335 UnicastPacketsReceived` 已在 current local repo state 完成 source-backed green-lock。stale workbook replay `20260411T200329824574` 先重現 `/proc/net/dev_extstats` `$21` 在三 band 全部 drift：5G `2001/746`、6G `788/391`、2.4G `483/235`（direct/getSSIDStats vs proc）；0403 source 則明確在 `whm_brcm_api_ext.c` 將 `UnicastPacketsReceived` 導出為 `PacketsReceived - MulticastPacketsReceived`，再由 `whm_brcm_vap.c` copy/accu 到 matching VAP/WDS stats。把 case metadata 刷到 workbook row `335`，並把 driver oracle 切到 `(wl if_counters rxframe + matching wds rxframe) - (wl if_counters rxmulti + matching wds rxmulti)` 後，source-backed rerun `20260411T200851584762` 已 exact-close 三 band：5G `2003/2003/2003`、6G `794/794/794`、2.4G `483/483/483`；next ready patch-driven workbook-Pass case=`D336`。
> P4-04 latest note (2026-04-11, continuation): `D336 UnicastPacketsSent` 目前已收斂成 source-backed blocker。stale workbook replay `20260411T201639103833` 先重現 `/proc/net/dev_extstats` `$22` 在三 band 全部掉成 zero-shape：5G `26434/0`、6G `21540/0`、2.4G `10563/0`（direct/getSSIDStats vs proc）；之後的第一版 source-backed formula trial `20260411T201939105374` 又在 `driver_5g` step 打到 `/tmp/_tp_cmd.sh` shell parse error，因此改成較保守的 `awk + expr` parser 再跑 `20260411T202824539933`。新的 parser 已能執行，但公式仍不夠 durable：attempt 1 的 6G 仍漂成 `21654/21682`，attempt 2 則變成 5G/6G exact-close、只剩 2.4G `11690/11691` 差 `+1`；因此目前不能把這條 txframe/txmulti 公式正式落到 YAML，blocker handoff 已補到 `plugins/wifi_llapi/reports/D336_block.md`，direct-stats 尾段暫時告一段落，next ready runtime-triage case=`D277`。
> P4-04 latest note (2026-04-11, continuation): `D277 getScanResults() Bandwidth` 已在 current local repo state 完成 source-backed live 對齊。isolated rerun `20260411T205454026707` 證明把 `getScanResults()` 先縮到第一個 scan object 後，舊的 6G full-payload serialwrap/broker recovery blocker 已不再重現；同一個 workbook-style target BSSID replay 也已能直接對位 `wl escanresults` Chanspec bandwidth：5G `80/80`、6G `320/160`、2.4G `20/20`。因此 committed YAML metadata 已由 stale row `279` 刷新為 workbook row `277`，並將 `results_reference.v4.0.3` 收斂成 `Pass / Fail / Pass`；next ready runtime-triage case=`D281`。
> P4-04 latest note (2026-04-11, continuation): direct-stats capture resolver 已修正；對於 `D321/D322` 這類 authored `ubus-cli "...getSSIDStats()" | sed ...` shell pipeline，runner 不再覆寫成 synthesized plain readback。對應 regression 已補上，command-budget inventory 也同步追到 `618`，完整 full repo regression 現在是 `1634 passed`。
> P4-04 latest note (2026-04-11, continuation): `D321 BroadcastPacketsReceived` 已完成真正的 runner live revalidation。單案 rerun `20260411T002336420469` 在實際 `testpilot run` 路徑下直接通過：5G `364/364/364`、6G `149/149/149`、2.4G `113/113/113`，三 band 都維持 `direct / getSSIDStats / /proc $23` exact close。
> P4-04 latest note (2026-04-11, continuation): `D322 BroadcastPacketsSent` 目前轉為 source-backed blocker。修正 resolver 後，live rerun `20260411T002336420469` 已證明三 band `direct == getSSIDStats()` 都 exact close；但 5G 連兩次都維持 `direct / getSSIDStats / /proc $24 = 1644/1644/1645`、`1647/1647/1648`，只有 `/proc` 後讀值固定大 `+1`。0403 source (`whm_brcm_vap.c`) 也顯示 final public `BroadcastPacketsSent` 會在後段被覆寫回 `tmp_stats`，因此目前不能把 `/proc $24` 當成穩定 authoritative oracle。blocker handoff 已落在 `plugins/wifi_llapi/reports/D322_block.md`；next ready patch-driven workbook-Pass case=`D323`。
| P4-05 | Wifi_LLAPI Excel report pipeline | done | template + run report + merged-cell 相容 |

## Phase 5：CLI & Integration

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P5-01 | cli-full | done | `run/list` + `wifi-llapi baseline-qualify` + `wifi-llapi build-template-report` |
| P5-02 | orchestrator-full | done | Orchestrator 拆分 + SDK session + hook policy + advisory/remediation hot-path wiring 已落地 |
| P5-03 | integration tests（mock transport） | done | 已補齊 realistic runtime 測試；最新完整 full suite `1622 passed`，已涵蓋 custom 6G gate/timing hardening、`execute_step()` skip-action short-circuit、paired-AP hard-disable tweak、6G OCV restart socket cleanup、以及 qualifier secondary-BSS-warning taxonomy 的回歸測試；3x live full run determinism 驗證通過（Run 2/3 = 100% 一致） |
| P5-04 | plugin agent-config schema/runtime | done | `agent_runtime.py` |
| P5-05 | agent selection trace | done | per-case selection / fallback trace |
| P5-06 | case-agent dispatcher（sequential） | done | `max_concurrency=1` |
| P5-07 | retry-aware timeout policy | done | 遞增 timeout + 上限 |
| P5-08 | per-case trace artifact writer | done | `reports/agent_trace/<run_id>/` |
| P5-09 | integration tests for per-case agent | done | selector / fallback / retry / timeout / trace 全覆蓋 |

---

## Phase R1：指令格式與 Report 清理

| ID | 項目 | 狀態 | 對應隱患 | 說明 |
|---|---|---|---|---|
| R1-01 | Report 保存不覆蓋 | done | H3 | 報告檔名加 `run_id`，copy 另有 `_01/_02` 防呆 |
| R1-02 | xlsx report output 清理 marker | done | H4 | `sanitize_report_output()` 清除舊 serial prompt / marker 汙染 |
| R1-03 | xlsx report cmd 欄單行化 | done | 需求 | G 欄改為單行一條指令 |
| R1-04 | YAML 指令單行化工具 | done | H5 | 新增 `wifi-llapi audit-yaml-commands` dry-run 稽核 |

## Phase R2：Orchestrator 解耦

| ID | 項目 | 狀態 | 對應隱患 | 說明 |
|---|---|---|---|---|
| R2-01 | 拆分 Orchestrator | done | H1 | 拆為 case_utils / runner_selector / execution_engine / orchestrator facade |
| R2-02 | Plugin 自宣告執行流程 | done | H2 | PluginBase.run_pipeline() |
| R2-03 | Plugin 自宣告 Reporter | done | H2 | PluginBase.create_reporter() + report_formats() |
| R2-04 | Retry 歷史保留 | done | H8 | ExecutionEngine.execute_with_retry() + RetryResult dataclass |

## Phase R3：Plugin Template

| ID | 項目 | 狀態 | 對應隱患 | 說明 |
|---|---|---|---|---|
| R3-01 | 乾淨 plugin template | done | 需求 | `plugins/_template/` 完整骨架 |
| R3-02 | Plugin scaffold CLI | done | 需求 | template loadable by PluginLoader |
| R3-03 | Plugin 拆檔示範 | done | H6 | CommandResolver 拆為 `command_resolver.py` |
| R3-04 | PluginBase 瘦身 | done | H14 | 最小合約：name + discover_cases + execute_step + evaluate |

## Phase R4：Copilot SDK 控制平面

| ID | 項目 | 狀態 | 對應隱患 / 需求 | 說明 |
|---|---|---|---|---|
| R4-00 | 第三次重構研究 / 文件基線 | done | 需求 | Copilot SDK 深度研究完成，docs / README / AGENTS 已同步基線 |
| R4-01 | Copilot SDK session foundation | done | 需求 | SDK session wire-in with create/cleanup lifecycle per case |
| R4-02 | hook policy layer | done | 需求 | 6 lifecycle hooks: pre/post_case, pre/post_step, on_failure, on_retry |
| R4-03 | custom agents roles | done | 需求 | executor / advisor / remediation / observer + role merging |
| R4-04 | skills packages | done | 需求 | SkillRegistry + SKILL.md discovery + role-based resolution |
| R4-05 | advisory agent outputs | done | 需求 | AdvisoryOutput + AdvisoryCollector + IHook handler factory |
| R4-06 | remediation planner loop | done | 需求 | RemediationPlanner + severity-prioritized action mapping + wifi_llapi in-run safe remediation loop |
| R4-07 | runtime policy alignment | done | 需求 | `plugins/wifi_llapi/agent-config.yaml` 已改為 copilot-only policy |
| R4-08 | selective MCP integrations | done | 需求 | MCPRegistry + role-selective server management |

## Phase R5：可靠性與測試補強

| ID | 項目 | 狀態 | 對應隱患 | 說明 |
|---|---|---|---|---|
| R5-01 | serialwrap RC 擷取修正 | done | H7 | `_extract_marker_output()` 改為 FIRST match |
| R5-02 | Plugin loader 改良 | done | H9 | sys.path try/finally cleanup |
| R5-03 | openpyxl API 隔離 | done | H10 | excel_adapter.py 封裝 |
| R5-04 | Session 解析簡化 | pending | H12 | 減少 fallback 層數並嚴格裝置綁定 |
| R5-05 | 測試覆蓋 >80% | done | 品質 | coverage baseline 81% (2492 stmts) |
| R5-06 | MD/JSON report 實作 | done | H13 | IReporter + MarkdownReporter + JsonReporter |
| R5-07 | `execute_step` heuristic 收斂 | done | 新增 | CommandResolver strategy pattern refactor |
| R5-08 | control-plane / verdict-plane 邊界測試 | done | 新增 | 15 boundary tests for hook/retry/timeout/band interactions |
