# TestPilot Todo Board（Single Source of Truth）

## 治理規則

1. 本檔是專案唯一待辦清單。
2. 狀態僅允許：`pending`、`in_progress`、`done`、`blocked`。
3. 非 Plan Mode：不得增減項次，不得調整 ID。
4. 非 Plan Mode：僅可更新「狀態」與必要註記。
5. 已核准的重構計畫實作，需對齊 `docs/plan.md` 的 phase 與邊界。
6. 本檔狀態需同步反映 `docs/plan.md` 的現況快照。

---

## Phase 0：Scaffold

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P0-01 | scaffold（目錄與 pyproject） | done | 已建立 |
| P0-02 | plugin-base（PluginBase + Loader） | done | 已建立 |
| P0-03 | case-schema 驗證 | done | 已建立 |
| P0-04 | testbed-config 解析 | done | 已建立 |
| P0-05 | transport-base（StubTransport） | done | 已建立 |
| P0-06 | skeleton-cli | done | 已具備 `list-plugins/list-cases/run` |
| P0-07 | skeleton-orchestrator | done | 已可載入 plugin/cases |
| P0-08 | wifi-plugin-stub | done | plugin hook 目前仍為 stub |
| P0-09 | docs-init | done | 已完成 master plan/todos/phases 對齊 |
| P0-10 | github-push | done | v0.0.3-draft tag 已推送 |

## Phase 1：Transport Layer

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P1-01 | serial-wrap transport | done | 已完成 serialwrap transport，實機全量 run 驗證 |
| P1-02 | adb transport | done | `src/testpilot/transport/adb.py` |
| P1-03 | ssh transport | done | `src/testpilot/transport/ssh.py` |
| P1-04 | network utils（ping/arping/iperf） | done | `src/testpilot/transport/network.py` |

## Phase 2：Environment Management

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P2-01 | topology module | pending | `src/testpilot/env/` 目前僅 `__init__.py` |
| P2-02 | provisioner module | pending | 尚未實作 |
| P2-03 | validator module | pending | 尚未實作 |

## Phase 3：Core Engine

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P3-01 | test-runner loop | done | ExecutionEngine + runner_selector 已落地 (R2-01) |
| P3-02 | monitor subsystem | pending | 尚未實作 |
| P3-03 | reporter（MD/JSON） | done | IReporter + MarkdownReporter + JsonReporter 已落地 (R5-06) |
| P3-04 | verdict merge policy | done | 與 R4-05/R4-06 合併；AdvisoryCollector + diagnostic_status / remediation_history 投影已落地 |
| P3-05 | post-run remediation loop | done | 與 R4-06 合併；RemediationPlanner 保留 post-run 彙整，wifi_llapi 已接上 in-run safe remediation loop |

## Phase 4：Wifi_LLAPI Plugin

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P4-01 | wifi-plugin full implementation | done | setup/verify/execute/evaluate 已完成 |
| P4-02 | case-getRadioStats | done | legacy compatibility fixture 已保留，改為 underscore-prefixed explicit fixture，不進 discover_cases |
| P4-03 | case-kickStation | done | legacy compatibility fixture 已保留，改為 underscore-prefixed explicit fixture，不進 discover_cases |
| P4-04 | 420 case source 對齊治理 | in_progress | discoverable inventory 維持 420；2026-04-15 latest committed closure 仍是 `D600 WiFi7STARole.NSTRSupport`（official rerun `20260415T173554269251`），這筆 stale getter-only case 已從 source row `416` / raw `Fail / Fail / Fail` 刷回 workbook row `600` / raw `Pass / Pass / Pass`，focused live survey 與 official rerun 都在 tri-band exact-close `WiFi.Radio.1/2/3.Capabilities.WiFi7STARole.NSTRSupport=1`，且 `diagnostic_status=Pass`。overlay compare 仍維持 `395/420` full matches、`25` mismatches，metadata drifts 維持 `43`；既有 `D277 getScanResults() Bandwidth`（`20260411T205454026707`）與 `D290 getScanResults() CentreChannel`（`20260411T220324862766`）兩筆 authoritative rerun 已補納，所以這兩列都從 stale `Fail / Fail / Fail` 修正成 source-backed `Pass / Fail / Pass`，per-band summary 也校正成 `5g 397/23`、`6g 395/25`、`2.4g 398/22`。latest official rerun `20260415T180502444191` 已把 `D020 FrequencyCapabilities` 重新確認成 verified/source-backed fail-shaped mismatch：兩次 attempt 都停在 `result_5g.FrequencyCapabilities`，因為 workbook 仍期待空字串，但 live getter 與 driver normalization 仍穩定回 `5GHz`，另外兩個 band 仍是 `6GHz` / `2.4GHz`。latest official rerun `20260415T182628238198` 也把 `D047 SupportedHe160MCS` 重新確認成 workbook/source authority conflict：rerun 仍 exact-close source/runtime-correct 的 `Not Supported / N/A / N/A` 並維持 `diagnostic_status=Pass`，但 same-window 5G getter 仍回 `error=4 / parameter not found`，而同一個 `AssociatedDevice.1` entry 仍暴露 `Rx/TxSupportedHe160MCS`。因此 `D020`、`D277`、`D289`、`D290` 現在都屬於 verified/source-backed fail-shaped mismatch bucket，而 `D047` 仍屬於 authority-conflict blocker bucket。focused serialwrap survey 也已把 `D588 SSID MLDUnit` 停成 workbook/source-driver authority blocker：三個 band 的 public getter 都維持 `MLDUnit=-1`，而 workbook `H588` 對應的 `wl -i wlX mld_unit 0` / `wl -i wlX mld_unit` 在 current DUT 一律回 `wl: Unsupported`，`/tmp/wl*_hapd.conf` 也沒有任何 `mld_unit=` readback 可用；在沒有可重播的 driver/config oracle 前，`D588` 不能升成 workbook closure，repo-visible blocker note 已落在 `plugins/wifi_llapi/reports/D588_block.md`。較早的 focused rerun `20260415T131457874117` 已把 `D508 SSID WMM AC_BE Stats WmmFailedBytesSent` 確認成 localized 2.4G zero-getter blocker：5G 仍 exact-close `0 / 0 / 0`、6G 仍 exact-close `708116 / 708116 / 708116`，但 2.4G 的 `getSSIDStats()` 與 direct getter 都固定停在 `0`，而 `wl2 wme_counters` `AC_BE` tx failed bytes 穩定為 `90`，因此 `D508` 仍不進 closure commit。focused serialwrap survey 也仍把 `D524 SSID WMM AC_BE Stats WmmPacketsSent` 停在 tri-band tx-frame oracle blocker：5G/6G/2.4G 的 `getSSIDStats()` 與 direct getter exact-close `452/452`、`510/510`、`547/547`，但 same-window `wl wme_counters` `AC_BE` `tx frames` 漂到 `896`、`1015`、`1090`，所以目前仍沒有可接受的獨立 oracle；repo-visible blocker note 已落在 `plugins/wifi_llapi/reports/D524_block.md`。既有 localized blockers `D490`、`D481`、`D482`、`D485`、`D454` 與 `D371` 仍維持，現在再加上 `D508`、`D524` 與 `D588`；targeted runtime/budget guardrails 已是 `1251 passed`，full repo regression 維持 `1660 passed`；`D355-D357` 仍保留在需要 CSI client setup 的 placeholder bucket，`D359 AccessPoint.IsolationEnable` 也因 workbook 需要 two-station isolation ping 而暫停在 current single-STA lab shape；strict compare 雖仍先列出 `D414/D415` AssociatedDevice RRM cases，但 workbook `G` 明示需要一台 802.11k enable、另一台 disable 的 dual-STA shape，因此目前仍維持 readiness review。現在仍沒有乾淨的 workbook-pass-ready 單案；接下來的 investigative track 轉到 shared-6G blocker review，先看 `D179 Radio.Ampdu`。`D211 Radio.OperatingStandards` 仍 park 在 `plugins/wifi_llapi/reports/D211_block.md`；`D204 Radio.MultiUserMIMOEnabled` 仍停在 `plugins/wifi_llapi/reports/D204_block.md`；`D182 Radio.RtsThreshold` 仍因 shared 6G baseline path 暫停，`D302 BytesReceived` 仍維持 `plugins/wifi_llapi/reports/D302_block.md` 的 source-backed blocker；systemic active blockers 維持 `D047` authority conflict + shared 6G baseline manifestations（`D179`、`D181`）。historical `D257` blocker context 保留在 `plugins/wifi_llapi/reports/D257_block.md`。 |

> P4-04 latest note (2026-04-13, D059): `D059 UplinkBandwidth` 已透過 official rerun `20260413T055159421076` 完成 source-backed 對齊；這案不是單純 metadata drift，而是把 stale row `61`、漂移的 `TestPilot_BTM` / `WPA3-Personal` baseline、以及未經 uplink trigger 的弱 driver reread 一起收斂回 workbook row `59` 與 authoritative generic `testpilot5G` / `WPA2-Personal` 路徑。最新 rerun exact-close `UplinkBandwidth=20`、`AssocMacAfterTrigger=DriverAssocMac=2C:59:17:00:04:85`、`DriverUplinkBandwidth=20`；overlay compare 現在是 `264/420` full matches、`156` mismatches、`58` metadata drifts，full repo regression=`1654 passed`，next ready workbook-Pass revisit=`D060`。

> P4-04 latest note (2026-04-13, D179 blocker): `D179 Radio.Ampdu` 目前不是 closure，而是新的 live blocker。workbook authority 仍是 row `179`，而 `0401 G/H` 明確要求先 connect station，再切 `Ampdu=1/0/-1`，最後用 `wl -i wlx ampdu` 做 driver oracle；因此 focused rerun `20260413T175446838229` 已直接否決舊 DUT-only replay：5G 已出現 `AfterSet0GetterAmpdu5g=0`，但 driver 仍是 `AfterSet0DriverAmpdu5g=1`，而且該 rerun 沒有任何 per-case STA evidence。之後 trial YAML 已改成 explicit `DUT + STA` tri-band topology，targeted D179/runtime + command-budget guardrails=`4 passed`；但 clean-start rerun `20260413T182427454124` 仍在進入 case steps 前反覆卡在 6G `verify_env` recovery（`6G ocv fix did not stabilize wl1 after retries`、`sta_baseline_bss[1] not ready after 60s cmd=wl -i wl1 bss`、`STA 6g link check failed ... Not connected.`）。因此 overlay compare 目前維持 `298/420` full matches、`122` mismatches、`58` metadata drifts；active blockers 已變成 `D047` authority conflict + `D179` 6G baseline blocker，next ready actionable compare-open case 改為 `D180 Radio.Amsdu`。

> P4-04 latest note (2026-04-14, D180/D184-D187 + D181 blocker): `D180 Radio.Amsdu` 與 `D184-D187` 已透過 official rerun `20260414T111010511593` / `20260414T111624033199` / `20260414T111633789177` / `20260414T111643078674` / `20260414T111652454052` 完成 low-risk getter closure；workbook authority 現在分別刷新到 row `180` / `184-187`，rerun 也 exact-close tri-band `Amsdu=-1` 與 `NrActiveRxAntenna` / `NrActiveTxAntenna` / `NrRxAntenna` / `NrTxAntenna` = `4`。本輪 targeted radio-getter/runtime guardrails=`201 passed`、command-budget=`1 passed`，compare refresh 後已更新為 `304/420` full matches、`116` mismatches、`58` metadata drifts。另一方面，`D181 Radio.FragmentationThreshold` 的 clean-start trial rerun `20260414T111023418516` 再次卡在 shared 6G `DUT + STA` baseline recovery（`sta_baseline_bss[1] not ready after 60s cmd=wl -i wl1 bss`、`STA band baseline/connect failed`、`6G ocv fix did not stabilize wl1 after retries`），只留下 partial xlsx，因此 provisional `D181/D182` setter rewrite 已回退、`D182` 暫停；active blockers 現在視為 `D047` authority conflict + shared 6G baseline blocker（manifested in `D179` / `D181`），next ready non-blocked compare-open case=`D190 ExplicitBeamFormingEnabled`。

> P4-04 latest note (2026-04-13, D178): `D178 Radio.ChannelLoad` 已透過 official rerun `20260413T172222999250` 完成 workbook-authoritative pass closure；workbook authority 是 row `178`，舊 authored shape 的 stale row `141` 加 bare getters 已在歷史 full run 證明會讓 6G 落到空值 / cache drift。landed case 現在改成 per-band same-window tight-capture：同一步先 `getRadioAirStats()` refresh，再抓 `ChannelLoad?` 與 `iw dev wlX survey dump`，rerun exact-close `AirLoad=ChannelLoad=SurveyChannelLoad`：`5G=84`、`6G=62`、`2.4G=98`；其中 2.4G survey 對位採 floor-based `int((busy * 100) / active)`，`69/70` 正確收斂成 `98` 而不是 `99`。targeted D178/runtime + command-budget guardrails=`4 passed`，overlay compare 已更新為 `298/420` full matches、`122` mismatches、`58` metadata drifts，full repo regression=`1662 passed`，next ready actionable compare-open case=`D179 Radio.Ampdu`。

> P4-04 latest note (2026-04-13, D053): `D053 TxBytes` 已透過 official rerun `20260413T164447027184` 完成 workbook-authoritative pass closure；workbook authority 是 row `53`，而舊 authored shape 把 getter / snapshot / driver 拆成多個 DUT steps，已在 earlier reruns 證明會造成 official-runner drift。landed case 現在改成 per-band same-window tight-capture，rerun exact-close `AssocTxBytes=TxBytes=DriverTxBytes` on tri-band generic baseline：`5G=992`、`6G=25207`、`2.4G=25586`；targeted D053/runtime + command-budget guardrails=`8 passed`，overlay compare 已更新為 `297/420` full matches、`123` mismatches、`58` metadata drifts，full repo regression=`1662 passed`，next ready actionable compare-open case=`D178 Radio.ChannelLoad`。

> P4-04 latest note (2026-04-13, D060): `D060 UplinkMCS` 已透過 official rerun `20260413T060855269192` 完成 source-backed 對齊；這案不是 semantic mismatch，而是 authoritative full-run 已先 exact-close same-STA `UplinkMCS` vs `DriverUplinkMCS`，但 case 仍帶著 stale row `62` 與缺少 `| head -n 1` 的 fragile `MACAddress` extractor，讓 `AssocMacAfterTrigger` / `DriverAssocMac` 被重複展開成 invalid regex shape。最新 rerun exact-close `UplinkMCS=10`、`AssocMacAfterTrigger=DriverAssocMac=2C:59:17:00:04:85`、`DriverUplinkMCS=10`；overlay compare 現在是 `265/420` full matches、`155` mismatches、`58` metadata drifts，full repo regression 仍是 `1654 passed`，next ready workbook-Pass revisit=`D062`。

> P4-04 latest note (2026-04-13, D062): `D062 VendorOUI` 已透過 official rerun `20260413T061743676049` 完成 source-backed 對齊；這案不是 firmware getter 缺失，而是 authoritative full-run 早就 exact-close 了 non-empty direct getter / same-entry snapshot / same-STA driver capture，但 case 還留著 stale row `64` 與舊 fail-shaped contract（誤把 `VendorOUI=""` 當成預期）。最新 rerun exact-close `VendorOUI=AssocVendorOUI=DriverVendorOUIList=00:90:4C,00:10:18,00:50:F2,50:6F:9A`、`DriverVendorOUICount=4`；overlay compare 現在是 `266/420` full matches、`154` mismatches、`58` metadata drifts，full repo regression 仍是 `1654 passed`，next ready workbook-Pass revisit=`D063`。

> P4-04 latest note (2026-04-13, D057): `D057 TxUnicastPacketCount` 已透過 official rerun `20260413T130448459477` 完成 workbook-authoritative fail-shaped closure；stale row `59` 已刷新回 workbook row `57`，而 row `57` 本身就是 `Fail / Fail / Fail` authority。舊 custom `TestPilot_BTM` / `WPA3-Personal` / `SAE` clean-start replay 因 `wpaie set error (-7)` / `wpa_state=DISCONNECTED` 被正式拒絕落地，改用 validated generic `testpilot5G` / `WPA2-Personal` baseline 後，current 0403 rerun exact-close `StaMac=AssocMAC=DriverAssocMac=2C:59:17:00:04:85`、`AssocTxPacketCount=DriverTxPacketCount=7`、`TxUnicastPacketCount=AssocTxUnicastPacketCount=0`、`DriverTxUnicastPacketCount=7`。overlay compare 現在是 `288/420` full matches、`132` mismatches、`58` metadata drifts，full repo regression=`1657 passed`，next ready compare-open case=`D014 ChargeableUserId`。

> P4-04 latest note (2026-04-13, D014): `D014 ChargeableUserId` 已透過 official rerun `20260413T132144592128` 完成 workbook-gated skip closure；stale row `16`（`DownlinkBandwidth`）已刷新回真正的 workbook row `14`。Current 0403 source 只宣告 read-only `ChargeableUserId` 與 Enterprise-only `RadiusChargeableUserId`，因此 validated non-Enterprise `testpilot5G` / `WPA2-Personal` baseline 下的 authoritative live shape 是 same-STA `assoclist 2C:59:17:00:04:85` 搭配 `ChargeableUserId=""`，而不是偽造 RADIUS path。landed case 現在投影 workbook-consistent `Skip / Skip / Skip`；targeted D014 / assocdev-getter guardrails=`41 passed`，overlay compare 現在是 `289/420` full matches、`131` mismatches、`58` metadata drifts，full repo regression=`1659 passed`，next ready compare-open case=`D019 EncryptionMode / AssociatedDevice`。

> P4-04 latest note (2026-04-13, D063): `D063 VhtCapabilities` 已透過 official rerun `20260413T062615392940` 完成 source-backed 對齊；這案不是 getter 缺失，而是 authoritative full-run 早就證明 same-STA direct getter / same-entry snapshot 是 concrete pass shape，但 case 還留著 stale row `65` 與舊 fail-shaped contract（誤把 `VhtCapabilities=""` 當成預期）。第一次 confirmation rerun 也證明 `wl sta_info` 的 human-readable `VHT caps` line 只能當 subset sanity oracle：LLAPI/snapshot 穩定回 `SGI80,SGI160,SU-BFR,SU-BFE,MU-BFE`，driver line 則穩定回 subset `SGI80,SGI160,SU-BFR,SU-BFE`。最新 rerun exact-close `VhtCapabilities=AssocVhtCapabilities=SGI80,SGI160,SU-BFR,SU-BFE,MU-BFE` 與同 STA driver subset；overlay compare 現在是 `267/420` full matches、`153` mismatches、`58` metadata drifts，full repo regression 仍是 `1654 passed`，next ready actionable workbook-Pass revisit=`D070`。

> P4-04 latest note (2026-04-13, D070): `D070 Enable` 已透過 official rerun `20260413T063442091882` 完成 source-backed 對齊；這案不是 runtime bug，而是 case 還留著 stale row `72` 與 over-authored AP toggle/readback path，偏離 workbook row `70` 只要求的 tri-band baseline pass shape。把 case 拉回 row `70` 的 AP-only getter+driver oracle 後，rerun 已 exact-close `Enable5g/6g/24g=1` 與 `DriverBss5g/6g/24g=up`；overlay compare 現在是 `268/420` full matches、`152` mismatches、`58` metadata drifts，next ready actionable workbook-Pass revisit=`D071`。

> P4-04 latest note (2026-04-13, D071): `D071 FTOverDSEnable` 已透過 official rerun `20260413T064002607672` 完成 source-backed 對齊；這案不是 runtime semantic gap，而是 case 同時留著 stale row `73`、會在 `setup_env` 先打 `IEEE80211r.Enabled/MobilityDomain` 的 stale 順序、以及 `results_reference.v4.0.3 = To be tested / To be tested / To be tested`。把 11r prerequisite 寫回正式 steps、把 `sta_env_setup` 縮回 AP baseline bring-up、並把 metadata 拉回 workbook row `71` / `Pass / Pass / Pass` 後，rerun 已 exact-close tri-band `FTOverDSEnable=0 -> 1 -> 0`、`MobilityDomain=4660`、以及 hostapd `mobility_domain=3412` / `ft_over_ds` 切換；overlay compare 現在是 `269/420` full matches、`151` mismatches、`58` metadata drifts，next ready actionable open case=`D079`。

> P4-04 latest note (2026-04-13, D079): `D079 MACFiltering.Mode` 已透過 official rerun `20260413T065809885285` 完成 workbook-authoritative / source-backed 對齊；真正 root cause 不是 firmware fail，而是 case 還留著 stale row `73`、fail-shaped `results_reference.v4.0.3 = Fail / Fail / Fail`、以及錯把 tri-band all-Off 當成 authority。live probe 與 0403 source trace 已證明 workbook row `79` 的正確 pass path 是先 deterministic 重建 AP1=`WhiteList` / `macaddr_acl=1` / `accept`、AP3/AP5=`BlackList` / `macaddr_acl=0` / `deny`，再驗 `Mode=Off` setter 會回 `invalid value` 且 after-state 不變；第一輪 confirmation rerun `20260413T065611644145` 額外證明真正要拿掉的是非 authoritative 的 `wl -i wl{0,1,2} bss` setup gate，它只會把 hostapd restart 時的 `--wlX FSM DONE--` / `down` 當成假 fail。最新 rerun exact-close tri-band baseline/readback 與 after-state invariance，overlay compare 現在是 `270/420` full matches、`150` mismatches、`58` metadata drifts，full repo regression=`1653 passed`，next ready actionable open case=`D080`。

> P4-04 latest note (2026-04-13, D080): `D080 MaxAssociatedDevices` 已透過 official rerun `20260413T071746618166` 完成 workbook-authoritative / source-backed 對齊；真正 root cause 不是 firmware semantic mismatch，而是 case 同時留著 stale row `74`、fail-shaped `results_reference.v4.0.3 = Fail / Fail / Fail`，以及 setter capture 把 bare ubus object line 黏進 `RequestedTempMax*` 欄位，讓 authoritative full-run 早已存在的 `32 -> 31 -> 32` 真實 pass path 被誤判成 fail。把 metadata 拉回 workbook row `80`、把 setter steps 改成 explicit `RequestedTempMax*` / `SetterEchoMax*` 輸出、並要求每個 band 都 exact-close `getter == hostapd max_num_sta` 後，rerun 已在 AP1 / AP3 / AP5 一次 exact-close temp `31` 與 restore `32`；overlay compare 現在是 `271/420` full matches、`149` mismatches、`58` metadata drifts，full repo regression 仍是 `1653 passed`，next ready actionable open case=`D082`。

> P4-04 latest note (2026-04-13, D082): `D082 MultiAPType` 已透過 official rerun `20260413T075200621380` 完成 workbook-authoritative / source-backed 對齊；真正 root cause 不是 firmware semantic gap，而是 stale case 同時踩了三個 drift：metadata 還停在 workbook row `76`、setter 只改 `AP1/AP3/AP5` 沒有跟 workbook `WiFi.AccessPoint.*.MultiAPType=FronthaulBSS` 一樣把每個 radio pair (`AP1/AP2`、`AP3/AP4`、`AP5/AP6`) 一起切到 fronthaul-only，以及 driver-map normalizer 用了會把 `0x3` 展成逐字元空白的 `sed 's/ */ /g'`。把 metadata 拉回 workbook row `82`、setup 先把六個 AP 都重建成 dual-role baseline、每個 band 改成 paired setter/restore、driver map 改用 `tr -s ' ' | xargs`，並把 setup settle 拉長後，rerun 已 exact-close tri-band `FronthaulBSS,BackhaulBSS -> FronthaulBSS -> FronthaulBSS,BackhaulBSS`，同時 exact-close hostapd `multi_ap=3/3 -> 2/2 -> 3/3` 與 driver `0x3 -> 0x1 -> 0x3`；overlay compare 現在是 `272/420` full matches、`148` mismatches、`58` metadata drifts，full repo regression 仍是 `1653 passed`，next ready actionable open case=`D083` metadata-drift refresh。

> P4-04 latest note (2026-04-13, D083): `D083 Neighbour` 已透過 official rerun `20260413T080405422245` 完成 workbook-authoritative metadata 對齊；這案沒有新的 firmware semantic gap，真正剩下的只有 `source.row` 還停在 stale workbook row `77`。authoritative full-run trace 原本就已 exact-close workbook row `83` 的 AP-only add/delete lifecycle：5G `11:22:33:44:55:66 / 36`、6G `11:22:33:44:55:77 / 1`、2.4G `11:22:33:44:55:88 / 11` 都能形成 `empty -> single entry -> empty`。把 metadata 拉回 workbook row `83`、同步刷新 contract test，再跑 official rerun 後，三個 band 都再次 exact-close add/delete 與 delete-after-empty `ABSENT`；overlay compare 仍是 `272/420` full matches、`148` mismatches、`58` metadata drifts，next ready actionable open case=`D084`。

> P4-04 latest note (2026-04-13, D084): `D084 EncryptionMode / AccessPoint.Security` 已透過 official rerun `20260413T081301178883` 完成 workbook-authoritative 對齊；這案回到低風險 metadata/results_reference family，因為直接讀 `0401.xlsx` row `84` 後已確認 workbook 自己就是 `Not Supported / Not Supported / Not Supported`，而且註解就是 `hardcode in pwhm`。live tri-band probe 仍穩定重現同一個 not-supported 形狀：AP1 / AP3 / AP5 的 getter 都回 `EncryptionMode="Default"`，但對應 `/tmp/wl{0,1,2}_hapd.conf` 仍暴露真實的 `CCMP` cipher（5G/2.4G=`WPA-PSK + CCMP`、6G=`SAE + CCMP`）。把 case 從 stale workbook row `78` / raw `Pass / Pass / Pass` 刷回 workbook row `84` / raw `Not Supported / Not Supported / Not Supported` 後，rerun 已 exact-close tri-band getter 與 hostapd not-equals evidence；overlay compare 現在是 `273/420` full matches、`147` mismatches、`58` metadata drifts，next ready actionable open case=`D085`。

> P4-04 latest note (2026-04-13, D085): `D085 KeyPassPhrase / AccessPoint.Security` 已透過 official rerun `20260413T082022613657` 完成 workbook-authoritative 對齊；真正 root cause 不是 tri-band setter/readback 壞掉，而是 stale case 還停在舊 row `79`，並額外把 workbook row `85` 根本沒要求的 6G `sae_password=` side-channel 當成 pass gate。authoritative full-run trace 已先證明 AP1 / AP3 / AP5 的 quoted leading-zero setter/readback 本來就能 exact-close `getter == hostapd wpa_passphrase`，所以這次只把 metadata 拉回 workbook row `85`、保留真正的 quoted `KeyPassPhrase=\"0689388783\"` convergence path、拿掉 stale 6G SAE gate；rerun 已在一輪 exact-close tri-band `00000000 -> 0689388783 -> 00000000`。targeted D085 tests 維持 `3 passed`、full repo regression 維持 `1653 passed`；overlay compare 現在是 `274/420` full matches、`146` mismatches、`58` metadata drifts，next ready actionable open case=`D086`。

> P4-04 latest note (2026-04-13, D086): `D086 MFPConfig / AccessPoint.Security` 已透過 official rerun `20260413T083419287730` 完成 workbook-authoritative 對齊；這案最後也回到跟 `D084` 同型的低風險 row/results_reference closure：直接讀 `0401.xlsx` row `86` 後可確認 workbook authority 是 `Not Supported / Not Supported / Not Supported`，註解是 `hardcode in pwhm`。active 0403 ODL 仍把 `MFPConfig` 宣告成預設 `Disabled`，而且只在 WPA2 family 下適用；stale case 卻還停在舊 row `80` / raw `Pass / Pass / Pass`。live probe 本身沒有壞：它仍穩定打出 tri-band getter 都是 `Disabled`，但 hostapd enforcement 會跟著 live mode 走（5G/2.4G `ieee80211w=0`、6G `ieee80211w=2`）。把 metadata 拉回 workbook row `86`、results_reference 刷回 `Not Supported / Not Supported / Not Supported` 後，rerun 一次即過；targeted D086 tests 維持 `3 passed`、full repo regression 維持 `1653 passed`，overlay compare 現在是 `275/420` full matches、`145` mismatches、`58` metadata drifts，next ready actionable open case=`D087`。

> P4-04 latest note (2026-04-13, D087): `D087 ModeEnabled / AccessPoint.Security` 已透過 official rerun `20260413T085025879532` 完成 workbook-authoritative 對齊；workbook row `87` 本來就是 tri-band `Pass / Pass / Pass`，但 stale case 還停在舊 row `81`，而且把 workbook 沒要求的 cleanup restore 與過度嚴格的 `wpa_key_mgmt` exact token 也一起綁成 pass gate。row `87` 的真正 authority 比較單純：三個 band 都接受 `ModeEnabled=WPA3-Personal`、getter readback 都是 `WPA3-Personal`，hostapd 則收斂到 WPA3 / SAE family 與 `ieee80211w=2`；restore 只保留做 lab cleanup。把 metadata 拉回 workbook row `87`，並把 pass_criteria 收斂到 setter/readback + SAE-family convergence 後，rerun 已在一輪 exact-close AP1 / AP3 / AP5。targeted D087 tests 維持 `3 passed`、full repo regression 維持 `1653 passed`，overlay compare 現在是 `276/420` full matches、`144` mismatches、`58` metadata drifts，next ready actionable open case=`D090`。

> P4-04 latest note (2026-04-13, D090): `D090 RekeyingInterval / AccessPoint.Security` 已透過 official rerun `20260413T090437438519` 完成 workbook-authoritative 對齊；這案也是典型 stale row / stale semantics closure：真正的 workbook authority 是 row `90`，不是 row `92`，而且 row `90` 的 Pass 形狀是 `RekeyingInterval=0` 搭配 hostapd `wpa_group_rekey=0` cross-check。stale case 卻把它反轉成 `set 3600 / expect divergence` 的 fail-shaped 敘事，雖然舊 full-run trace 其實早就已經在 AP1 / AP3 / AP5 打出 `getter=0`、`hostapd=0`。把 metadata 拉回 workbook row `90`，把 pass_criteria 收斂到 row-90 zero-shape，並補上 active tagged TR-181 data-model `RekeyingInterval -> wpa_gtk_rekey` / default `0` 的 source-backed 註記後，rerun 已在一輪 exact-close AP1 / AP3 / AP5。targeted D090 tests 現在維持 `4 passed`、full repo regression 現在是 `1654 passed`，overlay compare 現在是 `277/420` full matches、`143` mismatches、`58` metadata drifts，next ready actionable open case=`D092`。

> P4-04 latest note (2026-04-13, D092): `D092 WEPKey / AccessPoint.Security` 已透過 official rerun `20260413T092400687838` 完成 workbook-authoritative 對齊；真正的 workbook authority 是 row `92`，而且這案不是 tri-band 全 Pass：5G / 2.4G 必須先切到 `ModeEnabled=WEP-128`，再驗 `WEPKey` getter 與 hostapd `wep_key`，6G 則因 `D088` 已對齊的 `ModesSupported=None,WPA3-Personal,OWE` 維持 `Not Supported`。stale case 還停在舊 row `94`，只是在 WPA2 / WPA3 baseline 下寫 `WEPKey`，因此舊 full-run 只能打出 `123456789ABCD` no-op 與 hostapd 無 `wep_key`；第一輪 confirmation rerun `20260413T092109402810` 又進一步證明最後的殘留問題只是 hostapd extractor 的單引號 quoting 被 temp-script 包裝吃壞。把 metadata 拉回 workbook row `92`、把 5G / 2.4G 改成真正的 `WEP-128 -> WEPKey=AABBCCDDEEFF0 -> hostapd wep_key0 -> WPA2-Personal cleanup` 路徑、把 6G 固定成 unsupported proof branch，並把 extractor 改成 double-quote / `${line:-ABSENT}` 形狀後，rerun 已一輪 exact-close `Pass / Not Supported / Pass`。targeted D092 tests 維持 `4 passed`、full repo regression 現在是 `1655 passed`，overlay compare 現在是 `278/420` full matches、`142` mismatches、`58` metadata drifts，next ready actionable open case=`D093`。

> P4-04 latest note (2026-04-13, D093): `D093 SSIDAdvertisementEnabled` 已透過 official rerun `20260413T094515864676` 完成 workbook-authoritative 對齊；真正的 workbook authority 是 row `93`，不是舊的 row `95`，而且 active 0403 source 很清楚把 pass path 定在 advertised state：TR-181/ODL 仍宣告 `SSIDAdvertisementEnabled` 預設 `true`，unfwlcfg 仍以 bool-reverse `wlHide` 映射，`rcl_unfwireless.c` 也仍是 `SSIDAdvertisementEnabled ? wlHide=0 : wlHide=1`。stale case 卻把它反轉成 `set 0 -> expect ignore_broadcast_ssid=2` 的 hidden-state 敘事，而舊 full-run mismatch 也還混著歷史 setter-step 被 resolver 代成 getter 的 bug。把 metadata 拉回 workbook row `93`，把 pass path 收斂回 idempotent `set 1 -> getter 1 -> hostapd ignore_broadcast_ssid 0`，並補上 resolver guardrail 後，rerun 已一輪 exact-close AP1 / AP3 / AP5。targeted D093 tests 維持 `4 passed`、full repo regression 現在是 `1656 passed`，overlay compare 現在是 `279/420` full matches、`141` mismatches、`58` metadata drifts，next ready actionable open case=`D096`。

> P4-04 latest note (2026-04-13, D111-D113): `D111-D113 getStationStats()` 這三案已透過 grouped official rerun `20260413T122417812289` 完成 repo metadata drift cleanup。這批不是 compare-open mismatch，而是 authored YAML 還各自停在 stale `source.row`：D111 `113 -> 111`（AssociationTime）、D112 `114 -> 112`（AuthenticationState）、D113 `115 -> 113`（AvgSignalStrength）；runtime / compare 其實早就對得上 workbook answer row，所以 cleanup 後 overlay compare 數字維持不變。這次 grouped rerun 仍然 exact-close `3/3 Pass`：D111 關閉 `AssociationTime="2026-04-07T21:50:29Z"`、D112 關閉 `AuthenticationState=1`、D113 關閉 `AvgSignalStrength=0`。targeted D111-D113 runtime tests 維持 `9 passed`、final full repo regression 維持 `1658 passed`，overlay compare 仍是 `287/420` full matches、`133` mismatches、`58` metadata drifts；next ready actionable compare-open case 改回 `D057 TxUnicastPacketCount`。

> P4-04 latest note (2026-04-13, D110): `D110 getStationStats() Active / AccessPoint` 已透過 official rerun `20260413T121358780961` 完成 workbook-authoritative 對齊；這案的真正 drift 與 D109 同族：authored case 還停在 stale workbook row `112`（其實是 `AssociationTime`），而 evaluator 又把 nested `AffiliatedSta[].Active=0` 誤當成 top-level `Active`。calibrated case 現在保留 `wl assoclist` + `getStationStats()` 的 same-station MAC exact-close，並補回 workbook `H` 的 driver oracle：以 `wl sta_info ${STA_MAC}` 抽出包含 `AUTHORIZED` 的狀態行，最後 exact-close `AssocMac=2C:59:17:00:04:85`、`StationStatsMac=2C:59:17:00:04:85`、`TopLevelActive=1`、`StatsMatchesAssoc=1`、`DriverAuthorized=1`。過程中先驗證到嚴格 `grep '^state:'` 只會讓 shell shape 太脆、把 evidence case 打成 `step_command_failed`，因此 landed 版本改為容忍 empty grep output，讓 verdict 由 driver evidence 決定而不是 shell return code。targeted D110 tests 維持 `3 passed`、command budget 維持 `1 passed`、final full repo regression 維持 `1658 passed`，overlay compare 現在是 `287/420` full matches、`133` mismatches、`58` metadata drifts，next ready actionable open case=`D111`。

> P4-04 latest note (2026-04-13, D109): `D109 getStationStats() / AccessPoint` 已透過 official rerun `20260413T115620062809` 完成 workbook-authoritative 對齊；這案的真正 drift 是雙層的：authored case 還停在 stale workbook row `111`（其實是 `AssociationTime`），而且舊 parser 會把 nested `AffiliatedSta[].Active=0` 誤當成 top-level `Active`，再加上 workbook `H` 的 `hostapd_cli sta` 在 current 0403 baseline 下已不可重播（`/tmp/wl0_hapd.conf` 存在，但 `/var/run/hostapd/wl0` control socket 缺失，`hostapd_cli` 只會回 `wpa_ctrl_open: No such file or directory`）。calibrated case 現在改用 `wl assoclist` 做 driver-side association oracle，顯式抽出 `TopLevelActive=1`，並以 `AssocMac + StationStatsMac + StatsMatchesAssoc=1` exact-close same-station evidence；同時 runtime guardrail 也補上 `hostapd_cli` executable token，避免 hostapd-based shell steps 再被靜默 fallback 到 `verification_command`。targeted D109 tests 維持 `4 passed`、command budget 維持 `1 passed`、final full repo regression 現在是 `1658 passed`，overlay compare 現在是 `286/420` full matches、`134` mismatches、`58` metadata drifts，next ready actionable open case=`D110`。

> P4-04 latest note (2026-04-13, D108): `D108 UUID / AccessPoint.WPS` 已透過 official rerun `20260413T113456092168` 完成 workbook-authoritative 對齊；這案的真正 drift 不是 getter fail，而是 authored case 同時帶著 stale workbook row `110` 與過寬的 all-band pass story，錯把 workbook row `108` 的 `Pass / Not Supported / Pass` 改成 `Pass / Pass / Pass`。真正的 workbook authority 是：三個 band 的 getter 都能讀到同一個 valid UUID，但 6G 因 wl1 hostapd 沒有 `uuid=` 投影而維持 `Not Supported`，只有 wl0 / wl2 會投影 `uuid=`。calibrated case 現在把 getter 與 hostapd projection 一起收進 evidence：5G / 2.4G exact-close `UUID + HostapdUuid`，6G exact-close `UUID6g=<valid uuid>` 與 `HostapdUuid6g=`。targeted D108 tests 維持 `3 passed`、command budget 維持 `1 passed`、final full repo regression 維持 `1657 passed`，overlay compare 現在是 `285/420` full matches、`135` mismatches、`58` metadata drifts，next ready actionable open case=`D109`。

> P4-04 latest note (2026-04-13, D106): `D106 RelayCredentialsEnable / AccessPoint.WPS` 已透過 official rerun `20260413T112544193230` 完成 workbook-authoritative 對齊；這案的真正 drift 不是 live getter fail，而是 authored case 還停在 stale workbook row `108`（其實是 `UUID`）並保留 synthetic `Pass / Fail / Pass` results_reference。真正的 workbook authority 是 row `106`，而 row `106` 的 verdict 是 `Not Supported / Not Supported / Not Supported`。current 0403 runtime 仍會在三個 band 上穩定回 `RelayCredentialsEnable=0`，但 source survey 只找到 `wld_accesspoint.odl` 中的 persistent default-false definition，沒有找到 active `wps_cred_processing` backing；因此 calibrated case 現在保留 tri-band getter evidence，同時把 metadata / results_reference 對回 workbook row `106`。targeted D106 tests 維持 `2 passed`、command budget 維持 `1 passed`、final full repo regression 維持 `1657 passed`，overlay compare 現在是 `284/420` full matches、`136` mismatches、`58` metadata drifts，next ready actionable open case=`D108`。

> P4-04 latest note (2026-04-13, D105): `D105 PairingInProgress / AccessPoint.WPS` 已透過 official rerun `20260413T111530183752` 完成 workbook-authoritative 對齊；這案的真正 drift 不是 runtime method 壞掉，而是 stale authored case 同時帶著 workbook row `107` 與 getter-only semantics，錯把 workbook row `105` 的 method case 降格成單純 `PairingInProgress=0` readback。把 metadata 拉回 workbook row `105`，把 case 還原成 `Enable=1 -> InitiateWPSPBC() -> PairingInProgress? -> hostapd_cli wps_get_status -> cancel/cleanup` 後，current 0403 runtime 會 exact-close workbook intent：5G / 2.4G 都是 `Status=Success`、`PairingInProgress=1`、`PbcStatus=Active`，6G 在 WPA3/SAE baseline 下則回 `Status=Error_Other`、`PairingInProgress6g=0`、`PbcStatus6g=`，對齊 workbook 的 `Not Supported`。targeted D105 tests 維持 `2 passed`、final full repo regression 維持 `1657 passed`，overlay compare 現在是 `283/420` full matches、`137` mismatches、`58` metadata drifts，next ready actionable open case=`D106`。

> P4-04 latest note (2026-04-13, D096): `D096 UAPSDEnable` 已透過 official rerun `20260413T095836613095` 完成 workbook-authoritative 對齊；這案也是低風險的 stale row / results_reference closure：stale case 還停在 workbook row `98`，但 row `98` 其實是 `WDSEnable`，真正的 `UAPSDEnable` authority 是 row `96`，而 row `96` 的 workbook verdict 仍是 `Not Supported / Not Supported / Not Supported`。active 0403 source 雖然仍保留真實 setter path（`wld_accesspoint.odl` 仍把 `UAPSDEnable` 宣告成 default false 的 persistent bool，Broadcom apply hook 也仍把它寫進 `wl wme_apsd`），但這不改變 workbook row 96 的官方 verdict。把 metadata 拉回 workbook row `96`，並把 `results_reference.v4.0.3` 刷回 `Not Supported / Not Supported / Not Supported`，同時保留 tri-band live round-trip evidence `0 -> 1 -> 0`、hostapd `uapsd_advertisement_enabled` 與 driver `wme_apsd` 收斂後，rerun 已一輪 exact-close同一組 live evidence。targeted D096 tests 維持 `3 passed`、full repo regression 維持 `1656 passed`，overlay compare 現在是 `280/420` full matches、`140` mismatches、`58` metadata drifts，next ready actionable open case=`D101`。

> P4-04 latest note (2026-04-13, D104): `D104 Enable / AccessPoint.WPS` 已透過 official rerun `20260413T105418577078` 完成 workbook-authoritative 對齊；這案的真正 drift 不是 firmware 壞掉，而是 stale authored case 把 workbook row `104` 的 setter/readback + hostapd projection case，改寫成依賴前案殘留 WPS baseline 的 fail-shaped semantics，並且還保留 raw `Fail / Not Supported / Fail`。active 0403 source 仍把 `WPS.Enable` 保留為 default `false` 的 persistent bool，`WPS.Configured` 維持 default `true`，button action 也仍 gate 在 `[WPS.Enable==1]`，所以 calibrated case 現在先把每個 band deterministic normalize 到 `Enable=0`，再做 `0 -> 1 -> 0` replay：5G / 2.4G exact-close `wps_state=2`，6G 在 WPA3/SAE baseline 下 exact-close `wps_state=0`。official rerun attempt 1 雖在 `step_6g_setter` 命中 transient `serialwrap cmd status timeout`，但 attempt 2 已 exact-close `Pass / Not Supported / Pass`；targeted D104 tests 維持 `3 passed`、final full repo regression 維持 `1657 passed`，overlay compare 現在是 `282/420` full matches、`138` mismatches、`58` metadata drifts，next ready actionable open case=`D105`。

> P4-04 latest note (2026-04-13, D101): `D101 ConfigMethodsEnabled` 已透過 official rerun `20260413T103130805176` 完成 workbook-authoritative 對齊；這案的真正 root cause 不是 firmware setter fail，而是 authored case 同時帶著 stale workbook row `103`（其實是 `Configured`）與 stale setter semantics，錯把 row `101` 的 getter + hostapd projection case 改寫成 `ConfigMethodsEnabled=PushButton` replay。把 metadata 拉回 workbook row `101`，把 case 還原成 getter + hostapd config_methods 交叉驗證後，current 0403 runtime 會 exact-close workbook note 所描述的形狀：5G / 2.4G 讀到 `PhysicalPushButton,VirtualPushButton` 並在 hostapd 投影 `physical_push_button virtual_push_button`，6G 在 WPA3/SAE baseline 下則回 `CfgEnabled6g=None` 且 `/tmp/wl1_hapd.conf` 沒有 `config_methods` 行，與 workbook 的 `Not Supported` 註記一致。targeted D101 tests 維持 `4 passed`、final full repo regression 為 `1657 passed`，overlay compare 現在是 `281/420` full matches、`139` mismatches、`58` metadata drifts，next ready actionable open case=`D104`。

> P4-04 latest note (2026-04-12, continuation): shared baseline/runtime hardening now skips unresolved placeholder `sta_env_setup` templates and lets the 6G OCV repair accept a restarted wl1 `hostapd` process even when the control socket is briefly absent. Clean-start official rerun `20260412T033924192464` no longer failed in `verify_env`: both attempts reached D324 evaluation, which re-proved a pure 5G counter drift blocker (`329835/329835/305843`, then `562142/562414/537347`). Targeted guardrails are now `11 passed`, full repo regression is `1640 passed`, and the next ready open-set case is `D331`。
> P4-04 latest note (2026-04-12, D331): `D331 MulticastPacketsSent` is now aligned via clean-start official rerun `20260412T040941971904`. Reordering the source-backed formula into a raw-first single snapshot closed `direct / getSSIDStats / driver-formula` on all three bands (`904/904/904`, `732/732/732`, `973/973/973`), the case metadata is refreshed from stale row `255` to workbook row `331`, `results_reference.v4.0.3` is now `Pass/Pass/Pass`, and the next ready open-set case is `D333`。
> P4-04 latest note (2026-04-12, D333): `D333 PacketsSent` is now aligned via clean-start official rerun `20260412T054702963914`. After hardening the shared 6G baseline hot path with driver-assoc fallback, pre-restart `ocv=0` acceptance, and generic `sleep 15`, the anchored `PacketsSent` extractor plus source-backed snapshot formula exact-closed `direct / getSSIDStats / driver-formula` on all three bands (`461/461/461`, `592/592/592`, `707/707/707`); the case metadata is refreshed from stale row `257` to workbook row `333`, `results_reference.v4.0.3` remains `Pass/Pass/Pass`, and the next ready open-set case is `D324`。
> P4-04 latest note (2026-04-12, D324): `D324 BytesSent` is now aligned via official rerun `20260412T060612008433`. Keeping the source-backed `wl if_counters txbyte + matching wds txbyte` authority but moving it into a raw-first single snapshot removed the old `direct < getSSIDStats < driver` staircase and exact-closed all three bands (`1141986/1141986/1141986`, `1113827/1113827/1113827`, `1186105/1186105/1186105`); `plugins/wifi_llapi/reports/D324_block.md` is now historical resolution evidence, and the next ready open-set case is `D295`。

> P4-04 latest note (2026-04-12, D295): `D295 scan()` is now aligned via official rerun `20260412T064317622551`。`20260412T063939977577` 先證明 `first scan BSSID == first driver BSSID` 不是 durable oracle，因此 committed case 改成較穩定的 same-target presence model：prepared `STA + links` topology 下，只要求 `scan()` 第一個 BSSID 必須存在於同 band `wl escanresults`。resolving rerun exact-closed 5G `62:15:db:9e:31:f1`、6G `86:82:fe:58:ac:a6`、2.4G `6a:d7:aa:02:d7:bf`；next ready open-set case=`D281`。
> P4-04 latest note (2026-04-12, D009-D011 recovery): invalid full run `20260412T084218316557` was stopped after early `D007 Fail`、`D009 FailEnv`、`D010 FailEnv`、`D011 FailTest` multi-band instability. A minimal-risk DUT 6G recovery patch now short-circuits missing-adapter `wl1` states before direct hostapd restart; after dual `firstboot -y;sync;sync;sync;reboot -f` resets and COM0 U-Boot recovery, sequential live reruns `D009` / `D010` / `D011` all returned `Pass/Pass/Pass` on the same baseline via run `20260412T110545613993` / `20260412T111048362099` / `20260412T111549474171`. Regression after this patch is `8 passed` on targeted 6G recovery tests and `1645 passed` on the full repo suite; next ready action is to commit/push current HEAD and relaunch full run。
> P4-04 latest note (2026-04-13, D046): `D046 SignalStrengthByChain` 已透過 official rerun `20260413T021655844208` 完成對齊。authoritative trace 原本就已是 `evaluation_verdict=Pass`，rerun 再 exact-close 同一個 5G STA MAC `2C:59:17:00:04:85` 的 `SignalStrengthByChain="-33.0,-32.0,-41.0,-34.0"` 與 driver `DriverSignalStrengthByChain=-33.0,-32.0,-41.0,-34.0`；因此只需把 metadata 從 stale row `48` / raw `Fail / N/A / N/A` 刷新成 workbook row `46` / raw `Pass / Pass / Pass`。overlay compare 現在是 `249 / 420` full matches、`171` mismatches、`58` metadata drifts；next ready source-row drift revisit=`D014`。
> P4-04 latest note (2026-04-13, D061): `D061 UplinkShortGuard` 已透過 official rerun `20260413T022541033440` 完成對齊。authoritative trace 原本就已是 `evaluation_verdict=Pass`，rerun 再 exact-close post-trigger `UplinkShortGuard=1` 與同一 STA MAC `2C:59:17:00:04:85` 的 driver GI token `1.6us` / derived boolean `DriverUplinkShortGuard=1`；因此只需把 metadata 從 stale row `63` / raw `Pass / N/A / N/A` 刷新成 workbook row `61` / raw `Pass / Pass / Pass`。overlay compare 現在是 `250 / 420` full matches、`170` mismatches、`58` metadata drifts；先前推測的 `D014` 並非 workbook-Pass closure，next ready mixed-verdict revisit 改為 `D028`。
> P4-04 latest note (2026-04-13, D028): `D028 MaxBandwidthSupported` 已透過 official rerun `20260413T023259417785` 完成 mixed-verdict 對齊。這案不是 all-pass closure：executed 5G / 2.4G evidence 仍為 `Pass`（AP1 `160MHz`、AP5 `40MHz`），但 workbook row `28` 對 6G 本來就是 fail-shaped，而目前 lab 仍因 `BCME_NOTREADY` 跳過 AP3/wl1；因此只需把 metadata 從 stale row `25` / raw `Pass / Pass / Pass` 刷新成 workbook row `28` / raw `Pass / Fail / Pass`。overlay compare 現在是 `251 / 420` full matches、`169` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D065`。
> P4-04 latest note (2026-04-13, D065): `D065 BridgeInterface` 已透過 official rerun `20260413T024240506323` 完成對齊。authoritative trace 原本就已是 `evaluation_verdict=Pass`，rerun 再 exact-close AP1 / AP3 / AP5 `BridgeInterface="br-lan"`，並與 hostapd `bridge=br-lan` 以及 live Linux bridge master `BridgeMaster5g/6g/24g=br-lan` 對位；因此只需把 metadata 從 stale row `67` / raw `Fail / Fail / Fail` 刷新成 workbook row `65` / raw `Pass / Pass / Pass`。overlay compare 現在是 `252 / 420` full matches、`168` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D081`。
> P4-04 latest note (2026-04-13, D081): `D081 MBOEnable` 已透過 official rerun `20260413T025449283775` 完成 source-backed 對齊。這案不是 metadata-only drift：active 0403 `wifi_ap.c` 明確把 `handle_set_ap_mbo_enable()` 與 `handle_get_ap_mbo_enable()` 都映射到 `wl -i <if> mbo ap_enable`，因此先前以 hostapd `mbo=` absence 建立的 fail-shaped oracle 不是 authoritative path。committed rewrite 現在會先在 setup 強制 `WiFi.AccessPoint.{1,3,5}.MBOEnable=0` 去除前案污染，再 exact-close `ubus-cli ... MBOEnable?` 與 direct `wl mbo ap_enable` 的 `0 -> 1 -> 0` 三段 readback；metadata 也同步由 stale row `75` 刷新為 workbook row `81` / raw `Pass / Pass / Pass`。overlay compare 現在是 `253 / 420` full matches、`167` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D094`。
> P4-04 latest note (2026-04-13, D094): `D094 AccessPoint.Status` 已透過 official rerun `20260413T030202219754` 完成對齊。這案回到低風險 metadata/results_reference closure：authoritative full-run trace 原本就已 exact-close 三 band `Status="Enabled"` 與 direct driver `wl -i wl{0,1,2} bss = up`，isolated rerun 也一次重現同樣的 pass shape；因此只需把 metadata 從 stale row `96` / raw `Fail / Fail / Fail` 刷新成 workbook row `94` / raw `Pass / Pass / Pass`，並順手修正 case 內 `COM0/COM1` transport note 不一致。overlay compare 現在是 `254 / 420` full matches、`166` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D095`。
> P4-04 latest note (2026-04-13, D095): `D095 UAPSDCapability` 已透過 official rerun `20260413T030853360475` 完成對齊。這案同樣回到低風險 metadata/results_reference closure：authoritative full-run trace 原本就已 exact-close 三 band read-only `UAPSDCapability=1`，isolated rerun 也一次重現同樣 shape，並同時看到 `HapdUapsd=0`、`DriverWmeApsd=0`；這兩個值只代表功能目前未啟用，並不否定 capability getter 本身。因此只需把 metadata 從 stale row `97` / raw `Fail / Fail / Fail` 刷新成 workbook row `95` / raw `Pass / Pass / Pass`，並順手修正 case 內 `COM0/COM1` transport note 不一致。overlay compare 現在是 `255 / 420` full matches、`165` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D098`。
> P4-04 latest note (2026-04-13, D098): `D098 WDSEnable` 已透過 official rerun `20260413T031458311484` 完成對齊。這案同樣維持在低風險 metadata/results_reference closure：authoritative full-run trace 原本就已 exact-close 三 band setter round-trip `baseline=0 -> set=1 -> restore=0`，並與 direct driver `dwds 0 -> 1 -> 0` 對位；isolated rerun 也一次重現同樣的 pass shape。因此只需把 metadata 從 stale row `100` / raw `Fail / Fail / Fail` 刷新成 workbook row `98` / raw `Pass / Pass / Pass`，並順手修正 case 內 `COM0/COM1` transport note 不一致。overlay compare 現在是 `256 / 420` full matches、`164` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D099`。
> P4-04 latest note (2026-04-13, D050): `D050 SupportedVhtMCS` 已透過 official rerun `20260413T000249620932` 完成 workbook-authoritative closure。這案不是 `D047` 式的 authority conflict：row `50` 的 workbook `R/S/T`、`G/H` 與 note `V50` 本來就一致地把 5G 視為 sibling `RxSupportedVhtMCS` / `TxSupportedVhtMCS` pass path，而 6G / 2.4G 保留 `Not Supported`。Current rerun 也 exact-close 同一個 STA `2C:59:17:00:04:85` 的 5G pass shape：standalone getter `SupportedVhtMCS?` 回 `error=4 / parameter not found`，但 sibling `DriverRxSupportedVhtMCS=9,9,9,9`、`DriverTxSupportedVhtMCS=9,9,9,9`，且 `wl0 sta_info` 仍有 `VHT caps` / `MCS SET` / `VHT SET`。因此 landed case 現在投影 workbook-consistent `Pass / Not Supported / Not Supported`；targeted D050 guardrails=`5 passed`；full repo regression 維持 `1660 passed`；overlay compare 已提升到 `296 / 420` full matches、`124` mismatches、`58` metadata drifts。`D047` 仍保留在 workbook/source authority blocker bucket，next ready actionable compare-open case=`D053 TxBytes`。

> P4-04 latest note (2026-04-13, D047 blocker): `D047 SupportedHe160MCS` 目前確認為 workbook/source authority conflict，不能直接當成已關閉 row。`compare-0401` 讀的是 answer columns `R/S/T`，所以 row `47` 目前確實是 `Pass / Pass / Not Supported`；但同一 row 的 legacy `I/J/K` 與 note `V47` 又明寫 pWHM 沒有 standalone `SupportedHe160MCS`，只暴露 `RxSupportedHe160MCS` / `TxSupportedHe160MCS`。Current 0403 installed ODL 與 official rerun `20260412T235952361188` 也都站在 source/runtime 這邊：`WiFi.AccessPoint.{i}.AssociatedDevice.{i}.SupportedHe160MCS?` 回 `error=4 / parameter not found`，但 sibling Rx/Tx values 與 live HE capability lines 都存在。因此 current YAML 不可硬改成 workbook-pass semantics；blocker handoff 已落在 `plugins/wifi_llapi/reports/D047_block.md`，next ready actionable compare-open case 改為 `D050 SupportedVhtMCS`。

> P4-04 latest note (2026-04-13, D042): `D042 RxUnicastPacketCount` 已透過 official rerun `20260413T145000666925` 完成 workbook-authoritative non-pass closure；stale row `44` 已刷新回 workbook row `42`。這案不是把 live counter divergence 硬改成 pass，而是把 current 0403 supported-band evidence 與 workbook `Not Supported / Not Supported / Not Supported` 對齊：rerun exact-close 同一個 STA `2C:59:17:00:04:85` 的 DUT getter `RxUnicastPacketCount=0` 與 driver `DriverRxUnicastPacketCount=8`，STA 端則仍穩定連在 `TestPilot_BTM`。targeted counter-stub guardrails=`2 passed`；full repo regression 維持 `1660 passed`；overlay compare 已提升到 `295 / 420` full matches、`125` mismatches、`58` metadata drifts。`D020` 仍保留在已驗證 fail-shaped mismatch bucket，next ready actionable compare-open case=`D047 SupportedHe160MCS`。

> P4-04 latest note (2026-04-13, D035): `D035 OperatingStandard` 已透過 official rerun `20260413T144105373183` 完成 workbook-authoritative pass closure；stale row `37`（`EncryptionMode`）已刷新回 workbook row `35`。這案不是新的 runtime workaround，而是把舊的 row-37 fail-shaped metadata 拉回 workbook row 35 的 pass authority：current 0403 source 仍保留 read-only 的 AccessPoint AssociatedDevice `OperatingStandard` getter，而 rerun 也直接 exact-close `assoclist 2C:59:17:00:04:85` 與 `OperatingStandard="ax"`。targeted assocdev getter guardrails=`40 passed`；full repo regression 已提升到 `1660 passed`；overlay compare 已提升到 `294 / 420` full matches、`126` mismatches、`58` metadata drifts。`D020` 仍保留在已驗證 fail-shaped mismatch bucket，next ready actionable compare-open case=`D042 RxUnicastPacketCount`。

> P4-04 latest note (2026-04-13, D033): `D033 MUUserPositionId` 已透過 official rerun `20260413T142616419984` 完成 workbook-authoritative non-pass closure；stale row `35`（`OperatingStandard`）已刷新回 workbook row `33`。這案和 D032/D030 同樣不是把 stub evidence 寫成 pass，而是把 current 0403 live shape 與 workbook `Not Supported / Not Supported / Not Supported` 對齊：source survey 只找到 read-only ODL declaration、沒有 active tr181-wifi implementation，而 rerun 仍 exact-close 5G `AssocMac5g=2c:59:17:00:04:85` + `MUUserPositionId=0`、2.4G `AssocMac24g=2c:59:17:00:04:97` + `MUUserPositionId=0`，6G 則維持 current lab skip。targeted MU-stub guardrails=`2 passed`；overlay compare 已提升到 `293 / 420` full matches、`127` mismatches、`58` metadata drifts。`D020` 仍保留在已驗證 fail-shaped mismatch bucket，next ready actionable compare-open case=`D035 OperatingStandard`。

> P4-04 latest note (2026-04-13, D032): `D032 MUMimoTxPktsPercentage` 已透過 official rerun `20260413T141305083695` 完成 workbook-authoritative non-pass closure；stale row `34`（`MUUserPositionId`）已刷新回 workbook row `32`。這案和 D030 同樣不是把 stub evidence 寫成 pass，而是把 current 0403 live shape 與 workbook `Not Supported / Not Supported / Not Supported` 對齊：source survey 只找到 read-only ODL declaration、沒有 active tr181-wifi implementation，而 rerun 仍 exact-close 5G `AssocMac5g=2c:59:17:00:04:85` + `MUMimoTxPktsPercentage=0`、2.4G `AssocMac24g=2c:59:17:00:04:97` + `MUMimoTxPktsPercentage=0`，6G 則維持 current lab skip。targeted MU-stub guardrails=`2 passed`；overlay compare 已提升到 `292 / 420` full matches、`128` mismatches、`58` metadata drifts。`D020` 仍保留在已驗證 fail-shaped mismatch bucket，next ready actionable compare-open case=`D033 MUUserPositionId`。

> P4-04 latest note (2026-04-13, D030): `D030 MUGroupId` 已透過 official rerun `20260413T135928729951` 完成 workbook-authoritative non-pass closure；stale row `27`（`Capabilities`）已刷新回 workbook row `30`。這案不是把 stub evidence 寫成 pass，而是把 current 0403 live shape 與 workbook `Not Supported / Not Supported / Not Supported` 對齊：source survey 只找到 read-only ODL declaration、沒有 active tr181-wifi implementation，而 rerun 仍 exact-close 5G `AssocMac5g=2c:59:17:00:04:85` + `MUGroupId=0`、2.4G `AssocMac24g=2c:59:17:00:04:97` + `MUGroupId=0`，6G 則維持 current lab skip。targeted D029/D030 not-supported guardrails=`2 passed`；overlay compare 已提升到 `291 / 420` full matches、`129` mismatches、`58` metadata drifts。`D020` 仍保留在已驗證 fail-shaped mismatch bucket，next ready actionable compare-open case=`D032 MUMimoTxPktsPercentage`。
 
> P4-04 latest note (2026-04-13, D019): `D019 EncryptionMode / AssociatedDevice` 已透過 official rerun `20260413T133308180539` 完成對齊。這案不是新的 pass semantics，而是把 stale row-16 / stale `Pass / Pass / Pass` 敘事拉回 workbook row `19` 的 fail-shaped authority：STA rerun exact-close `SSID=TestPilot_BTM`、`pairwise_cipher=CCMP`、`key_mgmt=SAE`，DUT rerun exact-close `AssocMAC=2C:59:17:00:04:86` 與 getter `EncryptionMode="Default"`；因此 landed case 現在投影 `Fail / Fail / Fail`，並已移除舊的 `wifi-llapi-r016-*` alias metadata。targeted D019 contract guardrail 為 `1 passed`、full repo regression 維持 `1659 passed`；overlay compare 已提升到 `290 / 420` full matches、`130` mismatches、`58` metadata drifts。`D020` 仍保留在已驗證 fail-shaped mismatch bucket，next ready actionable compare-open case=`D030 MuGroupID`。
> P4-04 latest note (2026-04-13, D099): `D099 WMMCapability` 已透過 official rerun `20260413T032521733067` 完成對齊。這案也回到低風險 metadata/results_reference closure，而且已有 source-backed 補強：active 0403 仍把 `wifi_getApWMMCapability()` 導到 `wldm_AccessPoint_WMMCapability()`，該 getter 會 probe driver iovar `wme`，且在成功讀取時直接回 `TRUE`，看不出任何 6G 特例。authoritative full-run trace 原本就已 exact-close 三 band `WMMCapability=1` 與 `hostapd wmm_enabled=1`，isolated rerun 也一次重現同樣的 pass shape；因此只需把 metadata 從 stale row `101` / raw `Pass / Fail / Pass` 刷新成 workbook row `99` / raw `Pass / Pass / Pass`，並順手修正 case 內 `COM0/COM1` transport note 不一致。overlay compare 現在是 `257 / 420` full matches、`163` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D114`。
> P4-04 latest note (2026-04-13, D114): `D114 getStationStats() AvgSignalStrengthByChain` 已透過 official rerun `20260413T033856175894` 完成對齊。這案不是純 row/raw refresh，而是 source-backed stale-scope closure：active 0403 `whm_brcm_ap_mlo_fillAssocDevInfo()` 仍把 `AvgSignalStrengthByChain` 寫回 `pAD->AvgSignalStrengthByChain`，ODL 也仍把它宣告為 volatile read-only int32，沒有 band-specific branch。舊 YAML 卻停在 5G-only `Fail / N/A / N/A` artifact，所以這次把 topology/steps 補回 workbook 對應的 tri-band sequential `getStationStats()` 驗證。official rerun 在 verify_env 吸收了一次 6G OCV/hostapd recovery 噪音後，仍一次完成 tri-band pass，實際 exact-close `-33` (5G) / `-85` (6G) / `-23` (2.4G)。overlay compare 現在是 `258 / 420` full matches、`162` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D115`。
> P4-04 latest note (2026-04-13, D115): `D115 getStationStats() ConnectionDuration` 已透過 official rerun `20260413T035856845825` 完成對齊。這案同樣不是純 row/raw refresh，而是 source-backed stale-scope rewrite：ODL 仍把 `ConnectionDuration` 宣告為 volatile read-only uint32，`wlgetStationInfo()` 仍從 driver `wl sta_info ... in network` 解析 `connectTime`，`local_wl_util.c` 也仍把 `staInfo.connectTime` 寫進較高層 station 結構，沒有任何 band-specific branch。舊 YAML 卻停在 5G-only `Fail / N/A / N/A` artifact，因此這次把 case 擴成 tri-band sequential `getStationStats()` live-counter 驗證，並加上每 band 第二次讀值與 driver age cross-check。official rerun 在 verify_env 再次吸收了一輪 6G OCV / wl0 supplicant recovery 噪音後，仍一次完成 tri-band pass，實際 exact-close `7 -> 10 <= 12` (5G) / `11 -> 14 <= 16` (6G) / `9 -> 13 <= 15` (2.4G)。overlay compare 現在是 `259 / 420` full matches、`161` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D174`。
> P4-04 latest note (2026-04-13, D174): `D174 ActiveAntennaCtrl` 已透過 official rerun `20260413T042647797154` 完成對齊。這案回到低風險 metadata/results_reference closure，但補上了 workbook `-1` pass 形狀的 source-backed 解釋：active 0403 `wld_radio.odl` 仍把 `ActiveAntennaCtrl` 宣告為 persistent int32 並預設 `-1`，`wld.h` 仍保留 `actAntennaCtrl` / `txChainCtrl` / `rxChainCtrl` 欄位，而 vendor path `whm_brcm_rad_antenna_map()` / `whm_brcm_rad_mod_chains()` 仍在 specific chain controls 未設時用 `actAntennaCtrl` 作 fallback。也就是說 northbound getter 維持 `-1` 代表 driver-default sentinel，並不與 workbook `wl txchain` / `wl rxchain` 實際 mask 相衝突。authoritative full-run trace 原本就已 exact-close 三 band `ActiveAntennaCtrl=-1`，isolated rerun 也一次重現同樣 pass shape；因此只需把 metadata 從 stale row `138` / raw `Fail / Fail / Fail` 刷新成 workbook row `174` / raw `Pass / Pass / Pass`。overlay compare 現在是 `260 / 420` full matches、`160` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D176`。
> P4-04 latest note (2026-04-13, D028): `D028 MaxBandwidthSupported` 已透過 official rerun `20260413T023259417785` 完成 mixed-verdict 對齊。這案不是 all-pass closure：executed 5G / 2.4G evidence 仍為 `Pass`（AP1 `160MHz`、AP5 `40MHz`），但 workbook row `28` 對 6G 本來就是 fail-shaped，而目前 lab 仍因 `BCME_NOTREADY` 跳過 AP3/wl1；因此只需把 metadata 從 stale row `25` / raw `Pass / Pass / Pass` 刷新成 workbook row `28` / raw `Pass / Fail / Pass`。overlay compare 現在是 `251 / 420` full matches、`169` mismatches、`58` metadata drifts；next ready workbook-Pass revisit=`D065`。

> P4-04 latest note (2026-04-12, D281): `D281 getScanResults() Noise` 已透過 official rerun `20260412T080123446178` 正式對齊。真正可用的 same-scan public oracle 是 named-arg `scanCombinedData(channels=36/5/1,minRssi=-127,scanReason=Ssid)` 加上 same-target `getScanResults(minRssi=-127)`；rerun exact-close 5G `2c:59:17:00:03:e5 / -100 / -100 / -100`、6G `6e:15:db:9e:33:72 / -97 / -97 / -97`、2.4G `6a:d7:aa:02:d7:bf / -76 / -76 / -76`。committed YAML row 已由 stale `283` 刷新為 workbook row `281`；`plugins/wifi_llapi/reports/D281_block.md` 轉為 historical resolution notes；next ready open-set case=`D282`。

> P4-04 latest note (2026-04-12, relaunch + D024): guarded 6G recovery commit `338891b7115e5c41d04f45bd79c70ce4b117cebc` 已 push，authoritative full run `20260412T113008433351` 也已完整跑完 `420` cases，且舊的早期 baseline collapse 沒再重現（`D004`~`D013` 都維持 plain `Pass`）。`compare-0401` 先提升到 `235/420` full matches、`185` mismatches、`62` metadata drifts，actionable workbook-Pass gap 為 `156`；其後 `D024 LastDataDownlinkRate` 已透過 official rerun `20260412T172957084134` 正式對齊，`source.row` 由 stale `21` 刷新為 workbook row `24`，stale alias metadata 已移除，live evidence 也 exact-close `LastDataDownlinkRate=487400` 與 driver last-tx bucket window `[487400,487500]`。這個 D024 rewrite 的 targeted guardrails 為 `6 passed`、full repo regression 仍是 `1645 passed`；疊上 D024 rerun 後的 `compare-0401` 現在是 `236/420` full matches、`184` mismatches、`62` metadata drifts、actionable workbook-Pass gap `155`。`0315~0403` patch-scope true-open set 維持已清零；下一個 ready case 是 `D025`。

> P4-04 latest note (2026-04-12, D025): `D025 LastDataUplinkRate` 已透過 official rerun `20260412T174551843336` 正式對齊。stale alias metadata 已移除，`source.row` 由 stale `22` 刷新為 workbook row `25`，live evidence 也 exact-close `LastDataUplinkRate=6000` 與 `DriverLastUplinkRateRounded=6000`；workbook `H`、public HAL comments 與 repeated runtime replays 仍一致支持 `rate of last rx pkt` 作為 STA -> AP truth source。D025 targeted guardrails 為 `3 passed`；疊上 D025 rerun 後，`compare-0401` 已更新為 `237/420` full matches、`183` mismatches、`62` metadata drifts、actionable workbook-Pass gap `154`。`D020` 仍維持已驗證的 fail-shaped mismatch，因此下一個 ready case 轉為 `D022`。

> P4-04 latest note (2026-04-12, D022): `D022 HtCapabilities` 已透過 official rerun `20260412T175538121906` 正式對齊。stale alias metadata 已移除，`source.row` 由 stale `19` 刷新為 workbook row `22`；這次真正修掉的是 source-backed driver oracle：不再 brittle 地抓 human-readable token，而是直接解析 `wl sta_info` 的 `HT caps 0x...` bitmask，並 exact-close `HtCapabilities="40MHz,SGI20,SGI40"` 與 `DriverHt40MHz=1` / `DriverHtSgi20=1` / `DriverHtSgi40=1`。D022 targeted guardrails 為 `1 passed`；疊上 D022 rerun 後，`compare-0401` 已更新為 `238/420` full matches、`182` mismatches、`62` metadata drifts、actionable workbook-Pass gap `153`。`D020` 繼續保留為已驗證 fail-shaped mismatch；下一個 ready case 轉為 `D034`。

> P4-04 latest note (2026-04-13, D047-D050): `D047 SupportedHe160MCS` 與 `D050 SupportedVhtMCS` 已透過 official rerun `20260412T235952361188` / `20260413T000249620932` 完成 shared `step_command_failed` closure。兩案共用的 root cause 有兩層：一是 case YAML 漂到未經 authority full run 驗證的 custom `TestPilot_BTM` / `WPA3-Personal` bring-up，現已回到 generic `testpilot5G` / `WPA2-Personal` baseline；二是 `_env_command_succeeded()` 之前用過度寬鬆的 `"not found"` 黑名單，把合法的 getter payload `error=4 / message=parameter not found` 也提早打成 shell failure。`source.row` 現已刷新為 workbook row `47` / `50`，targeted runtime + command-budget guardrails 已回綠；由於 workbook 這兩列本來就是 non-pass (`Not Support`) semantics，overlay compare 維持 `239/420` full matches、`181` mismatches、`62` metadata drifts、actionable workbook-Pass gap `152`。下一個 ready `step_command_failed` case 轉為 `D079`。

> P4-04 latest note (2026-04-13, D079-runtime-fix): `D079 MACFiltering.Mode` 的 runtime truncation bug 已關閉：`CommandResolver.sanitize_cli_fragment()` 不再把 `OUT=$(ubus-cli WiFi.AccessPoint.1.MACFiltering.Mode=Off ...)` 這類 complex shell setter 剪成 `OUT=$(ubus-cli`。official rerun `20260413T002418591720` 兩次都完整執行 AP1/AP3/AP5 的 getter / setter / getter 序列，且 live evidence 一致收斂到 `Mode=Off`、hostapd 無 ACL 行、`Mode=Off` setter 回 `invalid value`。因此 `D079` 已從 `step_command_failed` bucket 移出，改列 semantic `pass_criteria_not_satisfied` / workbook-authority review；下一個 ready `step_command_failed` case 轉為 `D088`。

> P4-04 latest note (2026-04-13, D088): `D088 ModesSupported` 已透過 official rerun `20260413T003340845889` 完成 closure。shared setter-capture fix 先讓真正的 read-only setter 跑到 DUT，接著把 step2/4/6 補成可解析 `error=15` / `message=is read only` 的輸出形狀後，case 就能 exact-close workbook-pass semantics：5G/2.4G 維持 full mode list、6G 維持 `None,WPA3-Personal,OWE`，三個 setter 都回 `is read only`。`source.row` 現已刷新為 workbook row `88`；overlay compare 更新為 `240/420` full matches、`180` mismatches、`62` metadata drifts、actionable workbook-Pass gap `151`。下一個 ready `step_command_failed` case 轉為 `D460`。

> P4-04 latest note (2026-04-13, D460-D494): `D460 HePhyCapabilities` 與 `D494 VHTCapabilities` 已透過 official rerun `20260413T005520941756` / `20260413T005633950804` 完成 closure。`D460` 修的是 stale workbook mapping：case 原本還在用錯 API / row（`HECapabilities`, `462`），但 workbook row `460` 與 live 0403 `wld_radio.odl` 都已經是 `HePhyCapabilities`；修正後三 band 直接 exact-close base64 getter。`D494` 則改成 workbook-authoritative mixed-support 形狀：5G 走 protected getter 並顯式輸出 `VHTCapabilities=dliDDw==`，6G / 2.4G 走 plain getter 並解析 `error=4` / `message=parameter not found`；metadata 也刷新到 workbook row `494`，`results_reference.v4.0.3` 現在是 `Pass / Not Supported / Not Supported`。overlay compare 已更新為 `242/420` full matches、`178` mismatches、`61` metadata drifts；focused `step_command_failed` queue 現在清空，後續改進面轉到 calibration / mapping / results_reference bucket，另保留 `D079` semantic、env-only `D328/D336`、blocked `D053`。

> P4-04 latest note (2026-04-13, D461): `D461 HTCapabilities` 已透過 official rerun `20260413T010944709855` 完成 closure。這一案不是 runtime / command 問題，而是純 stale metadata：full-run trace 原本就已經 `evaluation_verdict=Pass`，但 case 還停在 `source.row=338` 與 `results_reference.v4.0.3 = Fail / Fail / Fail`。刷新到 workbook row `461`、raw `Pass / Pass / Pass` 後，overlay compare 已再前進到 `243/420` full matches、`177` mismatches、`61` metadata drifts；next ready phase-2 mapping/results_reference revisit 轉為 `D462`。

> P4-04 latest note (2026-04-13, D462): `D462 BssColor` 已透過 official rerun `20260413T011655056430` 完成 closure。這一案和 `D461` 同屬 trace 已 pass、但 compare 仍 fail-shaped 的 stale metadata family，不過還多一個 source-object drift：case 原本仍停在 `source.row=339`、`source.object=WiFi.Radio.{i}.`、`results_reference.v4.0.3 = Fail / Fail / Fail`。刷新到 workbook row `462`、object `WiFi.Radio.{i}.IEEE80211ax.`、raw `Pass / Pass / Pass` 後，overlay compare 已再前進到 `244/420` full matches、`176` mismatches、`60` metadata drifts；next ready phase-2 mapping/results_reference revisit 轉為 `D463`。

> P4-04 latest note (2026-04-13, D463): `D463 HESIGASpatialReuseValue15Allowed` 已透過 official rerun `20260413T012358700786` 完成 closure。這一案與 `D462` 屬於同一個 stale metadata / source-object drift family：case 原本仍停在 `source.row=340`、`source.object=WiFi.Radio.{i}.`、`results_reference.v4.0.3 = Fail / Fail / Fail`，但 authoritative full-run trace 早已是 `evaluation_verdict=Pass`。刷新到 workbook row `463`、object `WiFi.Radio.{i}.IEEE80211ax.`、raw `Pass / Pass / Pass` 後，overlay compare 已再前進到 `245/420` full matches、`175` mismatches、`59` metadata drifts；next ready phase-2 mapping/results_reference revisit 轉為 `D465`。

> P4-04 latest note (2026-04-13, D465): `D465 SRGInformationValid` 已透過 official rerun `20260413T013010016650` 完成 closure。這一案大致與 `D462/D463` 同屬 stale metadata / source-object drift family，但還多了一個 stale live note：case 原本仍寫 `Radio.2 (6g) ... (live=1)`，而 authoritative full-run trace 與 rerun 都已明確是三 band `0/0/0`。刷新到 workbook row `465`、object `WiFi.Radio.{i}.IEEE80211ax.`、raw `Pass / Pass / Pass`，並把 6G live note 改回 `0` 後，overlay compare 已再前進到 `246/420` full matches、`174` mismatches、`58` metadata drifts；next ready phase-2 mapping/results_reference revisit 轉為 `D467`。

> P4-04 latest note (2026-04-13, D467): `D467 RxBeamformingCapsEnabled` 已透過 official rerun `20260413T013545364055` 完成 closure。這一案比 `D462/D463/D465` 更單純：authoritative full-run trace 原本就已經 `evaluation_verdict=Pass`，且 live shape 穩定為 `DEFAULT/DEFAULT/DEFAULT`；case 只剩 stale `source.row=343` 與 `results_reference.v4.0.3 = Fail / Fail / Fail`。刷新到 workbook row `467`、raw `Pass / Pass / Pass` 後，overlay compare 已再前進到 `247/420` full matches、`173` mismatches、`58` metadata drifts；next ready workbook-Pass revisit 轉為 `D035`。

> P4-04 latest note (2026-04-13, D035 blocker): `D035 AssociatedDevice OperatingStandard` 沒有被強行落地。local tri-band rewrite 雖然把 case 形狀改成 workbook row `35` 的 tri-band sequential assocdev flow，但 official rerun `20260413T014428270219` 兩次都仍在 `step1_5g_sta_join` 失敗，trace 明確記錄 `iw dev wl0 link -> Not connected.`；同一份 STA verify-env log 卻又已經證明 `SSID: testpilot5G`、`wpa_state=COMPLETED`。後續 reconnect trial rerun `20260413T015210910141` 進一步證明問題已往 shared runtime 漂移：5G immediate join fail 消失，但 run 轉而陷入重複的 6G `ocv=0` / `ATTACH` recovery loop。這個 tri-band rewrite 已完整回退、compare 維持 `247/420` full matches、`173` mismatches、`58` metadata drifts，不把未收斂語義混進 repo；`D035` 現在以 blocker 形式記錄在 `plugins/wifi_llapi/reports/D035_block.md`，next ready workbook-Pass revisit 改為 `D045`。

> P4-04 latest note (2026-04-13, D045): `D045 SignalStrength` 已透過 official rerun `20260413T020657288045` 完成 closure。這一案不需要改 testcase semantics：authoritative trace 原本就已經 `evaluation_verdict=Pass`，rerun 也 exact-close 同一個 5G AssociatedDevice MAC 的 `SignalStrength=-33` 與 driver sample `DriverSignalStrength=-33`。真正剩下的只有 stale `source.row=47` 與 stale raw `2.4g=Fail`；刷新到 workbook row `45`、raw `Pass / Pass / Pass` 後，overlay compare 已前進到 `248/420` full matches、`172` mismatches、`58` metadata drifts；next ready low-risk workbook-Pass revisit 轉為 `D046`。

> P4-04 latest note (2026-04-12, D282): `D282 getScanResults() OperatingStandards` 已透過 official rerun `20260412T080338867826` 正式對齊。Durable same-source oracle 不是 `NeighboringWiFiDiagnostic()`，而是同一條 named-arg `scanCombinedData()` family：`scanCombinedData().BSS` 與 immediate same-target `getScanResults(minRssi=-127)` exact-close 5G `2c:59:17:00:03:e5 / a,n,ac,ax,be`、6G `6e:15:db:9e:33:72 / ax,be`、2.4G `6a:d7:aa:02:d7:bf / b,g,n,ax`。committed YAML row 已由 stale `284` 刷新為 workbook row `282`；`plugins/wifi_llapi/reports/D282_block.md` 轉為 historical resolution notes；目前 true-open open-set=`none`。

> P4-04 latest note (2026-04-10): `D284 getScanResults() SecurityModeEnabled` 已收斂成 blocker。Source 仍將 neighboring `SecurityModeEnabled` 映射到 `AKM Suites` / `RSN`，isolated rerun `20260410T170750425931` 已證明 5G（`38:88:71:2f:f6:a7` -> `WPA2-Personal`）與 2.4G（`8c:19:b5:6e:85:e1` -> `WPA2-WPA3-Personal`）可 same-target 對齊，但 6G 仍會在 `3a:06:e6:2b:a3:1a`（`.ROAMTEST_RSNO_P10P_1`）與 `2C:59:17:00:19:96`（`OpenWrt_1`）等多個 `WPA3-Personal` target 間漂移；第二次 isolated rerun `20260410T171358112868` 改用 associated-BSSID selector 也未能穩定產生 `LlapiBssid6g`。因此 D284 trial rewrite 已拒絕落地、committed YAML 維持原 generic shape 與 stale row `286`；回退後完整 plugin runtime regression 仍為 `1223 passed`；next ready case=`D285`。

> P4-04 latest note (2026-04-12, continuation): `D285 getScanResults() SignalNoiseRatio` 已完成 official-runner 對齊。舊 blocker 是 raw driver `SNR` replay，而不是 public row 本身：0403 neighboring scan internals 仍以 `SignalStrength + Noise` 為核心，但 public scan-result object 對外暴露 `SignalNoiseRatio`。將 case 改成 D283-style first-object capture 後，official reruns `20260412T020817105728` / `20260412T020839239161` 都 exact-close 同一個 public invariant（5G `38:88:71:2f:f6:a7 / -66 / -100 / 34`、6G `6e:15:db:9e:33:72 / -95 / -97 / 2`、2.4G `2c:59:17:00:03:f7 / -47 / -80 / 33`），即 `SignalNoiseRatio == RSSI - Noise`；committed YAML row 已由 stale `287` 刷新為 workbook row `285`，`plugins/wifi_llapi/reports/D285_block.md` 轉為 historical resolution notes；next ready case=`D286`。

> P4-04 latest note (2026-04-12, continuation): `D286 getScanResults() SignalStrength` 已完成 official-runner 對齊。舊 blocker 是 external replay model，而不是 public row 本身：active 0403 source 已確認 `SignalStrength` 與 `RSSI` 同屬 public `ssid->rssi` family，因此 committed case 可沿用 D283-style first-object capture，而不必再追 `iw` / raw-driver drift。official reruns `20260412T021725610895` / `20260412T021748934770` 都 exact-close 同一個 public invariant（5G `38:88:71:2f:f6:a7 / -66 / -66`、6G `6e:15:db:9e:33:72 / -95 / -95`、2.4G `2c:59:17:00:03:f7 / -47 / -47`），即 `SignalStrength == RSSI`；committed YAML row 已由 stale `288` 刷新為 workbook row `286`，`plugins/wifi_llapi/reports/D286_block.md` 轉為 historical resolution notes；next ready case=`D287`。

> P4-04 latest note (2026-04-12, continuation): `D287 getScanResults() SSID` 已完成 official-runner 對齊。舊 blocker 的真正問題是 stale 6G selector，而不是 public `SSID` row 本身：active 0403 仍從 parsed scan-result object 序列化 public `SSID`，改成 D283-style first-object capture 後，case 已不再追舊的 `3a:06:e6:2b:a3:1a`，而是穩定鎖到 first serialized 6G target `6e:15:db:9e:33:72`。official reruns `20260412T022708923585` / `20260412T022738004752` 都 exact-close 同一個 same-target replay（5G `38:88:71:2f:f6:a7 / Verizon_Z4RY7R / Verizon_Z4RY7R`、6G `6e:15:db:9e:33:72 / **TELUS0227 / **TELUS0227`、2.4G `2c:59:17:00:03:f7 / OpenWrt_1 / OpenWrt_1`）；committed YAML row 已由 stale `289` 刷新為 workbook row `287`，`plugins/wifi_llapi/reports/D287_block.md` 轉為 historical resolution notes；next ready case=`D281`。

> P4-04 latest note (2026-04-12, continuation): `D281 getScanResults() Noise` 仍維持 blocker，且最新官方證據已把問題縮到單一 2.4G spectrum-cache drift。split-step same-channel public-spectrum rewrite 已經把舊的 6G staged temp-script failure 清掉，但三次 official rerun `20260412T023953395097` / `20260412T024026359700` / `20260412T024310730084` 仍收斂成 `Fail -> pass after retry -> Fail`：5G/6G 全程 exact-close，只有 2.4G 在 `Noise=-80` 與 `SpectrumNoise=-78/-82/-80` 之間擺動。故 local rewrite 再次 rollback，committed YAML 維持 stale row `283` 不變，詳見 `plugins/wifi_llapi/reports/D281_block.md`；next ready case=`D282`。

> P4-04 latest note (2026-04-12, continuation): `D282 getScanResults() OperatingStandards` 仍維持 blocker，但這次已找到 active pWHM ubus path 的 same-source sibling 候選：`WiFi.NeighboringWiFiDiagnostic()` 會從同一個 `wld_scanResultSSID_t` 同時序列化 `OperatingStandards` 與 `SupportedStandards`。live probe 已證明 2.4G same-target `04:70:56:D2:22:4F` 可 exact-close `OperatingStandards=b,g,n,ax`，但同一個 diagnostic 目前仍回 `FailedRadios="wl0,wl1"`、`ReportingRadios="wl2"`，所以 5G/6G 尚無 durable same-target sibling replay。故 committed YAML 維持 stale row `284` 不變，詳見 `plugins/wifi_llapi/reports/D282_block.md`；next ready case=`D324`。

> P4-04 latest note (2026-04-11, continuation): `D281 getScanResults() Noise` 仍維持 blocker，但 authority 模型已修正。parser-fix trial `20260411T211133728869` 先證明舊 rewrite 仍有 local same-target capture bug；sanitized rerun `20260411T211327344984` 才打出真正 live shape：5G `-100/-100` exact-close、6G 穩定漂成 `-97/-102` 再到 `-97/-103`、2.4G 仍在 `-78/-76` / `-78/-78` 間擺動。最新 0403 source trace 已確認 public `getScanResults().Noise` 不是單純沿用 raw `wl escanresults` `noise:`，而是經 `wifiGen_rad_getScanResults()` / `s_updateScanResultsWithSpectrumInfo()` 以 per-channel spectrum `noiselevel` 回填；但 public `getSpectrumInfo()` 在現場仍回空 array，尚無 durable replayable oracle。故 committed YAML 仍保持 stale row `283` 不變，詳見 `plugins/wifi_llapi/reports/D281_block.md`；next ready case=`D282`。

> P4-04 latest note (2026-04-11, continuation): `D282 getScanResults() OperatingStandards` 也仍維持 blocker，但 authority 模型已修正。最新 0403 source trace 已確認目前 public ubus getter 不是沿用 legacy `_wldm_get_standards()`，而是走 nl80211 scan-result path：`swl_80211_parseInfoElementsBuffer(...)` 先把 beacon/probe IEs 解析進 `pWirelessDevIE->operatingStandards` bitmask，再複製到 `wld_scanResultSSID_t.operatingStandards`、留在 `lastScanResults`，最後用 `swl_radStd_toChar(..., SWL_RADSTD_FORMAT_STANDARD, 0)` 序列化成 public `OperatingStandards`。因此 cumulative LLAPI 形狀（如 `a,n,ac,ax`、`ax,be`）本身符合 active path；真正卡住的是 external replay 仍不是 same-source oracle：既有 isolated rerun `20260410T163026194231` 雖讓 5G exact-close，6G 仍沒有 same-target `WlOperatingStandards6g`，2.4G 仍漂成 `b,g,n,ax` vs `b,g,n,ax,be`。故 committed YAML 仍保持 stale row `284` 不變，詳見 `plugins/wifi_llapi/reports/D282_block.md`；next ready case=`D283`。

> P4-04 latest note (2026-04-12, continuation): `D283 getScanResults() RSSI` 已完成 live 對齊。舊 blocker 的真正問題是 committed full-payload `getScanResults()` transport shape：isolated rerun `20260411T214050136894` 仍會在 `setup_env` 後 hang；但把 case 改成 D277-style first-object capture 之後，official reruns `20260412T013944779069` 與 `20260412T014018880783` 都穩定 exact-close 同一個 public shape：5G `38:88:71:2f:f6:a7 / -66 / -66`、6G `6e:15:db:9e:33:72 / -95 / -95`、2.4G `2c:59:17:00:03:f7 / -47 / -47`。active 0403 source trace 也確認 public `RSSI` 與 public `SignalStrength` 都是從同一個 `ssid->rssi` 序列化，因此 committed oracle 現固定為「parseable first-object BSSID + numeric RSSI + `SignalStrength == RSSI`」；metadata 已由 stale row `285` 刷新為 workbook row `283`，`plugins/wifi_llapi/reports/D283_block.md` 轉為 historical resolution notes；next ready case=`D284`。

> P4-04 latest note (2026-04-12, continuation): `D284 getScanResults() SecurityModeEnabled` 已完成 live 對齊。舊 blocker 的真正問題是 6G selector 不穩：first-WPA-target 與 associated-BSSID 兩種 trial 都會在 `.ROAMTEST_RSNO_P10P_1`、`OpenWrt_1` 與缺失 placeholder 之間漂移；改成 D283-style first-object capture 後，official reruns `20260412T015141491861` 與 `20260412T015235280960` 都穩定 exact-close 同一個 same-target security replay：5G `38:88:71:2f:f6:a7 / WPA2-Personal / WPA2-Personal`、6G `6e:15:db:9e:33:72 / WPA3-Personal / WPA3-Personal`、2.4G `2c:59:17:00:03:f7 / WPA2-Personal / WPA2-Personal`。因此 committed oracle 現固定為「parseable first-object BSSID + non-empty LLAPI `SecurityModeEnabled` + same-target `IwSecurityMode == LlapiSecurityMode`」；metadata 已由 stale row `286` 刷新為 workbook row `284`，`plugins/wifi_llapi/reports/D284_block.md` 轉為 historical resolution notes；next ready case=`D285`。

> P4-04 latest note (2026-04-11, continuation): `D283 getScanResults() RSSI` 與 `D286 getScanResults() SignalStrength` 現在視為同一條 active public `ssid->rssi` field family。fresh isolated rerun `20260411T214050136894` 已證明 committed D283 generic case 仍會在 `setup_env` 後 hang：沒有 step output、沒有 markdown/json/xlsx report files、`plugins/wifi_llapi/reports/agent_trace/20260411T214050136894/` 保持空目錄，且 stop 後 `COM0` 一度落到可 recover 的 `TARGET_UNRESPONSIVE`。同時最新 source trace 也確認這不是獨立 field：`wld_nl80211_parser.c` 先填 `pResult->rssi`，`wld_rad_scan.c` 再把同一個 `ssid->rssi` 同時序列化成 public `RSSI` 與 public `SignalStrength`；`D286_block.md` 已同步改成這個 source model，但既有 live replay 仍維持 blocker（`5G -64/-65`、6G missing same-target replay、2.4G `-46` vs `-55/-56` / raw `-54`）。故 D283 仍 blocked on committed full-payload transport shape，D286 仍 blocked on shared semantic replay gap；next ready case=`D287`。

> P4-04 latest note (2026-04-11, continuation): `D287 getScanResults() SSID` 也仍維持 blocker，但 authority 模型已修正。active 0403 public `SSID` 已確認不是單純沿用 legacy raw `SSID:` helper，而是走 nl80211 scan-result path：`wld_nl80211_parser.c` 先把 `pWirelessDevIE->ssid` 複製到 `wld_scanResultSSID_t.ssid`，`wld_rad_scan.c` 再以 `wld_ssid_to_string(...)` 序列化成 public `SSID`。既有 isolated rerun `20260410T182739821870` 仍是決定性 live evidence：5G 與 2.4G 都能對同一 target BSSID exact-close，但 6G 仍只有 LLAPI `3a:06:e6:2b:a3:1a` / `.ROAMTEST_RSNO_P10P_1`，`iw` 與 direct raw `wl -i wl1 escanresults` 都無法 replay 同一 target。故 committed YAML 仍保持 stale row `289` 不變，詳見 `plugins/wifi_llapi/reports/D287_block.md`；next ready case=`D290`。

> P4-04 latest note (2026-04-10, continuation): `D287 getScanResults() SSID` 也已收斂成 blocker。source trace 仍將 neighboring `SSID` 直接綁在 `SSID: ` token path，因此先試了一版 workbook-style same-target compare；isolated rerun `20260410T182739821870` 已證明 5G `38:88:71:2f:f6:a7 -> Verizon_Z4RY7R` 與 2.4G `8c:19:b5:6e:85:e1 -> TMOBILE-85DF-TDK-2G` 可在 LLAPI 與 `iw` 間 same-target 對齊，但 6G `3a:06:e6:2b:a3:1a` / `.ROAMTEST_RSNO_P10P_1` 仍缺 `IwSSID6g`；補做 direct raw `wl -i wl1 escanresults | grep -m1 -B2 -A1 'BSSID: 3A:06:E6:2B:A3:1A'` 也回空。因此 D287 trial rewrite 已拒絕落地、committed YAML 已回復原 generic shape 與 stale row `289`；next ready case=`D288`。

> P4-04 latest note (2026-04-10, continuation): `D288 getScanResults() WPSConfigMethodsSupported` 已完成 live 對齊。source survey 已確認 0403 scan-result model 仍對外暴露此欄位，而 neighboring scan live payload 三 band 都是空字串；真正的缺口是 repo-side parser 不會吃 raw ubus transcript 的 `WPSConfigMethodsSupported = "",` trailing-comma shape。因此 committed YAML 現已改為每個 band 先用 explicit extractor 正規化輸出 `WPSConfigMethodsSupported=`，isolated rerun `20260410T183630583633` 三 band 全數 `Pass`，YAML row drift 也已由 `290` 刷新為 workbook row `288`；targeted D288 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`；next ready case=`D289`。

> P4-04 latest note (2026-04-10, continuation): `D289 getScanResults() Radio` 已完成 live 對齊，但對齊形狀是 source-backed fail-shaped absence。active 0403 scan model `wld_radio.odl` 的 `scanresult_t` 根本沒有 `Radio`，而 active HAL neighboring struct `wifi_neighbor_ap2_t` 也把 `ap_Radio` 註解掉，所以 live getter 沒有 backing field 可填。committed YAML 現已改為每個 band 先用 explicit extractor 正規化輸出 `Radio=`，isolated rerun `20260410T185434305989` 三 band 都吐 `Radio=` 且 `diagnostic_status=Pass`，YAML row drift 也已由 `291` 刷新為 workbook row `289`；完整 plugin runtime regression 維持 `1223 passed`；next ready case=`D290`。

> P4-04 latest note (2026-04-11, continuation): `D325 DiscardPacketsReceived` 已完成 live 對齊。舊 workbook row `325` 的 `/proc/net/dev_extstats` `$5` compare 先在 real runner replay `20260411T010859993578` 證明為 stale proc heuristic（`565 / 3854 / 356`-style drift），而 active 0403 source path 則與已完成的 `D304` 一致，都是由 `whm_brcm_get_if_stats()` / `wl if_counters rxdiscard` 覆寫 public field；改寫 direct-property YAML 後，real runner rerun `20260411T011321267947` 已在三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。對應 metadata 已由 stale row `249` 刷新為 workbook row `325`，official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D326`。

> P4-04 latest note (2026-04-11, continuation): `D326 DiscardPacketsSent` 已完成 live 對齊。舊 workbook row `326` 的 `/proc/net/dev_extstats` `$13` compare 先在 real runner replay `20260411T012137925510` 證明為 stale proc heuristic（`184 / 3467 / 87`-style drift），而 active 0403 source path 則由新完成的 D326 survey 與既有 `D305` 一起再確認，都是由 `whm_brcm_get_if_stats()` / `wl if_counters txdiscard` 覆寫 public field；改寫 direct-property YAML 後，real runner rerun `20260411T012538161460` 已在三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。對應 metadata 已由 stale row `250` 刷新為 workbook row `326`，official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D327`。

> P4-04 latest note (2026-04-11, continuation): `D327 ErrorsReceived` 已完成 live 對齊。第一輪 real runner replay `20260411T013241354703` 雖然沿用舊 `/proc/net/dev_extstats` `$4` 也 still pass，但 active 0403 source path 已由新完成的 D327 survey 與既有 `D306` 一起再確認為 `whm_brcm_get_if_stats()` / `wl if_counters rxerror`；因此 direct-property YAML 仍補做 source-backed green-lock rewrite，將 metadata 自 stale row `251` 刷新為 workbook row `327`，並在 updated rerun `20260411T013801878458` 繼續維持三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D328`。

> P4-04 latest note (2026-04-11, continuation): `D328 ErrorsSent` 已完成 live 對齊。第一輪 real runner replay `20260411T014458979418` 雖然沿用舊 `/proc/net/dev_extstats` `$12` 也 still pass，但 active 0403 source-backed path 已由新完成的 D328 survey 與既有 `D307` 一起再確認為 `whm_brcm_get_if_stats()` / `wl if_counters txerror`，且在有 `wds*` peer 時可經 `whm_brcm_vap_ap_stats_accu()` 再累加。focused live probe 也直接打出此欄位是 moving counter 而不是固定 workbook 樣本：`5G=1`、`6G=3347`、`2.4G=0` across `direct / getSSIDStats / txerror`。因此 committed YAML 仍補做 source-backed green-lock rewrite，將 metadata 自 stale row `252` 刷新為 workbook row `328`，並在 updated rerun `20260411T015126498621` 繼續維持三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D329`。

> P4-04 latest note (2026-04-11, continuation): `D329 FailedRetransCount` 已完成 live 對齊。第一輪 real runner replay `20260411T015905984272` 已在三 band exact-close `direct Stats == getSSIDStats() == 0`，但當時還缺獨立 driver oracle；active 0403 source path 已由新完成的 D329 survey 與既有 `D308` 一起再確認為 `whm_brcm_get_if_stats()` / `wl if_counters txretransfail`，且只有在存在 matching `wds*` peer 時才會額外累加。focused live probe 隨後直接關掉 `direct / getSSIDStats / txretransfail = 0 / 0 / 0` on 5G/6G/2.4G，並確認 current baseline 沒有 active `wds*` peer；因此 committed YAML 補做 source-backed green-lock rewrite，將 metadata 自 stale row `253` 刷新為 workbook row `329`，updated rerun `20260411T020534026608` 也繼續維持三 band exact-close `direct / getSSIDStats / if_counters = 0 / 0 / 0`。official-case guardrail `1 passed`，完整 repo regression `1634 passed`；next ready case=`D334`。

> P4-04 latest note (2026-04-11, continuation): `D334 RetransCount` 已完成 live 對齊。第一輪 real runner replay `20260411T021238026451` 已在三 band exact-close `direct Stats == getSSIDStats()`，但當時還缺獨立 driver oracle，且原本的 `getSSIDStats()` extractor 會把 `FailedRetransCount` 一起誤吃進來。active 0403 source path 已由新完成的 D334 survey 再確認為 `wldm_SSID_TrafficStats()` / `wl if_counters txretrans`，direct-property path 本身不走 WDS accumulation；focused live probe 先關掉 source-backed `direct / getSSIDStats / low-32(txretrans)`，其中 5G=`4294967295`、6G=`4294963915`、2.4G=`0`。改寫 case 後，updated rerun `20260411T022030741126` 在 retry policy 下收斂成功：attempt 1 的 6G 曾短暫漂成 `4294967294` vs driver `0`，但 attempt 2 已回到三 band exact-close `0 / 0 / 0`。因此 committed YAML 將 metadata 自 stale row `258` 刷新為 workbook row `334`，official-case guardrail `1 passed`，command-budget inventory 更新為 `630`，完整 repo regression `1634 passed`；next ready case=`D337`。

> P4-04 latest note (2026-04-11, continuation): `D337 UnknownProtoPacketsReceived` 已完成 live 對齊。第一輪 real runner replay `20260411T023258929853` 已在三 band exact-close `direct Stats == getSSIDStats() == 0`，但當時還缺獨立 driver oracle。active 0403 source path 已由新完成的 D337 survey 再確認為 `wldm_SSID_TrafficStats()` / `wl if_counters rxbadprotopkts`，direct-property path 本身不走 WDS accumulation；focused live probe 隨後關掉 `direct / getSSIDStats / rxbadprotopkts = 0 / 0 / 0` on 5G/6G/2.4G，而相鄰 getSSIDStats-family 的 `rxunknownprotopkts` 也維持 `0 / 0 / 0`，說明 workbook-era replay 雖 exact-close，但 source 上其實是兩條不同 counter family。改寫 case 後，updated rerun `20260411T024443960794` 繼續維持三 band exact-close `0 / 0 / 0`。因此 committed YAML 將 metadata 自 stale row `261` 刷新為 workbook row `337`，official-case guardrail `1 passed`，command-budget inventory 更新為 `633`，完整 repo regression `1634 passed`；next ready case=`D406`。

> P4-04 latest note (2026-04-11, continuation): `D406 MultipleRetryCount` 已完成 live 對齊。第一輪 real runner replay `20260411T025549740195` 已在三 band exact-close `direct Stats == getSSIDStats() == 0`，但當時還缺獨立 driver oracle。active 0403 source path 現已確認為 `wldm_SSID_TrafficStats()` / `wl if_counters txretrie`；focused live probe 隨後關掉 `direct / getSSIDStats / txretrie = 0 / 0 / 0` on 5G/6G/2.4G，且 current baseline `ls /sys/class/net | grep '^wds'` 為空。改寫 case 後，updated rerun `20260411T025954644775` 繼續維持三 band exact-close `0 / 0 / 0`。因此 committed YAML 將 metadata 自 stale row `301` 刷新為 workbook row `406`，official-case guardrails `1225 passed`，command-budget inventory 更新為 `636`，完整 repo regression `1634 passed`；next ready case=`D407`。

> P4-04 latest note (2026-04-11, continuation): `D407 RetryCount` 已完成 live 對齊。第一輪 real runner replay `20260411T031324456196` 已在三 band exact-close `direct Stats == getSSIDStats() == 0`，但舊 workbook extractor 仍會把 `MultipleRetryCount` 一起誤吃進來，且當時還缺獨立 driver oracle。active 0403 source path 現已確認為 `wldm_SSID_TrafficStats()` / `wl if_counters txretry`；focused live probe 隨後關掉 `direct / getSSIDStats / txretry = 0 / 0 / 0` on 5G/6G/2.4G，且 current baseline `ls /sys/class/net | grep '^wds'` 仍為空。改寫 case 後，updated rerun `20260411T031645170662` 繼續維持三 band exact-close `0 / 0 / 0`。因此 committed YAML 將 metadata 自 stale row `302` 刷新為 workbook row `407`，official-case guardrails `1225 passed`，command-budget inventory 更新為 `642`，完整 repo regression `1634 passed`；next ready case=`D528`。

> P4-04 latest note (2026-04-11, continuation): `D528 getSpectrumInfo bandwidth` 已完成 live 對齊。舊 workbook-era replay `20260411T032529278022` 已先證明 numeric-only regex `^\d+$` 不再成立，因為 active 0403 在三 band 都回 `bandwidth="20MHz"`。active 0403 source path 現已確認為 `_getSpectrumInfo()` / `s_prepareSpectrumOutput()` / `amxc_var_add_key(cstring_t, "bandwidth", swl_bandwidth_str[llEntry->bandwidth])`，而 sync refresh path `wifiGen_rad_getSpectrumInfo(..., update=false)` 在 `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()` 中會把 survey-derived spectrum entry 固定 seed 成 `SWL_BW_20MHZ`。改寫 case 後，real runner rerun `20260411T034134858534` 繼續維持三 band exact-close `20MHz / 20MHz / 20MHz`；補上的 spectrum guardrail fixture 也讓 official-case guardrails 維持 `1225 passed`，完整 repo regression 仍為 `1634 passed`；next ready case=`D529`。

> P4-04 latest note (2026-04-11, continuation): `D290 getScanResults() CentreChannel` 已完成 live 對齊。source-backed survey 先前已確認欄位本身在 0403 真實存在：`wld_radio.odl` 宣告 `CentreChannel`、`wld_rad_scan.c` 會把 `ssid->centreChannel` 寫入結果、`wld_nl80211_parser.c` 會以 `swl_chanspec_getCentreChannel()`（含 20MHz fallback）計算 live 值；真正缺的只是 same-target external replay path。這次在 one-shot `wifi-llapi baseline-qualify --repeat-count 1 --soak-minutes 0` 重新套回 deterministic DUT baseline 後，fresh isolated rerun `20260411T220324862766` 已用 same-target raw `wl escanresults` Chanspec 關掉三 band：5G `42/42` exact-close、2.4G `1/1` exact-close、6G 則鎖成 source-backed fail-shaped mismatch `LlapiCentreChannel6g=31` vs `WlCentreChannel6g=15`（同 target BSSID `6e:15:db:9e:33:72`）。故 committed YAML metadata 已由 stale row `292` 刷新為 workbook row `290`，`results_reference.v4.0.3` 現為 `Pass / Fail / Pass`，targeted D290 guardrails 已轉綠；next ready case=`D529`。

> P4-04 latest note (2026-04-11, continuation): `D529 getSpectrumInfo channel` 已完成 live 對齊。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "channel", llEntry->channel)`；fresh isolated rerun `20260411T221613327385` 加上兩輪 direct probes 也已把第一個 serialized spectrum-entry channel 鎖定為 `5g=36`、`6g=2`、`2.4g=1`。因此 committed case 已把 template metadata 對齊為 `object=WiFi.Radio.{i}.` / `api=getSpectrumInfo()`，並把舊的 generic numeric regex 收成 explicit first-entry channel extractors；workbook row 維持 `529`，`results_reference.v4.0.3` 仍為 `Pass / Pass / Pass`；next ready case=`D530`。

> P4-04 latest note (2026-04-11, continuation): `D530 getSpectrumInfo noiselevel` 已完成 live 對齊，但不採 fixed-value case。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(int32_t, "noiselevel", llEntry->noiselevel)`，因此這個欄位本質上是 survey-driven 的動態讀值，不是穩定常數。第一次 exact-value trial 已被拒絕：2.4G 在 retries/reruns 間實際漂成 `-75 / -77 / -78`，所以 committed case 只修正 template metadata 為 `object=WiFi.Radio.{i}.` / `api=getSpectrumInfo()`，保留 source-correct 的 numeric regex pass shape，並以 isolated rerun `20260411T222349217612` 作為綠鎖；next ready case=`D531`。

> P4-04 latest note (2026-04-11, continuation): `D531 getSpectrumInfo accesspoints` 已完成 live 對齊，且同樣採 metadata-only dynamic numeric case。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "accesspoints", llEntry->nrCoChannelAP)`，因此這個欄位本質上是 survey-driven 的 co-channel AP count，不是固定常數。committed case 因此只修正 template metadata 為 `object=WiFi.Radio.{i}.` / `api=getSpectrumInfo()`，保留 source-correct 的 numeric regex pass shape，並以 isolated rerun `20260411T223140870454` 作為綠鎖；next ready case=`D532`。

> P4-04 latest note (2026-04-11, continuation): `D532 getSpectrumInfo ourUsage` 已完成 live 對齊，且同樣採 metadata-only dynamic numeric case。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "ourUsage", llEntry->ourUsage)`，而 `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()` 會把 survey airtime (`ourTime / total_time`) 轉成 live 百分比；isolated rerun `20260411T223658523608` 已 clean pass。這次 workbook re-check 也同步確認 spectrum batch 整批 `source.row` 原本有 stale `+2` drift，所以 `D528-D533` 現已統一更正為實際 workbook rows `528-533`；next ready case=`D533`。

> P4-04 latest note (2026-04-11, continuation): `D533 getSpectrumInfo availability` 已完成 live 對齊，且是這個 spectrum batch 最後一筆 metadata-only dynamic numeric case。active 0403 source 已確認 public 欄位走 `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(uint32_t, "availability", llEntry->availability)`，而 `wld_rad_nl80211_updateUsageStatsFromSurveyInfo()` 會把 survey idle/free-time timing 轉成 live 百分比；isolated rerun `20260411T224035464927` 已 clean pass。至此 `D528-D533` spectrum block 已完成 row-corrected alignment；next unresolved queue 回到 `D322`、`D331`、`D333`、`D336`。

> P4-04 latest note (2026-04-11, continuation): `D322 BroadcastPacketsSent` 的 trial alignment 已被回退。Focused DUT-only probes 一度 exact-close 了 authored `direct / getSSIDStats / /proc $24` block（isolated rerun `20260411T225133238319`，外加 5G x5 與 multiband x3 repeat probes），但 superseding official rerun `20260411T230829194313` 仍在兩次 attempt 都重現同一個 5G `+1` drift：`4390 / 4390 / 4391`、`4394 / 4394 / 4395`，而 6G/2.4G 只是繼續 exact-close。由於 acceptance authority 仍是 full official runner path，committed YAML metadata 必須維持 stale row `246`；D323 已在後續 official rerun 對齊，因此 remaining direct-stats unresolved queue 現在只剩 `D322`、`D331`、`D333`、`D336`。

> P4-04 latest note (2026-04-10, continuation): `D295 scan()` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。committed YAML metadata 已由 stale row `220` 刷新為 workbook row `295`；manual serialwrap replay 也已證明三個 radio 在 live 0403 上本來就是 `Enable=1` / `Status=\"Up\"`。最終採納的 oracle 是較小顆粒的 `scan() returned + same-target driver-cache match`：5G `38:88:71:2F:F6:A7` 對到 `wl0 escanresults`、6G `6E:15:DB:9E:33:72` 對到 `wl1 escanresults`、2.4G `2E:59:17:00:06:F8` 對到 `wl2 escanresults`；直接再追一輪 `getScanResults()` 則會把 COM0 推到 `PROMPT_TIMEOUT`，因此不納入 committed oracle。next ready patch-driven workbook-Pass case=`D298`。

> P4-04 latest note (2026-04-10, continuation): `D298 startScan()` 也已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_radio.odl`：`startScan()` 有明確定義，且只列出 `Unable to start scan` / `A scan is already running` 兩種 error path；`wldm_lib_wifi.c` 也顯示 live path 會先走 `wldm_xbrcm_scan(..., "scan")`，再讀回 `num_scan_results` 與 `scan_results`。manual serialwrap replay 已證明三 band 都回 `WiFi.Radio.N.startScan() returned [ \"\" ]`，而 post-call driver cache 也各自有可見 BSSID：`wl0`=`A8:A2:37:4F:8C:5C`、`wl1`=`6E:15:DB:9E:33:72`、`wl2`=`62:82:FE:58:AC:B5`。因此 committed YAML metadata 已由 stale row `223` 刷新為 workbook row `298`；next ready patch-driven workbook-Pass case=`D299`。
> P4-04 latest note (2026-04-10, continuation): `D299 stopScan()` 也已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_radio.odl` 的 `void stopScan()`，active 0403 path 也可從 `wld_rad_scan.c` / `wld_scan_stop()` 看到：`_stopScan()` 會先要求 `wld_scan_isRunning()`，再經 `pRad->pFA->mfn_wrad_stop_scan(pRad)` 進到實際 abort path；public `wld_rad_nl80211.h` / `wld_nl80211_scan.c` 也明確暴露 `wld_rad_nl80211_abortScan()` -> `wld_nl80211_abortScan(...)`。manual serialwrap replay 則用 `WiFi.Radio.N.ScanResults.ScanInProgress` 關閉 state-bit oracle：5G 已證明 `0 -> startScan() -> 1 -> stopScan() -> 0`，再做第二輪 restart 仍會回到 `1/0`；6G 與 2.4G 的短 probe 也都穩定打出 `0 -> 1 -> 0`。中途曾試過把 6G/2.4G 合併成一條較長 probe，broker 以 `PROMPT_TIMEOUT_RECOVERED` 回收，因此 committed oracle 固定維持短版 per-band `startScan() -> ScanInProgress=1 -> stopScan() -> ScanInProgress=0` 重播。committed YAML metadata 已由 stale row `224` 刷新為 workbook row `299`；targeted D299 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`；next ready patch-driven workbook-Pass case=`D300`。
> P4-04 latest note (2026-04-10, continuation): `D300 getSSIDStats() BroadcastPacketsReceived` 也已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl` 的 `Stats.BroadcastPacketsReceived` 與 `htable getSSIDStats()`；active 0403 path 也可從 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` -> `wld_updateVAPStats()` 看到，且 `wld_statsmon.c` 已把 `rxBroadcastPackets` 寫進 `BroadcastPacketsReceived`。manual serialwrap replay 則用短版 per-band 三向數值 oracle 關閉：5G `getSSIDStats/direct/proc=$23` 為 `363/363/363`，6G 為 `144/144/144`，2.4G 為 `113/113/113`。中途曾試過把三 band 合併成一條較長 extraction probe，broker 以 `PROMPT_TIMEOUT_RECOVERED` 回收，因此 committed oracle 固定維持短版 per-band `getSSIDStats() -> Stats.BroadcastPacketsReceived -> /proc/net/dev_extstats $23` 重播。committed YAML metadata 已由 stale row `225` 刷新為 workbook row `300`；targeted D300 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`；next ready patch-driven workbook-Pass case=`D301`。
> P4-04 latest note (2026-04-10, continuation): `D301 getSSIDStats() BroadcastPacketsSent` 也已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl` 的 `Stats.BroadcastPacketsSent` 與 `htable getSSIDStats()`；active 0403 path 也可從 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` -> `wld_updateVAPStats()` 看到，且 `wld_statsmon.c` 已把 `txBroadcastPackets` 寫進 `BroadcastPacketsSent`。manual serialwrap replay 則用短版 per-band 三向數值 oracle 關閉：5G `getSSIDStats/direct/proc=$24` 為 `1432/1432/1432`，6G 為 `1590/1590/1590`，2.4G 為 `1680/1680/1680`。committed YAML metadata 已由 stale row `226` 刷新為 workbook row `301`；targeted D301 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`；next ready patch-driven workbook-Pass case=`D302`。
> P4-04 latest note (2026-04-10, continuation): `D302 getSSIDStats() BytesReceived` 目前收斂成 blocker。active 0403 source 已確認欄位存在：`wld_ssid.c` 會把 endpoint `rxbyte` 複製到 `pStats->BytesReceived`，而 `wld_statsmon.c` 也會把 `pSrc->rxBytes` 聚合進同一個 stats 結構。live replay 已證明三 band 都有 `direct Stats == getSSIDStats()`（5G `139402/139402`，6G `43232/43232`，2.4G `33066/33066`，5G focused rerun 仍是 `139618/139618`），但獨立 oracle 仍關不掉：舊 D323 式 `/proc/net/dev` / `/proc/net/dev_extstats` `$2` 固定落在 `111002/46112/35326`，`wl counters rxbyte` 只在 `6G/2.4G` 對上（`43232/33066`）而 5G 持續高於 getter（`144574`），`wl0.1/wl1.1/wl2.1` sub-interface counters 全為零，`AssociatedDevice.*.RxBytes` 也無法乾淨映射。因此 D302 trial rewrite 已拒絕落地、committed YAML 維持原 stale row `227` 與 `Fail/Fail/Fail`；blocker authority file=`plugins/wifi_llapi/reports/D302_block.md`；next ready patch-driven workbook-Pass case=`D303`。
> P4-04 latest note (2026-04-10, continuation): `D303 getSSIDStats() BytesSent` 目前也收斂成 blocker。active 0403 source 已確認欄位存在：`wld_ssid.c` 會把 endpoint `txbyte` 複製到 `pStats->BytesSent`，而 `wld_statsmon.c` 也會把 `pSrc->txBytes` 聚合進同一個 stats 結構。live replay 只部分關掉 method path：5G 與 2.4G 保持 `direct Stats == getSSIDStats()`（`95452542` / `67883586`），6G 第一次 replay 則是 `60438235` vs `60439059` 小幅漂移，focused rerun 才重新關到 `60582625/60582625`。但獨立 oracle 全部仍不收斂：`/proc/net/dev` / `/proc/net/dev_extstats` `$10` 只落在約 `66798080/64667313/66738514` 與 `66750876/64691013/66691690`，`wl counters txbyte` 又明顯更高（`114950286/80944128/77082489`），`wl0.1/wl1.1/wl2.1` sub-interface counters 是 blank / zero-like，`AssociatedDevice.*.TxBytes` 也無法乾淨映射。因此 D303 trial rewrite 已拒絕落地、committed YAML 維持原 stale row `228`；blocker authority file=`plugins/wifi_llapi/reports/D303_block.md`；next ready patch-driven workbook-Pass case=`D304`。
> P4-04 latest note (2026-04-11, continuation): `D324 BytesSent` 已完成 live 對齊。第一輪 real runner replay `20260411T005329099506` 先證明 workbook `/proc/net/dev_extstats` `$10` criterion 已 stale；但 follow-up source trace 已把 active 0403 vendor path 鎖到 `whm_brcm_get_if_stats()` / `wl if_counters txbyte`，後續 manual serialwrap probes 又先在 5G / 6G / 2.4G 分別 exact-close `140077` / `125263` / `133877`。因此 committed YAML 已改用 per-band `wl if_counters txbyte` oracle，`source.row` 也由 stale `248` 刷新為 workbook row `324`；更新後 real runner rerun `20260411T010328768651` 一次即過，三 band 均為 `direct == getSSIDStats == if_counters`（5G `79412272`、6G `47109866`、2.4G `79349200`）。next ready patch-driven workbook-Pass case=`D325`。
> P4-04 latest note (2026-04-10, continuation): `D304 getSSIDStats() DiscardPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`，再由 `whm_brcm_get_if_stats()` 用 `wl if_counters rxdiscard` 覆寫 `DiscardPacketsReceived`。live serialwrap replay 已關閉短版 per-band 三向數值 oracle：5G / 6G / 2.4G 都是 `direct Stats / getSSIDStats() / wl if_counters rxdiscard = 0/0/0`；反而舊 D325 式 `/proc/net/dev_extstats` `$5` 仍停在 `565/3752/356`、已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `229` 刷新為 workbook row `304`；targeted D304 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。另外，D302 的 blocker 也已補上新 caution：未來重查必須從 `wl if_counters rxbyte` 起手（6G/2.4G exact close、5G 穩定差 `104`），不能再只沿用舊 D323 proc oracle。next ready patch-driven workbook-Pass case=`D305`。
> P4-04 latest note (2026-04-10, continuation): `D305 getSSIDStats() DiscardPacketsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`，再由 `whm_brcm_get_if_stats()` 用 `wl if_counters txdiscard` 覆寫 `DiscardPacketsSent`。live serialwrap replay 已關閉短版 per-band 三向數值 oracle：5G / 6G / 2.4G 都是 `direct Stats / getSSIDStats() / wl if_counters txdiscard = 0/0/0`；反而舊 D326 式 `/proc/net/dev_extstats` `$13` 仍停在 `184/3467/87`、已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `230` 刷新為 workbook row `305`；targeted D305 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D306`。
> P4-04 latest note (2026-04-10, continuation): `D306 getSSIDStats() ErrorsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`，再由 `whm_brcm_get_if_stats()` 用 `wl if_counters rxerror` 覆寫 `ErrorsReceived`。live serialwrap replay 已關閉四向數值 oracle：5G / 6G / 2.4G 都是 `direct Stats / getSSIDStats() / wl if_counters rxerror / /proc/net/dev_extstats $4 = 0/0/0/0`。因此 committed YAML metadata 已由 stale row `231` 刷新為 workbook row `306`；targeted D306 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D307`。
> P4-04 latest note (2026-04-10, continuation): `D307 getSSIDStats() ErrorsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters txerror` 設 base-VAP `ErrorsSent`，再由 `whm_brcm_vap_ap_stats_accu()` 把 matching `wds*` interface 的 `ErrorsSent` 累加回 `SSID.stats`。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wds0.0.1 if_counters txerror = 56/56/56`，focused 6G rerun `direct/getSSIDStats/wds1.0.1 if_counters txerror = 46/46/46`，2.4G 則是 `direct/getSSIDStats/wl2 if_counters txerror = 0/0/0`；反而舊 D328 式 `/proc/net/dev_extstats` `$12` 在 base wl interface 上維持 `0`，已確認不再是 5G/6G 的 authoritative oracle。因此 committed YAML metadata 已由 stale row `232` 刷新為 workbook row `307`；targeted D307 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D308`。
> P4-04 latest note (2026-04-10, continuation): `D308 getSSIDStats() FailedRetransCount` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`，再由 `whm_brcm_get_if_stats()` 用 `wl if_counters txretransfail` 覆寫 `FailedRetransCount`；若存在 matching `wds*` interface，仍會走同一條 `whm_brcm_vap_ap_stats_accu()` merge path，但目前 live baseline 沒有出現額外累加。live serialwrap replay 已關閉三向數值 oracle：5G / 6G / 2.4G 都是 `direct Stats / getSSIDStats() / wl if_counters txretransfail = 0/0/0`。因此 committed YAML metadata 已由 stale row `233` 刷新為 workbook row `308`；targeted D308 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D309`。
> P4-04 latest note (2026-04-10, continuation): `D309 getSSIDStats() MulticastPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters rxmulti` 設 `MulticastPacketsReceived`，再由 `whm_brcm_vap_ap_stats_accu()` 視需要累加 matching `wds*` 介面，最後在 `whm_brcm_vap.c` 以 `tmp_stats.BroadcastPacketsReceived` 做扣減並 clamp 到 `0`。live serialwrap replay 已關閉這條 source-backed formula：5G `direct/getSSIDStats/wl0 if_counters rxmulti/BroadcastPacketsReceived = 0/0/10/363`，6G `0/0/0/145`，2.4G `0/0/0/113`，因此三 band 都落在 `max((rxmulti + matching wds_rxmulti) - BroadcastPacketsReceived, 0) = 0`。反而舊 D330 式 `/proc/net/dev_extstats` `$9` 仍停在 `363/146/113`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `234` 刷新為 workbook row `309`；targeted D309 tests `3 passed`、完整 plugin runtime regression 維持 `1223 passed`。next ready patch-driven workbook-Pass case=`D310`。
> P4-04 latest note (2026-04-10, continuation): `D310 getSSIDStats() MulticastPacketsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters txmulti` 設 `MulticastPacketsSent`，再由 `whm_brcm_vap_ap_stats_accu()` 視需要累加 matching `wds*` 介面，最後在 `whm_brcm_vap.c` 以 `tmp_stats.BroadcastPacketsSent` 做扣減並 clamp 到 `0`。live serialwrap replay 已關閉這條 source-backed formula：5G `direct/getSSIDStats/wl0 if_counters txmulti/BroadcastPacketsSent = 135098/135098/136632/1534`，6G `76033/76033/77722/1689`，2.4G `150648/150648/152429/1781`，因此三 band 都落在 `max((txmulti + matching wds_txmulti) - BroadcastPacketsSent, 0)`。反而舊 D331 式 `/proc/net/dev_extstats` `$18` 仍停在 `154277/148836/154585`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `235` 刷新為 workbook row `310`；targeted D310 tests `3 passed`。next ready patch-driven workbook-Pass case=`D311`。
> P4-04 latest note (2026-04-10, continuation): `D311 getSSIDStats() PacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters rxframe` 設 `PacketsReceived`，再由 `whm_brcm_vap_ap_stats_accu()` 視需要累加 matching `wds*` 介面的封包計數。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wl0 if_counters rxframe/wds0.0.1 rxframe = 1082/1082/1080/2`，6G `292/292/292/0`，2.4G `220/220/220/0`。反而舊 D332 式 `/proc/net/dev_extstats` `$3` 仍停在 `1086/438/333`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `236` 刷新為 workbook row `311`；targeted D311 tests `3 passed`。next ready patch-driven workbook-Pass case=`D312`。
> P4-04 latest note (2026-04-10, continuation): `D312 getSSIDStats() PacketsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：先由 `whm_brcm_get_if_stats()` 以 `wl if_counters txframe` 設 `PacketsSent`，再由 `whm_brcm_vap_ap_stats_accu()` 視需要累加 matching `wds*` 介面的封包計數。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wl0 if_counters txframe/wds0.0.1 txframe = 192311/192311/157138/35173`，6G `91211/91211/89703/1510`，2.4G `156926/156926/156926/0`。舊 D333 式 `/proc/net/dev_extstats` `$11` 已不再是全 band authority：5G 只停在 base `wl0` 計數 `157138`，6G 漂到 `151237`，只有沒有 WDS peer 的 2.4G 仍 exact close。因此 committed YAML metadata 已由 stale row `237` 刷新為 workbook row `312`；targeted D312 tests `3 passed`。next ready patch-driven workbook-Pass case=`D313`。
> P4-04 latest note (2026-04-10, continuation): `D313 getSSIDStats() RetransCount` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：由 `whm_brcm_get_if_stats()` 以 `wl if_counters txretrans` 設 `RetransCount`，若存在 matching `wds*` 介面也會經 `whm_brcm_vap_ap_stats_accu()` 累加。live serialwrap replay 已關閉這條 source-backed oracle：5G / 6G / 2.4G 都是 `direct/getSSIDStats/wl if_counters txretrans/matching wds txretrans = 0/0/0/0`。adjacent D334 direct case 的 v4.0.3 comment 也已經寫明 `direct Stats matched getSSIDStats(), but workbook v4.0.3 still remains To be tested`，因此 committed oracle 固定維持短版 per-band `direct/getSSIDStats/if_counters` 重播，不沿用這個 stale wording。committed YAML metadata 已由 stale row `238` 刷新為 workbook row `313`；targeted D313 tests `3 passed`。next ready patch-driven workbook-Pass case=`D314`。
> P4-04 latest note (2026-04-10, continuation): `D314 getSSIDStats() UnicastPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：`whm_brcm_get_if_stats()` 先以 `wl if_counters rxframe` 設 `PacketsReceived`、以 `wl if_counters rxmulti` 設 `MulticastPacketsReceived`，再直接導出 `UnicastPacketsReceived = PacketsReceived - MulticastPacketsReceived`；之後 `whm_brcm_vap.c` 只會再去修 visible 的 multicast 欄位，不會回寫 unicast。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wl0 if_counters rxframe/wl0 if_counters rxmulti/wds0.0.1 rxframe/wds0.0.1 rxmulti = 1084/1084/1092/10/2/0`，6G `292/292/292/0/0/0`，2.4G `220/220/220/0/0/0`。反而舊 D335 式 `/proc/net/dev_extstats` `$21` 仍停在 `360/146/107`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `239` 刷新為 workbook row `314`；targeted D314 tests `3 passed`。next ready patch-driven workbook-Pass case=`D315`。
> P4-04 latest note (2026-04-10, continuation): `D315 getSSIDStats() UnicastPacketsSent` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：`whm_brcm_get_if_stats()` 先以 `wl if_counters txframe` 設 `PacketsSent`、以 `wl if_counters txmulti` 設 `MulticastPacketsSent`，再直接導出 `UnicastPacketsSent = PacketsSent - MulticastPacketsSent`；之後 `whm_brcm_vap.c` 只會再去修 visible 的 multicast 欄位，不會回寫 unicast。live serialwrap replay 已關閉這條 source-backed oracle：focused 5G rerun `direct/getSSIDStats/wl0 if_counters txframe/wl0 if_counters txmulti/wds0.0.1 txframe/wds0.0.1 txmulti = 55992/55992/159684/140207/36515/0`，6G `13317/13317/92193/81711/2835/0`，2.4G `2338/2338/159419/157081/0/0`。反而舊 D336 式 `/proc/net/dev_extstats` `$22` 在三 band 都停在 `0`，已確認是 stale proc heuristic。因此 committed YAML metadata 已由 stale row `240` 刷新為 workbook row `315`；targeted D315 tests `3 passed`。next ready patch-driven workbook-Pass case=`D316`。
> P4-04 latest note (2026-04-10, continuation): `D316 getSSIDStats() UnknownProtoPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 path 同樣會由 `_SSID_getSSIDStats()` -> `s_updateSsidStatsValues()` 進到 `whm_brcm_vap_update_ap_stats()`：`whm_brcm_get_if_stats()` 以 `wl if_counters rxunknownprotopkts` 設 `UnknownProtoPacketsReceived`，`whm_brcm_vap_copy_stats()` 會保留 base VAP 的欄位值，而 `whm_brcm_vap_ap_stats_accu()` 也可再累加 matching `wds*` 介面的同名計數。live serialwrap replay 已關閉這條 source-backed oracle：5G `direct/getSSIDStats/wl0 if_counters rxunknownprotopkts/wds0.0.1 rxunknownprotopkts = 0/0/0/0`，6G `0/0/0/0`，2.4G `0/0/0/0`。adjacent D337 direct case 雖然 workbook 仍 gate 在 `To be tested`，但 v4.0.3 comment 已經記錄同一個 multiband zero-shape（`direct Stats matched getSSIDStats()`），因此 committed oracle 固定維持短版 per-band `direct/getSSIDStats/if_counters + WDS accumulation` 重播。committed YAML metadata 已由 stale row `241` 刷新為 workbook row `316`；targeted D316 tests `3 passed`。next ready patch-driven workbook-Pass case=`D317`。
> P4-04 latest note (2026-04-10, continuation): `D317 BSSID` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，而 active 0403 `WiFi.SSID` object 也在 `dm_info.c` 持續暴露 `BSSID` 欄位。live serialwrap replay 已關閉這條獨立 oracle：5G `ubus BSSID / iw dev wl0 info addr / wl -i wl0 cur_etheraddr = 2c:59:17:00:19:95/2c:59:17:00:19:95/2c:59:17:00:19:95`，6G `2c:59:17:00:19:96/2c:59:17:00:19:96/2c:59:17:00:19:96`，2.4G `2c:59:17:00:19:a7/2c:59:17:00:19:a7/2c:59:17:00:19:a7`。workbook v4.0.3 comment 本來就記錄了同一個 `matching iw dev info` 形狀，因此 committed oracle 固定維持短版 per-band `ubus BSSID -> iw dev info -> wl cur_etheraddr` 重播。committed YAML metadata 已由 stale row `242` 刷新為 workbook row `317`；targeted D317 tests `3 passed`。next ready patch-driven workbook-Pass case=`D319`。
> P4-04 latest note (2026-04-10, continuation): `D319 MACAddress` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，其中 `WiFi.SSID.{i}.MACAddress` 仍維持 read-only SSID property。live serialwrap replay 已關閉四向獨立 oracle：5G `ubus MACAddress / iw dev wl0 info addr / ifconfig wl0 HWaddr / wl -i wl0 cur_etheraddr = 2c:59:17:00:19:95/2c:59:17:00:19:95/2c:59:17:00:19:95/2c:59:17:00:19:95`，6G `2c:59:17:00:19:96/2c:59:17:00:19:96/2c:59:17:00:19:96/2c:59:17:00:19:96`，2.4G `2c:59:17:00:19:a7/2c:59:17:00:19:a7/2c:59:17:00:19:a7/2c:59:17:00:19:a7`。因此 committed oracle 固定維持短版 per-band `ubus MACAddress -> iw dev info -> ifconfig -> wl cur_etheraddr` 重播。committed YAML metadata 已由 stale row `321` 刷新為 workbook row `319`；targeted D319 tests `3 passed`。next ready patch-driven workbook-Pass case=`D320`。
> P4-04 latest note (2026-04-10, continuation): `D320 SSID` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。source-backed support 已直接寫在 `wld_ssid.odl`，其中 `WiFi.SSID.{i}.SSID` 仍維持 read-only SSID property。live serialwrap replay 已關閉這條獨立 oracle：5G `ubus SSID / wl -i wl0 ssid = testpilot5G/testpilot5G`，6G `testpilot6G/testpilot6G`，2.4G `testpilot2G/testpilot2G`。因此 committed oracle 固定維持短版 per-band `ubus SSID -> wl ssid` 重播。committed YAML metadata 已由 stale row `322` 刷新為 workbook row `320`；targeted D320 tests `3 passed`。next ready patch-driven workbook-Pass case=`D321`。
> P4-04 latest note (2026-04-10, continuation): `D321 BroadcastPacketsReceived` 已完成 live 對齊，並可收斂成 plain `Pass/Pass/Pass`。active 0403 source-backed support 與先前已關閉的 D300 family 一致，而 live serialwrap replay 已直接關閉三向 direct-property oracle：5G `direct Stats / getSSIDStats / /proc/net/dev_extstats $23 = 363/363/363`，6G `147/147/147`，2.4G `113/113/113`。因此 committed oracle 固定維持短版 per-band `Stats.BroadcastPacketsReceived -> getSSIDStats().BroadcastPacketsReceived -> /proc $23` 重播，不再沿用較吵雜的舊 traffic procedure。committed YAML metadata 已由 stale row `245` 刷新為 workbook row `321`；targeted D321 tests `3 passed`。next ready patch-driven workbook-Pass case=`D322`。
> P4-04 latest note (2026-04-11, continuation): direct-stats capture resolver 已修正；對於 `D321/D322` 這類 authored `ubus-cli "...getSSIDStats()" | sed ...` shell pipeline，runner 不再覆寫成 synthesized plain readback。對應 regression 已補上，command-budget inventory 也同步追到 `618`，完整 full repo regression 現在是 `1634 passed`。
> P4-04 latest note (2026-04-11, continuation): `D321 BroadcastPacketsReceived` 已完成真正的 runner live revalidation。單案 rerun `20260411T002336420469` 在實際 `testpilot run` 路徑下直接通過：5G `364/364/364`、6G `149/149/149`、2.4G `113/113/113`，三 band 都維持 `direct / getSSIDStats / /proc $23` exact close。
> P4-04 latest note (2026-04-12, continuation): `D322 BroadcastPacketsSent` 已完成 official-runner 對齊。先前 `20260411T230829194313` 看起來像 workbook `/proc $24` oracle stale，因為 5G 會穩定 `+1`；但 resolving rerun `20260412T002445088386` 證明真正問題是 `verify_env` 後的 runner timing。保持原 workbook `/proc/net/dev_extstats` `$24` compare、不改 driver authority，只在 case 最前面補一個 post-`verify_env` settle (`sleep 2`) 後，attempt 1 已先把 5G 關掉、只剩 6G `4766/4767/4767`；attempt 2 則 exact-close 三 band `4596/4596/4596`、`4772/4772/4772`、`5121/5121/5121`。因此 YAML metadata 已由 stale row `246` 刷新為 workbook row `322`，`plugins/wifi_llapi/reports/D322_block.md` 轉為 historical trial notes；remaining unresolved direct-stats queue 現在只剩 `D331`、`D333`。
> P4-04 latest note (2026-04-11, continuation): `D323 BytesReceived` 已完成 real-runner 對齊。先前 blocker 最終被證明是 stale workbook `/proc/net/dev_extstats` `$2` heuristic 加上錯誤的 source 解讀；修正後的 0403 tracing 顯示 direct-property row 應對 `whm_brcm_get_if_stats()` 的 `wl if_counters rxbyte`，再加 matching `wds*` `rxbyte` 累加，而 `whm_brcm_vap_update_ap_stats()` 本身並不會把 `BytesReceived` 回填成 `tmp_stats`。改寫 YAML 後，official rerun `20260411T231952006453` 已在三 band exact-close `direct / getSSIDStats / driver = 276282/276282/276282`、`122610/122610/122610`、`73193/73193/73193`；committed metadata 也已由 stale row `247` 刷新為 workbook row `323`，`plugins/wifi_llapi/reports/D323_block.md` 轉為 historical resolution notes。
> P4-04 latest note (2026-04-12, continuation): `D330 MulticastPacketsReceived` 已在 current local repo state 完成 source-backed green-lock。isolated rerun `20260411T191809490680` 最終以 active 0403 公式 `max((rxmulti + matching wds_rxmulti) - BroadcastPacketsReceived, 0)` 關掉三 band：attempt 1 雖然打到 `/tmp/_tp_cmd.sh: line 2: syntax error: unterminated quoted string`，但 attempt 2 已 exact-close `direct / getSSIDStats / driver-formula = 0 / 0 / 0` on 5G / 6G / 2.4G，且 YAML metadata 已由 stale row `254` 刷新為 workbook row `330`。同一輪後續 `D331 MulticastPacketsSent` 的兩次 source-backed trial rerun（`20260411T192138186700` / `20260411T192524301950`）先把 workbook `/proc $18` heuristic 排除，但 superseding official rerun `20260411T234124237416` 仍證明公式 rewrite 不具 runner-stability：5G 從 `286001/286006` 漂到 `286140/286192`。最新 settle retrial `20260412T003609854183` 再證明短暫 post-`verify_env` settle 雖能把 5G / 2.4G exact-close，卻仍在 6G 留下 `181336/181337`、`181375/181377` 的 residual drift，因此 local rewrite 已再次回退，`D331` 仍維持 blocker；remaining unresolved direct-stats queue=`D331`、`D333`，next ready case=`D333`。
> P4-04 latest note (2026-04-12, continuation): `D332 PacketsReceived` 已在 current local repo state 完成 source-backed green-lock。stale workbook replay `20260411T194312398713` 先同時重現了兩個問題：`/proc/net/dev_extstats` `$3` 在 5G 漂到 `2234/2237`、而舊 `getSSIDStats()` extractor 又 overmatch 成 `expected=0`；之後把 case metadata 刷到 workbook row `332`、把 `getSSIDStats()` extraction 錨定到真正的 `PacketsReceived` 欄位，再把 driver oracle 切到 `wl if_counters rxframe + matching wds rxframe`，source-backed rerun `20260411T194647490016` 已單次 `Pass`。同一輪後續 `D333 PacketsSent` 的 stale replay `20260411T194816992700` 與 source-backed trial `20260411T195140855058` 先收斂成 blocker；最新 settle retrial `20260412T004816450100` 再證明 post-`verify_env` `sleep 2` 雖已把舊的固定 `5G +5` 收斂成 attempt 1 `5G +1`、attempt 2 `6G +2`，但 official runner 仍未 exact-close all-band，所以 local rewrite 已再次回退。remaining unresolved direct-stats queue 現在回到 `D324`、`D331`、`D333`，next ready case=`D324`。
> P4-04 latest note (2026-04-12, continuation): `D324 BytesSent` 已完成新的 source-backed official trial，但結果仍需維持 blocker。這次先把 driver oracle 改成 active 0403 方向 `wlX txbyte + matching wds txbyte`，official rerun `20260412T005627796136` 也的確把舊的巨大 drift 收斂掉；但 runner path 仍打出 sequential refresh staircase：5G attempt 1/2 分別是 `148970905/148971033/148971377`、`149022147/149022215/149022559`，2.4G attempt 1 也同樣是 `157367729/157391543/157415359`，attempt 2 雖把 `direct == getSSIDStats()` 關到 `157517138/157517138`，driver 仍多出 `+316`。0403 pWHM source 現已補證：direct `Stats.*` 與 `getSSIDStats()` 都會各自呼叫 `s_updateSsidStatsValues()` 再輸出，因此 official sequential replay 不是同一個 frozen snapshot。local WDS-sum rewrite 已回退，remaining blocked direct-stats queue 維持 `D324`、`D331`、`D333`，next ready overall case=`D281`。
> P4-04 latest note (2026-04-12, continuation): `D281 getScanResults() Noise` 新的 same-channel public-spectrum trial 仍需維持 blocker。0403 source 方向已更正：`getScanResults().Noise` 不是 direct `wl escanresults noise:`，而是 `s_updateScanResultsWithSpectrumInfo()` 依同 channel 的 spectrum `noiselevel` 回填。依這條 source-backed path 改寫後，official rerun `20260412T011311621294` 雖曾一次 exact-close 三 band `36/-100/-100`、`5/-97/-97`、`1/-80/-80`，但立即 follow-up rerun `20260412T011644317498` 就失去 durability：attempt 1 在 `step_6g_scan` 打到 `/tmp/_tp_cmd.sh: line 2: syntax error: unexpected end of file`，attempt 2 又留下 2.4G `Noise=-80` vs `SpectrumNoise=-79`。因此 local rewrite 已再次回退、committed YAML 維持 stale row `283`，`plugins/wifi_llapi/reports/D281_block.md` 已更新為新的 blocker authority，next ready overall case=`D282`。
> P4-04 latest note (2026-04-12, continuation): `D282 getScanResults() OperatingStandards` 仍需維持 blocker，但 blocker authority 已再收斂。新的 0403 source trace 補證：nl80211 parser 同時填入 `operatingStandards` 與 `supportedStandards` 兩個 bitmask；然而 workbook row `282` 對應的 public `_getScanResults()` 只序列化 `OperatingStandards`，沒有把 sibling `SupportedStandards` 一起公開。因此既有 isolated rerun `20260410T163026194231` 的 2.4G 外部 `...,+be`（`b,g,n,ax` vs `b,g,n,ax,be`）更像 supported/capability-family readout，而不是 public `OperatingStandards` 的 same-source replay；6G 也仍缺 same-target external block。故 committed YAML 維持 stale row `284`，`plugins/wifi_llapi/reports/D282_block.md` 已更新為新的 blocker authority，next ready overall case=`D283`。
> P4-04 latest note (2026-04-11, continuation): `D335 UnicastPacketsReceived` 已在 current local repo state 完成 source-backed green-lock。stale workbook replay `20260411T200329824574` 先重現 `/proc/net/dev_extstats` `$21` 在三 band 全部 drift：5G `2001/746`、6G `788/391`、2.4G `483/235`（direct/getSSIDStats vs proc）；0403 source 則明確在 `whm_brcm_api_ext.c` 將 `UnicastPacketsReceived` 導出為 `PacketsReceived - MulticastPacketsReceived`，再由 `whm_brcm_vap.c` copy/accu 到 matching VAP/WDS stats。把 case metadata 刷到 workbook row `335`，並把 driver oracle 切到 `(wl if_counters rxframe + matching wds rxframe) - (wl if_counters rxmulti + matching wds rxmulti)` 後，source-backed rerun `20260411T200851584762` 已 exact-close 三 band：5G `2003/2003/2003`、6G `794/794/794`、2.4G `483/483/483`；next ready patch-driven workbook-Pass case=`D336`。
> P4-04 latest note (2026-04-12, continuation): `D336 UnicastPacketsSent` 已完成 official-runner 對齊。stale workbook replay `20260411T201639103833` 先重現 `/proc/net/dev_extstats` `$22` 在三 band 全部掉成 zero-shape，因此 workbook proc oracle 已正式淘汰；接著前兩版 source-backed trial (`20260411T201939105374` / `20260411T202824539933`) 分別暴露 parser syntax error 與 signed txframe/txmulti 公式的不穩定 drift。進一步 source/live tracing 後，已確認 active 0403 真正語意是 unsigned `((txframe + matching wds txframe) - (d11_txmulti + matching wds d11_txmulti)) & 0xffffffff`；official rerun `20260412T000744842751` attempt 1 雖仍有 6G `24709/24710/24710` 的 `+1`，但 attempt 2 已 exact-close 三 band `27172/27172/27172`、`24703/24703/24703`、`17117/17117/17117`。因此 YAML metadata 已刷新到 workbook row `336`，`plugins/wifi_llapi/reports/D336_block.md` 轉為 historical trial notes，remaining unresolved direct-stats queue 現在只剩 `D331`、`D333`。
> P4-04 latest note (2026-04-13, continuation): `D176 BeaconPeriod` 已完成 source-backed official-runner 對齊。這案不是單純 metadata refresh：workbook row `176` 明確要求 tri-band baseline getter `100`、setter `1000`，以及 hostapd `beacon_int` 同步收斂。committed rewrite 現在在 setup 先強制 `WiFi.Radio.{1,2,3}.BeaconPeriod=100`，再逐 band exact-close `ubus-cli ... BeaconPeriod?` 與 `/tmp/wl{0,1,2}_hapd.conf` `beacon_int` 的 `100 -> 1000 -> 100`；official rerun `20260413T044907394777` 一次 `Pass`，metadata 也已由 stale row `139` / raw `Fail / Fail / Fail` 刷新為 workbook row `176` / raw `Pass / Pass / Pass`。targeted runtime tests `213 passed`、完整 full repo regression `1650 passed`，overlay compare 現在是 `261 / 420 full matches`、`159 mismatches`、`58 metadata drifts`；`D020` 維持 verified fail-shaped mismatch、`D035` 維持 blocker，next clean stale-radio rewrite target=`D188`。
> P4-04 latest note (2026-04-13, continuation): `D188 DTIMPeriod` 已完成 source-backed official-runner 對齊。這案與 `D176` 同屬 workbook-authoritative AP-only setter family，不是單純 metadata refresh：workbook row `188` 明確要求 tri-band default getter `3`、setter `7`、hostapd `dtim_period=7`，再 restore 回 `3`。committed rewrite 現在在 setup 先強制 `WiFi.Radio.{1,2,3}.DTIMPeriod=3`，再逐 band exact-close `ubus-cli ... DTIMPeriod?` 與 `/tmp/wl{0,1,2}_hapd.conf` `dtim_period` 的 `3 -> 7 -> 3`；official rerun `20260413T050318932313` 一次 `Pass`，metadata 也已由 stale row `151` / raw `Fail / Fail / Fail` 刷新為 workbook row `188` / raw `Pass / Pass / Pass`。targeted runtime tests `210 passed`、完整 full repo regression `1650 passed`，overlay compare 現在是 `262 / 420 full matches`、`158 mismatches`、`58 metadata drifts`；`D020` 維持 verified fail-shaped mismatch、`D035` 維持 blocker，next ready workbook-Pass revisit=`D034`。
> P4-04 latest note (2026-04-13, continuation): `D034 Noise` 已完成 source-backed official-runner 對齊。第一輪確認 rerun `20260413T052117345208` 失敗在 `setup_env` 的 `iw dev wl0 link -> Not connected.`，但這反而把真正根因挖出來：case 還殘留 stale custom `TestPilot_BTM` / `WPA3-Personal` baseline，而 authoritative full run `20260412T113008433351` 其實已經證明 D034 是在 generic `testpilot5G` / `WPA2-Personal` 路徑上通過。committed fix 因此把 topology / setup 拉回 generic 5G baseline、移除 custom `sta_env_setup`、把 stale workbook row `36` 刷回 workbook row `34`，並把舊的 `Noise=0` fail-shape 改成 same-STA live negative noise compare；official rerun `20260413T052709875993` 一次 exact-close `AssociatedDevice.1.Noise=-100` vs `DriverNoise=-100`（MAC `2C:59:17:00:04:85`）。overlay compare 現在是 `263 / 420 full matches`、`157 mismatches`、`58 metadata drifts`；`D020` 維持 verified fail-shaped mismatch、`D035` / `D053` 維持 blocker，next ready workbook-Pass revisit=`D059`。
> P4-04 latest note (2026-04-11, continuation): `D277 getScanResults() Bandwidth` 已在 current local repo state 完成 source-backed live 對齊。isolated rerun `20260411T205454026707` 證明把 `getScanResults()` 先縮到第一個 scan object 後，舊的 6G full-payload serialwrap/broker recovery blocker 已不再重現；同一個 workbook-style target BSSID replay 也已能直接對位 `wl escanresults` Chanspec bandwidth：5G `80/80`、6G `320/160`、2.4G `20/20`。因此 committed YAML metadata 已由 stale row `279` 刷新為 workbook row `277`，並將 `results_reference.v4.0.3` 收斂成 `Pass / Fail / Pass`；next ready runtime-triage case=`D281`。
> P4-04 latest note (2026-04-11, continuation): direct-stats capture resolver 已修正；對於 `D321/D322` 這類 authored `ubus-cli "...getSSIDStats()" | sed ...` shell pipeline，runner 不再覆寫成 synthesized plain readback。對應 regression 已補上，command-budget inventory 也同步追到 `618`，完整 full repo regression 現在是 `1634 passed`。
> P4-04 latest note (2026-04-11, continuation): `D321 BroadcastPacketsReceived` 已完成真正的 runner live revalidation。單案 rerun `20260411T002336420469` 在實際 `testpilot run` 路徑下直接通過：5G `364/364/364`、6G `149/149/149`、2.4G `113/113/113`，三 band 都維持 `direct / getSSIDStats / /proc $23` exact close。
> P4-04 latest note (2026-04-11, continuation): `D322 BroadcastPacketsSent` 目前轉為 source-backed blocker。修正 resolver 後，live rerun `20260411T002336420469` 已證明三 band `direct == getSSIDStats()` 都 exact close；但 5G 連兩次都維持 `direct / getSSIDStats / /proc $24 = 1644/1644/1645`、`1647/1647/1648`，只有 `/proc` 後讀值固定大 `+1`。0403 source (`whm_brcm_vap.c`) 也顯示 final public `BroadcastPacketsSent` 會在後段被覆寫回 `tmp_stats`，因此目前不能把 `/proc $24` 當成穩定 authoritative oracle。blocker handoff 已落在 `plugins/wifi_llapi/reports/D322_block.md`；next ready patch-driven workbook-Pass case=`D323`。
| P4-05 | Wifi_LLAPI Excel report pipeline | done | template + run report + merged-cell 相容 |

## Phase 5：CLI & Integration

| ID | 項目 | 狀態 | 註記 |
|---|---|---|---|
| P5-01 | cli-full | done | `run/list` + `wifi-llapi baseline-qualify` + `wifi-llapi build-template-report` |
| P5-02 | orchestrator-full | done | Orchestrator 拆分 + SDK session + hook policy + advisory/remediation hot-path wiring 已落地 |
| P5-03 | integration tests（mock transport） | done | 已補齊 realistic runtime 測試；最新完整 full suite `1622 passed`，已涵蓋 custom 6G gate/timing hardening、`execute_step()` skip-action short-circuit、paired-AP hard-disable tweak、6G OCV restart socket cleanup、以及 qualifier secondary-BSS-warning taxonomy 的回歸測試；3x live full run determinism 驗證通過（Run 2/3 = 100% 一致） |
| P5-04 | plugin agent-config schema/runtime | done | `agent_runtime.py` |
| P5-05 | agent selection trace | done | per-case selection / fallback trace |
| P5-06 | case-agent dispatcher（sequential） | done | `max_concurrency=1` |
| P5-07 | retry-aware timeout policy | done | 遞增 timeout + 上限 |
| P5-08 | per-case trace artifact writer | done | `reports/agent_trace/<run_id>/` |
| P5-09 | integration tests for per-case agent | done | selector / fallback / retry / timeout / trace 全覆蓋 |

---

## Phase R1：指令格式與 Report 清理

| ID | 項目 | 狀態 | 對應隱患 | 說明 |
|---|---|---|---|---|
| R1-01 | Report 保存不覆蓋 | done | H3 | 報告檔名加 `run_id`，copy 另有 `_01/_02` 防呆 |
| R1-02 | xlsx report output 清理 marker | done | H4 | `sanitize_report_output()` 清除舊 serial prompt / marker 汙染 |
| R1-03 | xlsx report cmd 欄單行化 | done | 需求 | G 欄改為單行一條指令 |
| R1-04 | YAML 指令單行化工具 | done | H5 | 新增 `wifi-llapi audit-yaml-commands` dry-run 稽核 |

## Phase R2：Orchestrator 解耦

| ID | 項目 | 狀態 | 對應隱患 | 說明 |
|---|---|---|---|---|
| R2-01 | 拆分 Orchestrator | done | H1 | 拆為 case_utils / runner_selector / execution_engine / orchestrator facade |
| R2-02 | Plugin 自宣告執行流程 | done | H2 | PluginBase.run_pipeline() |
| R2-03 | Plugin 自宣告 Reporter | done | H2 | PluginBase.create_reporter() + report_formats() |
| R2-04 | Retry 歷史保留 | done | H8 | ExecutionEngine.execute_with_retry() + RetryResult dataclass |

## Phase R3：Plugin Template

| ID | 項目 | 狀態 | 對應隱患 | 說明 |
|---|---|---|---|---|
| R3-01 | 乾淨 plugin template | done | 需求 | `plugins/_template/` 完整骨架 |
| R3-02 | Plugin scaffold CLI | done | 需求 | template loadable by PluginLoader |
| R3-03 | Plugin 拆檔示範 | done | H6 | CommandResolver 拆為 `command_resolver.py` |
| R3-04 | PluginBase 瘦身 | done | H14 | 最小合約：name + discover_cases + execute_step + evaluate |

## Phase R4：Copilot SDK 控制平面

| ID | 項目 | 狀態 | 對應隱患 / 需求 | 說明 |
|---|---|---|---|---|
| R4-00 | 第三次重構研究 / 文件基線 | done | 需求 | Copilot SDK 深度研究完成，docs / README / AGENTS 已同步基線 |
| R4-01 | Copilot SDK session foundation | done | 需求 | SDK session wire-in with create/cleanup lifecycle per case |
| R4-02 | hook policy layer | done | 需求 | 6 lifecycle hooks: pre/post_case, pre/post_step, on_failure, on_retry |
| R4-03 | custom agents roles | done | 需求 | executor / advisor / remediation / observer + role merging |
| R4-04 | skills packages | done | 需求 | SkillRegistry + SKILL.md discovery + role-based resolution |
| R4-05 | advisory agent outputs | done | 需求 | AdvisoryOutput + AdvisoryCollector + IHook handler factory |
| R4-06 | remediation planner loop | done | 需求 | RemediationPlanner + severity-prioritized action mapping + wifi_llapi in-run safe remediation loop |
| R4-07 | runtime policy alignment | done | 需求 | `plugins/wifi_llapi/agent-config.yaml` 已改為 copilot-only policy |
| R4-08 | selective MCP integrations | done | 需求 | MCPRegistry + role-selective server management |

## Phase R5：可靠性與測試補強

| ID | 項目 | 狀態 | 對應隱患 | 說明 |
|---|---|---|---|---|
| R5-01 | serialwrap RC 擷取修正 | done | H7 | `_extract_marker_output()` 改為 FIRST match |
| R5-02 | Plugin loader 改良 | done | H9 | sys.path try/finally cleanup |
| R5-03 | openpyxl API 隔離 | done | H10 | excel_adapter.py 封裝 |
| R5-04 | Session 解析簡化 | pending | H12 | 減少 fallback 層數並嚴格裝置綁定 |
| R5-05 | 測試覆蓋 >80% | done | 品質 | coverage baseline 81% (2492 stmts) |
| R5-06 | MD/JSON report 實作 | done | H13 | IReporter + MarkdownReporter + JsonReporter |
| R5-07 | `execute_step` heuristic 收斂 | done | 新增 | CommandResolver strategy pattern refactor |
| R5-08 | control-plane / verdict-plane 邊界測試 | done | 新增 | 15 boundary tests for hook/retry/timeout/band interactions |
