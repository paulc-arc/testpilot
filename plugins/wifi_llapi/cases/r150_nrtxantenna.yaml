id: wifi-llapi-r150-nrtxantenna
name: NrTxAntenna — WiFi.Radio.{i}.DriverStatus.
version: '1.0'
source:
  report: "0302-AT&T_LLAPI_Test_Report_20260107.xlsx"
  sheet: Wifi_LLAPI
  row: 189
  object: "WiFi.Radio.{i}.DriverStatus."
  api: "NrTxAntenna"
platform:
  prplos: 4.0.3
  bdk: 6.3.1
hlapi_command: ubus-cli WiFi.Radio.{i}.DriverStatus.NrTxAntenna=4
llapi_support: Support
implemented_by: Vendor Module
topology:
  devices:
    DUT:
      role: ap
      transport: serial

test_environment: |
  Topology:
  - DUT: COM0 (B0, AP role)
  - STA: COM1 (A0, STA role, STA-required cases only)
  Transport:
  - serialwrap attach COM0 / COM1
  Band mapping:
  - 5G: DUT wl0 / AP1 / SSID.4
  - 6G: DUT wl1 / AP3 / SSID.6
  - 2.4G: DUT wl2 / AP5 / SSID.8
  Pre-check:
  - cat /proc/socinfo | grep Revision | cut -d: -f2
  - iw dev
  - ubus-cli "WiFi.Radio.?" | sed -n "1,120p"

setup_steps: |
  1) DUT baseline on COM0:
     ubus-cli WiFi.AccessPoint.1.Enable=1
     ubus-cli WiFi.AccessPoint.3.Enable=1
     ubus-cli WiFi.AccessPoint.5.Enable=1
  2) Apply case command context on DUT:
     ubus-cli WiFi.Radio.{i}.DriverStatus.NrTxAntenna=4
  3) For pure query cases, keep configuration unchanged and only collect readback evidence.

test_procedure: |
  1) Execute case command on DUT:
     ubus-cli WiFi.Radio.{i}.DriverStatus.NrTxAntenna=4
  2) LLAPI readback on DUT:
     ubus-cli "WiFi.Radio.*.DriverStatus.NrTxAntenna?"
  3) Cross-check with verification_command (driver/hostapd/iw/proc evidence).

steps:
- id: step1
  action: exec
  target: DUT
  command: '1. Check GW Radio DriverStatus.NrTxAntenna: ubus-cli WiFi.Radio.*.DriverStatus.NrTxAntenna? > WiFi.Radio.*.DriverStatus.NrTxAntenna?
    WiFi.Radio.1.DriverStatus.NrTxAntenna=4 WiFi.Radio.2.DriverStatus.NrTxAntenna=4 WiFi.Radio.3.DriverStatus.NrTxAntenna=4'
  capture: result
pass_criteria:
- field: result.NrTxAntenna
  operator: contains
  value: '4'
  description: NrTxAntenna 應回傳包含 NrTxAntenna=4
verification_command: 'wl -i wl0 txchain

  root@prplOS:/# wl -i wl0 txchain

  15 (0xf)

  root@prplOS:/# wl -i wl1 txchain

  15 (0xf)

  root@prplOS:/# wl -i wl2 txchain

  15 (0xf)


  Value _x0001_Hex_x0001_Meaning

  1 0x1_x0001_1×1 (chain 0 only)

  3 0x3_x0001_2×2 (chains 0–1)

  7 0x7_x0001_3×3 (chains 0–2)

  15 0xF_x0001_4×4 (chains 0–3)'
results_reference:
  v4.0.1:
    5g: Pass
    6g: Pass
    2.4g: Pass
    comment: ''
  v4.0.3:
    5g: Pass
    6g: Pass
    2.4g: Pass
    comment: ''
