id: d377-radio-maxbitrate
name: D377 Radio MaxBitRate
source:
  row: 377
  object: WiFi.Radio.{i}.
  api: MaxBitRate
  baseline: 0310-BGW720-300
bands:
- 5g
- 6g
- 2.4g
llapi_support: Not Supported
results_reference:
  v4.0.3:
    5g: Not Supported
    6g: Not Supported
    2.4g: Not Supported
topology:
  devices:
    DUT:
      role: ap
      transport: serial
      selector: COM1
steps:
- id: step_5g_getter
  target: dut
  action: read
  capture: getter_5g
  command: ubus-cli "WiFi.Radio.1.MaxBitRate?"
  expected: MaxBitRate 回傳有效值
  description: Radio.1 (5g) MaxBitRate getter（live=0）
- id: step_6g_getter
  target: dut
  action: read
  capture: getter_6g
  command: ubus-cli "WiFi.Radio.2.MaxBitRate?"
  expected: MaxBitRate 回傳有效值
  description: Radio.2 (6g) MaxBitRate getter（live=0）
- id: step_24g_getter
  target: dut
  action: read
  capture: getter_24g
  command: ubus-cli "WiFi.Radio.3.MaxBitRate?"
  expected: MaxBitRate 回傳有效值
  description: Radio.3 (2.4g) MaxBitRate getter（live=0）
pass_criteria:
- field: getter_5g.MaxBitRate
  operator: regex
  value: ^\d+$
- field: getter_6g.MaxBitRate
  operator: regex
  value: ^\d+$
- field: getter_24g.MaxBitRate
  operator: regex
  value: ^\d+$
