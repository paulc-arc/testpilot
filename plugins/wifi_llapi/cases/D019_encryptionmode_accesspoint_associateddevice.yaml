id: wifi-llapi-D019-encryptionmode-accesspoint-associateddevice
name: "EncryptionMode — WiFi.AccessPoint.{i}.AssociatedDevice.{i}."
version: '1.0'
source:
  row: 19
  object: WiFi.AccessPoint.{i}.AssociatedDevice.{i}.
  api: EncryptionMode
platform:
  prplos: 4.0.3
  bdk: 6.3.1
hlapi_command: ubus-cli "WiFi.AccessPoint.3.AssociatedDevice.1.EncryptionMode?"
llapi_support: Support
implemented_by: pWHM
topology:
  devices:
    DUT:
      role: ap
      transport: serial
      selector: COM1
    STA:
      role: sta
      transport: serial
      selector: COM0
      config:
      - iface: 6g
        mode: sta
        ssid: TestPilot_BTM
        key: testpilot6g
  links:
  - from: STA
    to: DUT
    band: 6g
test_environment: |
  Topology:
  - DUT: COM1 (AP role)
  - STA: COM0 (station role for this case; do not rely on the old A0/B0 heuristic)
  Transport:
  - serialwrap attach COM0 / COM1
  Live band mapping:
  - 5G: DUT wl0 / AP1 / SSID.4 / TestPilot_BTM
  - 6G: DUT wl1 / AP3 / SSID.6 / TestPilot_BTM
  - 2.4G: DUT wl2 / AP5 / SSID.8 / TestPilot_24G
  Preconditions:
  - keep only one associated station on AP3 so AssociatedDevice.1 stays deterministic
  - use the validated 6G single-band baseline for this case
setup_steps: |
  1) DUT 6G baseline:
     - enable Radio.2 / SSID.6 / AP3
     - configure AP3 as WPA3-Personal with MFP required
     - keep the validated live SSID/credential pair as TestPilot_BTM / testpilot6g
  2) STA 6G preparation:
     - disable local AP3/AP4 on COM1
     - move COM1 br-lan off 192.168.1.0/24 so later traffic checks do not self-hit the local bridge
     - reset wl1 and its wpa_supplicant control socket
     - connect wl1 to TestPilot_BTM with SAE
  3) Association evidence:
     - COM1: iw dev wl1 link ; wpa_cli -p /var/run/wpa_supplicant -i wl1 status
     - COM0: wl -i wl1 assoclist
sta_env_setup: |
  DUT 6G AP baseline:
    ubus-cli WiFi.Radio.2.Enable=1
    ubus-cli WiFi.Radio.2.BeaconPeriod=100
    ubus-cli WiFi.Radio.2.DTIMPeriod=3
    ubus-cli WiFi.Radio.2.DriverConfig.FragmentationThreshold=-1
    ubus-cli WiFi.Radio.2.DriverConfig.RtsThreshold=-1
    ubus-cli WiFi.Radio.2.DriverConfig.TPCMode=Auto
    ubus-cli WiFi.Radio.2.ExplicitBeamFormingEnabled=1
    ubus-cli WiFi.Radio.2.ImplicitBeamFormingEnabled=1
    ubus-cli WiFi.Radio.2.OfdmaEnable=1
    ubus-cli WiFi.Radio.2.ObssCoexistenceEnable=0
    ubus-cli WiFi.Radio.2.OperatingStandards=ax
    ubus-cli WiFi.Radio.2.RxChainCtrl=-1
    ubus-cli WiFi.Radio.2.TxChainCtrl=-1
    ubus-cli WiFi.Radio.2.Vendor.Brcm.RegulatoryDomainRev=0
    ubus-cli WiFi.Radio.2.Sensing.Enable=1
    ubus-cli WiFi.Radio.2.IEEE80211ax.BssColorPartial=0
    ubus-cli WiFi.Radio.2.IEEE80211ax.NonSRGOBSSPDMaxOffset=0
    ubus-cli WiFi.Radio.2.IEEE80211ax.PSRDisallowed=0
    ubus-cli WiFi.Radio.2.IEEE80211ax.SRGOBSSPDMaxOffset=0
    ubus-cli WiFi.Radio.2.IEEE80211ax.SRGOBSSPDMinOffset=0
    ubus-cli WiFi.Radio.2.LongRetryLimit=6
    ubus-cli WiFi.Radio.2.MultiUserMIMOEnabled=1
    ubus-cli WiFi.Radio.2.TargetWakeTimeEnable=1
    ubus-cli WiFi.SSID.6.SSID=TestPilot_BTM
    ubus-cli WiFi.AccessPoint.3.IEEE80211u.QoSMapSet=
    ubus-cli WiFi.AccessPoint.4.IEEE80211u.QoSMapSet=
    sleep 3
    ubus-cli WiFi.AccessPoint.3.Security.ModeEnabled=WPA3-Personal
    ubus-cli WiFi.AccessPoint.3.Security.SAEPassphrase=testpilot6g
    ubus-cli WiFi.AccessPoint.3.Security.MFPConfig=Required
    ubus-cli WiFi.AccessPoint.3.MultiAPType=FronthaulBSS
    ubus-cli WiFi.AccessPoint.3.Enable=1
  STA 6G preparation:
    ifconfig br-lan 192.168.88.1 netmask 255.255.255.0 up
    ubus-cli WiFi.AccessPoint.3.Enable=0
    ubus-cli WiFi.AccessPoint.4.Enable=0
    wpa_cli terminate 2>/dev/null || true
    sleep 1
    rm -f /var/run/wpa_supplicant/wl1 2>/dev/null || true
    iw dev wl1.1 del 2>/dev/null || true
    iw dev wl1 disconnect 2>/dev/null || true
    ifconfig wl1 down
    wl -i wl1 down
    wl -i wl1 ap 0
    iw dev wl1 set type managed
    wl -i wl1 up
    ifconfig wl1 up
    mkdir -p /var/run/wpa_supplicant
    printf 'ctrl_interface=/var/run/wpa_supplicant\nupdate_config=1\nsae_pwe=2\nnetwork={\nssid="TestPilot_BTM"\nkey_mgmt=SAE\nsae_password="testpilot6g"\nieee80211w=2\nscan_ssid=1\n}\n' > /tmp/wpa_wl1.conf
    wpa_supplicant -B -D nl80211 -i wl1 -c /tmp/wpa_wl1.conf -C /var/run/wpa_supplicant
    wpa_cli -p /var/run/wpa_supplicant -i wl1 enable_network 0
    wpa_cli -p /var/run/wpa_supplicant -i wl1 select_network 0
    sleep 3
  STA 6G connect verify:
    wpa_cli -p /var/run/wpa_supplicant -i wl1 reconnect
    sleep 15
    iw dev wl1 link
    wpa_cli -p /var/run/wpa_supplicant -i wl1 status
  DUT association evidence:
    wl -i wl1 assoclist
test_procedure: |
  1) Confirm the 6G STA has completed SAE association on wl1 from COM1.
  2) Confirm AP3 still shows one associated STA in wl1 assoclist so AssociatedDevice.1 identity is deterministic.
  3) Read WiFi.AccessPoint.3.AssociatedDevice.1.EncryptionMode on DUT.
  4) Workbook row 19 remains fail-shaped: the LLAPI getter stays `Default` while the same associated STA still shows `key_mgmt=SAE` plus `pairwise_cipher=CCMP`, so this case must not be authored back into a pass-style `AES` expectation.
steps:
- id: step1
  action: exec
  target: STA
  command: wpa_cli -p /var/run/wpa_supplicant -i wl1 status
  capture: sta_status
- id: step2
  action: exec
  target: DUT
  command: wl -i wl1 assoclist | sed -n 's/^assoclist 
    \([0-9A-Fa-f:]*\).*/AssocMAC=\1/p'
  depends_on: step1
  capture: assoc_driver
- id: step3
  action: exec
  target: DUT
  command: >-
    STA_MAC=$(wl -i wl1 assoclist | awk 'NR==1{print $2}');
    ENC=$(ubus-cli "WiFi.AccessPoint.3.AssociatedDevice.1.EncryptionMode?" | sed -n 's/.*EncryptionMode="\([^"]*\)".*/\1/p');
    PROBE_ENC=$(ubus-cli "WiFi.AccessPoint.3.AssociatedDevice.1.ProbeReqCaps.EncryptionMode?" | sed -n 's/.*EncryptionMode="\([^"]*\)".*/\1/p');
    INFO=$(wl -i wl1 sta_info $STA_MAC | grep -E 'auth:|crypto:');
    AUTH=$(printf '%s\n' "$INFO" | sed -n 's/.*auth: *//p' | head -n1);
    CRYPTO=$(printf '%s\n' "$INFO" | sed -n 's/.*crypto: *//p' | head -n1);
    case "$CRYPTO" in AES_CCM|CCMP) DRIVER_ENC=AES ;; TKIP) DRIVER_ENC=TKIP ;; TKIP+AES|AES+TKIP) DRIVER_ENC=TKIP-AES ;; *) DRIVER_ENC=${CRYPTO:-UNKNOWN_CRYPTO} ;; esac;
    printf 'EncryptionMode=%s\n' "$ENC";
    printf 'ProbeReqCapsEncryptionMode=%s\n' "$PROBE_ENC";
    printf 'DriverAuth6g=%s\n' "$AUTH";
    printf 'DriverCrypto6g=%s\n' "$CRYPTO";
    printf 'DriverEncryptionMode6g=%s\n' "$DRIVER_ENC"
  depends_on: step2
  capture: result
pass_criteria:
- field: sta_status.key_mgmt
  operator: equals
  value: SAE
  description: COM1 wl1 should first show an SAE-completed 6G connection.
- field: assoc_driver.AssocMAC
  operator: regex
  value: ^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$
  description: AP3 should first expose a single live 6G STA MAC from wl1 
    assoclist.
- field: result.EncryptionMode
  operator: equals
  reference: result.DriverEncryptionMode6g
  description: Workbook row 19 is Fail-shaped because LLAPI Default does not
    match the driver-normalized AES encryption mode.
verification_command:
- wpa_cli -p /var/run/wpa_supplicant -i wl1 status
- wl -i wl1 assoclist
- STA_MAC=$(wl -i wl1 assoclist | awk 'NR==1{print $2}')
- 'ubus-cli "WiFi.AccessPoint.3.AssociatedDevice.1.EncryptionMode?"'
- 'ubus-cli "WiFi.AccessPoint.3.AssociatedDevice.1.ProbeReqCaps.EncryptionMode?"'
- 'wl -i wl1 sta_info $STA_MAC | grep -E "auth:|crypto:"'
