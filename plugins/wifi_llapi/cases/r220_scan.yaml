id: wifi-llapi-r220-scan
name: scan() — WiFi.Radio.{i}.
version: '1.0'
source:
  report: 6.3.0GA_prplware_v403_LLAPI_Test_Report.xlsx
  sheet: Wifi_LLAPI
  row: 220
  object: WiFi.Radio.{i}.
  api: scan()
platform:
  prplos: 4.0.3
  bdk: 6.3.1
hlapi_command: ubus-cli WiFi.Radio.{i}.scan(SSID:string, channels:string, min_rssi:int):void
llapi_support: Support
implemented_by: pWHM
topology:
  devices:
    DUT:
      role: ap
      transport: serial
steps:
- id: step1
  action: exec
  target: DUT
  command: '1. Try scan() function: ubus-cli "WiFi.Radio.*.scan()"'
  capture: result
pass_criteria:
- field: result
  operator: not_empty
  value: ''
  description: scan() 應回傳非空結果
results_reference:
  v4.0.1:
    5g: Fail
    6g: Fail
    2.4g: Fail
    comment: 'ARC : Any parameters in ".scan(SSID:string, channels:string, min_rssi:int)" will always return syntex error.'
  v4.0.3:
    5g: To be tested
    6g: To be tested
    2.4g: To be tested
    comment: Need to check(Leon)
