id: d530-spectruminfo-noiselevel
name: D530 getSpectrumInfo noiselevel
source:
  row: 530
  object: WiFi.Radio.{i}.
  api: getSpectrumInfo()
  baseline: 0310-BGW720-300
bands:
- 5g
- 6g
- 2.4g
llapi_support: Support
results_reference:
  v4.0.3:
    5g: Pass
    6g: Pass
    2.4g: Pass
    comment: 'Multiband source-backed retest: active 0403 `getSpectrumInfo()` returns numeric spectrum noise through `_getSpectrumInfo()` -> `s_prepareSpectrumOutput()` -> `amxc_var_add_key(int32_t, "noiselevel", llEntry->noiselevel)`. Live rerun confirms all three bands emit numeric `noiselevel` values, but the survey-driven field itself remains dynamic across retries.'
topology:
  devices:
    DUT:
      role: ap
      transport: serial
      selector: COM1
steps:
- id: step_5g_method
  target: dut
  action: read
  capture: method_5g
  command: ubus-cli "WiFi.Radio.1.getSpectrumInfo()"
  expected: noiselevel 出現在回傳 array 中
  description: Radio.1 (5g) getSpectrumInfo → noiselevel
- id: step_6g_method
  target: dut
  action: read
  capture: method_6g
  command: ubus-cli "WiFi.Radio.2.getSpectrumInfo()"
  expected: noiselevel 出現在回傳 array 中
  description: Radio.2 (6g) getSpectrumInfo → noiselevel
- id: step_24g_method
  target: dut
  action: read
  capture: method_24g
  command: ubus-cli "WiFi.Radio.3.getSpectrumInfo()"
  expected: noiselevel 出現在回傳 array 中
  description: Radio.3 (2.4g) getSpectrumInfo → noiselevel
pass_criteria:
- field: method_5g.noiselevel
  operator: regex
  value: ^-?\d+$
- field: method_6g.noiselevel
  operator: regex
  value: ^-?\d+$
- field: method_24g.noiselevel
  operator: regex
  value: ^-?\d+$
