id: d474-radio-surroundingchannels-channel
name: D474 Radio ScanResults SurroundingChannels Channel
source:
  row: 474
  object: WiFi.Radio.{i}.ScanResults.SurroundingChannels.{i}.
  api: Channel
  baseline: 0310-BGW720-300
bands:
- 5g
- 6g
- 2.4g
llapi_support: Not Supported
results_reference:
  v4.0.3:
    5g: Not Supported
    6g: Not Supported
    2.4g: Not Supported
    comment: workbook row 474 marks ScanResults.SurroundingChannels.1.Channel as Not Supported; official rerun 20260415T081024815688 returned error 2 / object not found on all radios.
topology:
  devices:
    DUT:
      role: ap
      transport: serial
      selector: COM1
steps:
- id: step_5g_getter
  target: dut
  action: exec
  capture: getter_5g
  command: >-
    OUT=$(ubus-cli "WiFi.Radio.1.ScanResults.SurroundingChannels.1.Channel?" 2>&1 || true); printf '%s\n' "$OUT"; printf '%s\n' "$OUT" | sed -n 's/.*failed (\([0-9][0-9]*\) - \(.*\))/error=\1/p'; printf '%s\n' "$OUT" | sed -n 's/.*failed (\([0-9][0-9]*\) - \(.*\))/message=\2/p'
  expected: error 2 / object not found
  description: Radio.1 (5g) SurroundingChannels.1.Channel getter should be Not Supported
- id: step_6g_getter
  target: dut
  action: exec
  capture: getter_6g
  command: >-
    OUT=$(ubus-cli "WiFi.Radio.2.ScanResults.SurroundingChannels.1.Channel?" 2>&1 || true); printf '%s\n' "$OUT"; printf '%s\n' "$OUT" | sed -n 's/.*failed (\([0-9][0-9]*\) - \(.*\))/error=\1/p'; printf '%s\n' "$OUT" | sed -n 's/.*failed (\([0-9][0-9]*\) - \(.*\))/message=\2/p'
  expected: error 2 / object not found
  description: Radio.2 (6g) SurroundingChannels.1.Channel getter should be Not Supported
- id: step_24g_getter
  target: dut
  action: exec
  capture: getter_24g
  command: >-
    OUT=$(ubus-cli "WiFi.Radio.3.ScanResults.SurroundingChannels.1.Channel?" 2>&1 || true); printf '%s\n' "$OUT"; printf '%s\n' "$OUT" | sed -n 's/.*failed (\([0-9][0-9]*\) - \(.*\))/error=\1/p'; printf '%s\n' "$OUT" | sed -n 's/.*failed (\([0-9][0-9]*\) - \(.*\))/message=\2/p'
  expected: error 2 / object not found
  description: Radio.3 (2.4g) SurroundingChannels.1.Channel getter should be Not Supported
pass_criteria:
- field: getter_5g.error
  operator: equals
  value: '2'
- field: getter_5g.message
  operator: equals
  value: object not found
- field: getter_6g.error
  operator: equals
  value: '2'
- field: getter_6g.message
  operator: equals
  value: object not found
- field: getter_24g.error
  operator: equals
  value: '2'
- field: getter_24g.message
  operator: equals
  value: object not found
