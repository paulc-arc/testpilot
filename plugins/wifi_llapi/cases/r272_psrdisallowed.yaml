id: wifi-llapi-r272-psrdisallowed
name: PSRDisallowed — WiFi.Radio.{i}.IEEE80211ax.
version: '1.0'
source:
  report: 6.3.0GA_prplware_v403_LLAPI_Test_Report.xlsx
  sheet: Wifi_LLAPI
  row: 272
  object: WiFi.Radio.{i}.IEEE80211ax.
  api: PSRDisallowed
platform:
  prplos: 4.0.3
  bdk: 6.3.1
hlapi_command: ubus-cli WiFi.Radio.{i}.IEEE80211ax.PSRDisallowed=0
llapi_support: Support
implemented_by: Vendor Module
topology:
  devices:
    DUT:
      role: ap
      transport: serial
steps:
- id: step1
  action: exec
  target: DUT
  command: '1. Check API default value: ubus-cli WiFi.Radio.*.IEEE80211ax.PSRDisallowed? > WiFi.Radio.*.IEEE80211ax.PSRDisallowed?
    WiFi.Radio.1.IEEE80211ax.PSRDisallowed=0 WiFi.Radio.2.IEEE80211ax.PSRDisallowed=0 WiFi.Radio.3.IEEE80211ax.PSRDisallowed=0'
- id: step2
  action: exec
  target: DUT
  command: 'Modify the API value to 1: ubus-cli WiFi.Radio.*.IEEE80211ax.PSRDisallowed=1'
  depends_on: step1
- id: step3
  action: exec
  target: DUT
  command: Check the API value again, compare with driver return and beacon tagged capability- Spatial Reuse Parameter Set
    - SR Control - SRP Disallowed ubus-cli WiFi.Radio.*.IEEE80211ax.PSRDisallowed? > WiFi.Radio.*.IEEE80211ax.PSRDisallowed?
    WiFi.Radio.1.IEEE80211ax.PSRDisallowed=1 WiFi.Radio.2.IEEE80211ax.PSRDisallowed=1 WiFi.Radio.3.IEEE80211ax.PSRDisallowed=1
  depends_on: step2
  capture: result
pass_criteria:
- field: result.PSRDisallowed
  operator: contains
  value: '0'
  description: PSRDisallowed 應回傳包含 PSRDisallowed=0
verification_command: '(PSRDisallowed=0)

  root@prplOS:/# wl -i wl0 he sr


  HE Spatial Reuse Parameter Set element:


  <Enabled>


  SR Control:

  - HESIGA_Spatial_reuse_value15_allowed (is SET)

  root@prplOS:/# wl -i wl1 he sr


  HE Spatial Reuse Parameter Set element:


  <Enabled>


  SR Control:

  - HESIGA_Spatial_reuse_value15_allowed (is SET)

  root@prplOS:/# wl -i wl2 he sr


  HE Spatial Reuse Parameter Set element:


  <Enabled>


  SR Control:

  - HESIGA_Spatial_reuse_value15_allowed (is SET)



  (PSRDisallowed=1)

  root@prplOS:/# wl -i wl0 he sr


  HE Spatial Reuse Parameter Set element:


  <Enabled>


  SR Control:

  - PSR Disallowed (is SET)

  - HESIGA_Spatial_reuse_value15_allowed (is SET)

  root@prplOS:/# wl -i wl1 he sr


  HE Spatial Reuse Parameter Set element:


  <Enabled>


  SR Control:

  - PSR Disallowed (is SET)

  - HESIGA_Spatial_reuse_value15_allowed (is SET)

  root@prplOS:/# wl -i wl2 he sr


  HE Spatial Reuse Parameter Set element:


  <Enabled>


  SR Control:

  - PSR Disallowed (is SET)

  - HESIGA_Spatial_reuse_value15_allowed (is SET)'
results_reference:
  v4.0.1:
    5g: Pass
    6g: pass
    2.4g: Pass
    comment: ''
  v4.0.3:
    5g: To be tested
    6g: To be tested
    2.4g: To be tested
    comment: ''
